verify/node_modules/
verify/package.json
verify/package-lock.json
verify/shot-*.png
node_modules/
package-lock.json
package.json
# 예외 — 이건 저장소에 있어야 한다.
# Cloudflare가 저장소를 보고 푸시 서버를 배포할 때(Workers Builds) 이 파일을 읽는다.
!server/push/package.json

# 관리자 페이지 빌드 결과 (Cloudflare가 빌드할 때마다 새로 만든다)
_admin/dist/

# 관리자 미리보기 (필요할 때 만들어 쓰는 일회용 파일)
_admin/preview.html
verify/tour/

# 인스타 카드 렌더 결과 — 작업용. 매 실행 다시 만들어진다(넣으면 실행마다 diff)
# 🔴 게시용 `insta/pub/` 은 **일부러 커밋한다** — 인스타는 파일 업로드를 안 받고
#    공개 주소를 요구해서, GitHub Pages 가 서빙해야 가져갈 수 있다.
insta/out/

# 인스타 작업용 참고 스크린샷 — 조사하며 찍은 것이라 산출물이 아니다(15MB)
insta/ref/
