/* ============================================================
   한대장 — 신청서 AI 초안 설정
   ------------------------------------------------------------
   신청서의 **서술형 칸**을 학생이 준 재료로 엮어 초안을 만들어 주는 기능이다.

   ⚠️ 열쇠(API 키)를 이 파일에 넣으면 안 된다. 저장소가 공개라 누구나 꺼내 쓴다.
      열쇠는 서버(Cloudflare Worker)에만 두고, 앱은 그 서버 주소만 안다.
      도우미 서버(server/chat/)·푸시 서버(server/push/)와 똑같은 구조이고,
      앱의 보안 설정(CSP)도 workers.dev 주소를 이미 허용해 두었다.

   ── 이 기능의 계약 (2026-08-23 개발자 승인) ─────────────────
   ① 나오는 글은 **초안**이다. 화면에 그렇게 적고, 학생이 고칠 수 있게 둔다.
      앱이 대신 제출하지 않는다 — 읽고 고치는 것은 학생 몫이다.
   ② **사실 나열형 칸에는 가지 않는다.** 봉사기관·봉사시간·가족사항·학력 연월일을
      AI가 채우면 학생이 허위 서류를 제출하게 된다. 어느 칸이 어느 쪽인지는
      `data/forms.json`의 `kind`에 적혀 있다 — 짐작하지 않는다.
   ③ **버튼을 눌러야 나간다.** 무엇이 나가는지 화면에 그대로 보여 주고,
      안 눌러도 지금처럼 직접 쓸 수 있다.
   ④ 나갈 수 있는 것: 그 질문에 학생이 쓴 답 + 공고의 공개 정보 + 학교/학년/전공.
      🔴 절대 안 나가는 것: 주민등록번호 · 계좌 · 서류보관함 파일 ·
         장애/기초생활수급/국가유공자 여부 · 성적 · 소득분위 · 이름 · 연락처.
   ⑤ 실패하면 **학생이 쓴 원문을 그대로 둔다.** 절대 덮어쓰지 않는다.

   켜는 법: server/essay/README.md 대로 서버를 올리고 아래 endpoint 한 칸만 채운다.
           비워 두면 이 기능은 **아예 동작하지 않고**, 앱은 지금 그대로 쓰인다.
   ============================================================ */
const ESSAY_CONFIG = {
  /* 예: 'https://handaejang-essay.○○○.workers.dev' — 비어 있으면 AI 초안을 쓰지 않는다 */
  endpoint: '',

  /* 화면에 붙는 말. 개발자 결정 2026-08-23 — '초안'이라고 밝힌다. */
  label: '키워드로 글 만들기',
  notice: '반드시 읽고 고쳐서 제출하세요.',

  /* 한 번에 보낼 서술형 칸 수. 서버도 같은 상한을 갖고 있다. */
  maxFields: 8,

  /* 응답을 기다릴 시간(밀리초). A4 2장짜리 칸이 여럿이면 1분을 넘길 수 있어 넉넉히 둔다.
     넘으면 조용히 포기하고 학생 원문을 그대로 둔다. */
  timeoutMs: 90000,
};

if (typeof module !== 'undefined' && module.exports) module.exports = { ESSAY_CONFIG };
