# 인프라 개편안 검토 보고 (2026-08-03)

> **받는 사람**: 공동작업자 Josehyeon
> **보내는 사람**: 선주 세션 (작업 브랜치 `claude/admin-page-elements-210xu2`)
> **목적**: ① 오늘 수집 로봇을 급히 고친 경위 공유(그쪽 담당 영역이라 양해 필요)
> ② 선주님이 제안한 인프라 개편안(비공개 전환 + Cloudflare + Oracle 자체 러너) 검토 결과 공유
> ③ **결정 전이므로 아직 아무것도 실행하지 않았습니다.**

---

## 0. 한 줄 요약

브라우저 수집이 이틀간 멈춰 있어 급히 고쳤고(**40분 취소 → 7분 완료 · 신규 50건**),
인프라 개편안은 **방향은 맞지만 순서를 그대로 따르면 앱이 내려갑니다.**
그리고 오늘 수리로 **개편의 명분이었던 사용량 문제가 상당 부분 사라졌습니다.**

---

## 1. 오늘 급히 고친 것 — 브라우저 수집 시간초과

### 왜 제가 손댔는지 (담당 영역 침범 양해)
수집 로봇은 Josehyeon 담당 영역이지만, **8/2 이후 17개 학교 공고가 이틀간 갱신되지 않는**
상태여서 발견 즉시 조치했습니다. 선주님 지시로 진행했고, 사후에 이렇게 공유드립니다.

### 무슨 일이 있었나
13차 세션에서 학교를 13→29곳으로 늘리면서 브라우저 대상이 **17곳**이 됐는데,
시간 설계는 11곳 기준(8분) 그대로였습니다.

| 실행 | 결과 |
|---|---|
| 8/1 06:05 | ✅ 8.1분 성공 |
| 8/2 05:16 · 06:08 | ❌ 40분 · 48분 **취소** |
| 8/3 00:07 · 06:31 | ❌ 40분 **취소** |

마지막 성공 수집이 **8/2 00:27**에 멈춰 있었습니다.

### 핵심 — 왜 결과가 통째로 사라졌나
> **시간초과(취소)는 12차 세션의 '넘어져도 저장'으로 못 막습니다.**
> 그건 **크래시**를 막는 장치이고, 시간초과는 GitHub이 프로세스를 **강제 종료**하는 것이라
> 자바스크립트가 손쓸 틈이 없습니다. 저장 단계까지 통째로 죽습니다.

**그래서 상한을 올리는 것은 해법이 아닙니다**(학교가 늘면 또 걸립니다).
스크립트가 **스스로 예산 안에 끝내야** 합니다.

### 시간을 먹던 진짜 자리
| 위치 | 예산이 있었나 |
|---|---|
| 상세 방문 (학교당 최대 40건 × 30초 = **최악 20분/학교**) | ❌ **없었음** |
| 클릭 채집 (180초) | ✅ 있었음 |
| 실패 학교 재시도 (8곳 실패 시 순차로 전부 다시) | ❌ 병렬 아니었음 |
| 전체 | ❌ **전역 예산 없음** |

### 바꾼 것
- **`collector/harvest-budget.mjs` 신설** — 예산·순서 회전을 **순수 함수**로 분리
  (학교에 접속하지 않고도 검사할 수 있게)
- **전역 예산 22분** (`HARVEST_BUDGET_MS`) — 학교를 새로 집기 전 남은 시간을 확인,
  모자라면 건너뛰고 **정상 저장 경로로 마무리**
- **상세 방문 학교당 4분** (`DETAIL_BUDGET_MS`) — 잘려도 공고는 목록에서 확보돼 저장되고
  마감·첨부만 다음 실행에서 채워짐
- **실패 학교 재시도를 순차 → 병렬 풀**
- **학교 순서 회전** (`collector/browser-cursor.json`) — 안 하면 잘리는 학교가
  **매번 설정 파일 뒤쪽으로 고정**돼 그 학교는 영영 안 돕니다.
  **커서를 워크플로 `git add` 목록에 넣는 것까지가 한 세트**입니다
- `timeout-minutes` 40 → **30**
- `verify/test-collector.mjs`에 **회귀 16항목** (가짜 시계라 인터넷 불필요)

### 결과 (실제 실행)
| | 전 | 후 |
|---|---|---|
| 시간 | 40분+ → 취소 | **7분 완료** |
| 학교 | 0곳 | **17/17곳** |
| 신규 공고 | 0건 | **50건** |

예산 확인을 지우거나 상한을 예산 아래로 낮추면 **검사가 실제로 실패하는 것까지 확인**했습니다.

---

## 2. ⚠️ 확인 부탁 — 두 예산이 서로를 모릅니다

Josehyeon님이 오늘 23:05에 `browser-collect.yml`에 **원문 보충 수집(`deepfetch --fill`)** 단계를
추가하셨는데, 제 수리(13:21)와 **서로 모르는 채로 합쳐졌습니다.**

두 안전장치가 각각은 잘 되어 있습니다:
- 제 쪽: 수집 스크립트 **22분** 예산
- Josehyeon 쪽: `--fill` 상한 120건 · 8초 대기 · 연속 3회 실패 시 건너뜀
  *(주석에 "예산을 다 먹으면 수집분이 통째로 버려진다"고 이미 적어 두셨더군요 — 정확한 인식입니다)*

**문제는 둘을 더했을 때입니다:**

| | 보통 | 최악 |
|---|---|---|
| 수집 스크립트 | 7분 | 22분 (예산 상한) |
| `deepfetch --fill` | 2~3분 | **16분** (120건 × 8초) |
| 설치·감사·저장 | 5분 | 5분 |
| **합계** | **~15분** ✅ | **~43분** ❌ **30분 상한 초과** |

보통은 문제없지만, **학교가 여러 곳 안 열리는 날 다시 취소될 수 있습니다.**
이 조합은 아직 한 번도 같이 돌아 본 적이 없습니다(제 실행 13:46 UTC → 그쪽 추가 14:05 UTC).

### 제안 — 셋 중 하나
1. **작업(job) 전체 마감 시각을 공유**하는 방식 *(가장 깔끔 — 두 단계가 같은 시계를 봄)*
2. 수집 예산을 22분 → **15분**으로 낮춤 *(간단하지만 학교가 늘면 또 빡빡함)*
3. `timeout-minutes`를 40으로 올리고 각 예산을 조임 *(임시방편)*

**1번을 권하지만, 이건 그쪽 담당 영역이라 상의 후에 하겠습니다.** 어떻게 할지 알려 주세요.

---

## 3. 선주님이 제안한 인프라 개편안

| 목표 | 방법 |
|---|---|
| 보안 강화 | 저장소를 **비공개**로 전환 |
| 실행 제한 극복 | **Oracle Cloud Always Free**에 자체 러너(self-hosted runner) 구축 |
| 역할 분리 | GitHub(저장소) · Cloudflare(배포·서버) · Oracle(봇·크롤링) |
| 서버 공동 관리 | 계정 공유 대신 `~/.ssh/authorized_keys`에 공동개발자 공개키 등록 |

---

## 4. 검토 결과

### ✅ 제안에서 정확한 것 — 두 가지는 제가 못 짚은 좋은 통찰입니다

1. **자체 러너는 GitHub 분(minutes)을 소모하지 않습니다.**
   → 비공개 전환의 걸림돌이던 월 2,000분 제한이 실제로 무의미해집니다.
2. **비공개 전환이 오히려 자체 러너를 안전하게 만듭니다.**
   → 공개 저장소에서 자체 러너는 심각한 위험(외부인이 서버에서 코드 실행)인데,
     비공개면 그 위험이 사라집니다. **두 변경이 서로를 돕습니다.**

역할 분리 자체도 교과서적으로 맞고, `_admin/`이 이미 그 구조입니다.

### ❌ 그대로 실행하면 문제가 되는 것 4개

#### 🔴 ① 비공개로 바꾸는 순간 앱이 즉시 내려갑니다
**GitHub Pages는 무료 플랜에서 비공개 저장소를 배포하지 않습니다**(Pro 이상 필요).
제안서 순서는 ①비공개 → ②Cloudflare 연동인데, **그 사이에 앱이 404가 됩니다.**
→ **앱1을 Cloudflare로 완전히 옮기고 정상 작동을 확인한 뒤에만** 비공개 전환.

#### 🔴 ② Oracle ARM 서버에서 브라우저 로봇이 안 돕니다
옮기려는 작업의 **89%가 브라우저 수집**인데,
**Playwright는 Linux ARM64용 Chromium을 제공하지 않습니다.**
Oracle의 쓸 만한 무료 인스턴스(Ampere A1)가 바로 ARM입니다.
- AMD 무료 인스턴스는 **1GB 램**이라 Chromium이 못 돕니다
- 우회 가능(ARM용 Docker 이미지 · 시스템 Chromium)하지만 **수집기 코드도 손봐야 합니다**

#### 🟡 ③ Oracle이 놀고 있는 서버를 회수합니다
7일간 CPU·네트워크·메모리가 **전부 10% 미만**이면 회수합니다(A1 한정).
우리 로봇은 **하루 2회 몇 분**만 도니 정확히 회수 대상입니다.

#### 🟡 ④ Oracle 무료 ARM이 2026년 6월부터 반토막
4 OCPU/24GB → **2 OCPU/12GB**. 지역에 따라 "용량 없음"으로 몇 주씩 생성이 안 되기도 합니다.

---

## 5. 그리고 가장 중요한 것 — 명분이 줄었습니다

개편의 근거는 *"월 2,000분 제한 때문에 봇이 못 돈다"* 였습니다.

| | 수리 전 | 수리 후 (실측 기반) |
|---|---|---|
| 브라우저 수집 | 월 790분 | **월 ~480분** |
| 전체 | **월 2,051분** ⚠️ 한도 초과 | **월 ~700분** ✅ 한도의 35% |

790분이었던 이유는 **로봇이 40~360분씩 매달려 있었기 때문**이지 일이 많아서가 아니었습니다.
그 원인을 오늘 고쳤습니다.

> **즉, 지금은 Oracle 없이도 비공개 전환이 가능합니다.**

Oracle은 여전히 가치가 있습니다(학교를 50~100곳으로 늘릴 때의 여유, 브라우저 시간 제한 없음).
다만 **지금 당장 필요하지는 않고**, 관리 부담(OS 업데이트·디스크·러너 버전·Chromium 갱신)이
계속 생깁니다.

---

## 6. 권하는 순서

| 단계 | 무엇 | 왜 이 순서인가 |
|---|---|---|
| 1 | 관리자 화면 → Cloudflare + Access | 위험 0 · 준비 끝남(`_admin/README.md`) |
| 2 | 푸시·메일 서버 → Cloudflare Workers | 앱 주소와 무관 · 준비 끝남 |
| 3 | **앱1 → Cloudflare Pages** (도메인 구입 후) | ⚠️ **비공개 전환 전에 반드시** |
| 4 | 앱1 정상 작동 확인 (며칠 관찰) | 되돌릴 수 있는 마지막 지점 |
| 5 | **저장소 비공개 전환** | 이제 안전 |
| 6 | *(선택)* Oracle 자체 러너 | 학교를 크게 늘릴 때 |

---

## 7. 개발자 결정 (2026-08-03 확정)

| 항목 | 결정 |
|---|---|
| 서버(푸시·메일·회원 처리) · 관리자 화면 | ✅ **Cloudflare로 간다** |
| **Oracle 자체 러너** | ⏸ **아직 쓰지 않는다** — 수리로 사용량이 한도의 35%로 내려가 지금은 불필요.<br>학교를 50~100곳으로 늘려 한도에 다시 닿을 때 재검토 |
| 앱1 이전 · 저장소 비공개 전환 | 미착수 — **순서 주의**(4-①: 앱1 이전·확인이 끝난 뒤에만 비공개) |

### 아직 답을 기다리는 것 — Josehyeon님께
**2절의 예산 충돌**을 어떻게 처리할지. 그때까지 수집 로봇은 손대지 않습니다.

---

## 부록 A. 이번 조사 실측값 (재조사 불필요 — `CLAUDE.md`에도 기록함)

| 항목 | 실측 |
|---|---|
| GitHub Actions 사용량 | 월 **2,051분** (13일 889분) → 수리 후 **~700분** |
| 그중 브라우저 수집 | 790분 = **89%** |
| 기본 브랜치 커밋 | 하루 평균 **17건** |
| 배포 파일 | 312개 · 최대 7.1MB |
| **앱 주소가 박힌 곳** | **8군데** (아래) |

**앱 주소 8군데** — 옮길 때 `data.js` 상수 하나로 모아야 합니다. 하나만 놓쳐도 **메일 접수·푸시가 조용히 막힙니다.**
`server/mail-worker.js`(CORS 2) · `server/push/worker.js`(APP_ORIGIN) ·
`.github/workflows/check-live.yml`(3) · `proposals/src/*.html`(2종 · PDF 재생성)

## 부록 B. Cloudflare 무료 한도 (2026-08 확인)

| 서비스 | 무료 | 우리 소요 | 판정 |
|---|---|---|---|
| Pages 빌드 | 월 500회 | 감시 경로 없으면 월 510회+ | ⚠️ **감시 경로 필수** |
| Pages 파일 | 2만 개 | 312개 | ✅ |
| Workers 요청 | 10만/일 | 하루 2회 | ✅ |
| Workers 외부 호출 | **호출당 50건** | 코드는 800건 가정 | ⚠️ **사용자 50명에서 막힘** |
| Access 사용자 | 50명 | 2명 | ✅ |
| **Browser Rendering** | **하루 10분** | **하루 61분** | ❌ **6배 부족 — 옮길 수 없음** |

## 부록 C. 이전 시 반드시 지켜야 할 것

1. **`_admin` 명시적 제외** — 지금 관리자 화면이 학생 눈에 안 보이는 근거는
   **GitHub이 밑줄 폴더를 배포 안 하는 것 하나뿐**입니다. Cloudflare엔 그 규칙이 없습니다.
2. **빌드 감시 경로** — 로봇이 하루 17번 커밋하므로 없으면 월 500회를 며칠 만에 씁니다.
   앱 파일 + `data/*`만. `collector/*` `verify/*` `tools/*` `*.md`는 **반드시 제외**.
3. **푸시는 배포 이전과 무관** — 막힌 건 ⓐ `server/push/` 미배포 ⓑ `push-config.js` 빈 칸
   둘뿐이고, **앱을 GitHub에 둔 채로도 켤 수 있습니다.** 단 구독은 **주소에 묶이므로**
   나중에 앱 주소를 바꾸면 기존 구독이 전부 무효가 됩니다(사용자 0명인 지금이 가장 쌉니다).
