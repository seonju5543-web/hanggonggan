# 한대장 — 공동 개발 진척도 (PROGRESS)

> **이 문서는 두 개발자가 함께 보는 단 하나의 진척도 표입니다.**
> 항상 `main` 브랜치에 있으며, 브라우저로 아무 때나 열람 가능:
> https://github.com/seonju5543-web/hanggonggan/blob/main/PROGRESS.md
>
> **읽는 법**: ① 아래 "현재까지 진척도"로 앱 전체 상태를 본다 → ② 자기 칸에서 내가 할 일을 본다 → ③ 상대 칸에서 상대가 뭘 하는지 본다(같은 파일 겹침 방지).
> **갱신 규칙**: 각자 작업을 끝내면 Claude에게 "PROGRESS.md 내 칸 업데이트해줘"라고 말한다. 매일 아침 로봇이 '활동 로그'를 자동 갱신한다.

---

## 📊 현재까지 진척도 (전체 · 두 사람 공통)

*(마지막 수동 갱신: 2026-08-12 — 실제 데이터를 세어 맞춘 숫자입니다)*

### 완성되어 배포 중
- **앱 배포(PWA)**: https://seonju5543-web.github.io/hanggonggan/ — `main` push 시 자동 배포
- **온보딩 5단계** · **매칭 엔진(12신호+적합도 점수)** · **진척도 4단계 트래커**
- **정식 등록 공고 175건**(사람이 검수한 79건 + 로봇이 자동 등록한 **검수 전 96건**)
  · **양식(신청서) 61종**(앱에서 바로 작성되는 공고 44건) · **실시간 공고 피드 605건**
- **수집 로봇 44개 캠퍼스 등록**(주소 확보 37곳 + 미확보 7곳) — 최근 정상 수집 기록이 있는 곳 **41곳**
- **진짜 푸시 가동 중**(앱을 안 켜도 폰에 도착) · **관리자 화면 가동 중**(2026-08-09 잠금·열쇠 완료)
- **원문 발췌 시스템**(추론 금지 원칙) · **자격 요건 자동 확보**(175건 전부 자격 정보 보유)
- 현재 서비스워커 캐시 버전: **v51**
- ✅ 로봇 건강 상태: 최근 실행 30회 **전부 성공**(수집·배포·푸시·링크 사냥꾼·잠금 감시 모두 정상)

### ⚠️ 지금 확인된 이슈 (2026-08-12 점검)
| # | 무엇이 문제인가 | 왜 중요한가 | 누가 |
|---|---|---|---|
| ~~1~~ | ~~관리자 화면에 잠금이 없다~~ | ✅ **2026-08-09 해결** — Cloudflare Access 적용, 검사로 확인됨 | 완료 |
| 2 | **자동 등록 96건이 '검수 전'인 채 사용자에게 보이는 중** (마감 안 지난 것 87건 · 그중 **일주일 안에 마감 5건**) | 금액·마감이 '원문 확인'으로만 표시돼 앱 신뢰도가 깎입니다. 8/9보다 9건 늘었습니다(로봇이 매일 등록하므로 **검수를 안 하면 계속 늘어납니다**) | 선주(컨펌) |
| 3 | **데이터 감사 경고 28건**(8/14 저녁 기준 · 낮에는 18건) — 목록 주소 **17건** · 양식 미연결 6건 · 기타 | 신청서를 앱에서 못 쓰거나 원문으로 바로 못 갑니다. ⚠️ **숫자가 늘어난 것은 나빠져서가 아닙니다** — 복구 로봇이 중앙대 링크 13건을 다시 열어 보고 *엉뚱한 글이 열린다*는 것을 확인해 목록 안내로 되돌렸습니다. 잘못된 링크를 정직한 안내로 바꾼 것이라 학생에겐 나아진 쪽입니다 | Claude |
| 4 | **신청서 양식 스키마화 대기 23건** (원본은 이미 받아 둠) + **원본을 3회 이상 못 받아 중단된 것 5건** | 다 하면 그만큼 '앱에서 작성' 공고가 늘어납니다 | Claude |
| ~~5~~ | ~~열린 이슈 102건~~ | ✅ **자동 정리 작동 확인 — 50건으로 줄었습니다**(대부분 최근 2주 수집 리포트) | 완료 |
| ~~6~~ | ~~4개 학교 수집이 멈춤~~ | ✅ **2026-08-09 오진으로 확인** — 옛 이름의 빈 기록이었고, 청소기 부착 후 재발 없음 | 완료 |
| **7** | **🔴 Claude API 잔액이 바닥난 것으로 보입니다 — 8/11부터 양식 자동 변환의 유료 경로가 멈춰 있습니다** | 8/10까지는 정상 호출됐는데(2회), 8/11·8/12 리포트에 `API 호출 실패: 400 … "Your cred…"`(잔액 부족 오류의 첫머리)가 찍혔고 오늘 실행은 **API 0건**입니다. **무료 경로는 그대로 돌아서**(오늘도 13건 자동 변환) 앱이 멈추지는 않지만, PDF·표 서식처럼 무료로 못 읽는 것은 계속 밀립니다 | 선주(충전 여부 결정) |

### 대기 중인 큰 항목(개발자 액션)
| 항목 | 하면 생기는 결과 | 담당 |
|---|---|---|
| Resend API 키 발급 | 완전 자동 이메일 접수 활성화 | 선주 |
| **Claude API 잔액 충전** (열쇠는 이미 들어가 있습니다 — 아래 이슈 7) | 로봇이 스스로 신청서를 양식화하는 기능이 **8/11부터 멈춰 있는 것**이 풀림 | 선주 |
| 미확보 게시판 주소 7곳 | 수집망 44 → 완전체 | 선주 |
| 스토어 배포 결정 | 앱 스토어 출시 | 선주 |
| 담당 영역 분담 합의 | 같은 코드를 서로 다르게 고치는 충돌 차단 | 두 사람 |

---

## 🧭 개발자 업무 정리 (2026-08-12 기준 · 여기부터 순서대로만 하면 됩니다)

> 지금 앱은 **혼자 잘 돌아가고 있습니다**(로봇 최근 30회 연속 성공). 남은 일은 대부분
> **"열쇠를 만들어 넣는 것"과 "예/아니오를 정해 주는 것"** 이고, 코드 작업은 Claude 몫입니다.
>
> **2026-08-14 — 가장 컸던 일(자동 등록 검수 105건)이 끝났습니다.** 검수 대기는 0건입니다.
>
> **남은 것은 결정 두 가지뿐입니다**: ⓐ Claude API 잔액을 채울지 ⓑ Resend에 가입할지.
> 둘 다 안 하셔도 앱은 지금 그대로 정상입니다.
>
> **시간 예산**: 5분(Claude 잔액) → 10분(Resend 가입). 나머지는 Claude가 합니다.

### ✅ 끝난 것 — 관리자 화면 가동 (2026-08-09)
| 순서 | 한 일 | 결과 |
|---|---|---|
| ~~①~~ | ~~관리자 화면 잠그기~~ | ✅ Cloudflare Access 적용 — 허용한 두 메일만 **일회용 코드**로 들어갑니다. 검사 버튼으로 **실제로 잠긴 것 확인** |
| ~~②~~ | ~~GitHub 열쇠 넣기~~ | ✅ 화면에 공고 표시(현재 **175건**) · 컨펌 버튼 작동 확인 |

> 🔑 **열쇠는 90일(2026-11-07)에 만료됩니다.** 그때 화면이 *"열쇠가 만료됐습니다"* 라고 알려 주면
> 같은 방법으로 새로 만들어 넣으면 됩니다(2분). **Cloudflare 로그인은 만료되지 않습니다.**
>
> 👥 **공동작업자는 열쇠를 따로 만들어야 합니다** — 함께 쓰면 변경 이력의 '누가' 칸이
> 전부 한 사람으로 찍혀 무의미해집니다. Josehyeon님은 `Tokens (classic)` · 범위 `repo`+`workflow`.
> (Cloudflare 쪽은 이미 두 분 메일이 허용돼 있어 바로 들어가집니다.)

### ✅ 끝난 것 — 자동 등록 검수 (2026-08-14, Claude가 전량 처리)

개발자 지시(*"직접 모두 검수한 다음 앱에 등록하고 결과만 알려줘"*)로 **105건을 전부 검수했습니다.**
**검수 대기는 이제 0건입니다.** 판단은 전부 저장된 공고 원문(105건 중 99건 확보)에 근거했고,
근거가 없는 건 지어내지 않았습니다.

| 판정 | 건수 | 무엇을 보고 정했나 |
|---|---|---|
| **승인** | **82** | 등록 규칙 통과 + 결과공지·대출·대학원·채용이 아님을 원문으로 확인 |
| 제외 — 신청할 수 없음 | 6 | 전부 **선발 결과 안내**였습니다. 원문에 "선발 대상자를 안내합니다"만 있고 신청 절차가 없습니다 |
| 제외 — 철 지난 공고 | 7 | 전부 부경대. **2014·2016·2020·2021·2024학년도** 공고가 목록 위에 올라와 있었습니다 |
| 제외 — 전국판과 겹침 | 10 | 사랑나눔·화성시·5·18 등, 여러 학교가 같은 재단 사업을 각각 올린 것 |
| 마감 채움 | 8 | 제목·본문에 **글자 그대로 적혀 있던 것만** 옮겼습니다 |

> 위 🔴 목록에 있던 *"일주일 안에 마감하는 5건"* 도 이 안에 들어 있습니다.
> 5·18(광운·이화)은 **전국판 하나로 합쳐** 모든 학교 학생에게 보이고, 아주대 면학장학생·
> 방송대 국가근로·충남대 AI교육은 승인됐습니다.

**정식 등록 184 → 161건 · 전부 검수 완료.** 제외한 23건은 다시 등록되지 않도록 막아 두었습니다.

### 🔴 검수하다 발견해 함께 고친 것 두 가지 (읽어 두시면 좋습니다)

**① 다른 공고의 자격 요건이 붙어 있었습니다 — 613건 중 177건이 영향권**
경북대 *세종이도인재장학금* 공고에 **코나아이 소상공인 공고의 자격 요건**이 붙어 있는 것을
발견했습니다. '이 주소가 같은 글인가'를 판단하는 규칙이 일부 학교 게시판에서 **서로 다른 글을
하나로 뭉개고** 있었습니다. 피해가 두 갈래였습니다 —
남의 공고 내용이 붙고, 그 학교의 **새 공고가 전부 '이미 등록됨'으로 조용히 건너뛰어졌습니다.**
규칙을 고쳐 **177건 → 0건**이 됐고, 잘못 붙어 있던 발췌 7건은 지웠습니다.

**② 학생이 못 채우는 신청서 5종이 앱에 올라가 있었습니다**
무료 변환기가 원본 표를 잘못 옮겨, 학생에게 이런 질문이 나가고 있었습니다 —
`초` `중` `고` `대`(대상구분 표가 질문 4개로 쪼개짐) · `점`(성적 __ 점의 단위) ·
동의 선택지가 `")"`. 한 건은 아예 **선발계획 공고문을 신청서로** 만들어 놨습니다.
**연결을 끊고 원본 첨부 다운로드 안내로 되돌렸습니다.** 원본은 그대로 받아 쓰실 수 있습니다.
다시 생기지 않도록, 만들어진 양식이 채울 수 있는 모양인지 **검사하는 관문을 새로 달았습니다.**

- 채팅으로 "리포트 ○○번 전부 컨펌"이라고 말하는 기존 방식도 그대로 됩니다(화면 없이도 가능).

### 🟡 2순위 — 열쇠·주소만 주면 나머지는 Claude·로봇이 하는 것
| 주실 것 | 걸리는 시간 | 그러면 생기는 일 | 비용 |
|---|---|---|---|
| **Claude API 잔액 충전** — 열쇠(`ANTHROPIC_API_KEY`)는 **이미 들어가 있습니다.** console.anthropic.com → Plans & Billing에서 잔액만 확인·충전 | 5분 | 8/11부터 멈춰 있는 **유료 변환 경로**가 다시 돌아 PDF·표 서식까지 앱 양식이 됩니다. **급하지 않다면 안 하셔도 앱은 정상** — 무료 경로가 매일 10건 안팎을 처리 중 | 실행당 2건 상한이 걸려 있어 소액 |
| **게시판 주소 7곳** — 연세 미래 · 고려(안암/세종) · 중앙 공통 · 동국 WISE · 부산대 · 계명대 | 주소 붙여넣기 | 그 학교 학생에게 실공고 노출(수집망 37 → 44곳 완전체) | 0원 |

> 💡 **둘 중 하나라면 Claude API 잔액**입니다 — 밀려 있는 양식 23건과 자격 미확보 76건을
> 사람 손 없이 줄이는 유일한 열쇠이고, **전부 처리해도 몇천 원 규모**입니다(근거: `docs/API-활용방안-2026-08-12.md`).
> **Resend 키는 3순위로 내렸습니다** — 아래 사유 참조.

### 🟢 3순위 — 결정만 해 주시면 되는 것 (급하지 않음)
- **Resend API 키** (개발자 지시로 2026-08-12에 2순위 → 3순위) — **순위는 그대로 두되, 근거가 바뀌었습니다.**
  · 8/12: 이메일 접수로 연결된 공고가 **0건**이라 키를 넣어도 발송할 대상이 없었습니다
    (7월의 3건 — 호반·외대 국가근로·불광 — 은 마감돼 8/2 정리 때 삭제).
  · **8/14: 후보 10건의 원문을 확인해 4건을 연결했습니다** — 시립대 NCCOSS · 서강대 KIAT ·
    인하대 변호산 · **홍익대 봉사장학생(마감 8/19)**. 이제 키를 넣으면 **그 4건이 즉시**
    '내용 복사하고 메일 열기'에서 **진짜 자동 발송**으로 바뀝니다.
  · 나머지 6건은 연결하지 않았습니다 — 메일 주소가 **문의처이거나 예외 상황용**이었고,
    "이메일로 제출"이라고만 쓰고 **제출 주소를 안 밝힌 공고 1건**(서울과기대 물리실험)은
    주소를 지어내지 않고 남겨 뒀습니다(추론 금지 원칙).
  · ⚠️ 인수인계 문서의 '이메일 접수 3건 대기 중'은 낡은 숫자입니다 — 지금은 **4건**.
- **스토어 배포**: 지금 웹으로 이미 쓸 수 있음 / Play $25 1회 / Apple $99 1년 → 어디까지 갈지
- **저장소 비공개 전환 + 앱 Cloudflare 이전** ⚠️ *순서 주의* — 이전·확인이 끝난 뒤에만 비공개
- **Josehyeon님과 담당 영역 분담** — 예: 선주=데이터·공고 등록 / Josehyeon=수집 로봇·인프라
  (공동작업자 칸에도 "남은 사람 몫은 이것 하나"로 적혀 있습니다 — **두 분이 한 번 정하면 끝납니다**)

### 🗓 잊으면 안 되는 기한
| 언제 | 무엇 | 안 하면 |
|---|---|---|
| **2026-11-07** | GitHub 열쇠(관리자 화면용) 90일 만료 → 같은 방법으로 재발급(2분) | 관리자 화면이 *"열쇠가 만료됐습니다"* 만 띄우고 컨펌을 못 합니다 |

### 🔧 Claude가 할 일 (지시 없이도 다음 세션에서 진행)
1. ~~**데이터 감사 경고 18건 해소**~~ — **2026-08-14 진행 결과(둘 다 무료로는 여기까지)**
   · **양식 미연결 6건 → 잔액 충전 전까지 불가**(무료 변환기로 실제 돌려 본 증거는
     `docs/API-활용방안-2026-08-12.md` 4-1절). 무료 변환기가 "성공"이라 답한 1건도
     인적사항이 통째로 빠져 **등록하면 원본과 다른 신청서가 나갑니다** — 그래서 안 했습니다.
   · **목록 주소 7건 → 원인 하나를 찾아 고쳤습니다.** 복구 로봇의 실행 파일이 8/1부터
     `onlyBoard: dongguk.edu`(그것도 점검만)로 **고정돼 있어**, 부산대 2건·중앙대 1건은
     이 로봇 관점에서 한 번도 시도된 적이 없었습니다. 이번에 부산대로 돌려 뒀습니다.
     동국대 4건은 사냥꾼이 매일 재시도 중이라 **일부러 뺐습니다**(같은 날 둘이 두드리면
     멀쩡한 주소가 404로 뒤집힙니다 — 7/31 실사고).
2. **신청서 양식 스키마화 대기 23건** — 원본은 이미 받아 뒀습니다. 무료 변환기가 매 수집마다
   조금씩 처리하고 있고(오늘 13건), **무료로 못 읽는 것만** 잔액 충전을 기다립니다.
3. **원본을 3회 이상 못 받아 중단된 5건** 점검 — 첨부 주소가 바뀌었는지·로그인이 필요한지 확인해
   되살릴 수 있는 것만 되살리기
4. **원문 링크 사냥꾼이 못 찾은 공고**(현재 1건) — 사냥꾼이 간격을 늘려 가며 재시도 중이라
   **손대지 않는 것이 맞습니다.** 같은 학교를 하루에 여러 번 두드리면 멀쩡한 주소까지 404가 나는
   실사고 기록이 있습니다.
5. 새로 수집된 공고 중 **사람 검수가 필요한 것 정리** — 개발자가 컨펌 화면에서 덜 헤매도록 미리 분류

> ✅ 8/9에 적혀 있던 Claude 몫 중 **끝난 것**: 관리자 화면 신뢰성·가독성 개편(전 단계), 멈춘 학교
> 오진 확인, 열린 이슈 정리(102 → **50건**, 자동 정리 워크플로가 매주 돕니다), 인수인계 문서 숫자 최신화.

---

## 🟦 선주(나) 개발 칸  — 브랜치: `claude/work-seonju`

### ✅ 개발 완료
- 앱 전체 기획·개발(온보딩·매칭엔진·양식엔진·트래커·수집 로봇·보안 체계) — 1~8차 세션 누적
- 정식 등록 78건 큐레이션 및 양식 28종 스키마화
- 공동작업자 초대 및 협업 환경(작업 브랜치 분리) 구축
- **앱 알림 시스템 (2026-07-31)** — ✅ **개발·검증·배포 완료** (main 반영, 사용자 앱에 나감)
  - 알림 5종: ① 내 조건에 맞는 새 장학 공고 ② 마감 하루 전·당일 ③ 제출 리마인드
    ④ 우리 학교 새 공고 ⑤ 결과 기록 리마인드 (앱이 실제로 아는 사실만 알림으로 만듦)
  - **최초 1회 동의 안내**(온보딩 직후 1번만, 나중에를 눌러도 다시 안 뜸) + MY에서 항목별 켜기/끄기
  - 홈 종 아이콘·안읽음 배지 · **앱 안 알림함**(휴대폰 알림을 거절해도 여기엔 쌓여 놓치지 않음)
  - 알림을 누르면 그 공고 상세가 바로 열림
  - ⚠️ **현재 전달 방식**: 발송 서버가 없어 **앱을 열 때·열어둔 동안** 확인해 띄우는 로컬 알림.
    토스·테무처럼 앱을 안 켜도 오는 방식은 **아래 '개발 중'의 진짜 푸시 작업**으로 해결 중.

- **진짜 푸시 — 🎉 2026-08-06 실제로 가동 시작 (폰 2대 도착 실증)**
  앱을 켜지 않아도, 화면이 꺼져 있어도 폰에 알림이 도착합니다(토스·테무와 같은 방식).
  - 발송 서버 주소: `https://handaejang-push.seonju5543.workers.dev` (Cloudflare 무료)
    상태 확인은 브라우저로 뒤에 **`/health`** 를 붙여 열면 됩니다
  - 서버는 **"확인해 봐"라고 폰을 깨우기만** 하고 알림 문구는 **폰 안에서** 만듭니다
    → 서버에 저장되는 건 **폰 주소·학교·캠퍼스뿐**. 이름·성적·소득·서류는 기기 밖으로 안 나감
  - **저장소와 연결돼 있어 코드를 고치면 자동으로 다시 올라갑니다**(Cloudflare Workers Builds)
  - 하루 2회(아침 8:10 · 저녁 8:10) 자동 실행 · 앱을 지운 폰은 자동 정리
  - **새 사용자는 따로 할 게 없습니다** — 온보딩 뒤 뜨는 안내에서 '알림 받기'만 누르면 자동 등록.
    (아이폰은 홈 화면에 설치해야 푸시가 옵니다 — 애플 정책)
  - 이 과정에서 **등록된 폰이 0대이던 버그**를 찾아 고쳤습니다: 등록이 '최초 1회 안내'에서만
    일어나서, 이미 알림을 켜 둔 사람은 영영 등록되지 않았습니다. 이제 앱을 열 때마다 스스로 메꿉니다.
    '앱을 켜지 않아도 받기' 스위치는 없앴고 **알림을 켜면 기본으로 켜집니다**(개발자 지시).
  - 검증 74항목 통과(암호 서명 실제 검증 + 개인정보 미유출 전수 검사 + 무료 한도 회귀)

- **푸시 등록이 조용히 사라지던 문제 — ✅ 2026-08-07 수리 완료 (두 폰 도착 재실증)**
  공동개발자 폰이 "앱을 켜야만 알림이 온다"던 문제입니다. 서로 겹친 결함 3개를 찾아 고쳤습니다.
  - **폰이 스스로 등록을 끊던 것** — 앱이 설정 장부를 못 읽었을 때 "사용자가 껐다"로 단정했습니다.
    이제 **확실히 읽었고 확실히 꺼져 있을 때만** 정리합니다.
  - **끊긴 뒤 되살아나지 못하던 것** — 앱이 자기 메모만 믿어 "아직 등록돼 있다"고 착각했습니다.
    이제 앱을 열 때마다 실제 상태를 확인하고, 하루에 한 번 서버와 대조합니다.
  - **죽은 등록이 서버에 쌓이던 것** — 폰 주소가 바뀌면 옛 등록을 그대로 뒀습니다. 이제 즉시 지웁니다.
  - **왜 아무도 몰랐나**: 서버 화면(KV)의 등록 수는 **'실제로 받는 폰 수'가 아닙니다.**
    그래서 **Actions 탭에 `푸시 알림 검사` 버튼**을 만들었습니다 — 누르면 등록된 모든 폰에
    시험 알림을 보내고 몇 대가 받았는지 알려 줍니다(30초). 앞으로 의심되면 이것만 누르면 됩니다.
  - 실증: `등록 2대 · 깨우기 성공 2대 · 죽은 등록 0` + 두 분 폰에 실물 도착 확인

- **관리자 화면 — 🔨 2026-08-07 Cloudflare에 올라감 (4단계 중 2단계 완료)**
  - 주소: **https://hanggonggan-admin.pages.dev** (열쇠를 넣기 전에는 아무 내용도 안 보입니다)
  - 빌드 감시 경로까지 설정 완료 — 로봇이 커밋해도 헛빌드가 돌지 않는 것을 실제로 확인했습니다
  - ✅ **2026-08-09 잠금·열쇠까지 완료 — 실제로 쓸 수 있는 상태입니다**

- **관리자 화면 — ✅ 2026-08-09 가동 시작 + 크게 손봄**
  · **잠금 완료**: `hanggonggan-admin.pages.dev`는 이제 **허용한 두 메일만** 일회용 코드로
    들어갑니다. 잠그기 전 검사에서 실제로 `HTTP 200`(그대로 열림)이 나왔고, 잠근 뒤 다시 재
    검사해 `로그인으로 넘김`을 확인했습니다.
  · **열쇠 완료**: 두 분 모두 발급. **선주님 열쇠는 2026-11-07 만료** — 그때 화면이
    '열쇠가 만료됐습니다'라고 알려 주면 같은 방법으로 새로 만들어 넣으시면 됩니다(2분).
    ⚠️ **열쇠는 함께 쓰면 안 됩니다** — 변경 이력의 '누가' 칸이 전부 한 사람으로 찍힙니다.
  · **잠금 감시 버튼 신설**: Actions 탭 → `관리자 화면 잠금 확인`. **매일 14:23에 저절로** 돌아서
    잠금이 조용히 풀리면 🚨 이슈로 알리고, 다시 잠기면 그 이슈를 스스로 닫습니다.

- **관리자 화면에 새로 생긴 기능 (2026-08-09)**
  · **여러 건 한 번에 처리** — 줄마다 네모를 체크하거나 `전체 / 마감 임박만 / 경고등 없는 것만`
    묶음 버튼. 고르면 위에 바가 뜨고 `검수 완료로 · 되돌리기 · 등록 삭제`를 한 번에.
    **누르면 바로 실행하지 않고 무엇을 처리하는지 목록을 펼쳐 보여 준 뒤** 확인받습니다.
  · **줄에서 바로 컨펌** — 문제 없는 공고는 시트를 열 필요 없이 오른쪽 버튼 하나로.
  · **화면에서 직접 등록** — 수집된 공고 옆 `등록하기`. 왼쪽에 앱에 나갈 내용, 오른쪽에 공고
    원문을 놓고 확인한 뒤 등록합니다. 예전엔 "등록 버튼은 다음 단계에 붙습니다" 상태였습니다.
    대출·대학원 전용·행사 같은 것은 **사람이 눌러도 규칙이 막습니다.**
  · **잘린 목록 '더 보기'** — 등록 후보는 286건 중 80건, 죽은 링크는 60건만 보이고
    나머지는 화면에서 **도달할 방법이 아예 없었습니다.**

- **관리자 화면 손보기 — ✅ 2026-08-09 (읽기 신뢰성 + 글자·조작)**
  화면을 열기 전에, **화면이 거짓말하던 곳 6군데**를 먼저 고쳤습니다.
  - **가장 위험했던 것**: 데이터를 못 읽어도 화면이 조용히 빈 값으로 바꿔서
    인터넷이 끊겨도 *"검수 전 0건 · 모든 게시판 정상"* 으로 보였습니다.
    이제 못 읽으면 **맨 위에 빨간 경고**가 뜨고, 전부 못 읽으면 아예 들어가지지 않습니다.
  - **수집망 화면에서 4개 학교가 늘 '—'로 보이던 것**(성균관·경희·외대·명지) — 고친 뒤
    네 곳 모두 정상으로 표시됩니다. 실제로는 잘 수집되고 있었는데 화면만 몰랐습니다.
  - **같은 숫자가 화면마다 다르던 것**(양식 대기 18건 vs 23건) → 한 곳에서만 셉니다.
  - 스크롤하면 작업 진행 띠가 탭 줄을 덮던 것, 로봇을 돌려도 옛 리포트가 보이던 것 등.
  - **글자와 버튼 크기**: 공고 제목 13.8→16px · 학교/기관 줄 11.9→14px ·
    회색 글자 대비 미달(밝은 화면에서만 흐릿하던 원인) 교정 · 손가락 목표 40px로 확대.
  - **'오늘 할 일'이 진짜 할 일만** 보여줍니다 — 0건인 초록 카드 9장이 첫 화면을 먹던 것을
    한 줄로 접었습니다. 필터 5줄도 접어서 목록이 바로 보입니다.
  - 검증 31항목 통과(브라우저로 실제 클릭). 학생 앱은 **한 줄도 바뀌지 않았습니다.**

### 🔨 개발 중 (지금 하고 있는 것)
- 없음 — 관리자 화면 개편 계획서의 **전 단계가 끝났습니다**. 아래 절 참조.

### ✅ 관리자 화면 개편 — 계획서 전 단계 완료 (2026-08-09)
> 8월 6일에 세운 개편 계획서에 적힌 항목이 **하나도 남지 않았습니다.** 아래는 이번에 마지막으로
> 붙인 것들입니다. **학생 앱은 여전히 한 줄도 바뀌지 않았습니다.**

| 새로 되는 일 | 예전에는 |
|---|---|
| **로봇 탭** — 로봇 12종을 화면에서 직접 돌리고, 열린 경보·리포트·푸시 서버 상태를 한자리에서 봅니다 | 화면에서 돌릴 수 있는 로봇이 3종뿐이었고, 로봇이 멈춰도 화면이 안 알려 줬습니다 |
| **키보드만으로 조작** — 목록 줄에서 Enter로 상세를 열고, 탭은 좌우 화살표로 넘깁니다. 화면 읽어 주기도 진행 상황을 읽습니다 | 마우스로만 상세를 열 수 있었습니다 |
| **정렬** — 마감 임박순·등록 최신순·학교순·제목순. 같은 것을 다시 누르면 방향이 뒤집힙니다 | 어디에도 정렬이 없었습니다 |
| **확인 화면** — 지우기·합치기까지 전부 "무엇을 하는지 눈으로 보여 준 뒤" 확인받습니다 | 하나씩 지울 때는 브라우저 기본 창이었습니다(무엇을 지우는지 안 보임) |
| **밝게/어둡게 버튼** | 준비만 돼 있고 버튼이 없었습니다 |
| **자격 요건 고치기** — 기계가 판정하는 값(학년·성적·소득)과 사람이 읽는 문장을 **따로** 고칩니다 | 읽기만 됐습니다 |
| **양식 대기 목록 조작** — 그만 시도 / 다시 받기 / 없는 양식 연결 끊기 | 알려만 주고 못 고쳤습니다 |
| **훑던 자리를 안 잃습니다** — 필터·정렬을 눌러도 스크롤·입력 중이던 글자가 그대로입니다 | 칩 하나 누르면 맨 위로 튀어서 166줄을 다시 훑어야 했습니다 |
| **기한 구획** — 마감 임박순으로 볼 때 `오늘·내일 / 이번 주 / 이번 달 …`로 나뉘어 눈이 멈출 자리가 생깁니다 | 166줄이 같은 높이·색으로 이어졌습니다 |
| **밀도 버튼(≡)** — 한 화면에 더 많은 줄을 담습니다(글자는 그대로) | 훑을 때도 검수할 때도 같은 밀도였습니다 |
| **좁은 화면의 표** — 옆으로 밀어야 하는 표에만 안내가 붙습니다 | 오른쪽 칸이 아예 없는 것처럼 보였습니다 |
| **시트 아래 고정 버튼** — 긴 상세에서도 저장·컨펌이 늘 화면 아래에 붙어 있습니다 | 끝까지 내려야만 저장할 수 있었습니다 |

> 검증 **102항목** 브라우저 실클릭 통과. 검사 중에 잡은 것: 표 안내가 **한 화면만 재는 바람에
> 한 번도 안 뜬 채 통과**하던 것(지금은 7개 화면 전부 재고, 넘치는 표가 실제로 있는지도 따로 확인).

### ⛳ 선주님이 직접 해야 하는 것
> **2026-08-09부터 이 목록은 문서 앞쪽 [🧭 개발자 업무 정리](#-개발자-업무-정리-2026-08-09-기준--여기부터-순서대로만-하면-됩니다)로 옮겼습니다.**
> 두 군데에 적어 두면 한쪽만 고쳐져 어긋나기 때문에, 앞으로 **할 일은 그 한 곳에서만** 봅니다.

| 끝난 것 | 결과 |
|---|---|
| ~~하루 뒤 `/health` 열어보기~~ | ✅ 2026-08-07 확인 — `lastError: null`, 이상 없음 |
| ~~관리자 화면 올리기~~ | ✅ 2026-08-07 게시 완료 — `hanggonggan-admin.pages.dev` |

| ~~관리자 화면 잠그기~~ | ✅ 2026-08-09 — Cloudflare Access, 검사로 확인 |
| ~~GitHub 열쇠 넣기~~ | ✅ 2026-08-09 — 화면에 166건 표시·작동 확인 |

| ~~자동 등록 검수~~ | ✅ 2026-08-14 — Claude가 105건 전량 검수. **대기 0건**(앞쪽 절 참조) |

**지금 남은 것은 '해야 할 일'이 아니라 '결정할 일'입니다** → 앞쪽 머리말의 두 가지(Claude 잔액 · Resend).

### 💡 구상 중 (앞으로 할 것)
- 관리자 화면(2단계) → 메일 접수 서버(3단계, Resend 키 필요)
- 앱1 Cloudflare 이전·저장소 비공개 전환 (⚠️ 순서 주의 — 이전·확인이 끝난 뒤에만 비공개)
- Resend / Claude API 키 활용 방향 결정
- 스토어(웹/Play/Apple) 배포 결정

---

## 🟩 공동작업자(Josehyeon) 개발 칸 — 브랜치: `claude/work-josehyeon`

### ✅ 개발 완료
- Claude Code + GitHub 연동, 한대장 저장소 연결 완료
- **협업 충돌 해소 체계 (2026-07-31)** — 두 사람 작업이 겹쳐 생기던 문제의 원인을 계측하고 조치.
  ① 로봇이 매일 쓰는 기록장(공고 목록·본 공고 장부 등)은 **충돌해도 자동으로 합쳐짐**
  ② 사람끼리 같은 파일을 만지려 하면 **세션 시작 때 경고**
  ③ `main`을 직접 고쳐도 **기본 브랜치로 자동 되가져옴**(만들기 전 실패 가능성 11가지 분석·차단,
     6가지 상황 실증, 실전 1회차 정상). 규칙·경위·분석표는 `COLLAB.md`.
  *(브랜치를 하나 더 만드는 방식은 불가 — 시간 예약 로봇은 기본 브랜치에서만 돕니다. COLLAB.md 1절)*
  *(`main` 브랜치 보호도 켜지 않는 게 맞습니다 — 배포 로봇이 함께 막혀 앱 업데이트가 멈춥니다)*
- **배포 동기화 충돌 재발 차단 (2026-08-01)** — 8/1 저녁 "앱 배포 동기화 실패" 🚨 2건(#85·#86) 확인.
  증상은 그날 손으로 풀려 있었지만(앱에 최신 공고 정상 노출) **원인 두 개가 남아 있어** 조치했습니다.
  ① 링크 사냥꾼 리포트만 **합치기 규칙이 빠져** 있던 것 — 형제 리포트 3종은 있는데 이것만 없어서
     양쪽이 달라질 때마다 배포가 멈췄습니다. 규칙 추가 + 같은 실수 반복 방지 경고 주석.
  ② `main`을 고친 걸 되가져오는 로봇에 **보정 예약이 없던 것** — 로봇이 올린 변경은 이 로봇을
     깨우지 못해 조용히 어긋난 채 남습니다(그게 다음 충돌의 씨앗). 하루 2회 보정 예약 추가.
  8/1 충돌 상황을 재현해 **고치기 전 충돌 / 고친 뒤 자동 해소** 확인. 이슈 #85·#86 닫음.
- **자격 요건 자동 확보 (2026-08-03)** — 앱에서 '지원 자격'이 보이는 공고 **54 → 83건**.
  그동안 자격이 잘못 나올 때마다 그 공고 하나를 보고 규칙을 고쳤더니, 고칠 때마다 다른 공고가
  샜습니다. 이번엔 **실패 84건을 전부 세어 원인을 나눴고**, 규칙 문제가 아니었습니다:
  ① 수집기가 공고 원문을 **5,000자에서 잘라** 정작 '지원자격' 절이 잘려 나갔습니다
     (잘린 12건을 실제로 다시 받아 보니 5건이 자격 절을 5,300~6,200자 지점에 두고 있었습니다).
  ② 원문 파일을 매번 새로 만드는 바람에, 실시간 공고가 60일 뒤 목록에서 지워질 때
     **정식 등록 장학금의 원문까지 같이 사라지고**, 그러면 앱이 이미 뽑아 둔 자격을 지웠습니다.
  → 원문을 15,000자까지 받고, 등록 공고 원문은 보존하며, 없거나 잘린 것은 매일 자동 보충합니다.
  **`node verify/eligibility-report.mjs`** 를 새로 만들어 146건을 한 번에 채점합니다 —
  앞으로 규칙을 고칠 때 "몇 건 좋아지고 몇 건 나빠졌나"를 숫자로 보면 되고, 공고 하나씩
  고치는 일은 하지 않아도 됩니다. (무료 경로만 사용 — API 비용 0원, 개발자 결정)
  **남은 63건**은 자격이 줄글·표에 섞여 있거나(51) 학교 사이트 접속이 안 되는(12) 경우입니다.
  못 읽은 공고는 "지원 자격을 아직 읽지 못했어요"로 정직하게 표시됩니다.

### 🔨 개발 중 (지금 하고 있는 것)
- *(없음 — 위 작업 완료·배포됨)*

### 📌 확인만 하고 넘긴 것 (다음 세션 참고)
- **링크 사냥꾼 실패**(낱말 하나 틀려 로봇이 넘어지며 찾아낸 주소 13건이 통째로 버려짐) →
  이미 수정됨(넘어져도 저장하는 장치 추가). 그 뒤 3회 연속 성공.
- **데이터 감사 실패**(없는 양식을 가리키는 연결 때문 — 8/1에 2회 연속 발생) → 이미 수정됨
  (저장 전 자동 정리 + 감사를 스키마화 뒤로 이동). 데이터는 현재 깨끗(끊어진 연결 0건).
  단 **수정이 기본 브랜치에 들어온 19:52 이후로 수집이 아직 안 돌아 실전 증명은 다음 실행이 처음.**
- ⚠️ **남은 흠**: 감사 실패 알림 문구가 항상 "이번 자동 등록분은 되돌렸습니다"로 고정이라,
  실제로 아무것도 안 되돌렸을 때도 그렇게 알립니다(8/1 실제 사례 — 로그는 "되돌릴 것 없음,
  기존 데이터 문제"였음). 대응이 달라지는 문제라 문구를 실제 결과에 맞게 가르는 게 좋습니다.

### 💡 구상 중 (앞으로 할 것)
- **선주님과 담당 영역 분담 합의** — 예: 선주=데이터·공고 등록 / Josehyeon=수집 로봇·인프라.
  자동 병합은 데이터만 해결하고, 같은 코드를 서로 다르게 고치는 건 분담만이 막습니다.
  **지금 남은 사람 몫은 이것 하나입니다.**

---

## 🤖 활동 로그 (매일 아침 로봇이 자동 갱신 — 손대지 마세요)

<!-- AUTO-ACTIVITY-START -->
*(자동 갱신: 2026-07-28 23:34 KST · 최근 7일)*

**🟦 선주** (`claude/work-seonju`) — 본인 작업 0커밋
- 아직 이 브랜치의 개별 작업 없음

**🟩 공동작업자** (`claude/work-josehyeon`) — 본인 작업 0커밋
- 아직 이 브랜치의 개별 작업 없음

<!-- AUTO-ACTIVITY-END -->

---

## 📎 협업 규칙 (충돌 방지) — 자세한 규칙과 이유는 [`COLLAB.md`](COLLAB.md)
1. 각자 **자기 브랜치**에서 작업(`work-seonju` / `work-josehyeon`).
2. **같은 파일을 동시에 만지지 않기** — 특히 `data/registered.json`·`data/forms.json`(사람이 큐레이션).
   `data/notices.json`처럼 **로봇이 쓰는 파일은 이제 자동으로 합쳐지므로** 신경 쓰지 않아도 됩니다(2026-07-31).
3. **`main`을 직접 고치지 않기** — `main`은 앱이 배포되는 곳입니다. 고칠 게 있으면 기본 브랜치에서.
   (실수로 고쳐도 되가져오기 로봇이 자동으로 기본 브랜치에 반영합니다.)
4. 배포하려면 Claude에게 "내 브랜치 작업을 main에 반영·배포해줘".
5. 이 문서(`PROGRESS.md`)는 **항상 main에** 커밋 — 두 사람이 같은 최신본을 보도록.
6. 작업 시작 때 경고가 뜨면 그것부터 처리 (`node verify/check-collab.js`).
