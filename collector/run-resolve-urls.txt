# 원문 링크 복구 로봇 실행 트리거 (push-to-run)
#
# onlyBoard:   게시판 하나만 (주소 조각). 줄이 없으면 전체.
# recheckOnly: true면 '이미 고친 주소가 아직 그 공고로 가는지'만 점검(게시판을 덜 두드림).
onlyBoard: cau.ac.kr
recheckOnly: false

run: 2026-08-14 — 중앙대 1건 (동문장학재단)
  부산대 결과를 보고 넘어왔다. 아래 기록 참조.

run: 2026-08-14 00:01 KST — 부산대 2건 → **복구 0건. 이 로봇으로는 안 되는 게시판이다.**
  🔴 다음 세션이 같은 시도를 반복하지 말 것. 리포트에 원인이 그대로 남아 있다:
  · "목록 1페이지에서 행 0개 · 후보 주소 0개 생성" — 이 로봇은 목록 HTML의 행을 읽어
    게시판 폼으로 주소를 조립하는데, **부산대는 행이 `<a href="#popup">`인 클릭형**이라
    읽을 행 자체가 없다(2026-08-07에 브라우저 수집기에 `a[href^="#"]`를 추가해 고친 그 유형).
  · 소급 재검사 9건이 전부 HTTP 404 → 안전장치가 "게시판 목록조차 안 열린다"로 보고
    **아무것도 되돌리지 않았다.** 이게 설계대로 작동한 것이다(멀쩡한 링크를 지키는 쪽).
  → 부산대 2건은 **링크 사냥꾼 담당**이다(행을 실제로 눌러 주소를 받아 적는 방식).
     사냥꾼 기록(`link-hunt.json`)에 이미 부산대 항목이 있어 재시도 중이다.

run: 2026-08-01 01:30 KST — 확인 못 한 17건 표식으로 되돌리기
  형태 교정 뒤 44건 중 27건이 통과했고, 404는 완전히 사라졌다. 남은 17건은
  '읽었는데 그 공고가 아님'(제목 불일치 11 · 목록 화면 6)이라 주소가 틀린 것이 확실하다.
  안전장치가 '실패 비율'만 보고 이것까지 막고 있었는데, '못 읽음'과 '읽었는데 다른 화면'을
  구분하도록 고쳤다. 이번 실행에서 그 17건이 표식으로 되돌아가면, 앱은 엉뚱한 글 대신
  '게시판 목록 ↗'을 정직하게 안내한다.
  ⚠️ 이 뒤로 파일이 `onlyBoard: dongguk.edu` + `recheckOnly: true`로 **2주 넘게 고정**돼
  주 1회 예약 실행이 동국대만 점검하고 있었다(2026-08-14에 발견). 표적을 바꾼 뒤에는
  다음 세션이 볼 수 있도록 이 기록을 남길 것.
