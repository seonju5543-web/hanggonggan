# 원문 링크 복구 로봇 실행 트리거 (push-to-run)
# 이 파일을 아무렇게나 고쳐 push하면 resolve-detail-urls.yml이 실행됩니다.
# 하는 일: 앱에 담긴 공고 중 주소가 '게시판 목록 + 제목 표식(#n-…)'인 것을 찾아
#          진짜 공고 원문 주소로 바꿉니다.
run: 2026-07-31 3차 실행 — 실행 시간 상한 25분·게시판별 규칙 재사용
  2차 실행이 50분을 넘겨 끝나지 않았다. 원인은 공고마다 후보 주소를 매번 열어 확인하고,
  실패하면 한 번 더 열어 본 것. 같은 게시판 안에서는 주소 만드는 규칙이 같으므로
  두 번 통하면 그 뒤로는 믿는다. 시간을 넘기면 그때까지 고친 것만 저장하고 끝낸다
  (매주 도는 로봇이라 다음 실행이 이어서 고친다).
