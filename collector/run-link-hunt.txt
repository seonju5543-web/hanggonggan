# 링크 사냥꾼 실행 트리거 (push-to-run)
#
# ⭐ 이 로봇의 목적: 다른 수집 로봇이 못 잡은 원문 공고를 **어떻게든** 찾아서 올려둔다.
#    특정 학교에 묶지 말 것 — 기본이 전체 학교다.
#
# onlyBoard: (선택) 게시판 하나만. 줄이 없으면 전체 학교 ← 기본값
# maxPages:  목록을 몇 페이지까지 넘겨볼지 (기본 10)
# patrol:    (덤) 사냥이 끝나고 시간이 남을 때만, 멀쩡해 보이는 기존 링크를 몇 개
#            다시 열어 볼지. 기본 20 · **0이면 순찰을 아예 끈다.**
#            순찰에서 걸린 건은 이번 실행에서 고치지 않고 다음 실행의 사냥 대상이 된다.
maxPages: 20
patrol: 20

run: 2026-08-01(3회차) — 데이터 관문이 막고 있던 오류를 고치고 재실행
  2회차를 띄워 놓고 전면 점검을 하다가, 감사(audit-data)가 실패 상태인 것을 발견했다:
  reg-hufs-namgaju가 없는 양식(auto-남가주동문-apply-2)을 가리키고 있었다.
  감사가 실패하면 워크플로가 '되돌리기'를 해서 **사냥 결과를 저장하지 않는다** —
  2회차는 끝까지 가도 헛수고였을 것이라 취소하고 3회차로 다시 돈다.

이전 실행: 2026-08-01(2회차) — 남은 15건 사냥 · 넘어져도 저장하도록 고친 뒤 재실행
  1회차는 4분간 원문 주소 13건을 찾아 놓고 **리포트 마지막 줄에서** 넘어져(옛 이름
  GIVE_UP_AFTER를 한 군데 못 고쳤다) 저장 단계가 통째로 건너뛰어져 전부 버려졌다.
  고친 것 3가지: ① 그 줄 ② 넘어져도 그때까지의 결과를 저장하고 리포트에 자리를 남긴다
  ③ '선언 없는 이름'을 실행 전에 잡는 검사(verify/test-collector.mjs)를 상설화했다.

이전 실행: 2026-08-01 — 남은 15건 사냥 (동국대 12 · 경희대 3)
  전체 학교 대상으로 돈다. 3단계 구조 첫 실전:
    1단계 순찰 — 기존 링크 60건을 다시 열어 학생이 못 보는 것을 찾아낸다
                (경희대 로그인 벽 교정이 실제로 통했는지도 여기서 확인된다)
    2단계 사냥 — 표식 15건을 게시판에서 눌러 원문 주소를 찾는다
    3단계 끈질기게 — 못 찾으면 같은 학교 다른 게시판 + 학교 사이트 검색까지
  시작 전에 TDZ 버그를 하나 고쳤다: 순찰 단계를 넣으면서 today 선언이 사용처보다
  아래에 남아 로봇이 시작하자마자 죽는 상태였다.
