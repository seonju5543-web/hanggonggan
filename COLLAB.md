# 함께 작업할 때 부딪히지 않는 법 (COLLAB)

> 2026-07-31 신설. 공동작업자(Josehyeon)의 질문 — *"작업이 자꾸 겹쳐 문제가 생긴다.
> 하이젠버그 브랜치를 하나 더 만들어 각자 쓰면 충돌이 안 되지 않을까?"* — 에 대한 답과 조치.

---

## 1. 질문에 대한 답: 브랜치를 하나 더 만드는 건 오히려 위험합니다

아이디어의 방향(**따로 쓰자**)은 맞습니다. 다만 **복사해서 나누는 방식**은 이 저장소에서
반드시 실패합니다. 이유가 세 가지 있습니다.

**① 매일 도는 로봇은 '기본 브랜치' 딱 하나에서만 돕니다.**
GitHub은 시간 예약(매일 07:41 수집 등)으로 도는 로봇을 **저장소의 기본 브랜치 한 곳에서만**
실행합니다. 기본 브랜치는 정의상 하나뿐이라, 두 번째 하이젠버그 브랜치를 만들어도
그쪽에서는 **수집 로봇이 영영 돌지 않습니다.** 한쪽은 로봇이 있고 한쪽은 없는,
더 헷갈리는 상태가 됩니다.

**② 설령 둘 다 돌릴 수 있다 해도, 같은 공고를 두 번 수집하게 됩니다.**
로봇은 `collector/seen.json`(이미 본 공고 장부)을 보고 "이건 어제 봤으니 넘어가자"를
판단합니다. 브랜치가 둘이면 장부도 둘로 갈라져서, 한쪽 로봇이 본 공고를 다른 쪽 로봇은
못 봤다고 여겨 **같은 공고를 새 공고로 다시 담습니다.**
이건 이미 겪은 문제입니다 — 이슈 #75(서울시립대 피드 40건 중 실제 공고는 13건뿐,
중복이 자리를 채워 진짜 새 공고가 밀려남).

**③ 나중에 두 브랜치를 합칠 때 충돌이 지금보다 늘어납니다.**
갈라져 있던 기간이 길수록 양쪽 데이터가 서로 멀어지므로, 합칠 때 부딪히는 양이 커집니다.
지금은 하루치가 부딪히지만, 그때는 그동안 쌓인 전부가 부딪힙니다.

**그래서 나누는 축을 바꿨습니다: 브랜치 복사가 아니라 '누가 무엇을 쓰나(파일 소유권)'로.**

---

## 2. 진짜 원인은 무엇이었나 (최근 100커밋을 세어 본 결과)

가장 자주 바뀐 파일 순서입니다.

| 순위 | 파일 | 변경 횟수 | 누가 쓰나 |
|---|---|---|---|
| 1 | `data/notices.json` (공고 목록) | 45 | 🤖 로봇 |
| 2 | `collector/seen.json` (본 공고 장부) | 36 | 🤖 로봇 |
| 3 | `collector/browser-report.md` (리포트) | 28 | 🤖 로봇 |
| 4 | `data/registered.json` (정식 등록) | 19 | 🤖 + 🧑 |
| 5 | `CLAUDE.md` (인수인계 문서) | 18 | 🧑 사람 |
| … | `app.js` (앱 코드) | 5 | 🧑 사람 |

**충돌의 대부분은 사람이 쓴 코드가 아니라 로봇이 매일 새로 쓰는 기록장에서 났습니다.**
그리고 이런 기록장은 사실 *"누구 것이 맞나"를 고를 필요가 없습니다.* 양쪽 다 맞고,
**둘을 합치면 되는 것**입니다. 그런데 git은 파일 내용의 뜻을 모르니 줄 단위로만 보고
"여기 둘 다 고쳤는데요?" 하며 멈춰 섰고, 그때마다 사람이 손으로 합쳐야 했습니다.

여기에 사람끼리의 진짜 겹침이 하나 더 있었습니다 — **7/31 저녁 `browser-collect.yml` 사건**.
두 사람이 같은 파일을 25분 간격으로 서로 모르는 채 고쳤고(한쪽은 GitHub 화면에서 직접,
한쪽은 Claude 세션에서), 다음 세션이 '판 통합'에 시간을 썼습니다.

---

## 3. 그래서 무엇을 했나 (2026-07-31 조치)

### ① 로봇 기록장은 이제 **자동으로 합쳐집니다**
`data/notices.json`·`collector/seen.json`·`collector/pending-forms.json`·`collector/health.json`이
양쪽에서 달라져도, 전용 병합기(`tools/merge-json-union.mjs`)가 **둘을 합집합으로 합칩니다.**
수집기와 **같은 중복 제거 규칙**(`collector/url-key.mjs`)을 쓰므로 규칙이 갈라질 수 없습니다.

- 실증: 두 사람이 각자 새 공고를 담고 같은 공고를 다른 주소로 본 상황을 실제로 만들어
  병합해 봤습니다 → 충돌 0건, 공고 둘 다 살아남고, 같은 공고 중복은 1건으로 정리됨.
- 매 실행 새로 쓰이는 리포트(`*-report.md`)는 **내 쪽을 그대로 둡니다**(다음 수집이 어차피 덮어씀).
- **일부러 제외한 것**: `data/registered.json`·`data/forms.json`. 여기서는 '삭제'가 뜻을
  가지므로(잘못 등록한 공고 제거 등) 자동 합집합이 지운 항목을 되살릴 수 있습니다.
  이 둘은 **사람이 직접 보고 합쳐야 합니다.**

### ② 겹칠 것 같으면 **작업 시작 전에** 알려줍니다
Claude 세션이 시작될 때 자동으로 `verify/check-collab.js`가 돌면서 이렇게 알립니다.

- 상대가 최근에 만졌는데 아직 내가 못 받은 파일을 나도 고치려 하면 🚨
- 앱 배포 브랜치(`main`)에만 있고 기본 브랜치엔 없는 작업이 있으면 🚨
  (= 누군가 GitHub 화면에서 `main`을 직접 고침 → 배포 로봇이 매번 여기서 충돌)
- 내 브랜치가 기본 브랜치보다 뒤쳐져 있으면 🚨

작업을 끝내고 올리기 전에 한 번 더 돌리면 좋습니다: `node verify/check-collab.js`

### ③ 로봇도 같은 장치를 씁니다
수집 로봇 3종·배포 동기화 로봇이 저장하기 전에 병합기를 등록하도록 워크플로에 넣었습니다.
그동안 배포 동기화가 충돌로 멈추고 🚨 이슈만 남기던 상황이 크게 줄어듭니다.
진척도 문서 로봇에도 재시도를 넣었습니다(예전엔 그 사이 `main`이 움직이면 그냥 실패 →
그날 활동 로그가 비었습니다).

---

## 4. 앞으로의 규칙 (두 사람 공통)

### 지켜야 할 것 세 가지

1. **`main`을 직접 고치지 않습니다.**
   `main`은 *앱이 배포되는 곳*이지 작업하는 곳이 아닙니다. GitHub 화면에서 `main`의 파일을
   연필 아이콘으로 고치면, 그 변경은 기본 브랜치에 없어서 배포 로봇이 매번 그 지점에서
   부딪힙니다. 고칠 게 있으면 **기본 브랜치(`claude/nice-heisenberg-WESq5`)에서** 고치세요 —
   거기서 `main`으로는 로봇이 자동으로 옮겨 줍니다.

2. **작업 시작 전에 최신 것을 받습니다.**
   Claude에게 *"작업 시작 전에 최신 받아줘"* 라고 하면 됩니다. 세션 시작 훅이 겹침 경고를
   자동으로 띄우므로, 경고가 보이면 그때 합치고 시작하세요.

3. **누가 무엇을 맡는지 `PROGRESS.md`에 적습니다.**
   자기 칸의 '🔨 개발 중'에 지금 하는 일을 한 줄 적어 두면, 상대 세션이 그걸 읽고
   같은 파일을 피합니다. **이게 자동 장치보다 효과가 큽니다** — 자동 병합은 데이터만
   해결하지, 같은 코드를 서로 다르게 고치는 건 못 막습니다.

### 겹치기 쉬운 파일 (동시에 만지지 않기로 한 것)

| 파일 | 성격 | 동시 작업 시 |
|---|---|---|
| `.github/workflows/*.yml` | 로봇 설정 | ⚠️ 한 사람씩 — 7/31 사건이 여기서 남 |
| `app.js` `index.html` `style.css` | 앱 화면·엔진 | ⚠️ 한 사람씩 |
| `data/registered.json` `data/forms.json` | 사람이 큐레이션 | ⚠️ 한 사람씩 (자동 병합 제외) |
| `CLAUDE.md` | 인수인계 문서 | ⚠️ 세션 끝에 각자 자기 절만 |
| `PROGRESS.md` | 진척도 | ✅ 자기 칸만 고치면 안전 |
| `data/notices.json` `collector/seen.json` 등 | 로봇 기록장 | ✅ 자동으로 합쳐짐 |

---

## 5. 알아 두면 좋은 사실

- **두 사람의 커밋은 이름으로 구분되지 않습니다.** 둘 다 Claude 세션으로 작업하므로 커밋
  작성자가 똑같이 `Claude <noreply@anthropic.com>`로 찍힙니다. 그래서 겹침 검사는 사람이
  아니라 **브랜치**를 기준으로 봅니다. 진척도 문서의 활동 로그도 브랜치로 구분합니다
  (선주 = `claude/work-seonju`, Josehyeon = `claude/work-josehyeon`).
- **브랜치는 이 흐름으로만 움직입니다.**
  `내 세션 브랜치` → `기본 브랜치(claude/nice-heisenberg-WESq5)` → (배포 로봇) → `main` → 앱
  기본 브랜치는 *로봇이 사는 집*이고, `main`은 *앱이 나가는 문*입니다. 사람은 자기 브랜치에서
  작업해 기본 브랜치로 보냅니다.
- **개발자가 결정해 주면 더 좋아지는 것**은 이 문서 아래 '개발자 액션'에 있습니다.

---

## 6. 개발자 액션 (선주·Josehyeon 두 분 합의 필요)

| 할 일 | 하고 나면 | 왜 사람이 해야 하나 |
|---|---|---|
| GitHub 저장소 설정에서 **`main` 브랜치 보호** 켜기 (Settings → Branches → Add rule → `main`, "Restrict updates" 정도만) | `main` 직접 수정이 아예 막혀 규칙 4-1이 실수로도 깨지지 않음 | 저장소 관리자 권한이 필요 — Claude가 못 함 |
| 두 분이 **담당 영역 나누기** (예: 선주=데이터·공고 등록, Josehyeon=수집 로봇·인프라) | 같은 파일을 동시에 만질 일 자체가 사라짐 | 두 사람의 합의 사항 |

---

## 7. 문제가 생기면

```
node verify/check-collab.js        # 지금 부딪힐 곳이 있나
node verify/check-deploy-sync.js   # 내 작업이 앱에 나가 있나
bash tools/setup-collab.sh         # 자동 병합기 다시 등록 (여러 번 해도 안전)
```

충돌이 나서 화면에 `<<<<<<<` 같은 표시가 보이면, **그대로 두고 Claude에게 알려주세요.**
어느 쪽을 살려야 하는지는 파일마다 다르고, 잘못 고르면 한쪽 작업이 사라집니다.
