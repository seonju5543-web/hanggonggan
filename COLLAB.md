# 함께 작업할 때 부딪히지 않는 법 (COLLAB)

> 2026-07-31 신설. 공동작업자(Josehyeon)의 질문 — *"작업이 자꾸 겹쳐 문제가 생긴다.
> 하이젠버그 브랜치를 하나 더 만들어 각자 쓰면 충돌이 안 되지 않을까?"* — 에 대한 답과 조치.
>
> **2026-08-24 갱신 — CTO 유은서 합류로 세 사람 체제.** 아래 "두 사람"이라 적힌 곳은 전부
> **세 사람**으로 읽는다. 브랜치는 선주=`claude/work-seonju` · Josehyeon=`claude/work-josehyeon` ·
> **유은서=`claude/work-eunseo`(신설)**. 겹침 감지(`check-collab.js`)는 브랜치 개수를 하드코딩하지
> 않고 원격의 모든 브랜치를 자동으로 훑으므로 코드 변경 없이 그대로 작동한다.

---

## 1. 질문에 대한 답: 브랜치를 하나 더 만드는 건 오히려 위험합니다

아이디어의 방향(**따로 쓰자**)은 맞습니다. 다만 **복사해서 나누는 방식**은 이 저장소에서
반드시 실패합니다. 이유가 세 가지 있습니다.

**① 매일 도는 로봇은 '기본 브랜치' 딱 하나에서만 돕니다.**
GitHub은 시간 예약(매일 07:41 수집 등)으로 도는 로봇을 **저장소의 기본 브랜치 한 곳에서만**
실행합니다. 기본 브랜치는 정의상 하나뿐이라, 두 번째 하이젠버그 브랜치를 만들어도
그쪽에서는 **수집 로봇이 영영 돌지 않습니다.** 한쪽은 로봇이 있고 한쪽은 없는,
더 헷갈리는 상태가 됩니다.

**② 설령 둘 다 돌릴 수 있다 해도, 같은 공고를 두 번 수집하게 됩니다.**
로봇은 `collector/seen.json`(이미 본 공고 장부)을 보고 "이건 어제 봤으니 넘어가자"를
판단합니다. 브랜치가 둘이면 장부도 둘로 갈라져서, 한쪽 로봇이 본 공고를 다른 쪽 로봇은
못 봤다고 여겨 **같은 공고를 새 공고로 다시 담습니다.**
이건 이미 겪은 문제입니다 — 이슈 #75(서울시립대 피드 40건 중 실제 공고는 13건뿐,
중복이 자리를 채워 진짜 새 공고가 밀려남).

**③ 나중에 두 브랜치를 합칠 때 충돌이 지금보다 늘어납니다.**
갈라져 있던 기간이 길수록 양쪽 데이터가 서로 멀어지므로, 합칠 때 부딪히는 양이 커집니다.
지금은 하루치가 부딪히지만, 그때는 그동안 쌓인 전부가 부딪힙니다.

**그래서 나누는 축을 바꿨습니다: 브랜치 복사가 아니라 '누가 무엇을 쓰나(파일 소유권)'로.**

---

## 2. 진짜 원인은 무엇이었나 (최근 100커밋을 세어 본 결과)

가장 자주 바뀐 파일 순서입니다.

| 순위 | 파일 | 변경 횟수 | 누가 쓰나 |
|---|---|---|---|
| 1 | `data/notices.json` (공고 목록) | 45 | 🤖 로봇 |
| 2 | `collector/seen.json` (본 공고 장부) | 36 | 🤖 로봇 |
| 3 | `collector/browser-report.md` (리포트) | 28 | 🤖 로봇 |
| 4 | `data/registered.json` (정식 등록) | 19 | 🤖 + 🧑 |
| 5 | `CLAUDE.md` (인수인계 문서) | 18 | 🧑 사람 |
| … | `app.js` (앱 코드) | 5 | 🧑 사람 |

**충돌의 대부분은 사람이 쓴 코드가 아니라 로봇이 매일 새로 쓰는 기록장에서 났습니다.**
그리고 이런 기록장은 사실 *"누구 것이 맞나"를 고를 필요가 없습니다.* 양쪽 다 맞고,
**둘을 합치면 되는 것**입니다. 그런데 git은 파일 내용의 뜻을 모르니 줄 단위로만 보고
"여기 둘 다 고쳤는데요?" 하며 멈춰 섰고, 그때마다 사람이 손으로 합쳐야 했습니다.

여기에 사람끼리의 진짜 겹침이 하나 더 있었습니다 — **7/31 저녁 `browser-collect.yml` 사건**.
두 사람이 같은 파일을 25분 간격으로 서로 모르는 채 고쳤고(한쪽은 GitHub 화면에서 직접,
한쪽은 Claude 세션에서), 다음 세션이 '판 통합'에 시간을 썼습니다.

---

## 3. 그래서 무엇을 했나 (2026-07-31 조치)

### ① 로봇 기록장은 이제 **자동으로 합쳐집니다**
`data/notices.json`·`collector/seen.json`·`collector/pending-forms.json`·`collector/health.json`이
양쪽에서 달라져도, 전용 병합기(`tools/merge-json-union.mjs`)가 **둘을 합집합으로 합칩니다.**
수집기와 **같은 중복 제거 규칙**(`collector/url-key.mjs`)을 쓰므로 규칙이 갈라질 수 없습니다.

- 실증: 두 사람이 각자 새 공고를 담고 같은 공고를 다른 주소로 본 상황을 실제로 만들어
  병합해 봤습니다 → 충돌 0건, 공고 둘 다 살아남고, 같은 공고 중복은 1건으로 정리됨.
- 매 실행 새로 쓰이는 리포트(`*-report.md`)는 **내 쪽을 그대로 둡니다**(다음 수집이 어차피 덮어씀).
- **일부러 제외한 것**: `data/registered.json`·`data/forms.json`. 여기서는 '삭제'가 뜻을
  가지므로(잘못 등록한 공고 제거 등) 자동 합집합이 지운 항목을 되살릴 수 있습니다.
  이 둘은 **사람이 직접 보고 합쳐야 합니다.**

### ② 겹칠 것 같으면 **작업 시작 전에** 알려줍니다
Claude 세션이 시작될 때 자동으로 `verify/check-collab.js`가 돌면서 이렇게 알립니다.

- 상대가 최근에 만졌는데 아직 내가 못 받은 파일을 나도 고치려 하면 🚨
- 앱 배포 브랜치(`main`)에만 있고 기본 브랜치엔 없는 작업이 있으면 🚨
  (= 누군가 GitHub 화면에서 `main`을 직접 고침 → 배포 로봇이 매번 여기서 충돌)
- 내 브랜치가 기본 브랜치보다 뒤쳐져 있으면 🚨

작업을 끝내고 올리기 전에 한 번 더 돌리면 좋습니다: `node verify/check-collab.js`

### ③ `main`을 직접 고쳐도 자동으로 되가져옵니다 (2026-07-31 추가)
`main`(앱이 배포되는 곳)에 사람이 직접 올린 변경은 기본 브랜치에 없어서, 다음 세션이 그 내용이
빠진 판 위에서 작업하게 됩니다. 7/31 저녁 사건이 정확히 이것이었습니다.
이제 `.github/workflows/main-guard.yml`이 **그 변경을 자동으로 기본 브랜치에 되가져옵니다.**

브랜치 보호(Settings → Rules)로 막는 방법도 있지만, **이 저장소에서는 위험합니다** —
배포 로봇과 진척도 로봇이 `main`에 직접 push해서 앱을 내보내므로, 흔히 켜는
*"Require a pull request before merging"* 을 걸면 **앱 배포가 통째로 멈춥니다.**
그래서 막는 대신 고치는 방식을 택했고, 이 방식은 **관리자 권한도 필요 없습니다.**

### ④ 로봇도 같은 장치를 씁니다
수집 로봇 3종·배포 동기화 로봇이 저장하기 전에 병합기를 등록하도록 워크플로에 넣었습니다.
그동안 배포 동기화가 충돌로 멈추고 🚨 이슈만 남기던 상황이 크게 줄어듭니다.
진척도 문서 로봇에도 재시도를 넣었습니다(예전엔 그 사이 `main`이 움직이면 그냥 실패 →
그날 활동 로그가 비었습니다).

### ⑤ 로봇을 **내 컴퓨터에서** 돌릴 때 (2026-08-21 추가)
`bash tools/robot-run.sh node collector/link-hunter.mjs` 처럼 겉옷을 씌워 돌립니다.

왜 생겼나 — 2026-08-21에 실제로 겪은 일입니다. 링크 사냥꾼을 로컬에서 돌리는 동안
**클라우드의 같은 로봇도 예약 실행으로 돌고 있었습니다.** 둘 다 `data/registered.json`을
고쳤는데 그 파일은 일부러 자동 병합에서 빼 둔 것이라(아래 4절) 사람이 손으로 풀어야 했습니다.
이번엔 양쪽 다 '찾아낸 주소'뿐이라 손해가 시간뿐이었지만, 한쪽이 **등록분을 지우는** 조치였다면
그 삭제가 조용히 되살아났을 수 있습니다.

겉옷이 하는 일은 셋뿐이고 **로봇도 워크플로도 건드리지 않습니다**:
① 클라우드에서 데이터 로봇이 돌거나 줄 서 있으면 아예 시작하지 않음
② 시작 전에 최신을 받아 둠 (겹칠 거리를 줄임)
③ 도는 동안 원격이 움직였으면 크게 알림 + 합치는 순서를 알려 줌
(급하면 `ROBOT_RUN_FORCE=1`로 건너뛸 수 있습니다 — '겹쳐도 내가 손으로 푼다'는 뜻입니다.)

> **⚠️ 클라우드 로봇들의 대기줄(concurrency)을 하나로 합치는 방법은 쓰지 마세요.**
> GitHub은 같은 그룹에서 **대기 중인 실행을 새 실행이 오면 취소**해 버려서, 예약 수집이
> 조용히 사라집니다(deploy-sync·main-guard를 일부러 다른 그룹에 둔 것과 같은 이유).
> 클라우드끼리는 지금처럼 **push 재시도 + 실패 시 🚨 이슈**로 눈에 보이게 두는 것이 낫습니다.

---

## 4. 앞으로의 규칙 (두 사람 공통)

### 지켜야 할 것 세 가지

1. **`main`을 직접 고치지 않습니다.**
   `main`은 *앱이 배포되는 곳*이지 작업하는 곳이 아닙니다. GitHub 화면에서 `main`의 파일을
   연필 아이콘으로 고치면, 그 변경은 기본 브랜치에 없어서 배포 로봇이 매번 그 지점에서
   부딪힙니다. 고칠 게 있으면 **기본 브랜치(`claude/nice-heisenberg-WESq5`)에서** 고치세요 —
   거기서 `main`으로는 로봇이 자동으로 옮겨 줍니다.

2. **작업 시작 전에 최신 것을 받습니다.**
   Claude에게 *"작업 시작 전에 최신 받아줘"* 라고 하면 됩니다. 세션 시작 훅이 겹침 경고를
   자동으로 띄우므로, 경고가 보이면 그때 합치고 시작하세요.

3. **누가 무엇을 맡는지 `PROGRESS.md`에 적습니다.**
   자기 칸의 '🔨 개발 중'에 지금 하는 일을 한 줄 적어 두면, 상대 세션이 그걸 읽고
   같은 파일을 피합니다. **이게 자동 장치보다 효과가 큽니다** — 자동 병합은 데이터만
   해결하지, 같은 코드를 서로 다르게 고치는 건 못 막습니다.

### 겹치기 쉬운 파일 (동시에 만지지 않기로 한 것)

| 파일 | 성격 | 동시 작업 시 |
|---|---|---|
| `.github/workflows/*.yml` | 로봇 설정 | ⚠️ 한 사람씩 — 7/31 사건이 여기서 남 |
| `app.js` `index.html` `style.css` | 앱 화면·엔진 | ⚠️ 한 사람씩 |
| `data/registered.json` `data/forms.json` | 사람이 큐레이션 | ⚠️ 한 사람씩 (자동 병합 제외) |
| `CLAUDE.md` | 인수인계 문서 | ⚠️ 세션 끝에 각자 자기 절만 |
| `PROGRESS.md` | 진척도 | ✅ 자기 칸만 고치면 안전 |
| `data/notices.json` `collector/seen.json` 등 | 로봇 기록장 | ✅ 자동으로 합쳐짐 |

---

## 5. 알아 두면 좋은 사실

- **여러 사람의 커밋은 이름으로 구분되지 않습니다.** 다들 Claude 세션으로 작업하므로 커밋
  작성자가 똑같이 `Claude <noreply@anthropic.com>`로 찍힙니다. 그래서 겹침 검사는 사람이
  아니라 **브랜치**를 기준으로 봅니다. 진척도 문서의 활동 로그도 브랜치로 구분합니다
  (선주 = `claude/work-seonju`, Josehyeon = `claude/work-josehyeon`, 유은서 = `claude/work-eunseo`).
- **브랜치는 이 흐름으로만 움직입니다.**
  `내 세션 브랜치` → `기본 브랜치(claude/nice-heisenberg-WESq5)` → (배포 로봇) → `main` → 앱
  기본 브랜치는 *로봇이 사는 집*이고, `main`은 *앱이 나가는 문*입니다. 사람은 자기 브랜치에서
  작업해 기본 브랜치로 보냅니다.
- **개발자가 결정해 주면 더 좋아지는 것**은 이 문서 아래 '개발자 액션'에 있습니다.

---

## 6. 개발자 액션 (선주·Josehyeon·유은서 세 분 합의 필요)

### 🔴 CTO 유은서 합류 — 계정 연결 체크리스트 (2026-08-24, 선주님만 할 수 있음)
Claude 세션은 계정에 사람을 초대하는 버튼을 대신 눌러줄 수 없다(초대 도구 자체가 없음).
아래 세 곳을 **선주님이 직접** 열어드려야 유은서님이 조세현님과 동급으로 움직일 수 있다.

| # | 어디 | 무엇을 | 비고 |
|---|---|---|---|
| 1 | GitHub 저장소 | Settings → Collaborators and teams → Add people → 유은서님 GitHub 아이디, 권한 `Admin` | BUSINESS.md 1절에 CTO가 "설계·파트너 선정·일정 전권"이라 명시돼 있어 `Write`(조세현님과 동일)보다 `Admin` 권장. 개인 계정 저장소라 Admin이 줄 수 있는 최대치(저장소 삭제·양도만 소유자 고유 권한) |
| 2 | Cloudflare 계정 | Manage Account → Members → Invite Member → 유은서님 이메일, 역할 `Super Administrator` | 이게 있어야 `server/push/`(가동 중)·`_admin/`(관리자 화면)·앞으로 배포할 `server/essay/`·`server/chat/`를 직접 만질 수 있다 |
| 3 | 관리자 화면(`_admin`) 로그인 허용 목록 | Cloudflare Zero Trust → Access → Applications → `hanggonggan-admin` → Policies → 허용 이메일에 유은서님 이메일 추가 | 2번과 **별개의 문**이다 — 2번만 하면 Cloudflare 계정 권한은 있어도 관리자 화면 로그인 시트에서 막힌다 |

세 곳 다 해야 조세현님과 동급 접근이 완성된다. 저장소 쪽 준비(작업 브랜치·인수인계 문서)는
같은 날 Claude 세션이 먼저 처리했다 — 아래 참조.

### 저장소 쪽 준비물 (2026-08-24 완료)
- **전용 작업 브랜치 `claude/work-eunseo`** 신설 — 기본 브랜치(`claude/nice-heisenberg-WESq5`)에서 분기.
- **`docs/cto-onboarding/`** — 2026-08-20에 이미 만들어져 있었는데 별도 브랜치(`claude/cto-onboarding-docs-c8efrf`)에
  고립돼 기본 브랜치·main 어디에도 반영되지 않은 채 방치돼 있던 것을 되찾아 이 세션에서 다시 커밋했다.
  PDF 2종(제품 전체 개요 4쪽 · 코드·아키텍처 상세 11쪽) — 수치는 2026-08-20 스냅샷 기준이라
  최신 숫자는 본 CLAUDE.md·BUSINESS.md 9절을 함께 참조할 것.

| 할 일 | 하고 나면 | 왜 사람이 해야 하나 |
|---|---|---|
| 두 분이 **담당 영역 나누기** (예: 선주=데이터·공고 등록, Josehyeon=수집 로봇·인프라) | 같은 파일을 동시에 만질 일 자체가 사라짐 | 두 사람의 합의 사항 |
| (선택) `main`에 **Block force pushes · Restrict deletions**만 켜기 — Settings → Rules → Rulesets, 대상은 패턴 `main` | 실수로 `main`을 덮어쓰거나 지우는 사고를 막음 | 관리자 권한 필요 — 소유자(`seonju5543-web`)만 가능 |

> ⚠️ **`main`에 "Require a pull request before merging"은 켜지 마세요.** 배포 로봇·진척도 로봇이
> `main`에 직접 push해서 앱을 내보내므로, 켜면 **앱 업데이트가 통째로 멈춥니다.**
> 직접 수정 문제는 위 3-③(자동 되가져오기)이 이미 해결하고 있어, 보호 설정은 없어도 됩니다.
>
> 참고: 조세현님 계정은 저장소 권한이 `write`라 브랜치 보호 메뉴가 보이지 않습니다
> (*"You don't have access to repository options"*). 이 설정은 소유자만 켤 수 있습니다.

---

## 부록. 되가져오기 로봇을 만들기 전에 검토한 실패 가능성

새 로봇이 저장소를 망가뜨리면 협업이 더 어려워지므로, 만들기 전에 깨질 수 있는 지점을
먼저 세고 각각을 막았습니다. **여섯 가지 상황을 실제 저장소로 재현해 검증했습니다.**

| # | 걱정한 것 | 왜 안 일어나나 / 어떻게 막았나 | 검증 |
|---|---|---|---|
| 1 | **두 로봇이 서로를 깨워 무한 반복** (되가져오기 ↔ 배포 동기화) | 로봇의 push는 GitHub 규칙상 다른 워크플로를 깨우지 못한다. 게다가 양쪽이 갈라진 최악의 경우를 돌려 봐도 **2라운드에 완전히 멈춘다** | ✅ 반복 실행으로 수렴 확인 |
| 2 | 되가져오다 **충돌**나서 저장소가 반쯤 합쳐진 채 남음 | 충돌하면 즉시 되돌리고(`merge --abort`) 🚨 이슈만 남긴다 | ✅ 기본 브랜치·작업 폴더 그대로 |
| 3 | 합치는 사이 **수집 로봇이 먼저 저장**해 push가 튕김 | 최신을 받아 얹고 최대 3회 재시도 | ✅ 양쪽 다 보존 |
| 4 | 합쳐진 **데이터가 깨진 채 기본 브랜치로** 들어감 | 진짜 병합이 일어났을 때만 `audit-data.js`를 돌리고, 실패하면 **저장하지 않고** 이슈 | ✅ 깨진 데이터 차단 확인 |
| 5 | 정상 세션(세 브랜치 동시 push)마다 **쓸데없이 돌아 소음** | 되가져올 게 없으면 조용히 종료 | ✅ 아무 일도 안 함 |
| 6 | 같은 문제로 **이슈가 계속 쌓임** | 라벨(`main-backmerge`)로 열린 이슈를 찾아 있으면 코멘트만. 제목 검색은 이모지·한글에서 놓쳐서 안 씀 | ✅ |
| 7 | 기존 감시 도구(`check-deploy-sync.js`)가 **새 워크플로를 오탐** | 그 도구는 수집 로봇 3종만 검사하고, 되가져오기는 `main`에 push하지 않는다 | ✅ 통과 |
| 8 | 워크플로가 **없는 파일을 `git add`** 해서 실행 전체가 실패 (2026-07-19 사고 유형) | 이 로봇은 `git add`를 하지 않는다(병합만) | ✅ 해당 없음 |
| 9 | 배포 로봇과 **줄을 세우다 실행이 취소**됨 | 같은 대기줄에 넣으면 GitHub이 오래된 대기분을 취소해 되가져오기가 조용히 사라진다 → **일부러 다른 줄**을 쓰고 경합은 3번(재시도)으로 처리 | ✅ 설계 반영 |
| 10 | 이력이 잘린 채 받아와 **병합 판단이 틀림** | `fetch-depth: 0`으로 전체 이력을 받는다 | ✅ |
| 11 | 알림 스텝의 **따옴표·백틱이 셸을 깨뜨림** | 네 가지 실패 사유로 실제 실행해 본문까지 확인 | ✅ 전부 정상 |

---

## 7. 문제가 생기면

```
node verify/check-collab.js        # 지금 부딪힐 곳이 있나
node verify/check-deploy-sync.js   # 내 작업이 앱에 나가 있나
bash tools/setup-collab.sh         # 자동 병합기 다시 등록 (여러 번 해도 안전)
```

충돌이 나서 화면에 `<<<<<<<` 같은 표시가 보이면, **그대로 두고 Claude에게 알려주세요.**
어느 쪽을 살려야 하는지는 파일마다 다르고, 잘못 고르면 한쪽 작업이 사라집니다.
