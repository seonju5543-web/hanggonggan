# 한대장 — 세션별 작업 기록 (SESSIONS.md)

> **이 파일은 자동으로 안 읽힌다.** `CLAUDE.md`는 매 세션 항상 로드되지만, 이 파일은
> 필요할 때만(옛 사고·결정의 경위가 궁금할 때) 세션이 직접 열어 본다.
> `CLAUDE.md`의 "중요한 기술 사실" 절에 이미 정제(distilled)된 교훈은 여기 다시 안 옮겨졌다 —
> 그쪽이 먼저다. 여기는 **그 교훈이 어떤 과정으로 나왔는지**의 서술 기록이다.
>
> 2026-08-23에 `CLAUDE.md`(당시 2,316줄·260KB)의 60%가 이 세션 기록이라, 매 세션 항상
> 로드되는 부담을 줄이려고 분리했다(claude-md-improver 검사 결과). **삭제가 아니라 이동**이다 —
> 아래 세션 하나하나가 CLAUDE.md의 "중요한 기술 사실" 안에서 "(OO차 세션)"으로 계속 인용된다.

## 마지막 세션 상태 (2026-08-18 24차 세션 — 신청서 질문 방식 최적화)

> 개발자: 선주 · 작업 브랜치 `claude/document-question-optimization-vdk1jw`
> **앱1 코드가 바뀌었다** — sw.js CACHE **v57 → v58**. `form-plan.js` 신설(index.html·sw.js 배선 완료).
> 개발자 지시로 **선조치후보고**로 진행했다.

### 개발자 지시와 결과
*"꼭 직접 서술이 필요한 질문을 빼고 클릭형 우선. 클릭형 10개(+5까지) · 직접입력 10개 ·
전체 20개를 넘지 않는 선에서 신청서 채울 내용을 전부 얻어 제출 가능한 문서를 만들 것."*

| | 전 | 후 |
|---|---|---|
| 총 질문 | 675 | **320** |
| 직접입력 10개 초과 | **25종 / 48** | **2종** (둘 다 서술형이 7·9개라 더 못 줄인다 — 감사가 이름을 보고한다) |
| 전체 20개 초과 | 7종 | **0종** |
| 클릭형 15개 초과 | 0종 | 0종 |
| **생성 문서** | — | **48종 중 44종 한 글자도 안 변함** (나머지 4종은 원본 대조로 고친 것 — 아래) |

### 🔑 이 작업의 핵심 — 줄인 게 아니라 **묻는 방식을 바꿨다**
문서는 `renderFormDoc`이 **원본 스키마를 그대로 읽어** 만든다. 질문 설계기는 스키마를
건드리지 않고 **화면에 낼 질문 목록만** 새로 짠다. 그래서 질문을 아무리 줄여도 문서는 그대로다.
- **레버 ①·자동 채움(가장 큼)**: 앱이 아는 값은 묻지 않는다. `formAutoVal` 9키 → **22키**
  (생년월일 20종·주소 30칸이 그동안 매번 다시 입력되고 있었다). 로봇 양식은 `auto`가 0건이라
  **라벨을 정규화해 사전과 맞춘다**(`성명`/`성 명`/`성　명`/`1. 성    명`이 한 값).
- **레버 ②·클릭형**: `choice`(하나만 고르기) 신설. `checks` 69개 중 **62개 승격**.
  개수만이 아니라 **정확도**를 고쳤다 — 그동안 성별에 남·여를 둘 다 체크할 수 있었다.
- **레버 ③·병합**: `group` — 원본 표의 칸들을 카드 1개로 묻고 **문서에는 칸 수만큼 그대로** 낸다.
  직접입력이 10개를 넘을 때만 발동한다.
- **레버 ④·`static`**: 표 머리 칸은 묻지 않는다(문서에는 원본대로 남는다). **원본 대조로 확인된 것만.**

### 🔴 되돌리면 안 되는 지점
- **문서 출력을 바꾸는 것은 이 작업이 아니다.** `choice`는 `checks`와, `group`은 개별 `text`와
  **같은 HTML**을 내야 한다. 지키는 장치가 **`node verify/form-snapshot.mjs`** —
  48종 문서를 문자 단위로 대조한다. 답을 **필드 id가 아니라 라벨로** 채우는 것이 핵심이다
  (병합하면 id가 바뀌므로 id로 채우면 아무것도 증명하지 못한다).
- **런타임은 선택지를 지어내지 않는다**(원칙 8-1). 데이터에 있는 보기만 형태를 바꾼다.
  자동 채움도 **학생 본인이 입력한 값**을 옮기는 것뿐이고, 접힌 패널에서 전부 보이고 고칠 수 있다.
- **`verify/audit-data.js:20` `FIELD_TYPES`에 새 타입을 안 넣으면** 감사가 exit 1로 죽고
  **그날 수집 로봇 결과가 하나도 저장되지 않는다**(워크플로가 감사 실패 시 되돌린다).
- **세는 규칙은 `form-plan.js` 한 곳.** 화면·감사·관리자 화면이 같은 파일을 쓴다
  (`match-engine.js`와 같은 방식 — 브라우저·Node 겸용). 베끼면 "화면은 12개인데 감사는 20개"가 된다.
- **`collectProfile()`은 `common`을 이어 붙인다.** 온보딩에 칸이 없는 값(현주소·긴급연락처·
  보호자·성별·주민등록번호)은 신청서에서 배운 것이라, 새로 만들면 **프로필을 한 번 수정할 때마다
  사라진다.**
- **`app.js`의 칩 처리에서 `.fq-choice`를 `.fq-checks`보다 먼저 본다.** 뒤에 두면 단일 선택이
  영영 안 걸린다.
- **정렬을 병합보다 먼저 한다.** 원본 순서대로 두면 표 칸 사이에 서술형이 끼어 묶을 줄이 토막 난다
  (동산장학회에서 카드가 7개가 아니라 12개가 됐다).
- **`renderFormDoc`은 `formAnswerFor`로 답 모양을 맞춘 뒤 그린다.** 저장된 답(`formAns`)은
  기기에 남아 나중에 다시 그려지는데, 그 사이 타입이 바뀌면 `a.checks.includes(...)`에서 터진다.
  버리지 않고 맞춰 준다 — 학생이 이미 쓴 글이다.

### 개발자가 정한 것 (이 세션에서 확인 — 다시 묻지 말 것)
1. 앱이 모르는 정보는 **필요할 때 한 번만 묻고 프로필에 저장**('다음 신청서에도 쓸게요' 기본 켜짐).
   온보딩은 5단계 그대로. → 두 번째 신청서부터 안 묻는다. MY 화면에서 보고 지울 수 있다.
2. 클릭 보기는 **원본에 적힌 보기 + 앱이 아는 값**에서만. 지어낸 보기 0개.
3. **주민등록번호는 묻고 기기에만 저장**(외부 전송 코드 없음). 화면에는 뒷자리를 가려 보여 준다.
   ⚠️ 회원가입(⑧절)을 붙이면 **민감정보 별도 동의 대상**이다.
4. **20개를 넘어도 질문을 감추지 않는다.** 전부 보여 주고 감사가 리포트로 에스컬레이션한다
   (숨기면 학생이 못 채운 칸이 있는 문서를 제출하게 된다).

### 원본 대조로 고친 데이터 4종 (문서가 바뀐 것은 이 4종뿐)
원본은 `collector/extracted/*.hwp.body.txt`로 확인했다.
- **동산장학회·가송재단 2종 — 같은 서식이 2~3벌 저장돼 있었다.** 원본은 '장학지원서+자기소개서'
  한 벌뿐인데 로봇이 여러 공고의 같은 첨부를 합치며 겹쳤다(동산 45×2=90개, 가송 7×3).
  학생은 같은 질문을 두 번 받았고 **문서도 두 벌로 나가고 있었다.** → 한 벌만 남김.
- **동산 `주거사항`** — 원본에 `1. 자가 2. 전세 3. 월세 4. 기타`가 그대로 적혀 있어 클릭형으로.
- **이원길** — 로봇이 `선택 1`·`선택 2`라고 **이름을 지어 붙인** 두 항목의 진짜 이름을 원본에서
  되찾았다(`신청 등급`·`사업자등록 여부`). 표 머리 칸 4개(`붙임 1`·`신청 정보`·`창업 개요`·
  `인본주의 가치 연계`)는 `static`으로 — 채우는 자리가 아니다.

### 로봇 재발 방지 (데이터만 고치면 다음 주에 다시 벌어진다)
- `collector/schema-from-text.mjs` — ⓐ **되풀이된 섹션을 저장 전에 버린다** ⓑ `INFO_MAP`을
  12키로 넓히고 info로 못 간 칸에도 **`auto`를 붙인다** ⓒ **체크 항목 이름을 지어내지 않는다**
  (예전엔 `선택 1`·`선택 2` — 학생 화면에 "선택 1을(를) 선택해 주세요"라고 떴다. 이제 이름을
  못 찾으면 만들지 않고 사유를 남긴다).
  🔴 **`긴급연락처`를 `phone`으로 보내면 안 된다** — 본인 번호와 다른 칸이라 원본과 다른 값이 찍힌다.
- `collector/schematize-forms.mjs` — SYSTEM 프롬프트·SCHEMA에 `choice`와 `auto` 추가,
  **`validate()`가 이제 type 값을 실제로 검사한다**(예전엔 검사하지 않아 앱이 모르는 타입이
  들어오면 문서에 조용히 빈 칸이 찍혔다).

### 검사 도구 — 오래 빨간불이던 것 2개를 고쳤다 (이번 변경과 무관한 기존 문제)
**둘 다 원인이 같다: 알림 동의 시트가 온보딩 2.9초 뒤에 뜨는데 기다리지 않고 넘어가,
검사 도중에 떠서 화면을 덮고 그때부터 클릭이 전부 막혔다**(CLAUDE.md 14차 세션에 기록만 돼 있던 문제).
`verify-chat.js`의 `dismissNotify()`와 같은 방식으로 `verify-registered.js`·`personas.js`를 고쳤다.
- `verify/verify-new-forms.js`도 고쳤다 — `#fq-name` 입력칸이 있다고 전제했는데 이제 그 값은
  **자동 채움 패널**에 들어간다. `fqValue()`/`fqFill()`로 감쌌고, 이게 최적화가 실제로 먹었는지도 함께 검사한다.

### ⚠️ 이번 변경과 무관한 기존 실패 (건드리지 않음 — 다음 세션이 오해하지 말 것)
`node verify/verify-admin.js`의 **4개 항목**이 실패한다. **내 변경 전(HEAD)에서도 똑같이 4개가
실패하는 것을 별도 워크트리로 확인했다.** 원인은 데이터 흐름 — 마감 임박 공고가 지금 **1건뿐**이라
'여러 건 선택' 검사가 성립하지 않고, 그 때문에 선택 바 검사 3건이 연쇄로 실패한다.
마감 임박 공고가 늘면 저절로 통과한다.

### 검증
`form-snapshot`(48종) · `audit-data`(exit 0 · 경고 30 = 기존 28 + 상한 초과 2) ·
`test-collector` 전항목 · `drive` · `verify-registered` · `verify-new-forms` ·
`verify-forms-data` · `verify-chat` · `verify-notify-rules` · `personas` · `check-collab` · `check-deploy-sync`.

## 이전 세션 상태 (2026-08-17 23차 세션 — 홈 화면 장학금 도우미(챗봇) 신설)

> 개발자: 선주 · 작업 브랜치 `claude/home-screen-chatbot-placement-r5nzcf`
> **앱1 코드가 바뀌었다** — sw.js CACHE **v52 → v57**. 데이터는 `data/search-index.json` 신설(로봇이 갱신).
> **세 브랜치(작업·기본·main) 전부 배포 완료** — 개발자 지시로 컨펌 없이 배포했다.
>
> 이 세션은 **한 덩어리가 아니라 개발자와 주고받으며 다섯 번에 걸쳐** 진행됐다:
> ① 안내봇 신설 → ② 마스코트 '대장님' 승격 + 꾹 눌러 옮기기 → ③ 말귀 7가지 + AI 안전장치 2·3순위
> → ④ 홈 진입줄 삭제(길은 마스코트 하나) → ⑤ 마스코트 얼굴 '왕눈이' 확정.

### 개발자가 물은 것과 답
**질문**: *"홈 화면에 챗봇을 배치하고 싶은데 어떻게 생각해?"*

**답: 자리는 진짜로 비어 있었다.** 앱에 **글자로 물어볼 곳이 한 군데도 없다** — 장학금 탭의 고르는
장치는 칩 4개(전체·교내·교외·신청 가능만)뿐인데 보여 줄 것은 등록 170건 + 피드 600건이라,
"기숙사비 지원되는 거 있어?"를 손으로 스크롤해 찾아야 했다.

**다만 '챗봇'은 두 물건이라는 것을 먼저 갈랐다** — 개발자가 **'안내봇 + 나중에 AI 얹기'를 선택**했다.
- **안내봇**: 앱 안 정보로만 답한다. 비용 0 · 열쇠 불필요 · 오프라인 작동 · 개인정보가 기기 밖으로
  안 나감 · **지어낼 수가 없다**(있는 것을 골라 보여 주는 구조).
- **AI**: 말은 잘 알아듣지만 **모르면 그럴듯하게 지어낸다.** 그게 이 앱이 7월(가짜 공지 금지)·
  8월(추론 금지)에 지운 바로 그 위험이다. 게다가 API 잔액이 8/11부터 바닥 상태.

### 만든 것
- `chat.js`(안내봇) · `chat-config.js`(AI 자리 — endpoint 빈 칸) · index/style/sw 배선.
- **마스코트 '대장님' (개발자 지시로 승격·이름과 얼굴 모두 개발자가 시안 중 지정 — '왕눈이')** — 학사모 쓴 얼굴 SVG를 `index.html`에 `<symbol id="mascot">`
  한 벌만 두고 떠 있는 버튼·시트 머리말·말풍선이 `<use>`로 함께 쓴다(한 곳만 고치면 전부 바뀐다).
  **짧게 누르면 열리고, 꾹 누르면(360ms) 손가락을 따라 옮겨진다** — 떼면 가까운 쪽 가장자리에 붙고
  자리는 기기에 기억된다. 숨은 동작이라 **처음 한 번 '꾹 눌러서 옮길 수 있어요' 안내**가 뜬다.
- 답하는 것: 지금 지원 가능 · 마감 임박 · 금액 · 필요 서류(보관함 상태까지) · 신청 방법(채널 4종) ·
  내 신청 현황 · 낱말 검색(등록 공고 + 우리 학교 게시판 제목).
- **정직 원칙이 답 안에 들어가 있다**: 금액 확인분이 8건뿐이라 *"나머지 13건은 금액이 원문에만 있어서
  합계에서 뺐어요"* 라고 답한다. 마감을 짐작해 말하지 않는다. 못 알아들으면
  *"지어내서 답하지 않으려고 여기서 멈춰요"*.

### ⚠️ 되돌리면 안 되는 지점
- **하단 탭을 5개로 늘리지 않았다.** 도우미는 '가는 곳'이 아니라 '부르는 도구'라 탭을 늘리면
  홈·장학금·신청내역·MY만 좁아진다. **들어가는 길은 마스코트 하나뿐이다** —
  홈에 있던 진입줄('대장님에게 물어보세요')은 **2026-08-17 개발자 지시로 없앴다.**
  길이 하나뿐이므로 `chatBind()` 안의 **`chatBindFab()` 호출을 지우면 도우미에 들어갈 방법이
  아예 없어진다**(정리하다 실제로 지웠다가 검사에서 잡혔다). 마스코트가 '한 번에 신청' 버튼과
  하단 탭을 가리지 않는 것은 검사가 **자리를 실제로 재서** 지킨다.
- **🔴 `.chat-sheet[hidden] { display: none !important; }`를 지우지 말 것** — 클래스에 `display`를 주면
  브라우저 기본 규칙 `[hidden]{display:none}`을 이겨 **닫아도 안 닫힌다.** 화면 밖으로 밀려나 눈에는
  안 보이지만 요소는 살아 있어 화면 읽어 주기가 닫힌 대화를 계속 읽는다.
  **관리자 화면(`_admin/admin.css`)이 빠졌던 것과 똑같은 함정** — 검사에서 실제로 잡혔다.
- ~~도우미 시트에는 '쓸어내려 닫기'를 붙이지 않는다~~ → **2026-08-21 개발자 지시로 붙였다**
  (아래 '앱에서 뜨는 화면은 전부 쓸어내려 닫는다' 항목 참조). 예전에 안 붙였던 이유는 도우미가
  시트가 아니라 **안쪽 대화 목록**을 스크롤해 시트의 scrollTop이 늘 0이었기 때문이다 —
  지금은 판정이 손가락 아래 스크롤 영역까지 거슬러 올라가므로(`scrollableAtTop`) 그 함정이 없다.
- **`liveNotices`는 배열이 아니라 `{ updatedAt, items }`다.** 배열로 착각해 `.filter`를 부르는 순간
  chatRoute가 통째로 넘어져 **질문이 답 없이 남는다**(실제로 겪음). 그래서 chatSend가 규칙 호출을
  try/catch로 감싸 **무슨 일이 있어도 답은 하나 나가게** 했다.
- **마스코트는 '누르기'와 '옮기기'를 반드시 갈라야 한다.** 손이 조금 흔들렸다고 안 열리면 버튼이
  고장 난 것처럼 느껴지고, 반대로 아무 때나 따라오면 못 누른다. 규칙은 하나뿐이다 —
  **꾹(360ms) 누른 뒤에만 따라오고, 그 전에 8px 넘게 움직이면 아무 일도 안 일어난다.**
  (13차 세션 학교 목록 touchstart 사고와 같은 계열의 판단)
- **마스코트를 하단 탭 위로 못 내려오게 막은 것을 풀지 말 것** — 탭을 덮으면 학생이 화면을 못 옮겨 다닌다.
  `chatFabBand()`가 하단 탭 위까지로 범위를 자른다. 창 크기가 바뀌면 다시 넣는다.
- **🔴 AI 후보는 '인용할 원문이 있는 것'을 앞에 둔다 (2026-08-21)** — 이 구조에서 AI가 하는 일은
  **어느 공고의 어느 원문 문장인지 고르는 것 하나뿐**이라, 원문 없는 공고를 아무리 많이 보내도
  고를 것이 없고 AI에게 남는 길은 **근거 없이 한 줄 지어내는 것**뿐이다. 실측으로 후보 6건 중
  원문이 있는 것은 1건인데 그게 맨 뒤에 있었다(`verify-chat`의 '원문 인용 블록' 항목이 오래
  빨간불이던 원인이 이것이다 — 앱이 막은 게 아니라 **애초에 인용할 것을 안 보내고 있었다**).
  `chatAiCandidates` 마지막의 정렬을 지우면 그대로 되살아난다. 23차 세션의 '후보를 같은 검색으로
  뽑아 AI가 언제나 빈손이던' 결함과 같은 계열. **버리지 말고 순서만 바꿀 것** — 원문 없는 공고도
  카드로 보여 줄 값은 있고, 후보가 0건이면 AI를 아예 못 부른다.
- **AI를 켤 때도 프로필은 보내지 않는다** — 질문 + 앱이 고른 공고의 공개 정보만. 판정은 기기 안에서
  끝내는 지금 구조를 유지한다(푸시 서버가 폰 주소·학교·캠퍼스만 갖는 것과 같은 원칙).

### 이어서 한 것 (같은 세션 후반 — 개발자 지시 "2순위·3순위 + 업그레이드 전부")

**A. 지어냄 방지 — AI를 켜도 무너지지 않게 (2·3순위. 1순위는 개발자가 진행하지 말라고 함)**
- **2순위 · 인용만 시킨다**: AI에게 답을 쓰게 하지 않는다. **어느 공고(id)·어느 문장(번호)인지만**
  고르게 하고, 화면에 뜨는 원문 문장은 **앱이 제 데이터에서 번호로 꺼낸다.**
  서버가 보낸 글자는 화면에 나가지 않는다 → 문장을 지어낼 자리가 구조적으로 없다.
  계약 전문은 `chat-config.js` 첫머리, 참조 구현은 `server/chat/`(미배포).
- **3순위 · 나온 답을 앱이 다시 검사한다** (`chat.js chatVerifyAI` — 최후 관문):
  ① 앱에 없는 공고 id면 그 pick을 버린다 ② 인용 번호가 우리가 보낸 범위 밖이면 버린다
  ③ **근거가 0개면 AI 답 자체를 버리고** 안내봇 답을 낸다 ④ 한 줄 요약에 **앱이 모르는 숫자**가
  있으면 그 줄을 통째로 버린다(금액·날짜 지어내기가 가장 흔하고 해로운 실패라서).
  ⑤ 서버가 `needSource: true`면 접는다 — **모른다고 말할 자유**를 안 주면 AI는 반드시 지어낸다.
- **가짜 AI 서버로 실증**: 없는 공고 id·범위 밖 인용·`최대 987654원` 같은 지어낸 금액을
  일부러 돌려주고 전부 막히는 것을 확인했다(verify-chat 13항목). **요청 본문에 프로필이
  안 섞이는 것도 전수 검사**한다(이름·학교·성적·소득구간).
- ⚠️ **만들면서 잡은 설계 결함**: AI는 '안내봇이 못 찾았을 때'만 불리는데 후보를 **같은 검색으로**
  뽑고 있어서 언제나 빈손이었다 — AI가 할 일이 없었다. 지금은 후보를 한 겹 느슨하게 모은다
  (비슷한 이름 + 지금 지원 가능한 것). 되돌리면 AI를 켜도 아무 일도 안 일어난다.

**B. 말귀 업그레이드 7가지 (전부 0원)**
① **같은 뜻 다른 말**(`CHAT_SYNONYMS`) — 기숙사비=생활관, 등록금=학비 … 못 찾는 것의 가장 큰 몫.
② **되묻기** — 못 찾으면 비슷한 공고 3개를 눌러 볼 수 있게 준다(`chatClarify`).
③ **초성·오타** — 'ㅎㄱㅈㅎㅈㄷ' · 편집거리 1. **오타 봐주기는 엄밀히 찾아 0건일 때만** 쓴다
   (넓게 쓰면 엉뚱한 게 걸린다). 3글자 미만에는 쓰지 않는다.
④ **앞 대화 1턴 기억** — "그거 서류 뭐야?"가 앞에서 말한 공고를 가리킨다. **한 턴만** 기억한다
   (오래 들고 있으면 엉뚱한 공고 이야기를 계속하게 되고 그게 곧 지어내는 답이다).
⑤ **원문 전문 검색** — `collector/build-search-index.mjs` → `data/search-index.json`(51KB).
⑥ **답에서 바로 행동** — 보관함 열기 · 마감 알림 켜기 · 신청 준비하기 · 전체 보기.
⑦ **못 알아들은 질문 세기** — `handaejang.chatMiss`에 **기기 안에서만**. 밖으로 안 보낸다.

**C. 🆕 `collector/build-search-index.mjs` + `.github/workflows/search-index.yml`**
- 원문 전문(수 MB)을 앱이 통째로 받을 수 없어, **'그 공고만의 낱말'만** 추려 작은 파일로 만든다.
- 🔴 **여기서 나온 것은 '찾기용 낱말 자루'지 보여 줄 글이 아니다.** 문장처럼 보여 주면
  잘린 낱말이 사실처럼 읽혀 원칙 8-1이 깨진다.
- 🔴 **여러 공고에 나오는 낱말은 버린다(문턱 2건).** 저장된 원문에는 게시판 껍데기
  (로그인·사이트맵·입학·취업)가 섞여 있어, 그대로 담으면 "취업" 한 마디에 그 학교 공고가
  전부 걸려 **검색이 오히려 나빠진다.** 비율로 자르면 그 학교 글이 몇 건 안 될 때 그냥 통과한다.
- 🔴 **수집 워크플로를 고치지 않고 따로 도는 로봇으로 만들었다** — 수집 파일은 공동작업자가
  자주 손대는 곳이라 스텝을 끼워 넣으면 합칠 때마다 부딪힌다. 결과만 받아 쓰면 되므로 무해하다.
  단 `deploy-sync.yml`의 workflow_run 목록에는 **반드시 넣어야 한다**(안 넣으면 최대 12시간 앱에 안 나감).

### 마무리 — 마스코트 확정과 진입점 정리 (세션 끝)
- **얼굴은 '왕눈이'** (시안 6가지를 그림으로 만들어 개발자가 고름). 눈을 크게·반짝임 둘·볼 넓게.
  **56px과 28px에서 살아남는 것이 유일한 판단 기준**이었다 — 확대 그림이 예뻐도 실제 크기에서
  뭉개지면 소용없다(선 그림·책·서류 안이 그래서 떨어졌다).
- **홈의 진입줄을 없앴다** — 들어가는 길은 마스코트 하나. 정리하다 `chatBindFab()` 호출까지
  함께 지워 **도우미에 들어갈 방법이 아예 없어졌었고**, 검사('마스코트를 누르면 열린다')가 잡았다.
- **인사말은 "안녕하세요, 저는 대장님이에요!"** — 개발자가 직접 확인해 고른 문구다. 검사가 지킨다.

### ⚠️ GitHub Pages 배포 실패 메일 (2026-08-17 15:18·15:23 — 우리 잘못 아님)
개발자에게 `Run failed: pages build and deployment` 메일이 두 번 갔다. 로그를 열어 보니
**`actions/jekyll-build-pages`를 내려받다 429(Too Many Requests)·502로 실패**한 것이다.
빌드가 시작되기도 전이라 우리 코드·데이터와 무관하고, **다음 push(1f8a60e)에서 그대로 통과**했다.
· 배포가 실패해도 **앱은 안 죽는다** — GitHub이 직전 성공본을 계속 서비스한다(최신 변경만 안 나감).
· **구분법**: 오류 메시지에 **우리 저장소 파일 이름이 나오면** 우리 문제, `Failed to download action`·
  `429`·`502`면 GitHub 쪽 일시 장애라 조치할 것이 없다. 같은 메일이 또 오면 이 절부터 볼 것.

### 검증
`verify/verify-chat.js` **56항목 통과**(신설 — 마스코트·이름 12 · 말귀 9 · AI 안전장치 13항목 포함). 회귀: `audit-data`(경고 28 — 변경 전후 같은 수로 대조) ·
`test-collector` 전항목 · `drive` · `personas` 120명 이상 0·오류 0 · `verify-notify-rules`.
⚠️ 검사 도구를 고칠 때: 알림 동의 시트는 온보딩 **2.9초 뒤**에 뜬다 — 직후에 한 번 보고 넘어가면
검사 중간에 떠서 그때부터 클릭이 전부 막힌다. **뜰 때까지 기다렸다가** 치울 것.

---

## 이전 세션 상태 (2026-08-14 22차 세션 — 밀린 컨펌 전량 검수 + 그 과정에서 찾은 결함 2종 수리)

> 개발자: 선주 · 작업 브랜치 `claude/developer-tasks-organization-5u3h60`
> **앱1(학생 앱) 코드는 한 줄도 안 바뀌었다** — sw.js CACHE 인상 불필요. 데이터·로봇 규칙·관리자 화면만.

### 🔜 다음 세션이 첫 5분에 할 일
1. `node verify/audit-data.js` · `node verify/check-collab.js`
2. **검수 대기는 0건이다.** 105건 전부 처리했다(아래). 다시 세지 말 것.
3. **개발자에게 물을 것은 두 가지뿐**: ⓐ Claude API 잔액 충전 여부(양식 5건이 이것만 기다린다)
   ⓑ Resend 키(이메일 접수 4건 대기). 나머지는 다음 수집 로봇이 알아서 한다.

### 이 세션이 한 일

**① 밀린 컨펌 105건 전량 검수 — 검수 전 105 → 0건**
개발자 지시: *"직접 모두 검수한 다음 앱에 등록하고 개발자에게 결과만 알려줘."*
저장된 공고 원문(99/105건 확보)을 근거로 판정했고, 지어낸 판단은 넣지 않았다.

| 판정 | 건수 | 근거 |
|---|---|---|
| 검수 완료로 승격 | **82** | 등록 규칙(`entry-rules`) 통과 + 결과공지·대출·대학원·채용 아님을 원문으로 확인 |
| 제거 — 신청할 수 없는 공고 | 6 | 전부 **선발 결과 안내**였다(가천 봉은재단·가천 국가근로·건국 국가근로·세종 기숙지원·전남 도전장학생·성균관 성적우수). 원문에 "선발 대상자를 안내합니다", "선발인원 113명" 같은 문장이 있고 신청 절차가 없다 |
| 제거 — 철 지난 공고 | 7 | **전부 부경대**. 2014·2016·2020·2021·2024학년도 공고가 목록 위에 올라와 있었다. 하나는 제목만 "2026학년도"인데 원문 신청기간이 **2025.12.8~12.11**이었다 |
| 제거 — 전국판과 겹침 | 10 | 사랑나눔 3 · 화성시 소상공인 2 · 5·18 희망장학생 2 · 서울인재대학 1 · 포스코비전 1 · 한미첨단 1. 10차 세션 선례(외부 재단 사업은 전국 하나로) 그대로 |
| 마감을 원문에서 채움 | 8 | 제목·본문에 **글자 그대로 적혀 있던 것만**(예: 제목 "(7/24 마감…)", 원문 "접수기간 : '26. 6. 22 ~ 7. 24(금) 15:00"). 추론 0건 |
| 전국 승격 | 1 | 한미 첨단분야 청년교류 = KIAT 국가사업(동국·서강이 각각 게시) |

제거분은 전부 `auto-register-config.json`의 `blockIds`에 넣어 **다음 수집에서 다시 등록되지 않는다.**
결과: **정식 등록 184 → 162건 · 전부 검수 완료** (병합 중 로봇이 등록한 명지대 자연캠 국가근로 1건 포함).

**② 🔴 주소 열쇠가 서로 다른 공고를 하나로 뭉개고 있었다 — 613건 중 177건**
검수 중 "경북대 세종이도인재장학금 공고에 **코나아이 소상공인 공고의 자격 요건**이 붙어 있는" 것을 발견해
원인을 팠다. 자세한 내용·되돌리면 안 되는 지점은 `collector/canon-url.mjs` 첫머리 주석에 다 적혀 있다.
요약: '아는 이름의 파라미터만 남기고 나머지는 버린다'는 규칙이라, 아는 이름을 안 쓰는 게시판에서는
다른 글이 같은 열쇠가 됐다. 피해가 두 갈래였다 —
**ⓐ 남의 공고 원문이 붙는다**(원칙 8-1 정면 위반) **ⓑ 그 학교의 새 공고가 전부 '이미 등록됨'으로 조용히 건너뛰어진다.**
· 이름으로 가리는 방법은 못 쓴다: **경희대 `boardId`는 글 번호인데 전남대 `boardID`는 게시판 번호다.**
· 그래서 규칙을 **'군더더기만 버리고 나머지는 남긴다'**로 뒤집었다(`url-key.mjs`가 쓰던 방식 — 같은 실측에서 0건).
  방향의 근거: 두 쓰임 모두 **'다른 글'로 잘못 보는 쪽이 훨씬 싸다**(못 찾으면 정직하게 "아직 못 읽었어요",
  중복 등록은 눈에 보인다). 결과 **177 → 0건.**
· `notice-source.mjs`가 갖고 있던 **사본을 지우고** canon-url을 가져다 쓰게 했다.
· 남의 원문에서 뽑힌 발췌 **7건을 지웠다**(경희 의암·정읍시민·경희인턴·전남 도전장학생·충북 봉사장학생·
  가천 지성학Ⅱ·경북 세종이도). 지우는 조건은 '원문 제목이 다르다 + 새 규칙으로도 진짜 원문을 못 찾는다'
  둘 다 만족할 때로 좁혔다 — 애매한 1건(상명 국가유공자)은 사람이 눈으로 대조해 **남겼다**.
· 회귀 5항목(`test-collector.mjs`) — 경북대·경희대·전남대 실제 주소로 검사.

**③ 🔴 로봇이 만든 양식 19종 중 **쓸 수 있는 것은 9종뿐이었다** (원본 대조로 확인)
개발자 질문 *"양식도 모두 컨펌한 거야?"* 에 답하려고 **원본 첨부와 하나씩 대조**했다.
처음엔 '모양 검사'만 했는데, 그것으로는 **가장 위험한 실패인 조용한 누락을 못 잡는다.**
| 대조 결과 | 건수 |
|---|---|
| 원본 항목이 다 들어 있음 | **1종** |
| 원본 항목이 빠짐 → 내림 | **9종** |
| 원본 글자가 없어 대조 못 함 → 남김(재확보 예약) | 8종 |
| 모양 검사에 걸림 → 내림 | 1종 (새문안교회 — 제출서류가 질문) |
실제로 학생이 받고 있던 것:
· 인하대 변호산 — **자기소개서 4문항 중 3번 "학업계획 및 향후 진로계획"만** 빠짐(1·2·4는 있음)
· 방송대 중앙도서관 국가근로 — **우선선발 대상자 체크칸**(장애인·다자녀·다문화·보훈…)과
  **자기소개 및 지원 사유** 통째 누락. 대신 표 주석 `(과거 방송대 재학했던`이 질문이 됨
· 하림장학재단 — 첫 다섯 질문이 **제출서류 목록**("성적증명서 1통", "장학금 수령 계좌 사본")
· 염곡문화재단 — 원본 3장(서약서·지급원서·동의서)이 질문 8개로 줄고 생년월일·보호자 연락처 누락
→ **`collector/form-coverage.mjs` 신설** — form-quality와 짝이지만 보는 방향이 반대다
  (quality=이상한 게 들어갔나 / coverage=있어야 할 게 빠졌나). **원본이 없으면 '통과'가 아니라
  '모른다'로 답한다** — 이걸 뭉뚱그리면 확인한 적 없는 양식이 확인된 것처럼 보인다.
  무료 경로에 관문으로 배선했고 회귀 3항목을 뒀다.
⚠️ **남은 8종은 검증된 것이 아니다** — 원본 글자가 없어 대조를 못 했을 뿐이다.
  `collector/run-deepfetch.txt`에 재확보를 예약해 뒀다. 다음 세션이 받아서 대조할 것.
  · **손 제작 양식으로 바꾸고 싶어질 텐데, 추측으로 바꾸지 말 것.** `gasong-apply`(항목 8)·
    `yangcheon-apply`(항목 14)가 놀고 있고 로봇 판이 대신 연결돼 있는데, **로봇 판이 항목이 더 많다**
    (가송 21 · 양천 22). 손 제작이 지원서만 담고 자소서를 뺐을 수도, 로봇이 안내문을 섞었을 수도 있다.
    **원본 없이는 어느 쪽이 옳은지 알 수 없다** — 재확보 뒤 `form-coverage.mjs`로 양쪽을 대 볼 것.
    (산학협동재단만 바꾼 것은 손 제작이 서식Ⅰ+자소서Ⅱ를 다 담은 것이 문서로 확인돼서다.)

**③-1 🔴 무료 변환기가 만든 '못 쓸 신청서' 5종이 앱에 올라가 있었다**
학생이 이런 질문을 받고 있었다: `초` `중` `고` `대` (대상구분 표가 질문 4개로 쪼개짐) ·
`점` (성적 __ 점의 단위) · `화` `목` (시간표 요일) · 동의 선택지가 `")"` ·
그리고 **선발계획 공고문을 통째로 신청서로 만든 것**(질문이 '목적', '1) 신청대상', '2) 지원자격').
운영 원칙 4가 요구하는 "원본과 동일한 구조"를 못 지키는 문서라 **없는 것보다 나쁘다**(학생이 그대로 제출한다).
· 5건의 연결을 끊고 **원본 첨부 다운로드 안내로 되돌렸다**(첨부 링크는 공고에 그대로 있다).
· 어느 공고에도 안 붙은 로봇 양식 9종 삭제 · 산학협동재단 가톨릭대 게시분은 **손으로 만든
  `sanhak-foreign-apply`로 연결 교체**(로봇 판보다 원본에 충실).
· **재발 방지 — `collector/form-quality.mjs` 신설**: 판정을 **입력 글자가 아니라 결과물**로 옮겼다.
  예전엔 입력에 '시간표·원고지' 같은 낱말이 있나만 봐서 위 다섯 건이 전부 통과했다. 지금은 나온 양식에
  단위 한 글자·부스러기 선택지·공고문 절 제목이 질문으로 들어갔는지 본다. 걸리면 등록하지 않고
  API 경로로 넘긴다. 무료·유료 두 경로 **양쪽**에 관문을 달았다.
· **기준을 조이다 멀쩡한 양식까지 걸린 적이 있다** — 손으로 만든 조병두·산학·롯데는 '1. 성명'처럼
  번호를 붙여 쓰는데(원본이 그 모양), 번호만 보고 걸면 전부 탈락한다. 그래서 **번호를 뗀 뒤 내용을 본다.**
  교정 근거: 사람이 열어 확인한 못 쓸 5종은 전부 걸리고, 지금 앱의 48종은 전부 통과(실측).
· 5건은 `schematize-config.json`의 `alwaysApiIds`에 넣었다 — **무료 경로를 다시 태우면 같은 양식이 또 등록된다.**

**④ 관리자 양식 탭 — 미리보기 버튼과 원문 링크를 눈에 보이게**
개발자 지적: *"미리보기 칸을 확인할 수 없는데 어디 있어?"* 원인은 기능이 없어서가 아니라
**여는 방법이 '표의 줄을 아무 데나 클릭'뿐**이라 화면에 그 사실이 안 쓰여 있었기 때문이다.
· 양식 목록에 **'원본과 비교' 칸** 신설 — `미리보기` 버튼 + `원문 ↗` + `첨부 ↗`, 로봇이 만든 양식엔 `로봇 생성` 표.
· 미리보기 시트 안에도 원본 링크를 넣어 **원본과 미리보기를 나란히 놓고 항목 누락을 볼 수 있게** 했다.
· 회귀 3항목 추가.

### ④-1 이어서 — 양식을 원본 보고 다시 만들었다 (같은 세션 후반)

**🔴 먼저 찾은 것: HWP 원본을 앞 1023자만 읽고 있었다.**
`hwp-prvtext.py`가 읽는 `PrvText`는 한글이 **미리보기용으로 앞부분만** 담는 칸이라
1023자에서 잘린다. 저장된 HWP **91개 중 56개(62%)가 정확히 그 상태**였고 1023자를 넘는
파일은 하나도 없었다. 그래서 자동 변환기는 **모든 신청서의 앞 1000자만** 보고 있었다 —
③에서 본 '조용한 누락'의 구조적 원인이다.
→ **`collector/hwp-bodytext.py` 신설** (BodyText/Section*를 zlib로 풀고 HWPTAG_PARA_TEXT만 추림).
   같은 91개에서 글자가 **24만 자 늘었다**. `schematize-forms.mjs`가 **.body.txt를 .txt보다
   먼저** 본다 — 순서를 뒤집으면 재발한다. 수집 로봇 2종 + 심층 수집이 매 실행 돌린다.
   ⚠️ 제어문자의 14바이트 꼬리를 건너뛰고 서러게이트 짝을 맞춰야 한다(안 하면 저장이 터진다).

**그래서 API 없이 양식 12종을 만들었다** — 전부 원본 대조 통과:
홍익 봉사장학생 · 명지 자연캠 국가근로 · 방송대 출판문화원 이력서 · 한양 우편물실 ·
신협사회공헌재단 · 하림장학재단 · 가천 지성학Ⅱ · 상명 근로장학생 · 인하 변호산 ·
세종연구원 세종이도(경북·성균관 공용) · KOSAF AI멘토 확장 · 산학협동재단 서식Ⅳ 추가.
**손으로 만든 `sanhak-foreign-apply`에서도 서식Ⅳ 서약서가 통째로 빠져 있었다** — 대조 도구가 찾았다.

**🔴 뺀 공고 7건이 새 id로 되살아났다.** id가 `'auto-' + canonUrl(...).slice(-24)`라
**주소에서 파생된다.** ②에서 주소 규칙을 고치자 blockIds 23건이 전부 무효가 됐고, 다음
수집에서 부경대 2014·2016·2021·2024년 공고가 그대로 돌아왔다.
→ **`blockUrls`(정규화된 주소) 신설** — 되돌리기와 신규 등록 **양쪽**에서 본다. 규칙이 또 바뀌어도 산다.

**🔴 200으로 받았지만 오류 화면인 원문 16건.** 서울대가 점검 중이던 날 수집이 돌아
저장된 '원문'이 전부 "정보서비스 장애 조치 안내"였고, 발췌기가 **장학금 공고의 문의처를
전산실 헬프데스크 번호로** 만들어 놨다. → `looksLikeErrorPage`로 걸러 '원문 없음'으로 다룬다.

**로봇이 새로 등록한 16건 검수** — 승인 4(서울대 동행·계명 장애도우미·전남 행정업무보조
국가근로(마감 8/21 원문에서 채움)·중앙 인문100년) · 제거 12.

**최종: 정식 등록 166건 · 검수 전 0건 · 양식 48종(사람 제작 39 · 로봇 9) · formId 연결 38건.**
양식 전수 점검 — 원본 대조 통과 13 · 빠짐 0 · 모양 실패 0 · **원본이 없어 대조 못 함 35**
(이건 '통과'가 아니라 '모른다'. `run-deepfetch.txt`에 재확보 예약돼 있다).

### 검증
`audit-data` 통과(경고 28 — 전부 기존 항목) · `test-collector` 전항목(신규 13항목) ·
`verify-admin` 113항목 · `verify-notify-rules` · `drive` · `verify-registered` · `verify-forms-data` · `personas`.

### ⚠️ 이 세션에서 알게 된 것 (재발견 금지)
- **부경대 게시판은 옛 글이 목록 위에 올라온다.** 2014~2024년 공고 7건이 그래서 자동 등록됐다.
  수집 대상에서 빼자는 게 아니라, **부경대분은 등록 전에 작성일을 봐야 한다**는 뜻이다.
- **`verify-registered.js`의 'SKKU registered visible: 0'은 이 세션 이전부터 0이었다**(고친 판·안 고친 판
  양쪽으로 돌려 확인). 드라이버가 찾는 공고 이름(조병두·삼일·보건…)이 전부 마감돼 목록에서 내려갔다.
  대체 구동이 작동하므로 검사 자체는 유효하다 — **내 변경 탓이라고 오해하지 말 것.**
- **`verify-forms-data.js`는 저장소를 그냥 서빙하면 안 된다** — 더미 양식을 주입한 **복사본**을 8124에
  띄워야 한다(verify/README.md 70~73줄). 안 하면 'test-dummy 연결 공고: undefined'로 30초 타임아웃난다.

---

## 이전 세션 상태 (2026-08-09 21차 세션 — 관리자 화면 가동 + 개편 계획 전 단계 완료)

> 개발자: 선주 · 작업 브랜치 `claude/developer-tasks-organization-b5nxwj`
> **세 브랜치(작업·기본·main) 전부 동일 배포 완료.** **앱1(학생 앱) 파일 무변경 — sw.js CACHE 인상 불필요.**

### 🔜 다음 세션이 첫 5분에 할 일
1. `node verify/audit-data.js` · `node verify/check-collab.js`
2. **관리자 화면은 2026-08-09에 가동을 시작했다 — 잠금·열쇠 다시 묻지 말 것.**
   `hanggonggan-admin.pages.dev`가 Cloudflare Access 뒤에 있고(검사로 실증), 화면에 166건이 뜨고
   컨펌 버튼이 작동한다. 두 개발자 모두 열쇠 발급 완료.
3. **가장 값어치 있는 다음 일은 코드가 아니라 검수 87건이다.** 화면에 다중 선택이 붙어서
   `마감 임박만 (10건)` → `검수 완료로` 를 누르면 몇 초에 끝난다. 개발자에게 이걸 먼저 권할 것.
4. **관리자 화면 개편 계획서(`docs/ADMIN-PLAN-2026-08-06.md`)는 전 단계가 끝났다** —
   0·A·B0·B3·B1·C·E·B6·B2·B5·B8·D1·D2·B7까지. **새 항목을 지어내 이어 하지 말 것.**
   더 손볼 것이 생기면 개발자가 화면을 써 보고 말한 것부터 계획서에 추가한다.
   ⚠️ 계획서를 만든 브랜치(`claude/two-factor-admin-screen-44a4u9`)는 합쳐졌다 — 거기서 이어 하지 말 것.

### 이 세션이 한 일 (여섯 가지)

**① 개발자 업무 정리** — `PROGRESS.md` 숫자가 실제와 크게 벌어져 있어(등록 89→**166건**,
양식 28→**56종**, 피드 200→**600건**, 수집망 12→**45곳**) 전수로 세어 맞추고, 할 일을
**🧭 개발자 업무 정리** 한 절로 합쳤다. 할 일이 문서 두 곳에 중복돼 한쪽만 고쳐지던 구조를 없앴다.

**② Cloudflare Access 잠금 — 완료 (실증됨)**
- 21:01 검사: `/` `HTTP 200` 🔴 그대로 열림 → 21:39 검사: `302 → cloudflareaccess.com` ✅
- 설정값: Self-hosted · Public hostnames **두 줄**(`hanggonggan-admin.pages.dev` +
  `*.hanggonggan-admin.pages.dev`) · Policy `개발자만` = Allow + **Emails**(두 분 메일)
- **`Emails ending in`을 고르면 안 된다** — `@gmail.com`으로 끝나는 전 세계 사람이 들어온다.
- **`Service auth`가 기본 선택돼 있다** — 그건 기계끼리 쓰는 것이라 사람 로그인이 안 된다.
  `Public DNS`를 골라야 한다.
- **Domain 드롭다운에는 `pages.dev`가 없다**(우리 소유가 아니므로). `Switch to custom input`으로
  주소를 통째로 친다. 별표 줄은 **운영 주소 자신을 안 덮는다** — 그래서 두 줄이 필요하다.
  (⚠️ 옛 `_admin/README.md`는 별표 한 줄만 적어 뒀었다. 2026-08-09에 고쳤다.)

**③ GitHub 열쇠 — 두 개발자 모두 발급 완료**
| | 선주 | 공동작업자(Josehyeon) |
|---|---|---|
| 종류 | Fine-grained | **Classic** (fine-grained는 자기 소유 저장소에만 붙는다) |
| 범위 | `hanggonggan` 하나 · Actions 쓰기 + Contents 읽기 | `repo` + `workflow` (좁힐 수 없음) |
| 만료 | **2026-11-07** | 90일 |

**🔑 열쇠를 공유하면 안 되는 이유(코드 근거)**: `admin-apply.yml:58`이 `ACTOR: ${{ github.actor }}`를
넘기고 `tools/admin-apply.mjs:212`가 `by: actor`로 변경 이력에 남긴다. 커밋 메시지에도 들어간다.
공유하면 '누가 했나'가 전부 한 사람으로 찍혀 변경 이력의 절반이 무의미해진다.

**④ 잠금 확인 버튼 신설 — `.github/workflows/admin-lock-check.yml`**
- 로그인 없이 관리자 주소 3곳을 열어 보고 **잠김 / 안 잠김 / 판정 불가** 셋으로 답한다.
- 매일 14:23 KST 자동 실행. 안 잠겼으면 🚨 이슈(라벨 `admin-lock`), 다시 잠기면 **스스로 닫는다**
  (실제로 이슈 #110을 자동으로 닫았다).
- **🔴 판정을 셋으로 나눈 것이 핵심.** 처음 만든 판은 접속 실패(000)를 "✅ 잠김"으로 결론냈다.
  가짜 응답 4종으로 시험하다 잡았다 — '못 읽음'을 '괜찮음'으로 단정하지 말 것(동국대 교훈과 같은 계열).
  같은 이유로 **경보 이슈 닫기는 `verdict == 'locked'`일 때만** 한다.
- 왜 필요한가: Cloudflare 설정 화면은 '만들어진 것처럼' 보여도 호스트 이름이 어긋나면 실제 주소가
  열린 채 남는다. **설정을 보는 것으로는 증명이 안 된다** — 푸시 검사 버튼과 같은 계열.

**⑤ 관리자 화면 A·B0·B3·B1·C단계** — 아래 두 절.

**⑥ 개편 계획서 나머지 전부 — E · B6 · B2 · B5 · B8 · D1 · D2 · B7 · B0 잔여**
- **E(로봇 통제판)**: 새 탭 '로봇'. 열린 경보·조치 요청을 맨 위에, **로봇 12종 실행 버튼**,
  리포트 5종, 푸시 서버 상태. 수동 실행 버튼은 예약 실행에 **더해지는 것**이라 정해진 시간
  수집을 깨지 않는다(`schedule`과 `workflow_dispatch`는 서로 독립).
- **B6(키보드·읽어 주기)**: 목록 줄에 초점+Enter/Space, 시트를 대화상자로 알리고 초점 가두기·
  닫을 때 제자리 복귀, 탭 좌우 화살표, 진행·알림 `aria-live`.
- **B2·B5·B8**: 정렬(같은 것을 다시 누르면 방향 뒤집기) · 확인 시트(**`window.confirm` 0개**가 됐다) ·
  밝게/어둡게 토글.
- **D1(자격 요건 편집)**: 기계 판정용 값과 사람이 읽는 문장을 **따로** 고친다.
  `eligibilityLines`·`eligibilityVerified`는 **학생 앱이 이미 읽는 칸**이라 앱1 코드 변경이 없었다.
- **D2(양식 큐 조작)**: 그만 시도 / 다시 받기 / 없는 양식 연결 끊기.
  ⚠️ 연결 끊기는 **양식이 진짜로 없을 때만** 동작한다 — 있는데 끊으면 학생이 앱에서 못 쓴다.
- **B7·B0 잔여**: 스크롤·초점·커서 자리 보존, 기한 구획(스캔 지점), 밀도 토글,
  넘치는 표에만 붙는 안내, 시트 아래 고정 버튼 줄. 되돌리면 안 되는 지점은 아래 절.

### ⚠️ 관리자 화면에서 되돌리면 안 되는 지점
**A단계(신뢰성)**
- **읽기 실패를 조용히 빈 값으로 바꾸지 말 것** (`readJson` → `D.failed` → 빨간 배너).
  예전엔 인터넷이 끊겨도 '검수 전 0건 · 모든 게시판 정상'으로 보였다 —
  **실패가 안심되는 쪽으로 틀린다**는 게 핵심이다. 전부 실패면 아예 입장시키지 않는다.
- **`hEntry`는 `??`로 폴백할 것.** `|| {}`는 `{}`가 참이라 폴백이 **한 번도 실행되지 않는다** —
  '공통' 캠퍼스 4곳(성균관·경희·외대·명지)이 언제나 `—/0`이었다(고친 뒤 4곳 다 정상 표시로 실증).
- **스키마화 대기는 `pendingForms()` 한 곳에서만 센다.** 예전엔 화면마다 18 vs 23으로 달랐다.
- **`.job`은 헤더 안에 둔다.** 밖에서 각자 `top:0` sticky를 걸면 작업 띠가 탭 줄을 덮는다.

**B0단계(치수·대비)**
- **`--ink-3`는 14px 이상에만.** 밝은 화면 대비 3.72:1로 작은 글자 기준(4.5:1) 미달이고,
  다크 모드는 5.05:1로 통과라 **낮에만 안 보인다**. 그 아래 크기는 `--ink-2`(7.58:1).
- **`th`에 `text-transform:uppercase`를 다시 넣지 말 것** — 한글엔 효과가 없고 자간만 벌어진다.
- 본문 16px · 공고 제목 16px · 메타 14px · 터치 목표 40px(칩·작은 버튼·입력) · 탭 44px.

**B3(다중 선택)·C(등록)**
- **체크할 때 목록 전체를 다시 그리지 말 것** — 스크롤이 맨 위로 튀어 87건을 훑으며 고를 수 없다.
  `refreshSelBar()`가 선택 바만 갈아 끼운다.
- **일괄 처리는 대상을 목록으로 펼쳐 보여 준 뒤** 확인받는다. 삭제·되돌리기는 git 말고 되돌릴
  방법이 없다. 예전 '문제 없는 것 한꺼번에 컨펌'은 건수만 알려 주고 무엇인지 안 보여 줬다.
- **🔴 `tools/admin-apply.mjs`에 새 동작을 추가하면 `WRITES_REG` 목록에도 반드시 넣을 것.**
  2026-08-09에 `register`가 실제로 빠져 있어서 **"✅ 성공"이라고 말하면서 파일은 그대로**였다
  (로컬 시험에서 "등록했다는데 목록에 없다"로 잡음). 이슈 #79와 같은 유형이다.
- **등록 규칙은 로봇과 같은 것을 쓴다** — 중복은 `canonUrl(sourceUrl)`(auto-register의 `classify`가
  보는 그 집합), 유효성은 `verify/entry-rules.cjs`의 `checkEntry`. 대출 공고로 실증했다.

### 🆕 `collector/canon-url.mjs` 신설 — 이 파일을 다시 합치지 말 것
주소 정규화(`canonUrl`)는 원래 `auto-register.mjs` 안에 있었는데, **그 파일은 불러오는 순간
수집·등록을 실행한다**(맨 아래 최상위 코드 — CLAUDE.md에 이미 기록된 함정). 그래서 관리자 화면의
쓰기 경로가 import하면 로봇이 통째로 돌아 버린다. 순수 함수만 담은 모듈로 빼고 auto-register는
re-export한다. **베껴 두면 사람 등록분과 로봇 판정이 갈라져 같은 공고가 두 번 등록된다.**

### 검증
`node verify/verify-admin.js` **102항목 전부 통과**(이 세션에서 71항목 추가).
- playwright-core가 이 샌드박스에 없다 → `npm i playwright-core` 후 `NODE_PATH`로 물려 돌린다.
  브라우저는 `/opt/pw-browsers`에 이미 있다.
- 핵심 항목: **'다중 선택이 실제로 여러 id를 보낸다'**(요청을 가로채 id 10개 확인) ·
  '실행 전에 대상 목록을 실제로 보여 준다' · '금액은 비워 둔다(지어내지 않는다)' ·
  '데이터를 못 읽으면 경고가 뜬다' · '정렬을 바꿔도 훑던 자리를 잃지 않는다'.
- **⚠️ 검사 도구를 고칠 때 알아야 할 것 3가지**
  ① `applyAction`에는 `jobBusy` 잠금이 있어 **한 번에 한 작업만** 보낸다(제품이 옳다).
     검사에서 연속으로 두 번 보내려면 `runs` 응답을 흉내 내 앞 작업이 '반영 완료'로 끝나게
     해야 한다 — 안 그러면 6분간 막혀 뒤 항목이 전부 실패한다.
  ② **Playwright의 `click()`은 누르기 전에 그 요소를 화면 안으로 끌어온다.** 그래서
     '스크롤 위치가 지켜지는가'를 `click()`으로 검사하면 **검사 도구가 먼저 스크롤을 망가뜨린다**
     (실제로 900 → 126으로 나와 멀쩡한 코드를 실패로 읽었다). 그런 항목은
     `page.evaluate(() => el.click())`으로 끌어오지 않고 눌러야 한다.
  ③ **한 화면만 재는 검사는 헛돌 수 있다.** '표가 넘칠 때만 안내가 붙는가'를 수집망 화면에서만
     쟀더니 그 화면의 표는 안 넘쳐서 **안내가 한 번도 안 뜬 채 통과**했다. 지금은 7개 화면을
     모두 돌며 재고, **넘치는 표가 실제로 있는지**를 따로 한 항목으로 확인한다(현재 5/7개).
- 회귀: `audit-data` 0 · `test-collector` 전항목 · `check-deploy-sync` 통과.

### ⚠️ B7·B0 잔여에서 되돌리면 안 되는 지점 (2026-08-09 마무리분)
- **`rerender()`를 거치지 않고 `renderScreen()`을 직접 부르지 말 것.** 필터·정렬을 누를 때마다
  화면을 통째로 새로 만드는 구조는 그대로 두되, **잃는 것(스크롤 위치·초점·커서 자리)만**
  `rerender()`가 되돌린다. 예전엔 검색칸만 손으로 초점을 되살리고 있었는데 그게 증상이었다.
- **구획 제목(`.group-head`)의 `top`은 `--head-h`로 준다.** 헤더 높이는 글자 크기·화면 폭에 따라
  달라져 숫자로 박을 수 없다. `measureHead()`가 재서 넣고 창 크기가 바뀌면 다시 잰다.
  숫자로 박으면 좁은 화면에서 **구획 제목이 헤더에 가려진다**(`.job`·선택 바와 같은 유형).
- **구획은 마감 임박순일 때만 만든다.** 제목순·학교순에서 기한 구획은 뜻을 잃고, 같은 구획이
  여기저기 흩어져 오히려 훑기를 방해한다.
- **'촘촘하게'는 줄 간격만 줄인다 — 글자 크기·색은 건드리지 않는다.** 글자를 줄이면 B0의
  대비·치수 규칙(`--ink-3`는 14px 이상에만)이 깨진다. 터치 목표 40px도 그대로 둔다.
- **표 안내(`.scroll-hint`)는 실제로 넘칠 때만 붙인다.** 안 넘치는데 "옆으로 밀어 보세요"라고
  하면 없는 칸을 찾게 된다. `markScrollers()`가 재어 보고 붙이고, 창 크기가 바뀌면 다시 잰다.
- **시트 아래 버튼 줄(`.sheet-foot`)보다 안내 문구를 위에 둘 것.** 버튼 줄이 화면에 붙어 있으므로
  그 아래에 글을 두면 **영영 가려진다.**

## 같은 날 병행 세션 (2026-08-07~09 브라우저 수집 수리 · 회원가입 설계 확정)

> ⚠️ **이 세션은 위 '20차 세션'(관리자 화면 게시)과 같은 날 병행 진행됐다.** 작업 영역이 겹치지
> 않아(이쪽은 `collector/browser-collect.mjs`·회원가입 설계 문서, 저쪽은 `_admin/`·푸시) 각자
> 독립적으로 진행하고 기본 브랜치에서 자동으로 합쳐졌다. 개발자: Josehyeon.

### 이 세션이 한 일 — 세 가지

**① 브라우저 수집기 — 학교 두 곳이 비어 있던 원인 2가지 수리 (완료·실증됨)**
- **부산대**: 공고 행이 `<a href="#popup">`이고 클릭 처리가 스크립트로만 붙어 있어(onclick 속성 없음)
  기존 클릭 대상 선택자 두 가지에 하나도 안 걸렸다. `a[href^="#"]`를 클릭 대상에 추가.
- **서울교대**: 옆 메뉴의 '장학제도'·'장학'·'학자금대출' 링크 3개가 "이미 상세 주소가 3개 있다"로
  세어져 클릭 수집이 아예 안 돌았다. 가동 조건에서 `isMenuEntry`로 메뉴를 제외(수집 본체와 같은 모듈 사용).
- 수정: [collector/browser-collect.mjs:147](collector/browser-collect.mjs) · 회귀 5항목을
  `verify/test-collector.mjs`에 추가(되돌리면 실제로 실패하는 것까지 확인).
- **실행으로 실증 완료**: 부산대 1건 → **2건**, 서울교대 0건 → **14건** (`collector/browser-report.md`).
  기본·main 브랜치까지 반영됨.

**② 회원가입·로그인(Supabase) 설계 확정 — 위 '### ⑧ 회원가입·로그인' 절 참조**
개발자 지시로 착수는 나중으로 미뤘고, 그때 다시 논의하지 않도록 결론만 문서에 남겼다.
**재설계·재조사 금지** — 자세한 내용은 해당 절을 볼 것. 요약: DB는 Supabase 확정(Cloudflare 직접
구현 아님), 회원 데이터는 git을 안 타고 폰↔Supabase 직행, 약관·개인정보처리방침이 선행 필수,
착수 전 카카오 지원 여부·무료 등급 한도 확인 필요.

**③ 용인시 창업지원센터 입주신청서 + K-Startup 상담 준비 — 개발자 개인 작업, 저장소 문서화 대상 아님**
이 세션에서 함께 진행했으나 **인수인계 문서에는 기록하지 않는다**(개발자 요청). 다음 세션이 이
작업의 존재를 몰라도 무방하다 — 저장소·앱 코드와 무관한 산출물이었다.

### 다음 세션이 바로 이어서 할 수 있는 것
1. **세션 시작 시 관례대로**: `node verify/audit-data.js` + `node verify/check-collab.js` 먼저.
2. **부산대·서울교대는 해결됨** — 재조사 불필요. 인수인계 문서의 '게시판 미확보·미작동 현황' 절
   (12차 세션 기록)에 이 두 학교가 '거의 안 잡히는 곳'으로 남아 있다면 그 항목만 지울 것.
3. 회원가입·로그인은 **아직 착수하지 않는다** — 위 ⑧ 절의 "지금 하는 일이 아니다" 경고를 지킬 것.
4. 관리자 화면이 어디까지 갔는지는 **맨 위 21차 세션 절**을 볼 것(20차 게시 → 21차 A·B0단계) —
   Access 잠금 여부가 다음 개발자 액션의 분기점이다.
5. 오늘 처리하지 못하고 남은 일반 잡무(양식 원본 미확보 6건, 자격요건 미확보 71건, 원문 링크 3건)는
   인수인계 문서 상단 '완성해야 하는 것' 표와 이전 세션 기록들을 참고.

---

---

## 이전 세션 상태 (2026-08-07 20차 세션 — 관리자 화면 Cloudflare 게시 · 푸시 등록 버그 3종 수리)

> 개발자: 선주 · 작업 브랜치 `claude/two-factor-admin-screen-15kh66`
> **세 브랜치(작업·기본·main) 전부 동일 배포 완료.** sw.js CACHE **v51**.

### 🔜 다음 세션이 첫 5분에 할 일 (이 문단만 읽어도 이어서 작업 가능)

1. `node verify/audit-data.js` · `node verify/check-collab.js` (관례 — 감사가 실패면 로봇 결과가 안 저장된다)
2. **선주님께 물을 것 한 가지**: *"Access 잠금 하셨나요?"*
   · 했으면 → **GitHub 열쇠 만들기(5분) → 컨펌 1건 눌러 한 바퀴 실증(1분)** = 2단계 완료
   · 아직이면 → 아래 '남은 업무' 표의 3단계부터 그대로 안내
3. 그다음이 **3단계(관리자 화면 개편)** — 계획서가 이미 있다:
   **`docs/ADMIN-PLAN-2026-08-06.md`** (브랜치 `claude/two-factor-admin-screen-44a4u9`).
   ⚠️ **그 계획을 세운 세션과 이 세션이 병행 중이다.** 개편 착수 전에 **어느 세션이 할지 선주님께
   확인**할 것(같은 화면을 두 세션이 고치면 합칠 때 사람이 손으로 풀어야 한다).

### 1. 관리자 화면 — Cloudflare에 올라갔다 (4단계 중 2단계 완료)

| 항목 | 값 |
|---|---|
| 주소 | **https://hanggonggan-admin.pages.dev** (열림 확인) |
| 프로젝트 | Cloudflare Pages `hanggonggan-admin` |
| Production branch | `claude/nice-heisenberg-WESq5` (저장소 기본 브랜치라 Cloudflare가 자동으로 채웠다) |
| Build command / 출력 | `bash _admin/build.sh` / `_admin/dist` · Root directory 비움 |
| **Build watch paths** | `_admin/*` `data.js` `forms.js` `verify/entry-rules.cjs` `collector/url-key.mjs` — **설정·실동작 둘 다 확인** |

- **빌드 감시 경로가 실제로 작동하는 것을 실증했다**: 감시 목록에 없는 커밋 4건이 Deployments에서
  전부 `Skipped`로 표시됐다. 이걸 안 넣으면 로봇이 하루 10번 커밋할 때마다 헛빌드가 돌아
  무료 한도(월 500회)를 며칠에 쓴다.
- **남은 것은 아래 2·3단계뿐**이다(잠금 + 열쇠). 화면 코드·쓰기 경로는 이미 다 준비돼 있다.
- **🔴 Access를 아직 안 걸었으면 주소가 열려 있다.** 열쇠가 없으면 화면에 아무 내용도 안 그려지지만,
  **이 저장소가 공개라서 `verify-key`가 아무나의 GitHub 토큰도 통과시킨다**(`_admin/admin.js`의
  `verifyKey`는 `GET /repos/OWNER/REPO`가 200이면 통과 — 공개 저장소는 누구 토큰으로도 200).
  즉 주소를 아는 사람은 **볼 수 있다**(고칠 수는 없다 — 쓰기는 우리 저장소 Actions 권한이 필요).
  **이것이 Access가 선택이 아니라 필수인 이유다.**

### 2. 🔴 푸시 등록 버그 3종 — 원인·수리·회귀 (되돌리면 안 되는 지점)

공동개발자 폰이 "앱을 켜야만 알림이 온다"는 제보에서 시작해, **서로 겹쳐 있던 결함 3개**를 찾아 고쳤다.
**KV 지표의 `Deletes: 0`이 판정을 뒤집은 결정적 증거였다** — 서버는 아무것도 지운 적이 없었다.

| # | 원인 | 조치 | 회귀 |
|---|---|---|---|
| ① | **`pushEnsure`가 브라우저의 진짜 구독이 아니라 앱 메모(`pushEndpoint`)만 봤다.** 구독은 앱 모르게 죽는다(폰이 앱 정리·저장공간 삭제·주소 변경). 한 번 죽으면 앱을 아무리 열어도 **영영 재등록되지 않았다** | 열 때마다 ⓐ 브라우저에 구독이 없으면 죽은 메모 지우고 새로 등록 ⓑ 주소가 다르면 재등록 ⓒ 같아도 하루 지났으면 서버에 재확인(`pushSyncedAt`) | `verify-push-client.js` 2항목 |
| ② | **서비스워커가 '껐다'와 '못 읽었다'를 구분하지 않았다.** 장부를 못 읽으면 `normalizeLedger(null)`이 `enabled:false`인 기본 장부를 만들어 주는데, 그걸 "사용자가 껐다"로 읽고 **스스로 구독을 끊었다** | **확실히 읽었고 확실히 꺼져 있을 때만** 정리. 판정은 `notify-rules.js`의 **`shouldSelfUnsubscribe(raw, readOk)`** 한 곳 | `verify-notify-rules.js` 6항목 |
| ③ | **폰이 주소를 갈아탈 때 옛 주소를 서버에 방치**했다 → KV에 죽은 등록이 산 것처럼 쌓인다(“2개 보이는데 받는 폰은 1대”) | 등록 성공 시 옛 주소로 `/unsubscribe` 호출. 실패해도 다음 발송의 404 정리가 받친다(2중) | `verify-push-client.js` 1항목 |

- **①②는 고치기 전 코드로 되돌려 실제로 실패하는 것까지 확인했다**(증상이 제보와 정확히 일치).
- **MY 화면이 원인을 말해 준다**: 예전엔 '⏳ 연결하는 중'만 떴다. 지금은 `pushReasonText()`가
  '휴대폰이 알림을 막고 있어요' / '이 브라우저에서는 쓸 수 없어요(카톡 인앱 금지)' / '연결 실패' 로 갈라 준다.
- **⚠️ 되짚을 수 없는 것 하나(정직 기록)**: ①의 방아쇠가 ②였는지 iOS의 저장공간 정리였는지는
  로그가 없어 **끝내 특정하지 못했다.** 둘 다 막았으므로 재발하지 않지만, "원인을 확정했다"고 쓰지 말 것.

### 3. 신설 — 푸시가 진짜 되는지 30초에 확인하는 버튼

**`.github/workflows/push-check.yml`** (Actions 탭 → `푸시 알림 검사` → Run workflow)
- `/health` 조회 + **`/test`로 등록된 전 기기 시험 발송**. 저장소 시크릿 **`PUSH_ADMIN_KEY`** 사용
  (2026-08-07 등록됨 · Cloudflare `handaejang-push`의 `ADMIN_KEY`와 같은 값).
- **왜 필요했나**: Claude 실행 환경은 `workers.dev` 접속이 막혀 있고(프록시 403),
  **KV 화면의 등록 수는 '살아 있는 폰 수'가 아니다.** 실제로 받는지는 **발송해 보는 것만이 증명**이다.
- `/health`에 **`subs`(등록 기록 수)** 를 추가했다 — KV 화면을 눈으로 세던 일을 없앴다.
- **실증(2026-08-07 14:14 KST)**: `{"subs":2}` · `{"tried":2,"woke":2,"dropped":0}` ·
  **두 폰(선주·공동개발자) 실물 도착 확인.**

### 4. 알아 둘 한계 — 사용자가 늘 때 (지금은 문제 없음)

무료 등급 제한(실행당 바깥 요청 50건) 때문에 **2분마다 25대**씩 깨운다(`SEND_PER_TICK`).

| 등록 폰 | 전원 깨우는 데 |
|---|---|
| 100대 | 8분 |
| 500대 | 40분 |
| 1,000대 | 1.3시간 |
| 2,000대 | 2.7시간 (`MAX_SUBS_PER_WAKE` 상한) |

수백 명까지는 무해하다(마감 알림이 몇십 분 늦는 정도). **1,000명을 넘길 때의 해법**: ⓐ 걸음당
25→45대(제한 50 안에서) ⓑ 학교별로 회차 쪼개기 ⓒ Workers 유료 $5/월. **미리 하지 말 것.**

### 5. 일부러 손대지 않은 것 (판단 근거 기록 — 다시 검토하지 말 것)

`sw.js`의 `backgroundCheck()`에도 ②와 같은 '못 읽으면 기본값' 패턴이 있다(`sw.js:141`).
다만 그 자리의 결과는 **'이번 회차 건너뛰기'** 일 뿐 구독을 잃지 않고 다음 회차에 재시도된다.
손대면 오히려 위험을 만들 수 있어 **그대로 두는 것이 맞다고 판단**했다.

### 6. 곁다리로 고친 검사 도구 2건 (앱 코드와 무관)

- `verify/verify-push-client.js` — 8/6에 `push-config.js`에 진짜 주소가 채워지자 드라이버가 찾던
  빈 칸(`endpoint: ''`)이 사라져 **가짜 시험 서버 대신 진짜 서버로 등록을 보내고 있었다.**
  이제 저장소 값과 무관하게 정규식으로 **항상 덮어쓰고, 덮어썼는지 확인한 뒤** 진행한다.
- `verify/verify-notify.js` — 같은 이유로 8/6부터 빨간불이었다. 검사용 브라우저가 시크릿 모드라
  크롬이 남기는 안내(`Push API in incognito`)였고 **앱이 피할 수 없는 것**이라 무시 목록에 넣었다.
  *교훈: 설정값이 채워지면 그 값이 '비어 있음'을 전제하던 검사가 조용히 무의미해진다.*

### 7. 남은 업무 — 2단계 관리자 화면 (약 16분)

| 단계 | 할 일 | 시간 | 누가 |
|---|---|---|---|
| ~~1~~ | ~~Cloudflare Pages 게시~~ | — | ✅ 완료 |
| ~~2~~ | ~~빌드 감시 경로~~ | — | ✅ 완료 |
| **3** | **Access 잠금** — `Zero Trust` → `Access` → `Applications` → `Add an application` → `Self-hosted` · **Subdomain 칸에 `*`** · Domain `hanggonggan-admin.pages.dev` · Policy `Allow` → Include **Emails** → 두 분 메일 | 10분 | 선주 |
| **4** | **GitHub 열쇠** 만들어 화면에 넣기 (fine-grained · `hanggonggan`만 · Actions **Read and write** · Contents **Read-only**) | 5분 | 선주 |
| **5** | **컨펌 1건 눌러 한 바퀴 실증** (화면→검사→저장→학생 앱) | 1분 | 선주 + Claude 판독 |

> 3단계에서 **Subdomain `*`를 빼면 미리보기 주소만 잠기고 진짜 주소는 열린 채로 남는다.**
> 로그인 코드 메일은 `noreply@notify.cloudflare.com` — **Gmail 스팸함·프로모션 탭**으로 자주 간다.
> 4단계에서 공동작업자(Josehyeon)는 fine-grained를 만들 수 없다 → **classic 토큰(`repo`+`workflow`)**.

### 8. 이 세션에서 얻은 사실 (재발견 금지)

- **Cloudflare Pages 만들기 경로가 바뀌었다**: `Compute` → `Workers & Pages` → `Create` →
  화면 **맨 아래 "Looking to deploy Pages? Get started"** → `Import an existing Git repository`.
  위쪽 카드들은 전부 **Worker(서버)** 만들기라 그대로 진행하면 화면이 안 만들어진다.
- **Cloudflare Secret은 볼 수 없고 `Rotate`로 갈아끼우기만 된다**(값 옆 연필 → Rotate → 새 값 → Deploy).
  `ADMIN_KEY`는 잃어버려도 무해하다(다시 Rotate + GitHub 시크릿 교체 30초). **VAPID 3종은 다르다 —
  갈아끼우면 등록된 폰이 전부 무효가 된다.**
- **`mcp__github__actions_list`의 응답은 매우 크다**(runs 1건에 90k자+). 컨텍스트를 아끼려면
  `python3 -c "import re; ..."`로 저장된 결과 파일에서 필요한 값만 뽑아 읽을 것.
- **GitHub Actions는 이 샌드박스의 유일한 외부 발송 경로다** — `workers.dev`·`pages.dev`·`github.io`
  전부 프록시 403이다. 원격 확인이 필요하면 워크플로를 만들어 `workflow_dispatch`로 돌린다.

## 이전 세션 상태 (2026-08-06 18차 세션 — 🎉 진짜 푸시 실가동 · Cloudflare 1단계 완료)

> 개발자: 선주 · 작업 브랜치 `claude/cloudflare-server-connection-5wwp5p`
> **세 브랜치(작업·기본·main) 전부 동일 배포 완료.** sw.js CACHE **v47**.

### 한 줄 요약
**푸시 발송 서버를 Cloudflare에 올려 실제로 폰 2대에 알림이 도착하는 것까지 실증했다.**
7월 31일에 코드만 만들어 두고 "개발자 액션 대기"로 남아 있던 항목이 **가동 상태**가 됐다.
그 과정에서 **무료 등급의 벽 2개**와 **등록이 0대이던 진짜 원인**을 찾아 고쳤다.

### 1. 배포된 것 (지금 살아 있는 상태)
| 항목 | 값 |
|---|---|
| 워커 주소 | `https://handaejang-push.seonju5543.workers.dev` |
| 상태 확인 | `GET /health` → `{"ok":true,"configured":true,"step":"idle","lastError":null}` |
| KV 네임스페이스 | `SUBS` = `b2eb1c8f30814fad91249e2f08334fbf` (wrangler.toml에 기재) |
| 예약 실행 | cron `*/2 * * * *` (2분마다 한 걸음 — 아래 3번 참조) |
| 배포 방식 | **GitHub 연결(Workers Builds)** · Root directory `server/push` · 프로덕션 브랜치 = **기본 브랜치** |
| 빌드 감시 경로 | `server/push/*` (없으면 로봇 커밋마다 헛빌드) |
| 비밀값 4개 | `VAPID_PUBLIC` `VAPID_JWK` `VAPID_SUBJECT` `ADMIN_KEY` — **Cloudflare에만** 있다 |
| 앱 연결 | `push-config.js` 두 칸 채움 → `pushConfigured()` true |

- **`ADMIN_KEY`는 저장소에 없다.** `/test`·`/run`을 부르려면 선주님께 받아야 한다(Cloudflare
  Settings → Variables에서 새로 만들어 덮어써도 된다). VAPID 개인키도 마찬가지로 저장소에 없다.
- **코드를 고치면 자동 배포된다** — `server/push/` 안을 고쳐 기본 브랜치에 push하면 Cloudflare가
  받아서 올린다. 비밀값·KV는 배포해도 지워지지 않는다.
- **실증**: `POST /test` → `{"tried":2,"woke":2,"dropped":0}` · 아이폰 2대(선주·공동개발자)에
  알림 도착 확인(2026-08-06 01:42 KST).

### 2. 🔴 등록된 폰이 0대이던 진짜 원인 (되돌리면 안 되는 수정)
비밀값까지 다 넣었는데 KV에 폰이 **한 대도** 등록되지 않았다. 원인은 서버가 아니라 앱이었다.
> **발송 서버 등록(`pushSubscribe`)이 '최초 1회 동의 시트'에서만 일어나고 있었다.**
> 그 시트는 사람당 딱 한 번만 뜬다. 그래서 ⓐ 발송 서버가 생기기 전에 이미 알림을 켠 사람(=전원)
> ⓑ MY 화면에서 알림을 켠 사람(그 경로엔 등록 호출 자체가 없었다)은 **영영 등록되지 않았다.**
> 유일한 등록 경로가 'MY의 앱을 켜지 않아도 받기 스위치'였는데 그걸 누른 사람이 아무도 없었다.

조치(개발자 지시로 **푸시를 기본값으로**):
- **`notify.js pushEnsure()` 신설** — 알림이 켜져 있고 권한이 있으면 자동 등록. `notifyInit`과
  화면 복귀(visibilitychange)에서 호출하므로 **기존 사용자도 앱만 다시 열면 저절로 메꿔진다.**
- MY의 알림 켜기 경로에서도 `pushEnsure()`를 부른다.
- **'앱을 켜지 않아도 받기' 스위치 삭제** — 이제 상태만 보여준다. 끄는 것은 '알림 끄기' 하나로 충분
  (그 경로가 이미 구독 해지까지 한다).
- 회귀: `verify/verify-push-client.js`에 **'등록이 비어 있으면 앱을 열 때 자동 등록'** 항목 추가.
  이 검사를 지우면 등록 0대 상태로 되돌아간다. 삭제된 스위치를 누르던 검사 3곳도 교체.

### 3. 무료 등급의 벽 2개 — 배포 전에 고쳤다
자세한 내용·수치는 위 **'Cloudflare 배포 이전 — 실측 근거'** 절의 푸시 항목 참조. 요약:
- ⓐ **바깥 요청 50건/실행** → 한 번에 800대를 깨우려던 코드는 48대에서 잘렸다
- ⓑ **계산 시간 10ms/실행** → 공고 데이터 해석만으로 6~9ms라 **사용자 0명이어도 강제 종료**됐다
→ **'한 걸음씩' 구조**(reg→notices→plan→send 25대씩)로 바꾸고, 걸음이 5회 실패하면 포기하고
`state:lastError`에 사유를 남겨 `/health`에 노출한다. 검증 `verify/verify-push-server.mjs` **74항목**.
- 검증 중 **발송 재개 커서가 묶음 중간을 가리켜 이미 깨운 폰을 다시 깨우던 버그**도 잡았다
  (60명이 70번 깨워짐). 묶음은 반드시 끝까지 처리하도록 고쳤다.

### 4. 새 사용자는 자동인가 (개발자 질문 — 코드로 확인함)
**자동이다.** 온보딩을 마치면 2.9초 뒤 동의 시트가 저절로 뜨고(`app.js:1482`), '알림 받기'를
누르면 **그 자리에서 발송 서버에 등록**된다. 이후 앱을 열 때마다 등록 상태를 스스로 점검·복구한다.
- **다만 '알림 받기' 클릭 한 번은 없앨 수 없다** — 브라우저 규칙상 알림 권한은 사용자가 직접 누른
  클릭에서만 요청할 수 있다. 토스·당근도 동일.
- **아이폰은 홈 화면에 설치해야만** 푸시를 받는다(애플 정책). 동의 시트에 설치 안내가 뜬다.

### 5. ⚠️ 다음 세션이 반드시 확인할 것 — 하루 지난 뒤 `/health`
`https://handaejang-push.seonju5543.workers.dev/health`를 열어 **`lastError`** 를 본다.
- `null` → 정상. 더 볼 것 없다.
- `{"step":"reg"}` 또는 `{"step":"notices"}` → **계산 시간 10ms에 걸린 것.**
  해법은 이미 정해져 있다: **GitHub Actions가 작은 요약 파일(`data/push-digest.json`)을 만들고
  Worker는 그것만 읽게 한다.** 해석 비용이 0에 수렴한다. (Node 실측 4~12ms라 경계선이었고,
  Workers가 더 빠를 것으로 보이나 미검증 상태로 배포했다.)

### 6. 남은 일 (다음 세션이 이어서) — ⚠️ 1번은 20차 세션에서 절반 진행됨(위 절 참조)
1. ~~**2단계 — 관리자 화면**~~: 게시·빌드 감시 경로까지 **2026-08-07 완료**. 남은 것은 Access 잠금 + 열쇠.
2. **3단계 — 메일 접수 서버**: Resend 키를 받으면 `server/mail-worker.js` 배포.
3. 공동개발자 Cloudflare 초대(Administrator) — 이 세션에서 초대 화면까지 진행함.
4. 앱1 이전·저장소 비공개 전환은 **아직 하지 않는다**(순서 주의 — 위 🔴 항목).

### 7. 이 세션에서 얻은 사실 (재발견 금지)
- **Claude 샌드박스는 `workers.dev`에 접속할 수 없다**(프록시가 403). 워커 상태 확인은
  **개발자 브라우저로 `/health`를 열게 하는 것**이 유일한 방법이다.
- Cloudflare 새 대시보드는 메뉴 이름이 다르다: `Workers & Pages` → **Compute** ·
  Root directory 칸의 이름은 **`Path`**(Advanced settings 안) · KV 만들기는 **`+ Create Instance`**.
- **KV 네임스페이스 ID는 주소창 맨 뒤에 있다**(`.../namespaces/<이 값>`). 목록 화면엔 안 보인다.
- **workers.dev 주소는 기본적으로 꺼져 있다** — Domains 탭에서 Production 스위치를 켜야 접속된다.
- `.gitignore`가 모든 `package.json`을 무시하고 있어 `!server/push/package.json` 예외를 넣었다
  (Workers Builds가 이 파일로 wrangler를 설치한다).
- Cloudflare 무료: 계정 멤버 초대 가능하지만 **Super Administrator는 계정에 한 명뿐** —
  초대받는 사람은 `Administrator`(결제·멤버관리만 제외, 실제 작업 권한은 동일).

## 이전 세션 상태 (2026-08-03 16차 세션 — 수집 로봇 시간초과 수리 · 인프라 개편안 검토)

> 개발자: 선주 · 작업 브랜치 `claude/admin-page-elements-210xu2`
> **앱1 파일 무변경 — sw.js CACHE 인상 불필요.** 로봇 코드·문서만 바뀌었다.

### 이 세션이 한 일 — 한 줄 요약
Cloudflare 이전을 검토하다 **브라우저 수집이 이틀간 멈춰 있는 것**을 발견해 먼저 고쳤고
(40분 취소 → **7분 완료 · 17/17개 학교 · 신규 50건**), 개발자가 제안한 인프라 개편안을
실측으로 검토해 **Oracle은 아직 쓰지 않기로 결정**했다.

### 1. 🚨 브라우저 수집 시간초과 수리 (본체)
경위·해법·되돌리면 안 되는 지점은 위 **'⚠️ 시간초과(취소)는 넘어져도 저장으로 못 막는다'** 절 참조.
핵심만: **상한을 올리는 게 아니라 스크립트가 스스로 22분 예산 안에 끝내게 했다.**
신설 `collector/harvest-budget.mjs`(순수 함수) · 상세 방문 학교당 4분 · 실패 재시도 병렬 ·
학교 순서 회전(`browser-cursor.json`) · `timeout-minutes` 40 → 30 ·
`verify/test-collector.mjs` 회귀 16항목(가짜 시계라 인터넷 불필요, 되돌리면 실제로 실패하는 것까지 확인).

### 2. 인프라 개편안 검토 — 개발자 결정 반영
개발자 제안: 저장소 비공개 + Cloudflare 배포 + Oracle 자체 러너 + SSH 공동 관리.
· **합의된 것**: 서버(푸시·메일·회원 처리)와 관리자 화면은 **Cloudflare로 간다.**
· **결정: Oracle 자체 러너는 아직 쓰지 않는다** — 근거는 위 'Oracle Cloud 자체 러너' 절.
· **아직 안 한 것**: 앱1 이전, 비공개 전환. 순서 주의(위 🔴 항목 — 순서를 지키지 않으면 앱이 죽는다).
· 산출물: `INFRA-REVIEW-2026-08-03.md`(전문) + `docs/INFRA-REVIEW-2026-08-03.pptx`(12장, 공동작업자 공유용).

### 3. ⚠️ 다음 세션이 반드시 처리할 것 — 두 예산이 서로를 모른다
이 세션의 수집 예산(22분)과 **공동작업자가 같은 날 추가한 `deepfetch --fill`**(최악 16분)이
**서로 모르는 채 합쳐졌다.** 각자의 안전장치는 잘 되어 있지만 더하면 최악 **43분 > 상한 30분**이다.

| | 보통 | 최악 |
|---|---|---|
| 수집 스크립트 | 7분 | 22분 |
| `deepfetch --fill` | 2~3분 | 16분 |
| 설치·감사·저장 | 5분 | 5분 |
| **합계** | **15분** ✅ | **43분** ❌ |

보통은 문제없지만 **학교가 여러 곳 안 열리는 날 다시 취소될 수 있다.** 이 조합은 아직 한 번도
같이 돌아 본 적이 없다(수리 13:46 UTC → `--fill` 추가 14:05 UTC).
**해법 권고: 작업(job) 전체 마감 시각을 두 단계가 공유하게 한다**(각자 예산을 조이는 것보다 깔끔).
수집 로봇은 공동작업자 담당 영역이라 이 세션은 **상의 대기 상태로 두었다.**

### 4. 검증
`audit-data` 0 · `test-collector` 전항목(신규 16항목 포함) · `check-deploy-sync` 통과 ·
브라우저 수집 **실제 실행으로 실증**(7분·17/17곳·50건).

### 5. 다음 세션이 바로 할 수 있는 것
1. 위 **3번 예산 충돌** 처리 (공동작업자와 상의 후)
2. 거의 안 잡히는 학교 2곳 손보기 — **부산대**(브라우저 대상에 추가) · **상명대**(동적 렌더)
3. 앱1 Cloudflare 이전 준비 — 주소 상수화(8군데) · `_headers` 신설 · `_admin` 제외 설정
4. 분교 3곳 게시판 주소 요청 — 연세 미래 · 고려 세종 · 동국 WISE

## 이전 세션 상태 (2026-08-03 15차 세션 — 자격 요건 무료 확보, 54 → 81건)

> 개발자: Josehyeon · 작업 브랜치 `claude/handaejang-project-continue-104357`
> sw.js CACHE **v45** (match-engine 기호 목록 한 줄만 바뀜)

- **개발자 지적**: "앞으로 너가 못 쓴 몇십 건의 공고를 다 하나하나 고치라고 할 수는 없잖아."
  → 비용 질문("최대한 무료로 할 수는 없나")에 실측 비용표를 제시했고, **개발자가 '완전 무료 — 규칙만
  개선'을 선택**했다. AI 경로는 만들지 않았다.
- **한 건씩 고치기를 그만두고 전수로 세었다.** 그랬더니 규칙 낱말 문제가 아니었다 —
  원문이 **잘리고(5,000자 컷) 사라지고(파일 통째 재생성)** 있었다. 자세한 내용·되돌리면 안 되는
  지점은 위 '**자격 요건 채우기 — 2026-08-03 무료 경로로 구축 완료**' 절 참조.
- **결과**: 화면에 자격이 보이는 공고 **54 → 81건**(39% → 59%), 의심 줄 7개 유지
  (채점기가 잡음 유형을 3가지 더 보게 한 뒤에도 7개 — 진짜로 깨끗하다).
- **작업 도중 스스로 만든 버그 2개를 재채점 도구로 잡았다**(이 방식이 실제로 값을 했다):
  ① 채점기의 `^[\d\W]{1,6}$`가 **한글도 \W라서** '한부모가정'을 기호로 잘못 잡았다.
  ② `cut`을 잘렸을 때만 남겨 잘리지 않은 긴 원문이 **매 실행 다시 받히는 무한 반복**이 됐다.
- **검증**: audit-data 0 · test-collector 전항목(공동작업자 시간예산 검사와 합침) ·
  verify-notify-rules 전항목 · tidyRequirement 기호 정리 직접 확인 · YAML 확인.
  ⚠️ **playwright가 로컬에 없어 브라우저 회귀(personas·drive)는 이 세션에서 못 돌렸다.**
  match-engine 변경은 문자 목록 한 줄이라 직접 검사로 대신했지만, 다음 세션에서 한 번 돌려 볼 것.
- **다음 세션이 할 것**: ① 브라우저 회귀 실행 ② 미확보 57건 중 '자격 제목 없음' 31건을 더 줄일지
  판단(재채점 도구로 전후 비교) ③ 첨부 HWP 안에만 자격이 있는 공고는 무료로 불가 — 개발자와 상의.

## 이전 세션 상태 (2026-08-03 14차 세션 — 관리자 페이지 1단계 구축)

> 개발자: 선주 · 작업 브랜치 `claude/admin-page-elements-210xu2`
> **앱1(학생 앱) 파일은 한 줄도 바뀌지 않았다** — sw.js CACHE 인상 불필요.

- **개발자 지시**: "관리자 페이지를 만들고 싶다 — 지금은 각 학교 학생인 척 접속해야만 공고를 볼 수 있다."
  기능 목록 8개 영역을 개발자가 제시 → 검토 후 **회원 관리 제외**(앱1에 회원 개념이 없고, 만들려면 학생
  개인정보를 서버에 모아야 함)·**신고→사용자 제보함 전환**·**정보 앱에 필요한 10가지 추가** 합의.
  이어진 논의로 **관리자 화면만 Cloudflare Pages + Access**로 결정(개발자 원안인 '이메일 일회용 코드
  로그인'이 설정만으로 구현되고, 앱1과 저장 공간도 분리됨). **앱1은 옮기지 않는다.**
- **이번 세션 산출물 (1단계 — 서버 없이 되는 범위 전부)**
  ① `_admin/` 화면 6개: 오늘 할 일 · 공고 전체(4축 분류) · 컨펌 작업대(**원문 대조**) · 양식(**생성 문서
     미리보기**) · 수집망 · 데이터 품질(+변경 이력). 잠금은 **GitHub에 실제로 확인하는 열쇠** —
     열쇠 전에는 운영 현황이 한 글자도 안 그려진다(Access 설정 전에도 이미 진짜 잠금).
  ② 쓰기 경로 `tools/admin-apply.mjs` + `admin-apply.yml` — action 7종(confirm·revert·remove·edit·
     merge·autoRegister·addBoard). **감사 통과해야만 저장**, 실패 시 되돌리고 🚨 이슈. 고칠 수 있는
     항목은 ALLOWED로 못 박아 브라우저에서 온 값을 믿지 않는다.
  ③ 기존 워크플로 2곳 연결: `deploy-sync.yml`의 workflow_run에 '관리자 조정' 추가(빠뜨리면 최대 12시간
     앱에 안 나감), `check-live.yml`에 **`/_admin/`이 학생 주소에서 정말 404인지 매일 확인**하는 스텝.
  ④ `verify/verify-admin.js` 신설 — 바깥 요청을 가로채 저장소 실제 파일로 응답시켜 검사.
- **검증**: verify-admin 23항목 전부 통과(양식 46종 전부 문서 생성 성공) · admin-apply 7종 로컬 실행 +
  **감사 관문이 실제로 되돌리는지 일부러 깨뜨려 실증** · audit-data 통과 · 앱1 회귀(drive·personas) 통과.
  검증 중 발견해 고친 것: `[hidden]`을 클래스 display가 덮어 **숨은 시트가 화면을 가로막던 버그**,
  메타 태그로는 무시되는 `frame-ancestors`(→ `_admin/_headers`로 이동).
- **⚠️ 이번 세션이 발견한 기존 문제 (내 변경과 무관 — 손대지 않음)**: `verify/verify-registered.js`가
  **원본(ddb9869)에서도 실패한다.** 7/31에 들어간 알림 동의 시트(`#notify-backdrop`)가 화면을 덮어
  드라이버의 클릭이 막힌다. 별도 워크트리에 원본을 꺼내 돌려 재현 확인함. 알림 쪽은 다른 개발자
  작업 영역이라 이번엔 고치지 않았다 — **드라이버 첫머리에서 알림 시트를 닫아 주면 된다.**
- **다음 세션이 할 일**: 선주님이 `_admin/README.md`대로 Cloudflare Pages + Access를 올린 뒤
  → 실제 주소에서 컨펌 1건을 눌러 앱1까지 반영되는 한 바퀴 실증. 그다음이 2단계(푸시 발송·사용자 제보함·
  이메일 로그인 통합)와 3단계(익명 집계 + 개인정보처리방침 — **앱1에 약관·방침이 아직 없다**).

## 이전 세션 상태 (2026-08-02 13차 세션 — 수집망 2배 확장 · 수집기 버그 5건 · 검색 UI)

> 개발자: Josehyeon(josehyeon0926) · 작업 브랜치 `claude/scholarship-data-url-verify-544ab2`
> **세 브랜치(작업·기본·main) 전부 `e9b3223`로 동일 배포 완료.** sw.js CACHE **v23**.

### 한 줄 요약
개발자가 게시판 주소를 대량으로 주셔서 **수집 대상 13 → 29곳**으로 늘렸고, 그 과정에서
**수집기 버그 5건**이 드러나 함께 고쳤다. 앱 대학 목록도 55 → 67곳으로 늘렸다.

### 1. 수집망 확장 (13 → 29캠퍼스)
| 구분 | 학교 |
|---|---|
| **신규 (일반 수집기 13곳)** | 숙명·가천·아주·국민·숭실·세종·이화·인하·방통대·경북·가톨릭·항공대·경기·서울과기 |
| **주소 교체 3곳** | 건국(2096=NEWS→2239) · 명지(255=일반공지→259) · 한양(전체공지→장학 필터) — **셋 다 옛 주소가 장학 게시판이 아니어서 0건이었다** |
| **브라우저형 6곳** | 고려 안암 · 서강 · 중앙 · 부산 · 계명 · 서울교대 (전부 클릭형/SPA라 일반 fetch로는 0건) |
| **아직 없음 3곳** | 연세 미래(원주) · 고려 세종 · 동국 WISE(경주) |

- **캠퍼스 통합 주의**: 건국(서울+글로컬)·한양(서울+ERICA)은 한 게시판에 함께 올라온다.
  같은 주소를 두 줄에 넣으면 **`seen` 장부를 공유해 뒤쪽 캠퍼스가 0건**이 되므로 `campus:"공통"`으로 합쳤다.
- 실제 수집 결과: 공고 **146 → 411건**, 0건인 학교 없음.
- 서강대는 Nuxt SPA라 막히면 내부 API를 쓸 것: `/api/api/v1/mainKo/BbsData/boardList?bbsConfigFk=141`(=장학공지, JSON).

### 2. **함정 — curl로 안 나온다고 '학생도 못 본다'고 단정하지 말 것 (이 세션에서 4번 틀렸다)**
인하·경북·부산·중앙·서강 다섯 곳을 "차단됨/0건"으로 잘못 보고했다. 개발자가 "나는 잘 들어가진다"고
짚어 준 뒤 **세션 없는 브라우저로 열어 보니 전부 정상**이었다.
- **인하대** — SSO가 자바스크립트 자동제출 폼을 한 번 거쳐 `ac=N`(로그인 불필요)으로 되돌아온다. curl은 JS를 실행하지 않아 중간 페이지(596바이트)에서 멈춘 것.
- **경북대** — 막힌 게 아니라 옛날 `<frameset>`. 브라우저는 프레임을 따라 들어간다.
- **부산·중앙·서강** — 목록을 자바스크립트가 그린다.
→ **'로봇이 못 읽는다'와 '학생이 못 본다'는 다른 판정이다.** 후자는 반드시 브라우저로 확인할 것.
   로봇용 주소(프레임 안쪽·enc 푼 것)와 학생에게 보여 줄 주소는 달라도 된다. (기술 사실 절에도 기록)

### 3. 수집기 버그 5건 (전부 시험 수집으로 드러났다 — 돌려보지 않았으면 못 찾았다)
| # | 버그 | 피해 | 조치 |
|---|---|---|---|
| ① | **`안내$`가 실공고를 지웠다** | 세종대 13건 중 11건 사라져 **0건** · 명지대 10→1건. `2026-2 주거안정지원장학금 신청안내` 같은 진짜 공고들 | 브라우저 수집기엔 있던 `NOTICE_SIGNAL` 우회가 일반 수집기엔 없었다 → **판정을 `clean-title.mjs`의 `isMenuEntry()` 한 곳으로 합쳐** 두 로봇이 같이 부른다. 세종 0→9 · 명지 1→9 |
| ② | HTML 특수문자 미해제 | 앱에 `&quot;근로지담당자&quot;`로 보임 | `cleanTitle`에 해제 추가(**`&amp;`는 반드시 마지막** — 먼저 풀면 이중 해제) |
| ③ | 게시판 옆 메뉴가 공고로 | '학자금 대출' 등 20건 | 연도·학기·회차 표시 없고 짧으면 메뉴. **순서 주의** — `NOTICE_SIGNAL`보다 먼저 봐야 '장학생 선발'(아주대 메뉴)이 안 샌다 |
| ④ | **리포트가 이슈 한도(65,536자) 초과** | 265건 나오자 `Body is too long`로 실패. 수집·저장은 다 끝난 뒤였다 | 60,000자 넘으면 잘라내고 전문 위치 안내. 수집 로봇 2종에 적용 |
| ⑤ | 문서에 `undefined`가 찍힘 | 서약문 없는 양식에서 `esc(undefined)`가 `"undefined"` | `pledge` 있을 때만 그린다 + 감사에서 pledge를 **필수에서 제외**(원본에 서약이 없는 양식이 실제로 있다 — 사랑나눔 자기소개서, 서약은 별개 동의서에 있음) |

**안전 확인 방법**: 새 필터를 만들면 **배포 중인 공고 전체에 대 봐서 몇 건이 사라지는지** 먼저 센다.
①③ 도입 때 146건 중 걸린 것은 1건뿐이었고 그것도 장학 공고가 아니었다.

### 4. 앱 대학 목록 55 → 67곳 (data.js)
방통대·한국교원대 + 교대 10곳(서울·경인·공주·광주·대구·부산·전주·진주·청주·춘천).
IST 5곳은 이미 있어 **정식 명칭 별칭만** 보강('한국과학기술원'→KAIST 등). 전공에 초등교육과·유아교육과 추가.
**통용 약자는 '방통대'다**(개발자 확인).

### 5. 학교 검색 스크롤 — **이 세션 최대의 실패, 반드시 읽을 것**
개발자 제보: "'서울'을 치면 후보가 여러 개인데 스크롤이 안 돼 아래 학교를 못 고른다."
**같은 문제를 세 번 고쳤는데 전부 헛다리였다.**
- 시도 1: `max-height:240px` + `overflow-y:auto` → 안 됨
- 시도 2: `visualViewport`로 키보드 뺀 공간 측정 → 안 됨
- 시도 3: 카톡 인앱브라우저 대비 + `scrollIntoView` → 안 됨

**진짜 원인은 높이가 아니었다:**
```js
list.addEventListener('touchstart', e => { e.preventDefault(); pick(...) })
```
**손가락이 닿는 순간** 스크롤을 막고 즉시 항목을 골라 버렸다. 끌어 내릴 방법이 없었다.
('교육에 4개만 나온다'도 같은 원인 — `slice(0,30)`은 이미 배포돼 10개가 다 그려져 있었고 못 내렸을 뿐이다.)

> **왜 못 잡았나**: 브라우저 검사를 **mousedown으로만** 했다. 마우스는 누르는 즉시 선택이 정상이라
> 전부 통과했고, **손가락으로 끄는 동작은 한 번도 재현하지 않은 채** "5가지 화면 크기 전부 통과"라고
> 보고했다. **터치 UI는 반드시 TouchEvent로 '끌기'와 '탭'을 각각 재현해 검사할 것.**

조치: mousedown은 그대로, 손가락은 `touchstart`에서 위치만 기억(`passive:true`)하고
`touchend`에서 **10px 넘게 움직였으면 스크롤로 보고 고르지 않는다**.
검증: 끌기 → 선택 안 됨 · 탭 → 선택됨. 둘 다 TouchEvent로 확인.
곁들여: 후보 6→30개, 스크롤 막대 항상 표시.

### 6. 이 세션의 환경 사실
- **로컬 맥에 node가 없다.** `~/.local/bin`에 v22 심볼릭 링크를 걸어 뒀다(PATH에 이미 있음).
  없어졌으면 nodejs.org에서 darwin-arm64 tar.gz를 받아 같은 자리에 링크하면 된다.
- **이 샌드박스는 학교 사이트·github.io에 실제로 접속된다** (예전 세션 메모와 다르다).
  다만 `curl`은 JS를 실행하지 않으므로 위 2번 함정에 주의.
- **GitHub 인증**: 처음엔 없어서 push가 안 됐다. 개발자가 `gh` v2.97을 `~/.local/bin`에 설치하고
  `gh auth login`(HTTPS·브라우저)을 마쳐 지금은 push된다. 계정 `Se-Hyeon-Jo`, 권한 `repo`·`workflow`.
- 앱 회귀 검사 전 로컬 서버 필요: `python3 -m http.server 8123 &`

### 7. 다음 세션이 할 일
1. **개발자에게 스크롤 수정(v23)이 실제로 먹었는지 확인**받을 것. 안 되면 카카오톡 인앱브라우저 문제일 수 있으니 사파리로도 열어 보게 안내.
2. **브라우저형 6곳 첫 수집**(고려 안암·서강·중앙·부산·계명·서울교대) — `collector/run-browser-collect.txt` 수정 후 push.
   클릭 방식이 학교마다 달라 **0건이 나올 가능성이 높다**(경희대 때 8회 걸림). 리포트의 진단(프레임 수·클릭 시도·본 글자)부터 볼 것.
3. 남은 게시판 3곳 주소 요청: 연세 미래(원주)·고려 세종·동국 WISE(경주).
4. 새로 들어온 공고 265건 중 정식 등록 후보 검수(`node verify/list-unregistered.js`).
5. 경기대 주소는 **전용 게시판이 아니라 '작성자에 장학이 든 글' 검색 결과**다 — 장학팀이 아닌 부서 공고는 빠질 수 있으니 더 나은 주소가 있으면 교체.

## 이전 세션 상태 (2026-08-01 12차 세션 — 원문 링크 사냥꾼 안정화 · 학교 확장 대비)

> 개발자: 선주(josehyeon0926@gmail.com 계정으로 진행) · 작업 브랜치 `claude/scholarship-notice-link-fix-ef4lk2`
> **세 브랜치(작업·기본·main) 전부 같은 내용으로 배포 완료.** 검사 전부 통과.

### 이 세션이 한 일 — 한 줄 요약
어제 만든 **링크 사냥꾼이 실제로는 결과를 한 번도 저장하지 못하고 있었다.** 그걸 고치고,
동국대 원문 링크 7건을 사람이 직접 확인해 복구했으며, **학교를 더 붙여도 견디도록** 상한·시간제한·병렬을 넣었다.

### 1. 사냥꾼이 결과를 못 남기던 3중 사고 (전부 해결)
| # | 무엇 | 왜 나빴나 | 조치 |
|---|---|---|---|
| ① | 리포트 마지막 줄에서 옛 이름 `GIVE_UP_AFTER`를 부름 | **4분간 찾아낸 원문 주소 13건이 통째로 버려짐**(저장 단계가 통째로 건너뛰어짐) | 그 줄 수정 |
| ② | 넘어지면 결과 전량 폐기 구조 | 25분짜리 일에 '전부 아니면 전무'는 너무 비싸다 | `saveAll()` 일원화 + `uncaughtException`/`unhandledRejection`에서 저장 후 종료(**코드 1 유지** — 실패는 실패라고 알린다). 워크플로 감사·저장 단계를 `always()`로 |
| ③ | 이런 오타를 실행 전에 못 잡음 | 실행해 보기 전엔 안 보인다 | `verify/test-collector.mjs`에 **collector/*.mjs 전수 '선언 없는 대문자 이름' 정적 검사** 추가(자기 검증 포함) |
| ④ | `reg-hufs-namgaju`가 없는 양식(`auto-남가주동문-apply-2`)을 가리킴 | **감사 실패 → 워크플로가 되돌리기를 해서 사냥 결과가 저장 안 됨** | 실제 존재하는 `auto-남가주동문-apply`로 연결 |
| ⑤ | 복구 로봇(resolve-detail-urls)도 같은 구조 | 같은 사고가 날 자리 | 같은 안전장치 적용 |

**교훈(반드시 기억)**: 오래 걸리는 로봇은 **저장 경로를 하나로 모으고**, 넘어져도 그 경로를 타게 하라.
그리고 **데이터 관문(감사)이 실패 상태면 로봇이 아무리 잘 돌아도 결과가 안 남는다** — 세션 시작 때 `node verify/audit-data.js`를 꼭 먼저 돌릴 것.

### 2. 동국대 원문 링크 — "주소는 처음부터 맞았고 우리 판정이 틀렸다"
사냥꾼이 동국대 12건을 전부 `제목 불일치(다른 글이 열림)`로 버렸다. **정찰 도구로 직접 열어 보니 전부 맞는 공고였다**(200 · 로그인 불필요).
- **진짜 원인**: 동국대는 짧은 시간에 여러 번 두드리면 응답을 막는다(정찰에서도 주소 3개 뒤 4번째가 `ERR_CONNECTION_CLOSED`).
  그때 오는 것은 404가 아니라 **본문이 안 그려진 껍데기 화면**인데, 로봇이 그걸 '다른 글'로 단정했다.
- 조치: ⓐ 불일치 시 **4초 더 기다려 재확인** ⓑ 본문 표지(등록일·첨부·이전글…)가 아예 없으면 **'판정 불가'**로 다뤄 멀쩡한 주소를 버리지 않음 ⓒ 대상 간 간격 900ms→**2500ms**.
- **정찰로 확인한 7건을 데이터에 직접 반영**(정식 등록 표식 5→2건). 확인 안 된 것은 손대지 않음 — 지어낸 주소 0건.
- **동국대 목록 행은 `href="#none"`** — 주소가 아예 없어 **반드시 눌러야** 한다. 그런데 `디지털 역사관 … 오늘 하루 보지 않기 X` **팝업이 화면을 덮어** 클릭이 5초 대기 후 실패했다(진담거사 등). → 팝업을 먼저 닫고, 그래도 막히면 위치를 따지지 않는 방식(`el.evaluate(e=>e.click())`)으로 누른다.
- 남은 2건(산학협동재단·진담거사)은 표식 그대로 — 앱이 '게시판 목록 ↗'으로 정직 안내. 사냥꾼이 간격을 늘려 가며 계속 재시도한다.

### 3. 후보 주소 규칙을 한 곳으로 (`rowDetailCandidates`)
사냥꾼이 **'행에 원래 적혀 있던 주소(row.abs)'를 아예 후보에 안 넣고** 있었다(복구 로봇엔 있던 규칙).
→ `collector/detail-url.mjs`에 **`rowDetailCandidates({row, listUrl, forms, landed, dom})`** 신설, 두 로봇이 같이 쓴다.
순서: ① 눌러서 실제로 간 주소 ② 행에 적힌 주소 ③ 조립한 주소. `test-collector.mjs`에 순서 회귀 4항목.
(동국대엔 ②가 없어서 이 수정이 동국대 해법은 아니지만, 다른 학교에는 유효하고 규칙 갈라짐을 막는다.)

### 4. 사냥꾼의 대상 — **본업은 '원문 공고가 안 열리는 링크'** (개발자 지시로 순서 변경)
- 예전엔 **순찰(기존 링크 재확인)이 1단계**여서 본업이 밀렸다. 이제 **사냥이 1단계, 순찰은 '덤'**으로
  **사냥이 다 끝나고 시간이 남을 때만** 돈다. 기본 20건(예전 40~60), `collector/run-link-hunt.txt`에 **`patrol: 0`이면 완전히 끔**.
- **순찰을 아주 없애지 않은 이유(중요)**: '주소는 멀쩡해 보이는데 눌러 보면 로그인 벽/다른 글'인 링크는
  표식(`#n-`)이 없어 눈에 안 띈다. **경희대 7건이 실제로 그랬고**, 개발자가 "로그인하라고 뜬다"고 알려 주기 전까지 아무도 몰랐다.
  이 세션 순찰에서도 광운·서강 1건씩 더 잡았다. 순찰에서 걸린 건은 **다음 실행의 사냥 대상**이 된다.

### 5. 학교를 더 붙여도 견디게 — 3가지 (개발자 지시)
| # | 문제 | 조치 |
|---|---|---|
| ① | 공고 전체 상한이 **200건 고정** → 학교를 10곳쯤 더 붙이면 **넘친 만큼 오래된 공고가 말없이 사라짐**(오류도 리포트도 없음) | `url-key.mjs`에 **`capNotices()`** 신설 — **학교 수 × 15건(최소 200)**. 넘칠 때 뒤에서 뭉텅이로 자르지 않고 **학교당 몫을 함께 줄인다**(30개교 시험에서 7개교가 통째로 0건이 되던 것을 막음). 수집 로봇 2종이 같은 함수를 쓴다 |
| ② | **어느 워크플로에도 시간 상한이 없어** GitHub 기본값 6시간까지 매달림(7/31 브라우저 수집이 실제로 **360분**). 수집 2종이 같은 대기줄(`concurrency: collector`)이라 그동안 다음 수집이 줄을 섰다 | 로봇 워크플로 6종에 `timeout-minutes` (수집 25 · 브라우저 40 · 사냥 40 · 복구 40 · 심층 30 · 정찰 15) |
| ③ | 브라우저 수집이 학교를 **하나씩 차례로** 봐서 학교가 늘수록 뒤쪽이 불리 | **서로 다른 학교 3곳씩 동시에**(`BROWSER_PARALLEL`, 기본 3 · 1이면 예전 방식). 같은 학교를 몰아치는 일은 없고, 리포트는 설정 파일 순서대로 되돌린다 |

**검증**: `test-collector.mjs`에 상한 회귀 4항목 추가(지금 규모 유지 · 30개교에서 200건에 안 잘림 · 30개교 전부 보존 · 한 학교 40건 상한).

### 6. 확장 한계에 대한 사실 (개발자 질문 "왜 30개교가 마지노선인가")
**30은 한계가 아니다 — 시험용 예시 숫자였다.** `학교 수 × 15`에는 천장이 없다. 진짜 걸림돌은 하나:
> **앱은 켤 때마다 `data/notices.json`을 통째로 내려받은 뒤 자기 학교 것만 골라 쓴다**(`app.js` `liveNoticesHtml`).
> 고려대 학생도 동국대 공고 35건을 같이 받는다. 실측 **1건당 654바이트**.

| 학교 수 | 공고 수 | 폰이 받는 크기 |
|---|---|---|
| 13(지금) | 146 | 93 KB |
| 30 | 450 | 0.28 MB |
| 50 | 750 | 0.47 MB |
| 100 | 1,500 | 0.93 MB |

- 서비스워커가 `no-cache` 재확인 → 안 바뀌면 **304**라 반복 실행은 거의 0바이트. 실제로 다 받는 건 **공고가 바뀐 날 첫 실행**뿐.
- 실행 시간·학교 서버·GitHub 요금은 **안 걸린다**(공개 저장소라 Actions 무료).
- **실질 한계 50~100개교.** 서울권 주요 대학을 다 붙여도 30~40곳이라 지금 구조로 충분.
- **100개교를 넘길 때의 해법(지금은 하지 말 것)**: `data/notices/<학교>.json`으로 **학교별 파일 분리** → 학생은 자기 학교 것만 받으므로 상한 자체가 필요 없어진다. `app.js`도 같이 고쳐야 하니 **학교가 40곳쯤 넘을 때** 하면 된다.
- '학교당 40건'은 화면과 무관하다(앱은 장학 8칸+대출 2칸만 표시) — **파일 크기 조절용**이다.

### 7. 게시판 미확보·미작동 현황 (다음 세션이 개발자에게 물어볼 것)
**🔴 주소가 아예 없는 곳** — 숙명여대(브라우저 후보 2, 0건) · 가천대(후보 3, 0건) · 서강대(후보 3, 1건) ·
연세 미래(원주) · 고려 세종 · 한양 ERICA · 건국 글로컬 · 동국 WISE.
*(중앙대는 schools.json엔 없지만 브라우저 로봇이 11건 수집 중 — 사실상 정상)*

**🟡 주소는 있는데 공고가 안 들어오는 곳 (이쪽이 더 급함 — 그 학교 학생 화면이 비어 있다)**
| 학교 | 공고 | 등록된 주소 | 추정 |
|---|---|---|---|
| 고려대 안암 | **0건** | `scholarship.korea.ac.kr` | 안내 홈페이지라 개별 공고 없음 |
| 건국대 서울 | **0건** | `konkuk.ac.kr/konkuk/2096/subview.do` | 주소 변경 추정 |
| 명지대 | **0건** | `mju.ac.kr/mjukr/255/subview.do` | 주소 변경 추정 |
| 서울대 | 1건 | `snu.ac.kr/snunow/notice/scholarship` | 봇 차단 추정 |
| 상명대 서울 | 1건 | `smu.ac.kr/kor/life/notice.do` | 동적 렌더 |
| 한양대 서울 | 2건 | `hanyang.ac.kr/web/www/notice_all` | 전체 공지라 장학이 묻힘 |

**🟢 잘 되는 곳(9)**: 동국 35 · 외대 23 · 경희 19 · 광운 14 · 홍익 11 · 중앙 11 · 성균관 11 · 연세신촌 10 · 시립 7

> 개발자에게는 "**개별 장학 공고가 목록으로 쭉 보이는 화면**의 주소"를 달라고 요청할 것.
> 학교 홈·안내 페이지는 소용없다(고려대가 지금 그 상태).

### 8. 답하다 만 개발자 질문 (다음 세션이 이어서 답할 것)
> **"`subview.do`와 경희대처럼 주소가 긴 것의 차이는 뭐지? 웬만한 데는 다 subview로 보이는데.
>   이게 그렇게 오래 걸릴 일인가?"**

아직 답변 못 했다. 답의 뼈대(사실 확인 후 설명할 것):
- `subview.do`는 **한국 대학이 많이 쓰는 홈페이지 제품(Kwangwoon/솔루션형 CMS) 계열**이라 겉주소가 짧고 비슷하게 보이는 것뿐,
  그 안에서 **진짜 게시판 경로는 따로 있다**(연세대 `enc=` base64 해독 사례 — 기술 사실 절 참조).
- 경희대는 클릭이 **form POST**라 주소창이 안 바뀌고, 목록/상세의 **경로·파라미터 이름·메뉴 번호가 전부 다르다**(`bbs→contents`, `nttId→boardId`, `200318→200226`).
- '오래 걸리는' 이유는 주소 형태 자체가 아니라 **확인 절차**다: 조립한 주소를 세션 없는 새 탭에서 실제로 열어 제목까지 맞는지 봐야 하고, 학교 서버가 연속 요청을 막는다.
- 답할 때 추측하지 말고 실제 게시판 두어 곳을 정찰(`collector/run-probe.txt`)로 열어 근거를 만들 것.

### 9. 이 세션에서 얻은 기술 사실 (재발견 금지)
- **`get_job_logs`의 tail만 보면 오해한다.** 실패한 단계의 로그가 아니라 그 뒤 `cat 리포트` 출력이 보여, **저장소에 커밋돼 있던 옛 리포트를 이번 실행 결과로 착각**했다(실제로 "13건 확보"를 잘못 보고했다). 실패 원인은 **`tail_lines`를 넉넉히 잡아 node 단계의 에러 줄까지** 봐야 한다.
- **정찰 도구(`probe-links.mjs`)도 확인용 브라우저를 하나만 재사용해야 한다.** 주소마다 새로 만들면 학교가 연결을 끊는다(`ERR_CONNECTION_CLOSED`) — 저장소에 이미 있던 규칙인데 이 도구만 안 지키고 있었다. 지금은 재사용 + 주소 간 2.5초 간격.
- **공동작업자 브랜치(`claude/audit-failure-analysis-15op88`)를 합칠 때 주의**: 그 브랜치가 오래된 판에서 갈라져 나와 **경희대 주소를 로그인 벽 주소(`menuNo=200226`)로 되돌리는 내용**이 들어 있었다. 3-way 병합이 올바로 처리했지만(내 값 유지), 합친 뒤 **`grep -c "user/contents/view.do?menuNo=200226" data/registered.json`이 0인지 반드시 확인할 것.**
- 앱 회귀 검사 전에 **로컬 서버를 먼저 띄워야 한다**: `python3 -m http.server 8123 &` (안 띄우면 `ERR_CONNECTION_REFUSED`).

### 10. 다음 세션이 바로 할 수 있는 것
1. **세션 시작 즉시**: `node verify/audit-data.js`(감사가 실패면 로봇 결과가 안 저장된다) + `node verify/check-collab.js`.
2. 개발자 질문 **8번**에 답하기(정찰로 근거 확보 후).
3. 개발자가 게시판 주소를 주면 **7번 표**대로 등록 → `run-collect.txt`/`run-browser-collect.txt` push로 즉시 수집 확인.
4. 동국대 남은 2건(산학협동재단·진담거사)이 팝업 수정 이후 잡히는지 사냥 리포트로 확인.
5. **검토만 하고 조치하지 않은 것 — 개발자 지시 대기**: 게시판 읽는 3형제(사냥꾼·복구·정찰)가 아직 중복으로 갖고 있는
   `scrapeRows` · `scrapeForms` · `looksLikeList` · `freshPage` · 제목 일치 판정을 **`collector/board-reader.mjs` 한 곳으로 합치기**(약 250줄 감소).
   **수집 로봇 2종은 합치면 안 된다** — 수집은 빨라야 하고 사냥꾼은 정확해야 해서 목표가 반대다.
   개발자에게 "동국대 남은 2건이 잡히는 걸 확인한 뒤에 하는 게 안전하다"고 이미 말해 뒀다.

## 이전 세션 상태 (2026-07-31 11차 세션 — 앱 알림 시스템 구축)
- **개발자 지시**: "앱 내 푸쉬 알림 발송 시스템 구축 — ① 조건에 맞는 새 장학공고 ② 마감 하루 전.
  필요한 알림·시스템은 직접 추가. 최초 1회 동의 버튼도. 마지막에 브라우저로 테스트."
- **핵심 판단(정직 원칙)**: 한대장은 정적 배포(GitHub Pages)라 **발송 서버가 없다**. 서버 없이도
  가능한 **로컬 알림**으로 구현하고, 화면 문구에 전달 시점을 그대로 밝혔다("앱을 열 때 · 열어 둔 동안 ·
  지원 기기의 백그라운드 확인"). 서버 푸시 수신구(VAPID)는 sw.js에 미리 열어 둬, 나중에 발송 서버가
  생기면 **앱 코드 변경 없이** 진짜 푸시로 확장된다.
- **알림 5종** (요구 2종 + 앱이 아는 사실만으로 만든 3종):
  ① 내 조건에 맞는 새 장학 공고(자격 미달·마감·타교·상시제도 제외, 4건 이상이면 묶음 1건)
  ② 마감 하루 전 · 마감 당일(이미 제출 기록한 공고는 재촉하지 않음)
  ③ 제출 리마인드(앱에서 준비만 하고 공식 제출 기록이 없는데 마감 3일 이내)
  ④ 우리 학교 실시간 공고(내 학교·캠퍼스만, 대출·융자 제외, 묶음 1건)
  ⑤ 결과 기록 리마인드(제출 기록 + 접수 마감 2주 경과)
  — "합격했어요"처럼 **앱이 알 수 없는 사실은 알림으로 만들지 않았다**(운영 원칙 1·8).
- **최초 1회 동의**: 온보딩 완료 2.9초 뒤(저장 토스트가 사라진 뒤) 안내 시트가 뜬다. 5종을 미리 보여주고
  '알림 받기 / 나중에'. 어느 쪽을 눌러도 `askedAt`이 기록돼 **다시 자동으로 뜨지 않고**, MY 화면에서
  언제든 켜고 끌 수 있다. 거절해도 **앱 안 알림함**에는 계속 쌓여 사용자가 놓치지 않는다.
- **함께 만든 것**: 홈 종 아이콘 + 읽지않음 배지 · 알림함 시트(읽음·모두읽음·눌러서 공고 상세로) ·
  MY 알림 설정(항목별 스위치 5개·테스트 알림·지금 확인) · 알림 클릭 딥링크(`?sch=`, `?screen=`) ·
  서비스워커 notificationclick/push/periodicsync.
- **구조 리팩터링(중요)**: 자격 판정을 `match-engine.js`로 분리했다. 서비스워커가 같은 파일을 읽으므로
  **화면과 알림의 판정 기준이 갈라질 수 없다**(운영 원칙 7의 정신). app.js의 evaluate/fitScore/registeredFor는
  이 파일을 쓰도록 바뀌었고 호출부는 그대로다.
- **검증**: 신규 `verify/verify-notify-rules.js`(규칙 단위 24항목) + `verify/verify-notify.js`(브라우저
  33항목 — 권한 거절/허용 두 사용자, 조건별 실제 발송, 알림함, 설정 저장, 딥링크) **전부 통과**.
  회귀 `audit-data · drive · verify-registered · verify-new-forms · verify-forms-data · personas(120, 이상 0)` 전부 통과.
  sw CACHE **v11→v12**.
- **곁다리 수정(선조치후보고)**: `verify-new-forms.js`가 마감된 공고(삼일 전 접수분·명지 고시)를
  하드코딩해 눌러 **이번 변경과 무관하게 이미 깨져 있었다**(stash 후 재현 확인). 마감 여부를 먼저 보고
  마감이면 그 UI 구간만 건너뛰도록 고쳤다 — 양식 스키마 검사는 앞 단계에서 이미 끝난다.
- **main 병합 완료 (2026-07-31)**: 이 작업 도중 공동작업자가 main을 크게 바꿔(서비스워커 네트워크 우선
  재작성 v14, 자동 스키마화, 협업 인프라 82개 파일) 분기가 벌어졌다. 최신 main을 브랜치로 병합해
  해소했다 — sw.js는 **main의 네트워크 우선 구조 위에 알림 절을 얹어** 합치고 CACHE v15로 올렸다.
  `verify-new-forms.js`의 마감 공고 하드코딩 문제는 **공동작업자가 main에서 먼저 해결**(pickTarget)해
  뒀으므로 그 버전을 채택하고 내 중복 수정은 버렸다.
- **알림도 notStale을 쓴다**: main이 추가한 '마감 미확정 공고 60일 후 숨김'을 match-engine.js로 옮겨
  화면·알림이 같이 쓰게 했다. 안 그러면 **화면에는 없는 공고를 알림으로 알려** 눌러도 찾을 수 없다.
- **배포 완료 (2026-07-31, 개발자 지시)**: main에 병합해 배포했다(충돌 0건). `check-deploy-sync.js`
  통과 · sw CACHE **v15**. 배포 직전에 전 검사를 배포될 내용 그대로 재실행해 통과 확인
  (audit-data · test-collector · verify-notify-rules · verify-notify · drive · verify-registered ·
  personas 120명 이상 0건).

### ⚠️ 아직 안 합쳐진 브랜치와의 충돌 — 해법을 미리 검증해 뒀다 (2026-07-31)
`claude/scholarship-notice-link-fix-ef4lk2`(원문 링크 복구, 20커밋)가 아직 main에 없다.
그 브랜치를 main에 합칠 때 **딱 2곳이 충돌한다**. 별도 클론에서 실제로 합쳐 보고 해법까지
검증했으니, 다음 세션은 그대로 적용하면 된다(추측 아님 — 양쪽 검사 전부 통과 확인):

| 충돌 파일 | 원인 | 해법 |
|---|---|---|
| `sw.js` | 양쪽 다 CACHE를 **v15로 올려** 같은 줄에서 부딪힌다. 알림 쪽은 ASSETS에 파일 3개(match-engine·notify-rules·notify)를 추가했다 | **알림 쪽(HEAD) 블록을 채택**하고 CACHE만 **v16으로 올린다**. 내용이 달라졌는데 버전이 같으면 옛 캐시가 안 지워진다 |
| `verify/README.md` | 양쪽이 같은 실행 목록에 드라이버를 한 줄씩 추가 | **둘 다 남긴다**(verify-notify* 3줄 + verify-source-links 1줄) |

`app.js`·`CLAUDE.md`·`data/*`는 **자동 병합된다**(손댈 것 없음).
합친 뒤 검증: `verify-notify.js`(알림) + `verify-source-links.js`(원문 링크) **둘 다** 돌릴 것 —
별도 클론에서 이 조합으로 audit-data · drive · test-collector 까지 전부 통과하는 것을 확인했다.

## 같은 날 병행 세션 (2026-07-31 협업 세션 — 충돌 해소 체계 · 공동작업자 Josehyeon)

> ⚠️ **이 세션은 선주님 세션과 같은 시각에 병행 진행됐다.** 그래서 이 날짜에 '10차 세션'(선주,
> 양식 자동 승격·컨펌 대기 검수)과 이 세션이 함께 존재한다. 차수보다 **날짜와 담당자**로 구분할 것.
> 이 세션이 만든 것은 전부 협업 인프라이고, 앱 화면·데이터·매칭 엔진은 **하나도 건드리지 않았다.**

### 개발자가 물은 것과 답
**질문**: "공동작업자 작업과 겹쳐 계속 문제가 생긴다. 하이젠버그(기본) 브랜치를 하나 더 만들어
각자 쓰면 충돌이 안 되지 않나? 필요하면 보고하고 진행해라."

**답: 브랜치 복사는 반드시 실패한다.** ① GitHub의 시간 예약 실행은 **기본 브랜치 한 곳에서만** 돌아
두 번째 브랜치엔 수집 로봇이 영영 안 돈다 ② `seen.json`(본 공고 장부)이 갈라져 같은 공고를 두 번
담는다(이슈 #75와 같은 병) ③ 나중에 합칠 때 그동안 쌓인 전부가 부딪힌다.
→ **나누는 축을 '브랜치 복사'가 아니라 '누가 어떤 파일을 쓰나(파일 소유권)'로 바꿨다.**

### 원인 계측 (추측하지 않고 셌다)
최근 100커밋에서 가장 자주 바뀐 파일: **notices.json 45 · seen.json 36 · 리포트 28 ·
registered.json 19 ↔ app.js 5**. 즉 **충돌의 대부분이 사람 코드가 아니라 로봇 기록장**이었고,
이건 "누구 것이 맞나"를 고를 필요 없이 **합치면 되는 것**이다. git이 뜻을 몰라 멈춰 섰을 뿐.

### 조치 ① 로봇 기록장 자동 병합
`.gitattributes` + `tools/merge-json-union.mjs` — notices/seen/pending-forms/health 4종을 합집합으로
자동 병합. 중복 판정은 수집기와 **같은 모듈**(`collector/url-key.mjs`)을 써 규칙이 갈라질 수 없다.
리포트 3종은 `merge=ours`. **`registered.json`·`forms.json`은 일부러 제외** — 거기선 '삭제'가 뜻을
가져(잘못 등록분 제거) 합집합이 지운 항목을 되살린다. 등록은 `tools/setup-collab.sh`.

### 조치 ② 겹침 조기 경보 + 세션 시작 훅
`verify/check-collab.js` — ⓐ 내가 아직 못 받은 남의 작업과 같은 파일을 고치는가 ⓑ main에만 있는
직접 수정이 있는가 ⓒ 내 브랜치가 뒤쳐졌는가. `.claude/hooks/session-start.sh`가 **세션마다 자동으로**
병합기를 등록하고 이 경보를 띄운다(`.claude/settings.json`의 SessionStart 훅).

### 조치 ③ main 직접 수정 되가져오기 로봇
`.github/workflows/main-guard.yml` — main에 사람이 올린 변경을 기본 브랜치로 자동 병합.
deploy-sync(기본→main)의 **반대 방향·짝**이다. **브랜치 보호를 안 쓴 이유**는 위 기술 사실 참조
(PR 필수를 걸면 배포 로봇이 막혀 앱 업데이트가 멈춘다 — 공동작업자 계정은 애초에 `write`라
보호 메뉴가 보이지도 않았다). 만들기 전에 **실패 가능성 11가지를 먼저 세고 각각을 막았다**(COLLAB.md 부록).

### 조치 ④ 로봇 워크플로 보강
수집 3종·deploy-sync에 병합기 등록 스텝, update-progress에 push 재시도 3회
(예전엔 그 사이 main이 움직이면 그냥 실패해 그날 활동 로그가 비었다).

### 검증 (전부 실제 저장소로 재현 — 말로만 하지 않았다)
- **자동 병합**: 두 사람이 각자 새 공고를 담고 같은 공고를 다른 주소로 본 상황 → 장치 없이는
  notices·seen 둘 다 충돌, 장치 후 **충돌 0건**(양쪽 공고 보존·중복 1건 정리·seen 이른 날짜 유지·
  JSON 서식 보존). registered.json은 의도대로 여전히 충돌.
- **되가져오기 6상황**: 정상 세션(아무 일 안 함)·main만 직접수정(되가져옴)·양쪽 갈라짐(둘 다 보존)·
  진짜 충돌(저장소 그대로 두고 멈춤)·데이터 오염(저장 안 함)·push 경합(재시도로 양쪽 살림) 전부 통과.
- **무한 루프 없음**: 양쪽이 갈라진 최악의 경우도 **2라운드에 수렴**(반복 실행으로 실측).
- **알림 스텝**: 4가지 실패 사유로 실행해 이슈 본문까지 확인.
- **실전 1회차**: 배포하며 main에 push한 것이 되가져오기 로봇을 깨웠고, 8초 만에
  `기본 브랜치가 이미 main을 포함 — 되가져올 것 없음`으로 조용히 종료. **정상 작업에 소음 0** 확인.
- 기존 검사(audit-data · test-collector · check-deploy-sync) 전부 통과.

### 이 세션에서 발견한 사실 (다음 세션이 재발견에 시간 쓰지 말 것)
1. **`merge=ours`는 git 내장이 아니다** — 이름만 예약돼 있고 `git config merge.ours.driver true`로
   등록해야 동작한다. 등록 없이 쓰면 그냥 충돌한다(실험으로 잡음).
2. **두 개발자의 커밋은 작성자로 구분되지 않는다** — 둘 다 `Claude <noreply@anthropic.com>`.
   누가 한 작업인지는 **브랜치로만** 알 수 있다.
3. **main 브랜치 보호는 켜면 안 된다** — 배포 로봇이 main에 직접 push하므로 PR 필수를 걸면 앱이 멈춘다.
4. **공동작업자 권한은 `write`** (소유자 `seonju5543-web`만 admin) — 저장소 설정 메뉴가 보이지 않는다.

### 도구가 실전에서 작동한 기록
작업 도중 선주님 세션이 두 번(11:12·11:22) 작업을 올렸고, 겹침 경보가 즉시 잡아냈다. 두 번 다
겹치는 파일이 없다고 정확히 판정했고, 받아서 합친 뒤 재검증까지 마쳤다(충돌 0건). **이 세션에서
새로 만든 장치가 이 세션 안에서 이미 한 번 값을 했다.**

### 남은 개발자 액션 — 이게 유일하다
**선주님과 담당 영역 나누기** (예: 선주=데이터·공고 등록 / Josehyeon=수집 로봇·인프라).
자동 병합은 **데이터 충돌만** 해결한다. 두 사람이 **같은 코드를 서로 다르게 고치는 것**은 기계가
판단할 수 없어 분담으로만 막힌다. 나눈 뒤 `PROGRESS.md` 각자 칸에 적으면 상대 세션이 읽고 피한다.

### 다음 세션이 바로 할 수 있는 것
- 세션 시작 시 경보가 뜨면 그것부터 처리(`node verify/check-collab.js`로 자세히).
- 충돌이 나서 `<<<<<<<` 표시가 보이면 **파일마다 판단이 다르니** COLLAB.md 4절의 표를 볼 것.
- 협업 인프라 자체를 손볼 일은 없다 — 규칙 추가는 `.gitattributes`와 `merge-json-union.mjs`의
  RULES 배열에만 추가하면 된다.

## 이전 세션 상태 (2026-07-31 10차 세션 — 앱 자동 반영 점검·수리)
- **개발자 지시**: "작업 후 앱에 업데이트나 재설치 없이 바로 반영되고 있는지 확인 후 조치."
  점검 결과 **반영되지 않고 있었다 — 끊긴 곳이 두 군데**. 둘 다 고치고 브라우저로 실증했다.
- **① 로봇 수집분이 앱까지 못 갔다 (가장 큰 구멍)**: 로봇은 기본 브랜치에만 커밋하는데 앱은 main만
  배포한다. 사람이 손으로 옮기던 이 단계가 7/30 이후 비어 있어, **공고 5건·정식 등록 5건이 만들어져
  놓고 앱에 안 보이는 상태**였다(main 155건/96건 ↔ 실제 160건/101건). 조치: `deploy-sync.yml` 신설 —
  수집 로봇 3종 종료 직후·기본 브랜치 push·하루 2회 자동 병합, 실패 시 🚨 이슈. 밀린 분은 즉시 배포함.
- **② 앱 코드가 캐시에 굳어 있었다**: sw.js가 js/css/html을 '캐시 우선'으로 서빙해, CACHE 버전을 올리지
  않으면 설치된 앱에 새 코드가 **영영** 안 갔다. 7/30 커밋(e8566a9, 대출 공고 자리 보장)이 v12를 그대로
  둬서 실제로 이 상태였다. 조치: **sw.js v13 — 앱 코드도 네트워크 우선 + `cache:'no-cache'` 재확인**
  (그냥 fetch하면 브라우저가 Pages의 10분 캐시를 내주는 것을 디버그로 확인) + 3.5초 시간초과 캐시 폴백.
- **실증(브라우저)**: ⓐ 코드 배포 후 재설치 없이 새 코드 도달 ✅ ⓑ **v12 설치 상태 → v13 자동 승격 후
  새 코드 도달** ✅(지금 사용자 폰이 이 경로) ⓒ 오프라인 실행 유지 ✅ ⓓ 데이터는 `no-store`라 원래 정상.
- **재발 방지**: `verify/check-deploy-sync.js` — 세션 종료 전 "내 작업분이 앱에 나가 있나"를 main과 대조
  + **수집 로봇 3종의 저장 위치·실패 알림까지 점검**(로봇이 main에 직접 저장하도록 바뀌면 즉시 잡힘).
- **③ 공동작업자 판 통합 (같은 날 저녁)**: Se-Hyeon-Jo가 같은 문제를 반대 방향(로봇이 main에 직접
  저장)으로 고친 판을 main에 올려 두었다. 좋은 부분 2가지(**push 3회 실패 시 빨간불+이슈 코멘트**,
  **건수 없는 빈 리포트 이슈 방지 가드**)는 살려 **수집 로봇 3종 전체에 공통 적용**했고, 저장 위치는
  기본 브랜치로 통일했다(위 '로봇 저장 위치 원칙' 참조). 심층 수집에는 없던 **실패 알림도 신설**.
- 검증: audit-data(경고 1건은 기존 피드 이슈) · drive · verify-registered · personas 통과.
  워크플로는 YAML·셸 문법 검사 + push 재시도 로직 모의 실행(3회 실패→종료코드 1, 2회째 성공→0)으로 확인.

## 이전 세션 상태 (2026-07-30 9차 세션 — 양식 스키마화 무인화 + 첨부 누락 버그)
- **개발자 지시**: "Claude API 키 발급받았다 → 스키마화부터 진행" → API 자동화 구축 + 큐 4건 처리.
- **① 첨부 필터 구멍 발견·수정 (가장 중요)**: `auto-register.mjs`의 첨부 수집 조건이
  `신청서|지원서|서식|양식|공고`뿐이라 **"원서"·"동의서"·"서약서"·"추천서" 이름의 파일이 통째로
  버려지고 있었다**. 하필 진짜 신청서인 "장학금지급**원서**"가 걸려 염곡은 첨부 3건 중 1건만,
  연재는 2건 중 1건만 잡혔다. 필터 보강 + **소급 적용**으로 기존 등록분 6건의 누락 첨부 복원
  (곰두리·롯데장학관·산학디딤돌·대청교·염곡·연재).
- **② `fetched` 표시 버그 수정**: 워크플로가 deepfetch 직후 큐 **전체**를 `fetched:true`로 찍어서,
  원본을 못 받은 항목도 확보 완료로 기록 → 다시 시도되지 않고 영구 대기. 큐 4건 중 3건이 이 상태였다.
  `collector/mark-fetched.mjs` 신설 — forms-index에 실제로 들어온 항목만 표시.
- **③ Claude API 스키마화 (무료 우선 · 개발자 지시로 비용 최소화)**: `collector/schematize-forms.mjs`.
  **핵심은 분류기(triage)** — 돈이 드는 API는 "무료 경로로는 원본과 100% 동일한 문서를 만들 수 없는 것"
  에만 쓴다.
  · **무료 경로(비용 0)**: 글자가 깨끗하게 뽑히는 hwp/docx/hwpx → API 호출 없이 큐에 남겨 두고,
    다음 채팅 세션이 손으로 옮긴다(지금까지 하던 방식 그대로). 이번 세션의 연재 동의서가 이 경우.
  · **PDF도 공짜로 읽는다 (`collector/pdf-text.mjs`, 2026-07-30 추가)**: PDF는 두 종류 —
    ① 글자층 있는 PDF(한글·워드 '내보내기') → 자체 추출기로 무료 처리. 한글은 CID 폰트라 바이트가
    글자가 아니어서 폰트의 `/ToUnicode` CMap(bfchar·bfrange)을 읽어 되돌린다. 외부 라이브러리 없음.
    ② 스캔 PDF(종이 사진 — `/Font` 자체가 없음) → 글자가 없어 눈으로 봐야 하므로 API.
    판별은 자동: 추출 글자가 없거나 읽을 수 있는 글자 30자 미만이면 스캔으로 본다.
  · **유료 경로(API)**: ⓐ 스캔 PDF(원본 파일을 document 블록으로 첨부해 배치까지 보게 함)
    ⓑ 글자가 거의 안 뽑힘(`minTextChars` 미만) ⓒ 표·시간표·원고지 등 줄글로 펴면 배치가 무너지는 서식
    ⓓ 항목이 너무 많아 손으로 옮기면 누락 위험(`maxManualChars` 초과).
  · **비용 안전장치**: `collector/schematize-config.json` — `maxApiCallsPerRun`(기본 2, **0이면 완전 정지**),
    `enabled:false` 킬스위치, `alwaysApiIds`/`neverApiIds`로 건별 지정. 리포트에 호출 횟수와 사유가 남는다.
  · 구조화 출력(json_schema) + 자체 검증(info 키 화이트리스트·필드 id·체크 선택지) 통과분만 등록.
    제3자 작성 서식(추천서 등)은 무료/유료 판정 전에 제외 — 원본 다운로드 안내 유지.
  · **함정 주의**: `maxApiCallsPerRun`은 반드시 `??`로 읽을 것. `|| 2`로 읽으면 0(절대 호출 금지)이
    기본값 2로 되돌아가 돈이 나간다 — 실제로 이 버그를 만들었다가 테스트에서 잡았다.
- **④ 큐 처리**: UOS 빅데이터 성과형은 기존 `reg-uos-bigdata-cert`와 **중복**(같은 seq·마감)이라
  blockIds로 제거. 연재장학재단 개인정보동의서 **스키마화 완료**(`yeonjae-consent-apply`, 양식 29종).
  염곡·도레이는 원본 미확보라 `fetched:false`로 되돌려 다음 수집 때 재확보 예약.
- 검증: audit-data + drive + verify-registered + verify-forms-data + personas(120, 이상 0) 전부 통과.
  앱 코드 무변경이라 sw CACHE 인상 불필요.
- **개발자 확인 필요**: 자동 스키마화가 켜진 상태로 배포됨. 매 수집마다 새 첨부가 있으면 API 비용이
  발생한다(공고 1건당 수천 원 미만). 끄려면 워크플로에서 `ANTHROPIC_API_KEY` 시크릿을 지우면 된다.

## 이전 세션 상태 (2026-07-15 8차 세션 후속3 — 정직 페이스리프트: 추론 삭제·원문 발췌)
- **개발자 지시**: "추론해 제시된 내용 전부 삭제, 원문이 요구하는 내용을 앱이 찾아 제시, 앱 제작 문서는
  실제 제출 가능한 경우로 한정" → 전면 반영(위 원칙 8-1·8-2 신설). 구체 조치:
  ① **원문 발췌 시스템**: `collector/extract-excerpts.mjs` — 확보된 공고 전문(200건)에서 신청기간·
     방법·제출서류·문의 문장을 그대로 발췌해 36건에 반영, 수집 워크플로 2종에 자동 갱신 스텝 추가.
     상세 화면에 "공고 원문 안내 · 원문 그대로" 블록 신설.
  ② **추론 삭제**: 금액 미확인 52건의 임의 amountValue(200만원 등) → 0 처리, 홈 합계에서 제외하고
     '금액 미확인 n건 제외' 명시. 추측성 문구 1건(uos-bigdata note) 교정. SUBMIT_GUIDES는 '확인해요'
     형 안내 + 재단 공식 절차라 유지 판정.
  ③ **지원문서 게이팅**: 직전 세션의 '준비용 문서' 전면 부착을 철회 — prepDoc:true(자유 형식 제출
     원문 확정) 공고에만 부착, 문구도 '이 문서를 그대로 제출할 수 있어요'로 교체(현재 불광 1건).
  ④ sw CACHE **v11**. 검증: 발췌 블록·게이팅·히어로 문구 브라우저 확인 + 회귀 전부 통과.
- **포털 요구 내용 확보 로드맵(개발자 논의용)**: 로봇은 학생 로그인 뒤 포털에 못 들어감 → ⓐ 원문 발췌
  (구현됨) ⓑ **학교당 1회** 포털 신청화면 캡처(공고당 아님 — 같은 학교는 신청 UI가 공통) ⓒ 신청해 본
  사용자 1탭 제보 ⓓ 장학팀 정보 연계(제안서) ⓔ Claude API로 스키마화 무인화.

## 이전 세션 상태 (2026-07-15 8차 세션 후속2 — 준비용 지원문서 + 양식 원본 자동 확보)
- **준비용 지원문서 엔진 (개발자 지시)**: 지정 신청서 양식이 없는 정식 등록 공고(포털 입력형·자유형식
  이메일 등)에 질문형 작성 → 문서 생성 흐름을 붙였다. `forms.js buildPrepTemplate()` + `app.js
  attachPrepTemplates()/formTplIdFor()` — 공고별 템플릿을 로드 시 동적 생성(prep:공고id), 채널 라벨은
  유지하고 **문서 상단·화면에 '준비용 문서 — 공식 양식 아님'을 반드시 표시**(정직 원칙). 한국장학재단
  제도(전산 신청)는 제외. sw CACHE **v10**.
- **양식 원본 자동 확보 파이프라인**: 자동 등록분에 신청서 첨부가 있으면 `collector/pending-forms.json`
  큐에 넣고, 수집 워크플로가 **같은 실행에서 즉시 deepfetch+HWP 텍스트 추출까지 완료**(다운로드형이
  하루 이상 남지 않게). 스키마화는 다음 Claude 세션이 큐(fetched:true·schematized:false)를 읽어 처리
  후 schematized:true로 마킹. 스키마화 자체의 완전 자동화는 Claude API 키 확보 시 가능(개발자 검토 중).

## 이전 세션 상태 (2026-07-15 8차 세션 후속 — 다운로드형 전량 승격)
- **개발자 지시 "다운로드형 8건 모두 승격"** 완료: deep-fetch 2·3차로 원본 전부 확보 → **양식 9종 신규 스키마화**
  (정읍 지원서는 HWPX를 로컬에서 직접 해석 — **HWPX는 zip이라 Contents/section0.xml의 hp:t 텍스트로 전문 추출 가능**, 기술 사실).
  결과: **다운로드형 0건**, 채널 분포 앱 양식작성 36·포털 34·이메일 3·재단 3. 미래이음은 원문 확인 결과
  재단 홈페이지 온라인 접수(동의서만 앱 작성)·의암은 참여의향서 마감 9/11 확정·롯데장학관 2건은 마감(7/10)이라
  재공고 대비로 양식만 등록. 제3자 작성 서식(추천서·학습계획서 원고지 등)은 원본 다운로드 안내 유지.
- **자동 등록 첫 실전 가동 성공**: 7/15 낮 수집에서 로봇이 3건(외대 총동문회·성림교회·경희인턴)을 스스로 등록
  — 전부 진짜 공고로 검수 확인 후 승격, 신청서 2종도 스키마화. docx 원본은 zip으로 열어 word/document.xml에서 추출.

## 직전 세션 상태 (2026-07-15 8차 세션 — 서핑 개선·자동 등록 체계·전수 컨펌)
- **이번 세션 처리 완료 (3가지 지시)**:
  ① **앱 전체 브라우저 서핑 + 선조치 개선 4건**: (a) **dday 판정 버그 수정** — 마감 다음 날 새벽까지
     D-DAY로 잘못 표시되던 off-by-one(Math.ceil의 -0)을 날짜 단위 비교로 교정. 남정(7/14 마감)이 7/15에
     D-DAY로 떠 있던 실사례. (b) **탐색 탭 순서 교정** — 실시간 피드(대출 공고가 상단 점령)가 큐레이션
     카드보다 위에 있던 것을 카드 우선으로 스왑. (c) 피드에서 대출·융자 공고 후순위 정렬. (d) 상세 화면
     "현재 시연 단계" 낡은 문구를 정직한 설명으로 교체 + 홈 히어로 문구 명확화("바로 신청 n건 · 선발
     심사형 n건"). **sw.js CACHE v8→v9**.
  ② **renderFormDoc 버그 발견·수정 (forms.js)**: 섹션에 info(인적사항)와 fields(질문)가 둘 다 있으면
     fields가 통째로 빠진 문서가 생성되던 조기 return — 산학디딤돌 등 7/14 스키마 4종이 영향권이었음.
     수정 후 전 양식 회귀 통과 (양식 원칙 4 위반 해소).
  ③ **정식 등록 자동화 구축** (운영 원칙 2 예외 — 상단 원칙 절 참조): collect/browser-collect 워크플로가
     수집 직후 `auto-register.mjs`를 실행. 시험 실행을 3회 반복하며 규칙 강화(마감 표시·2027년 이후·연수/
     행사·국가장학금 상시·재단명 중복·타교 접수분 hold·클릭형 #n- URL 정규화·제목 부스러기 청소).
     최종 시험: 진짜 신규 3건만 등록·애매 10건 컨펌 대기 — 전부 수동 검수로 정확성 확인 후 이번 배치는
     검수 완료로 승격. **주의: 다음 수집부터는 자동 등록분에 auto:true가 붙어 앱에 '검수 전' 배지로 표시됨.**
  ④ **도구 소급 수정**: list-unregistered.js URL 정규화 공유(클릭형 게시판 공고가 전방일치에 전부 삼켜져
     후보 누락되던 버그 — 이번 전수 컨펌에서 3건이 빠졌던 원인), verify-forms-data.js 동적 대상 선택
     (reg-skku-samil 하드코딩이 마감 후 깨짐), verify-new-forms.js 삼일 구동을 외대 접수분으로 + 캠퍼스
     선택 지원. **더미 양식 주입 시: 필드 id는 'memo', 마감 전 항목에 연결할 것.**
  ⑤ 검증: audit-data + drive + verify-registered + verify-new-forms + verify-forms-data + personas(120,
     이상 0) 전부 통과.
- **개발자 확인 필요 (이번 세션 선조치 보고)**: 자동 등록 체계가 켜진 상태로 배포됨(원칙 2 예외).
  의도와 다르면 `collector/auto-register-config.json`의 enabled를 false로 바꾸라고 지시만 하면 됨.

## 이전 세션 상태 (2026-07-08 6차 세션 — UX·보안 점검·전수 컨펌)
- **이번 세션 처리 완료 (3가지 지시)**:
  ① **UX 훑기(브라우저 자동화 120 페르소나)**: 학교·학년·성적·소득·특별자격·미연결 학교 등 조합을 로봇이 클릭 →
     크래시·빈 상태·콘솔 오류 0건 확인(엔진 견고). 발견·개선: **(a) 탐색 화면의 잘못된 안내문 수정** —
     "시연용 예시 데이터입니다"가 실제 큐레이션 공고 위에 떠서 정직 원칙 위반이라 "정식 등록=실공고 큐레이션 /
     실시간 공고=매일 자동수집, 마감·금액은 원문 확인"으로 교체. **(b) 자동추천 키보드 접근성** — ↑/↓·Enter·Esc
     선택 추가(데스크톱·보조기기). **(c) data.js 낡은 '시연용' 주석 정정.**
  ② **대대적 보안 점검**: 앱 코드 XSS/링크 스킴/CSP·프라이버시·수집 로봇·메일 서버 전수 검토.
     조치 — **`safeUrl()` 도입**(app.js): 수집·등록 URL이 오염돼도 `javascript:`·`data:` 스킴이 href·window.open에
     못 들어가게 막는 2차 방어(CSP 위). 원문공고·첨부·제출처 링크 전부 통과. **원본에서 esc() 없이 들어가던
     sourceUrl·채널 링크 이스케이프 보강.** 메일 서버(server/mail-worker.js) 수신자 정규식에 시작 앵커+로컬파트
     검증 추가하고 검사한 값(trim)으로 발송. 결과: 개인정보는 기기 내(localStorage/IndexedDB)에만,
     외부 fetch는 same-origin(data/*.json)뿐, `connect-src 'self'`로 유출 원천 차단 — **프라이버시 견고**.
     (font CDN jsdelivr IP 노출은 referrer 미전송으로 완화된 저위험 잔여 항목 — 자가호스팅은 용량 부담으로 보류.)
  ③ **수집 공고 전수 컨펌**: 피드 124건을 새 도우미 `verify/list-unregistered.js`로 분류 →
     이미 등록 65 · 메뉴/대출/행사/파편 42 · 마감경과 0 · **실제 등록 후보는 1건**(대부분 7/7에 이미 등록됐고
     이후 수집은 같은 게시판 재수집이라 중복). 유일한 신규 실공고 **중앙대 보건장학회 다문화가정(reg-cau-bogun-multi,
     bogun-multi-apply 양식 재사용)** 등록 → **정식 등록 48건**. 제외분(선조치후보고): UOS커리어원정대·호반·AI멘토 등은
     이미 등록분과 동일, 점프업(자동선발)·학자금대출/융자·국가장학금 안내·전북 참가팀(행사)·미산러닝(마감)·
     각종 메뉴 및 PDF 파일명 파편은 정직 원칙상 피드 유지.
  ④ **새 개발자 도구**: `verify/list-unregistered.js`(컨펌 후보 자동 추출) + `verify/personas.js`(120 페르소나 회귀) 추가·README 문서화.
  ⑤ 검증: audit-data.js + drive/verify-registered/verify-forms-data/personas 전부 통과. sw.js CACHE **v6→v7**.
- **(참고) 5차 세션 처리 완료**: ① 경희대 게시판(news.khu.ac.kr, 클릭형·내부 전송형) 연결 — 이 유형 대응을
  수집기에 영구 기능으로 구축(기술 사실 절 참조). ② 개발자 컨펌으로 7/7 리포트 전량 등록 — 동국 10·경희 4·
  중앙 3·숙명 1·시립 1 = **19건 추가(누적 45건)**. ③ 누락 원인(게시판당 상한 10건) 해결 → 25건/학교 30건으로 상향.
  ④ 양천·유공자 신청서 hwp 확보·스키마화(**양식 12종**). ⑤ 수집 저장 충돌 시 rebase 재시도(로봇 3종),
  예약 혼잡 회피(홀수 분 2회), 0건 실행 코멘트 — 자동화 안정화.
- **진행 중 대화의 맥락 (다음 세션 필독)**: 개발자는 사용자 입장에서 앱을 눌러보며 관찰·컨펌을 계속 전달하는 방식.
  게시판 주소를 채팅으로 주면 그 자리에서 schools.json/browser-targets.json에 등록·수집 확인까지 한 세트로 처리해 왔다.
  "진짜 원클릭"(버튼=신청 완료)은 이메일 접수(Resend 키 대기)·장학팀 파일럿으로만 정직하게 가능 — '제안서 → 진짜 원클릭' 절 참조.
- **주소가 `subview.do?enc=...` 형태면 해독할 것 (2026-07-30 연세대 연결로 확인)**: 개발자가 주는 주소는
  브라우저 주소창 것이라 겉포장인 경우가 많다. `enc` 값은 base64이고, 풀면
  `fnct1|@@|%2Fbbs%2Fsc%2F58%2FartclList.do%3FfindClSeq%3D257%26` 처럼 **진짜 게시판 경로**가 들어 있다.
  URL 디코드해서 그 경로를 등록하는 편이 안정적이다(연세 신촌 = 외대와 같은 artclList.do 계열).
  낡은 주소가 이미 박혀 있을 수 있으니(연세는 notice.jsp였음) 교체 여부도 확인할 것.
- **새 게시판 연결 절차 (반복 패턴)**: ① 개발자가 채팅으로 장학공지 목록 주소 제공 →
  ② schools.json(일반) + browser-targets.json(클릭·동적)에 등록 → ③ run-collect.txt/run-browser-collect.txt push로 즉시 수집 →
  ④ 리포트 확인 후 개별 실공고를 컨펌받아 registered.json 등록(+양식 있으면 원칙 5로 스키마화).
  클릭형 게시판이 1건 이하로 잡히면 browser-report.md의 진단(프레임·클릭 시도·본 글자)부터 볼 것.
- **다음 세션이 이어서 할 만한 것**: 새 수집 공고 컨펌 → 정식 등록+양식(원칙 5), 개발자가 주소 주면 게시판 등록,
  Resend 키 오면 mail-worker 배포 + data.js mailEndpoint 연결, 스토어 결정 시 TWA 패키징.
  **작업 후 반드시 `node verify/audit-data.js` + 회귀 드라이버(verify/) 실행 후 3브랜치 배포.**

