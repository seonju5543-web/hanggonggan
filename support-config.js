/* ============================================================
   한대장 — 고객센터 문의처 설정
   ------------------------------------------------------------
   push-config.js · chat-config.js · supabase-config.js 와 **같은 방식**이다.
   아래 칸이 비어 있으면 그 기능이 화면에 아예 안 나온다 — 눌러도 아무 일이 없는
   가짜 버튼을 두지 않기 위해서다.

   ✅ **2026-09-23 에 채웠다** — 개발자가 정한 서비스 주소다. 로그인이 실제로 켜져
      배포된 뒤라(supabase-config.js 에 열쇠 두 칸이 차 있다) 개인정보를 받고 있는데
      지우거나 물어볼 곳이 없는 상태를 더 둘 수 없었다.
      🔴 `terms.html` 9번(개인정보 보호책임자 및 문의처)을 **같은 날 같은 주소로** 고쳤다.
         예전 문장은 *"베타 기간에는 별도 문의처를 두지 않습니다"* 였다 —
         여기만 채우고 그 문장을 안 고치면 **약관과 앱이 서로 다른 말을 한다.**

   주소를 바꿀 때도 둘을 같이 한다
     ① 아래 email 을 고친다   ② terms.html 9번을 같은 주소로 고친다
   관문 `verify/test-collector.mjs` 「문의처」 절이 둘이 갈라지면 실패시킨다.

   ⚠️ 개인 메일 주소를 적으면 학생 전체에게 공개된다. 서비스용 주소를 따로 만드는 것을 권한다.
   ============================================================ */
const SUPPORT_CONFIG = {
  /* 🔴 바꾸면 terms.html 9번도 같이 고친다 (관문이 대조한다) */
  email: 'handaejang24@gmail.com',

  /* 예: '평일 10:00~18:00 (주말·공휴일 휴무)' — 비어 있으면 응대 시간을 **적지 않는다**.
     🔴 지키지 못할 시간을 적는 것은 없는 것보다 나쁘다(운영 원칙 1).
     ⚠️ 2026-09-23 에 주소만 채우고 **여기는 일부러 비워 뒀다** — 지킬 수 있는 시간을
        확인한 적이 없다. 정해지면 그때 적는다. */
  hours: '',
};

/* 문의처가 준비됐는가 — 화면 곳곳에서 이 함수로 판단한다 */
function supportConfigured() {
  return !!SUPPORT_CONFIG.email;
}

if (typeof module !== 'undefined' && module.exports) {
  module.exports = { SUPPORT_CONFIG, supportConfigured };
}
