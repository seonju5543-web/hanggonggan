# 한대장 — 프로젝트 인수인계 문서 (Claude 세션용)

> 새 세션은 반드시 이 문서를 먼저 읽고 이어서 작업한다. 개발자(이선주, seonju5543@gmail.com)는
> 코드 지식이 없으므로 **전문 용어 없이** 설명하고, 인과관계를 명확히 한다.
> 지난 세션들의 서술 기록은 `SESSIONS.md`에 있다 — 이 문서는 자동으로 안 읽히니, 옛 사고의
> 경위가 궁금할 때만 직접 연다(평소엔 안 읽어도 된다).

## 서비스 개요
**한대장(한국 대학교 장학금)**: 대학생이 프로필 1회 등록으로 교내·교외 장학금을 자동 매칭받고,
증명서류 준비와 **공고 원본 양식 작성**까지 앱에서 끝내는 모바일 PWA.
- 배포 주소: https://seonju5543-web.github.io/hanggonggan/ (main에 push하면 자동 배포)
- **공고 검색·추천·알림은 학생에게 영구 무료.** AI 지원서 초안만 소액 크레딧 유료(원가 47원 / 건당 500~700원).
  수익화 전략 전문은 `BUSINESS.md` — 2026-08-24 개발자 지시로 전면 개정됨(B2C 크레딧 + B2B 온디바이스 타깃 광고 양대 축).

## 운영 원칙 (개발자와 합의됨 — 변경 금지)
1. **정직한 신청 상태**: 실제 접수가 일어나지 않는 것을 "신청 완료"로 표기하지 않는다.
   현재 모델 = "신청 준비 완료" + 최종 제출처 안내. 이메일 접수 공고만 실제 전송 가능.
2. **장학금 정식 등록(매칭+양식 지원)은 개발자 컨펌 후에만** — 단, **2026-07-15 개발자 지시로 자동 등록
   예외가 추가됨**: 수집 로봇이 보수적 규칙(개별 실공고·미등록·마감 전·대출/행사/고용/대학원 제외)을
   전부 통과한 공고만 `collector/auto-register.mjs`로 자동 등록하고, 앱에 **'자동 등록 · 검수 전' 배지**를
   붙여 정직하게 표시하며, 리포트 이슈에 선조치후보고한다. 끄기: `collector/auto-register-config.json`
   `enabled:false` / 잘못 등록분 제거: 같은 파일 `blockIds`에 id 추가. 애매한 건(타교 접수분·미래 사업 등)은
   자동 등록하지 않고 '컨펌 대기'로 리포트에만 올린다 — 이 컨펌은 여전히 채팅에서 개발자가 직접 지시한다.
   실시간 공고 피드(제목+링크)는 컨펌 없이 자동 게재 — 이건 합의된 예외.
3. **선조치후보고**: 방향 결정이 필요하면 최적안으로 먼저 조치하고 "어떤 상황에서 어떤 조치"를 보고.
4. **양식의 정의**: 공고에 첨부된 실제 신청서 파일(HWP 등)과 **동일한 구조**의 문서를 생성해야 한다.
   자유 서식 텍스트로 대체하면 안 됨. 신규 양식은 스키마화 후 `data/forms.json`에 등록.
5. **양식 확보 절차 (개발자 컨펌 이후 — 정식 등록의 기본 절차)**: 정식 등록할 때 첨부에 신청서
   양식 파일이 있으면 **양식 등록까지가 한 세트**다.
   ① `collector/run-deepfetch.txt`에 `targets: 공고키워드1,키워드2` 줄을 적어 기본 브랜치에 push(push-to-run)
   → 첨부 원본 + HWP 미리보기 텍스트(.hwp.txt)가 `collector/extracted/`에 자동 커밋됨
   ② 원본 항목·문구·체크옵션 그대로 `data/forms.json`에 스키마화, registered.json 항목에 `formId` 연결
   ③ `verify/verify-registered.js`·`verify-forms-data.js`로 검증 후 배포 (앱 파일 무변경 — 자동 반영).
   제출 방식이 양식 제출이 아니거나(포털 입력형 등) 양식 확인에 문제가 있으면,
   **모든 수단(공고 상세, 첨부, 주관 재단 홈페이지, fetch-page 원격 열람, 정찰 도구)을 동원해 찾아본 뒤**
   그래도 못 찾을 때만 개발자에게 필요한 정보를 정리해 문의한다.
   채울 신청서 양식이 아예 없는 공고는 registered.json 항목에 `noForm: "사유"`를 기재한다(감사 통과 조건).
6. **가짜 공지 금지 (2026-07-05 개발자 지시)**: 임의로 만든 장학금 공지를 사용자에게 보여주지 않는다.
   실존 상시 제도(국가장학금 등)는 유지하되 임의 마감일 없이 '상시 제도' 배지 + 재단 공지 확인으로 표기.
7. **소급 적용 원칙 (2026-07-05 개발자 지시)**: 엔진·정책이 업데이트되면 새 데이터뿐 아니라
   **기존 데이터 전체에 적용**해야 한다. 강제 장치: `node verify/audit-data.js` — 정식 등록·양식
   전수를 현재 기준으로 감사(양식 첨부 있는데 formId 없으면 경고). 페이스리프트·엔진 변경 후 필수 실행.
8-1. **추론 금지 · 원문 발췌 원칙 (2026-07-15 개발자 지시)**: 공고의 신청 방법·요구 서류를 앱이
   '추론'해 제시하면 안 된다. 확보된 공고 전문에서 **원문 문장을 그대로 발췌**(`collector/
   extract-excerpts.mjs`, 수집 때마다 자동 갱신 — registered.json `excerpts`)해 "공고 원문 안내 ·
   원문 그대로" 블록으로만 표시하고, 발췌가 없으면 지어내지 않고 '원문 보기' 링크만 준다.
   금액 미확인 공고는 amountValue 0 — 합계에 지어낸 숫자를 섞지 않고 '금액 미확인 n건 제외'로 표시.
8-2. **앱 제작 지원문서 한정 원칙 (2026-07-15 개발자 지시)**: 앱이 직접 만든 지원문서는 **자유 형식
   제출이 원문으로 확인된 공고(registered.json `prepDoc: true`)에만** 제공한다(예: 불광 — 지정 양식
   없음 확정·이메일 제출). 제출할 수 없는 공고에 '준비용 문서'를 붙이는 것은 사용자 혼란·신뢰 훼손이라
   금지. 등록·검수 때 원문에서 자유 형식 제출이 확인되면 prepDoc을 켠다.
8. **진척도 기록 방식 (2026-07-05 합의)**: 공식 제출·발표 결과는 앱이 학교 전산을 볼 수 없으므로
   **사용자가 직접 기록**한다(제출했어요/선정·미선정 버튼). '심사' 단계만 객관적 사실
   (제출 기록 + 접수 마감 경과)로 자동 표시. 장학팀 파일럿이 성사되면 자동화로 전환 가능.

## 🟢 양식 자동 작성 — 26차 세션 (2026-08-24): 빈 칸을 없애고, 위험을 관문으로 막고, 출처를 넓혔다

> **새 세션은 이 절부터 읽는다.** 25차가 초안 서버를 만들었고, 26차가 **학생 의존을 없애고
> 품질·안전을 올렸다.** 서버 배포와 endpoint 채우기는 여전히 남아 있다(아래 25차 절 참조).
> 다음 작업 설계는 이미 적혀 있다 — **`docs/designs/essay-edit.md`**.

### 26차가 한 것 (전부 배포됨 · 검사 통과)
1. **빈 칸을 없앴다** — "장학 신청 사유를 자소서로(A4 2장)" 같은 **빈 칸을 학생에게 던지던**
   것을 없앴다. 학생은 **키워드만 누르고 한 줄만 직접 쓴다** → AI가 문단·스토리텔링을 갖춘
   초안을 만든다. 질문은 그 서술형 칸 **안**에 들어가 질문 수 상한(FORM_LIMITS)을 안 넘는다.
   파일: `essay-ask.js`(질문 설계) · `essay.js`(앱) · `server/essay/worker.js`(프롬프트).
2. **B — 재단이 보는 것을 앞세운다**(`essay-ask.js foundationFocus`). '저는 이 재단이 찾는
   인재입니다' 같은 자기규정 문장이 아니라(9곳 조사에서 그런 걸 권한 곳 0), 공고 원문이 밝힌
   **평가 순위대로 보기·문단 순서**를 정한다. 원문에 없으면 안 켜진다(지어내지 않는다).
3. **스토리텔링 클릭 질문**(`essay-ask.js STORY_ASKS`) — 배운 규칙에서 나왔다:
   concrete-scene→언제·누구와·얼마나 오래 / episode-star→그래서 어떻게 됐나요 /
   growth-lesson→언제부터 달라졌나요. '얼마나 오래'가 **숫자를 만든다**(규칙집이 숫자로 쓰라 함).
4. **🔴 위험 원문을 관문으로 막았다** (개발자 지적으로). 코퍼스 전수에서 초안을 탈락시키는
   규정 세 갈래를 찾았다: ①**블라인드 심사**(학교명→심사 제외 — `draft-guard.mjs scrubSchool`이
   초안에서 학교명·별칭 '외대'까지 지운다) ②**분량 미달/초과**(1페이지 미만 제외·2페이지 초과 감점)
   ③내용 부실(품질 규칙이 짚음). '대필' 9건은 전부 '수업 대필 도우미'(근로장학 역할명)라 오해 아님.
   · **자격 매칭과 엮은 예방 = 관문 공유**: `verify/audit-data.js`가 코퍼스를 다시 계산해
     '블라인드'인데 앱(`data/essay-form-rules.json`)이 모르면 **배포 차단**. 자격 잡음이 관문이
     되어서야 재발이 끝난 것과 같은 방식. 판정은 자격 추출과 같은 원문 링크(notice-source·canon-url).
   · 학생에게도 규정을 보여 준다(`essay.js essayRulesBannerHtml`) — 원문 그대로, 실격·감점은 빨강.
   · **🔴 27차(2026-08-24) 재확인 — "또 있나?"**: 코퍼스 214파일 전수 재스캔 결과 **초안이
     건드려 탈락하는 블라인드형 위험은 여전히 1건(서울인재대학장학금)뿐**이고 관문이 정상
     재도출한다(`verify-essay-ask.mjs` [14]가 코퍼스 블라인드 ≥1건을 못박아 리팩터로 무력화되면
     검사 실패). 다른 "심사에서 제외" 문구들은 **증명서 스캔 화질·증빙자료 미비**(초안 무관 서류
     위험)라 `isFormRule=false`로 올바르게 규정에서 빠진다. "개인정보(연락처·계좌·카드) 포함 금지"는
     `isFormRule=true`로 학생에게 규정으로 보이고, 초안 전송 검사(`scanOutgoing`)가 그 꼴을 막는다.
     자격 매칭과는 같은 `notice-source.mjs`를 공유 — 자격 작업이 링크를 깨면 이 검사도 함께 실패한다.
5. **출처를 스스로 넓힌다 — 두 갈래**(개발자 지시):
   · **집 안부터**(`collector/essay-house-mine.mjs` · 비용 0 · 네트워크 0) — 이미 가진 공고
     원문 403·첨부 211에서 **재단이 직접 적은 작성 규정**을 캔다(`data/essay-form-rules.json`).
   · **링크 따라가기**(`collector/essay-playbook-learn.mjs`) — 읽은 글에서 다음 읽을 곳을 주워
     실행당 6곳 새로 읽는다. **robots.txt 준수** · 규칙 준 곳은 seeds 승격 · 헛걸음은 30일 대기.
   · **지속가능성·오류예방**: seed 상한 60 · 시든 seed(3회 연속 규칙 0종) 리포트(**자동 삭제 안 함**) ·
     못 읽은 곳은 벌하지 않음 · 헛걸음은 `tried`에 30일 쿨다운(자동 확장이 다시 줍지 않음) ·
     판정 순수 모듈(`collector/essay-rule-line.mjs`)로 검사가 돌려 봄.
   · **🔴 27차(2026-08-24) 주 1회 예약 추가** — 버튼만으로는 '한 번'에 그쳐 출처가 안 넓어졌다.
     `essay-playbook.yml`에 화 05:37 KST 예약을 걸어 매주 스스로 집 안 재마이닝 + 링크 6곳 확장.
     예약 실행도 `--write`(schedule 분기) · **바뀐 게 있거나 실패했을 때만 이슈**(소음 방지).
     🔴 예약은 기본 브랜치에서만 도므로 이 변경이 기본 브랜치에 병합돼야 켜진다.
   · **네이버 블로그**: `naverMobile`로 m.blog.naver.com 모바일 주소로 읽는다(데스크톱은 프레임
     껍데기). ⚠️ **네이버 검색결과는 robots 막힘+JS라 자동 크롤링 불가** — 개발자가 글 주소를 주면 seeds.
6. **정직한 출처 보고**: 지금 학습 출처는 **논문이 아니다.** 전부 블로그·커뮤니티 **작성 팁 글**
   (brunch·linkareer 커뮤니티·한경 생글이·soomgo·skillagit·owlapply). 다음 크롤링 출처 계획은
   `collector/essay-sources.json`의 `nextCrawl` + 학습 리포트 이슈에 있다.
   · **정책 정정**: 앞서 예시문 걱정에 '후기·예시'가 든 제목까지 막았는데, 그게 개발자가 원한
     글이었다. 이제 막는 것은 **예시문 DB 뿐**(cover-letter·jasoseol·검색결과·'모음'). 팁·후기 글은 연다.
     진짜 방어선: **규칙 문장만 뽑고 예시문은 한 줄도 저장 안 함.**

### 🔴 다음 작업 — 순서대로 (개발자가 26차 끝에 지시)
1. **양식 작성 고도화 추가 방안 검토** — 위 기능들 위에 무엇을 더 할지.
2. **완성된 문서 수정하기** — 설계 완료: **`docs/designs/essay-edit.md`**. 세 갈래:
   ① AI 대화창구(학생이 말로 고쳐 달라 — `revise` 엔드포인트, 초안과 같은 안전장치)
   ② 직접 수정 칸 유지 ③ 빠진 팁 제시("이렇게 하면 향상돼요" — 이미 다 반영돼 있어야 정상).
   + 판 되돌리기·diff·분량 미터(공고 min/max와 잇기)·재단 정렬 표시.
   · **✅ 27차(2026-08-24)에 착수 ①② 완료·배포**: 빠진 팁 카드(`.essay-tips`)를 **서버 없이도**
     화면에 띄우고(품질 검사를 겸용 `essay-quality.js` 한 곳으로 옮겨 draft-guard 가 재수출 —
     서버·화면이 같은 함수를 쓴다), 판 되돌리기(`↶ 이전 판으로`)·바뀐 곳 diff(🟩 낱말 LCS)를 붙였다.
     검사 UI 44/0·guard 91/0·ask 111/0. **경위는 `docs/designs/essay-edit.md` 「①② 실제로 한 일」.**
   · **남은 것 ③④**(서버 필요): AI 대화창구(`revise` 엔드포인트 — 초안과 같은 안전장치) · 분량
     미터·재단 정렬. `essay-config.js` endpoint 가 비어 있어 대화창구는 **서버 배포 후(아래 4번)** 켜진다.
3. **약관·개인정보처리방침 초안** — 앱에 지금 0건, 국외 이전(Cloudflare·Anthropic) 고지 법적 필수.
4. **서버 배포**(`server/essay/README.md`) → `essay-config.js` endpoint 채우기 → 초안·대화창구 켜짐.

### '학습 끝나고 다시 하기로 한 것' — 이번에 처리 완료 (재개 불필요)
- B(재단 정렬) · 스토리텔링/개인특징 클릭 질문 — **크롤링 후로 미뤘던 것, 26차에 다 했다.**

### 26차 새 파일
| 파일 | 하는 일 |
|---|---|
| `essay-ask.js` | 키워드·스토리텔링 질문 설계 + B(foundationFocus)·블라인드 판정. 브라우저·Node 겸용 |
| `collector/essay-house-mine.mjs` | 집 안 코퍼스에서 작성 규정 캐기 → `data/essay-form-rules.json` |
| `collector/essay-rule-line.mjs` | 규칙 후보·링크·robots·네이버 판정 **순수 모듈**(학습 로봇·검사 공용, 베끼지 말 것) |
| `data/essay-form-rules.json` | 공고별 작성 규정(원문 그대로) + 블라인드 표시. 앱·감사·초안 서버가 읽는다 |
| `docs/designs/essay-edit.md` | **다음 작업 설계** — 완성 문서 수정하기 |
| `docs/designs/essay-tailoring.md` | 예시문을 안 쓰고 규칙만 배우는 이유(표절·저작권·획일화) |
| `verify/demo-essay-ask.js` | 실제 앱을 눌러 B·스토리텔링 화면을 담는 시연 드라이버 |

## ✅ 양식 자동 작성 고도화 — 만들었고, 서버만 올리면 켜진다 (2026-08-23, 25차 세션)

> **지금 상태**: 코드·검사·버튼까지 전부 끝났고 `essay-config.js`의 `endpoint`가 비어 있어
> **꺼져 있다**. 서버를 올리고 그 한 칸을 채우면 그 순간 켜진다 — 앱 코드는 고칠 것이 없다.
> (자격 AI·도우미 AI와 같은 방식.) 올리는 절차는 `server/essay/README.md`.

### 개발자가 정한 것 (2026-08-23 — 다시 묻지 말 것)
| # | 질문 | 결정 |
|---|---|---|
| 1 | AI가 쓴 글의 지위 | **'초안'이라고 밝힌다.** 칸마다 '✨ AI 초안이에요 — 반드시 읽고 고쳐서 제출하세요' |
| 2 | 정보입력형(fact)에 AI | **안 붙인다.** 데이터 `kind`로 못 박음 |
| 3 | 개인정보 전송 범위 | **최소한만 + 버튼을 눌러야 전송.** 나가는 것 = 학생이 그 화면에 직접 친 글 + 공고 공개 정보 + 학교/학년/전공. 안 나가는 것 = 주민번호·계좌·서류보관함·장애/기초수급/국가유공자·성적·소득분위·이름·연락처 |
| 4 | 모델 | **`claude-sonnet-5`** (서류 1건당 약 47원). `wrangler.toml` 한 줄로 opus-5(약 78원)로 바꿀 수 있다 |
| 5 | API 잔액 | **채워졌다** (공동작업자, 2026-08-23 · $19). 실제 호출 성공 확인 |
| 6 | 가짜 'AI 초안 만들기' 버튼 | **이름을 고쳤다** → '초안 문장 만들기'. 진짜 AI는 등록 양식 쪽에 붙었다 |

### 비용 (2026-08-23 저장소 실데이터로 실측 — 재조사 금지)
서술형 칸이 있는 양식 28/48종 · 한 양식당 채울 칸 중앙값 3개 · 입력 1,751자 / 출력 1,800자 기준.
한글 1자 = 1토큰으로 잡은 **보수적**(실제보다 비싸게 나오는) 값이다.

| 모델 | 서류 1건 | 가장 무거운 양식 | 학생 1명이 3건 신청 + 각 2번 고쳐쓰기 | 학생 100명(월) |
|---|---|---|---|---|
| `claude-opus-5` | 78원 | 219원 | 702원 | 70,150원 |
| **`claude-sonnet-5`** | **47원** | 131원 | 421원 | 42,090원 |
| `claude-haiku-4-5` | 16원 | 44원 | 140원 | 14,030원 |

**이 기능에서 비용은 걸림돌이 아니다.** 학생 100명이 활발히 써도 월 3~7만 원이다.

### 경계선 — 이 기능만 다르다
이 저장소의 다른 AI 경로 셋(도우미·자격 읽기·양식 스키마화)은 전부 **"AI에게 글을 쓰게 하지
않는다"** 가 뼈대다. **이것만 AI가 진짜 문장을 쓴다.** 그래서 금지의 정의를 다시 그었다:

> 금지인 것은 **'글을 쓰는 것'이 아니라 '사실을 만드는 것'** 이다.
> - ✅ 허용 — 학생이 준 재료를 문장으로 엮기: `등록금이 부담돼서` → *"가계 사정으로 등록금 마련이 어려워…"*
> - ❌ 금지 — 학생이 안 준 사실 넣기: 없는 수상·봉사시간·동아리·가족사항·금액·날짜

이 경계는 **프롬프트로만 지키지 않는다.** `server/essay/draft-guard.mjs`가 받은 초안을 한 칸씩
되받아 검사해서, 걸리면 그 칸은 초안 없이 돌려준다. 모르는 자리는 `[ ]`로 비우게 했다 —
**비우는 것은 실패가 아니고 지어내는 것이 실패다**(자격 AI의 '모른다고 말할 자유'와 같은 계열).

### 어디에 무엇이 있나
| 파일 | 하는 일 |
|---|---|
| `data/forms.json`의 `kind` | **AI가 닿을 곳과 닿으면 안 될 곳.** 서술형 102칸 전부에 적혀 있다 (story 66 · fact 36 · 애매 0). 사람이 원본 라벨과 대조해 갈랐다. **런타임 정규식이 아니라 데이터가 기준이다** |
| `server/essay/draft-guard.mjs` | 이 기능의 심장. 서버와 검사 드라이버가 **같은 파일**을 쓴다. ①보내기 전 민감정보 ②받은 뒤 지어냄 |
| `server/essay/worker.js` | Cloudflare Worker. KV 없음(글이 안 남는다). 기본 sonnet-5 |
| `server/essay/smoke.mjs` + `.github/workflows/essay-smoke.yml` | **진짜 API가 이 요청 모양을 받아 주는지** 호출 1회로 확인하는 버튼(약 50원) |
| `essay.js` + `essay-config.js` | 앱 쪽. 버튼 · 보낼 내용 확인 화면 · 초안 채우기 |
| `verify/verify-essay-guard.mjs` | 53항목. 가짜 응답으로 **모델이 일부러 나쁘게 굴게** 해서 검사. 돈 0 |
| `verify/verify-essay-ui.js` | 19항목. 진짜 앱을 눌러서 확인. 가짜 서버 · 돈 0 |
| `verify/essay-survey.mjs` | 서술형 전수 조사. **규칙을 고치기 전후로 돌려 숫자를 비교하는 것이 이 영역의 작업 방식** |

### 🔴 `fallbacks` 를 붙이지 말 것 (2026-08-23 실측)
`claude-sonnet-5` 는 이 파라미터를 지원하지 않아 **첫 호출부터 400**이 난다.
Fable 5 · Opus 5 전용이다. 자격 AI가 습관적으로 붙였다가 잔액을 채우자마자 시범 3건이
전부 죽었다. 코드·README·회귀 검사 세 곳에 왜 안 붙였는지 남겨 뒀다 — 안 남기면 또 붙인다.

### 남은 것
1. **서버 올리기** (`server/essay/README.md` · 10분) → `essay-config.js`의 `endpoint` 채우기.
2. **약관·개인정보처리방침** — 앱에 지금 0건인데 **국외 이전(Cloudflare·Anthropic) 고지가
   법적 필수**다. 개발자 지시: *"개인정보처리방침과 필요한 서버를 타 작업 이후 바로 진행."*
3. 추천 문구(`sugg`)가 없는 서술형 **62칸** — AI가 켜지면 대부분 메워지지만,
   `schematize-forms.mjs`의 API 스키마에 `sugg` 필드가 없어서 **로봇이 만든 양식에는
   추천 문구가 한 개도 안 붙는다.** 로봇이 양식을 만들수록 빈칸 비율이 오른다.

## 🔴 이어서 할 일 — 자격 요건 매칭 (2026-08-23 세션 끝 상태)

> **새 세션은 이 절부터 읽는다.** 지난 세션이 하루 종일 여기에 매달렸고, 남은 일은
> **돈을 쓰는 한 걸음**뿐이다. 코드는 다 만들어져 있고 검증도 끝났다.

### 지금 숫자 (2026-08-23 실측)
```
등록 공고 194건 · 자격 확보 145건(75%)
🟡 "자격을 아직 읽지 못했어요"  48건
     본문이 있다 → AI가 시도 가능   45건
     본문이 없다                     3건  (중앙대·가송재단=링크 사냥꾼 몫, 조선대=원문에 자격 없음)
자격 자리의 잡음                     0줄  (아침엔 52줄/41건)
```

### 다음 세션이 할 일 (순서대로)
1. **읽을 재료 마저 내려받기 — 무료.** `자격요건 매칭 · AI 자격 읽기` 워크플로를
   `첨부만` 모드로 **3~4번** 돌린다(실행당 12건 상한). 공고문 첨부 17건 + 포스터 그림 16건이
   대기 중이다. 돌릴 때마다 `node verify/eligibility-report.mjs` 로 숫자를 본다.
2. **AI 전수 — 약 2,200원. 개발자 컨펌 후.** 같은 워크플로 `전부` 모드.
   본문이 있는 45건 시도 + 이미 뜨는 카드의 표 구조 복원(공통/둘 중 하나)이 함께 된다.
   ⚠️ 돈이 나가는 자리다. **반드시 채팅에서 개발자에게 물을 것.**
3. 남은 것은 링크 사냥꾼(2건)과 '원문에 자격이 없는 공고'뿐이다.

### 만들어 둔 것 — 자격을 읽는 길 네 갈래 (전부 실증됨)
| 자격이 어디 있나 | 읽는 길 | 상태 |
|---|---|---|
| 게시판 본문 | 무료 규칙 → 안 되면 AI가 **구조로**(공통/둘 중 하나/성적/제외) | ✅ |
| 공고문 HWP·DOCX | 무료 해석기 (`attachment-text.mjs`) | ✅ |
| 공고문 PDF (글자층 없음) | AI가 문서째 — `document` 블록 | ✅ 7~8줄 읽어냄 |
| **본문이 포스터 그림뿐** | AI가 그림째 — `image` 블록 | ✅ 5906×8268에서 읽어냄 |

⚠️ **API가 그림에 거는 한계는 둘이다** — 한 변 8,000픽셀 · 파일 10MB. 둘 다 400이 난다.
   `sharp` 로 긴 변 3,500픽셀 JPEG 로 줄여 보낸다(A4 300dpi가 2480×3508이라 충분).

### 🔴 자격 잡음이 0인 이유 — 되돌리지 말 것
개발자가 **네 번** 같은 것을 지적했다("마일리지 산정기간이 지원 자격으로 뜬다").
원인은 셋이고 셋 다 구조였다: ①막는 방식이 열거형 ②통과 조건이 덩어리 단위
③아무도 세지 않음(발견이 개발자 눈에 맡겨져 있었다).
- **통과 조건을 뒤집었다** — 버릴 것을 열거하는 대신 **줄마다 '요건임'을 증명해야** 나간다
  (`match-engine.js` 의 `REQ_SIGNAL`). 증명 못 하면 안 보여 준다.
- 그 잣대를 **화면으로 나가는 문에 처음으로** 뒀다. 예전엔 수집기 쪽 두 파일에 복사돼 있고
  정작 `match-engine.js` 에는 없었다 — 앱이 무엇을 내보내는지 아무도 검사하지 않았다.
- 제외 대상은 **버리지 않고 자리를 옮긴다**(`EXCLUDE_LINE` → '이런 경우는 제외돼요' 블록).
- **감사가 오류로 올린다**(`audit-data.js`). 수집 워크플로가 실패 시 그 실행분을 되돌리므로
  잡음 섞인 데이터는 앱에 하루도 못 나간다. **리포트가 아니라 관문이어야 재발이 끝난다.**
- ⚠️ 제외·우선 블록에는 그 잣대를 대면 안 된다(`opts.loose`) — 대면 그 블록이 통째로 사라진다.
- ⚠️ 채점기를 조일 것: 조이기 전에는 `한부모가족증명서 발급 대상 가정의 대학생`·
  `재학생 228명 내외`·`소득분위 9분위 이하(산정 기준)` 같은 **진짜 요건**을 잡음으로 셌다.
  **못 믿는 숫자는 관문으로 쓸 수 없다.**

### 🔴 오늘 판 함정 — 같은 실수를 반복하지 말 것
지난 세션이 하루에 **짐작으로 다섯 번** 틀렸고 개발자가 매번 밀어 줘서 발견됐다.
- "AI가 못 읽는다" → 실은 **본문이 저장돼 있지 않았다**
- "재수집 11/12 성공" → **메뉴를 본문으로 셌다**(빈 말뭉치로 판정)
- "원문이 존재하지 않는다" → **못 받고 있었다**(팝업·JS·봇차단·로그인벽)
- "본문이 없다" → 실은 **포스터 그림**이었다(7건 전부)
- "AI 써도 20건대 중반" → **재지 않은 숫자**였다. 병목은 AI가 아니라 재수집 대상 기준이었다
→ **짐작하지 말고 `collector/run-probe.txt` 에 `checkUrl:` 을 적어 정찰을 돌릴 것.**
  정찰 한 번에 원인 넷이 한꺼번에 드러났다.

### 로봇 이름 (Actions 탭)
`자격요건 매칭 · AI 자격 읽기` / `자격요건 매칭 · 공고 본문 재수집 (브라우저)` — 접두어로 짝을 이룬다.
- 재수집 대상 기준은 **'본문이 없나'가 아니라 '아직 자격을 못 읽었나'**다. 읽을 재료를
  찾으러 가는 로봇이라 그렇다(기준을 `!hasText()` 로 두었다가 43건에 아예 안 가고 있었다).
- 학교 서버에 붙는 워크플로는 **전부 `NODE_EXTRA_CA_CERTS`** 를 갖고 있어야 한다.
  없으면 조선대·계명대에서 `TypeError`로 조용히 죽는다. 검사가 7개 워크플로를 훑는다.

## 아키텍처 (전부 정적 파일, 빌드 없음)
| 파일 | 역할 |
|---|---|
| `index.html` `style.css` | 화면. 5단계 온보딩(학교 자동추천+별칭, 캠퍼스, 계열, 특별자격, 공통 서류정보) |
| `data.js` | **한국장학재단 상시 제도 6종**(실존 제도 — program:true, 임의 마감일 없음) + kosaf-ai-mentor 실공고 1 + 대학/별칭/학과/캠퍼스/서류슬롯/제출채널. **가상 샘플·교내 템플릿은 2026-07-05 전량 삭제됨** |
| `app.js` | 화면·서류보관함(기기내 저장)·신청 플로우, **진척도 4단계 트래커**(effectiveStep — 제출·결과는 사용자 기록), 실시간 공고 렌더(정식 등록분 URL 전방일치 중복 제거), 정식 등록 로더 |
| `match-engine.js` | **매칭엔진(12신호+적합도점수 — schoolOnly 포함) + 오래된 공고 숨김(notStale)**. 2026-07-31에 app.js에서 분리 — 서비스워커(백그라운드 알림)가 같은 파일을 importScripts해서 **화면과 알림의 판정이 갈라지지 않는다**(화면에서 숨긴 공고를 알림으로 알리는 모순 차단). Node에서도 require 가능 |
| `notify-rules.js` | **알림 규칙 엔진(순수 로직)** — 언제 무엇을 알릴지의 단 하나의 기준. 화면(notify.js)과 서비스워커가 공유. 알림 5종·중복방지 장부·알림함 적재 |
| `push-config.js` | **진짜 푸시 설정** — endpoint·publicKey 두 값. **비어 있으면 로컬 알림만**(현재 상태). 채우면 그 순간 '앱을 안 켜도 오는 알림'이 켜진다(앱 코드 무변경) |
| `server/push/` | **푸시 발송 서버 (Cloudflare Worker · 무료)** — 하는 일은 "확인해 봐"라고 폰을 깨우는 것 하나. **내용 없는 깨우기 푸시**만 보내고 문구는 폰이 만든다 → 서버에 저장되는 건 **폰 주소·학교·캠퍼스뿐**. VAPID 서명·구독 저장(KV)·죽은 구독 자동 정리·하루 2회 예약. 설치 절차는 `server/push/README.md` |
| `notify.js` | **앱 알림 UI** — 최초 1회 동의 시트, 권한 요청, 발송(휴대폰 알림 + 앱 내 알림함), 알림함 시트, MY 설정(항목별 스위치·테스트·지금 확인), 딥링크(`?sch=`) 처리 |
| `chat.js` + `chat-config.js` | **장학금 도우미(챗봇) — 2026-08-17**. 앱이 **이미 아는 것만** 답한다(등록 공고·서류·신청 현황·원문 발췌). 판정을 새로 만들지 않고 `evaluate`·`dday`를 그대로 쓴다 — 여기에 규칙을 한 벌 더 두면 화면과 챗봇이 다른 말을 한다. 못 찾으면 **지어내지 않고 "못 찾았다"**고 말하는 것이 이 파일의 존재 이유(원칙 6·8-1). AI는 `chat-config.js`의 endpoint가 비어 있어 **꺼져 있고**, 채우면 **안내봇이 못 알아들은 질문에만** 쓴다(무료 우선 → 실패분만 API — 양식 스키마화와 같은 방식). 보낼 수 있는 것은 질문 + 앱이 고른 공고의 공개 정보뿐, **프로필은 절대 안 보낸다** |
| `collector/build-search-index.mjs` + `.github/workflows/search-index.yml` | **검색용 요약 (2026-08-17)** — 공고 원문 전문은 수 MB라 앱이 못 받는다. 그래서 **'그 공고만의 낱말'만** 추려 `data/search-index.json`(51KB)으로 발행하고 도우미가 그것만 읽는다. **여러 공고에 나오는 낱말은 버린다(문턱 2건)** — 게시판 껍데기(로그인·사이트맵·취업)가 섞여 있어 안 버리면 검색이 오히려 나빠진다. **수집 워크플로를 고치지 않고 따로 도는 로봇**이라 공동작업자 파일과 부딪히지 않는다 |
| `server/chat/` | **도우미 AI 서버 (미배포 · 선택)** — 하는 일은 "어느 공고·어느 원문 문장이 맞는지 **고르는 것**" 하나. **답 문장을 쓰지 않는다** → 지어낼 자리가 없다. 질문도 답도 저장하지 않고(KV 없음), 앱이 보내는 것은 질문 + 공고 공개 정보뿐. 켜는 법은 `server/chat/README.md` |
| `form-plan.js` | **신청서 질문 설계기 (2026-08-18)** — 원본 스키마를 **건드리지 않고** 화면에 낼 질문만 다시 짠다. 앱이 아는 값은 안 묻고(자동 채움 22키), 답이 정해진 것은 클릭(`choice`)으로, 원본 표의 칸들은 카드 1개(`group`)로. 그래서 **질문을 줄여도 문서는 한 글자도 안 바뀐다**(증명: `verify/form-snapshot.mjs`). `match-engine.js`처럼 브라우저·Node 겸용이라 화면·감사·관리자 화면이 **같은 파일로 같은 개수**를 센다 |
| `essay.js` + `essay-config.js` | **신청서 AI 초안 (2026-08-23)** — 서술형 칸을 학생이 준 재료로 엮어 **초안**을 만든다. 이 저장소에서 **AI가 진짜 문장을 쓰는 유일한 경로**라, 금지의 정의를 '글을 쓰는 것'이 아니라 **'사실을 만드는 것'**으로 다시 그었다(개발자 승인). 어느 칸에 붙는지는 `data/forms.json`의 `kind`가 정한다 — 짐작하지 않는다. `endpoint`가 비어 있으면 버튼조차 안 나온다. **27차(2026-08-24)**: 초안·학생 글을 **서버 없이도** 검사해 '이렇게 하면 더 좋아져요' 팁 카드(`.essay-tips`)·판 되돌리기·바뀐 곳 diff를 붙였다(설계 `docs/designs/essay-edit.md` ①②) |
| `essay-quality.js` | **초안/완성 문서 품질 검사 (2026-08-24) — 브라우저·서버·검사 공용 단일 출처**. `qualityCheck`·`rulesFor`·`paraCount`. 원래 `draft-guard.mjs` 안에 있어 **서버가 켜져야만** 보이던 것을, 화면(essay.js)에서도 같은 함수로 돌리려고 겸용 파일로 옮겼다. `draft-guard.mjs`가 여기서 **가져다 재수출**한다(worker·검사 import 는 그대로). 🔴 원본은 여기 하나 — 베끼면 "서버는 통과, 화면은 잡는" 갈라짐이 생긴다. wrangler에 `nodejs_compat`가 없어 `createRequire`는 못 쓰지만 esbuild/Node의 CJS named import 로 충분(실증) |
| `server/essay/` | **초안 서버 (Cloudflare Worker · 배포 대기)** — KV 없음(학생 글이 안 남는다). `draft-guard.mjs`가 **서버와 검사 드라이버 공용**으로 ①보내기 전 민감정보 ②받은 뒤 지어냄을 본다(품질 검사는 `essay-quality.js`에서 재수출). 기본 `claude-sonnet-5`(서류 1건 약 47원). 🔴 `fallbacks` 금지 — sonnet-5는 400 |
| `forms.js` | **양식 엔진**: 스키마 → 질문 → 원본 동일 문서(.doc 저장/인쇄/공유). 내장 2종은 오프라인 폴백일 뿐 — **양식 원본은 `data/forms.json`** |
| `data/forms.json` | **양식 스키마 원본(single source of truth)** — 여기에만 추가하면 설치된 앱에도 자동 반영. **등록 34종**(2026-08-01 기준): …+ uos-bigdata-cert-apply, gasong-apply + **다운로드형 전량 승격분 9종**(jeongeup·uos-fund·ihanae·lotte-dorm·uiam·jeju-consent·skku-merit-plan·khu-intern·hufs-alumni — 2026-07-15) |
| `data/notices.json` | 수집 로봇이 발행하는 실시간 공고 (앱이 fetch) |
| `data/registered.json` | **정식 등록 공고 100건** (2026-08-01 기준 · 실공고 큐레이션 — 학교한정 매칭 schoolOnly/campusOnly, 마감·금액 파싱, 원본 첨부 링크, formId 연결·이메일 접수 3건, 양식 없는 공고는 noForm 사유, 불명확분은 '원문 확인' 정직 표기). **원문 주소를 못 찾은 2건만 `#n-` 표식**(앱이 '게시판 목록 ↗'으로 정직 안내) — 실시간 공고 146건 중에는 7건 |
| `verify/audit-data.js` | **소급 감사 도구** — 정식 등록·양식 전수가 현재 엔진 기준을 충족하는지 검사. 엔진·정책 업데이트 후 필수 실행 (운영 원칙 7) |
| `verify/eligibility-report.mjs` | **자격 요건 전수 재채점 (2026-08-03)** — 등록 138건을 한 번에 채점해 확보/미확보를 원인별로 나누고, 자격 자리에 들어간 엉뚱한 줄을 유형별로 센다. **규칙을 고치기 전후로 돌려 숫자를 비교하는 것이 이 영역의 작업 방식** — 공고 하나씩 보고 고치던 굴레를 끝낸 도구. `--list` `--bad` |
| `collector/notice-source.mjs` | **'등록 공고 ↔ 저장된 원문' 잇는 규칙 한 곳 (2026-08-03)** — 주소·제목 정규화, 원문 유무·잘림 판정. 발췌기·심층수집·재채점 도구가 같이 쓴다. 갈라지면 "발췌기는 찾았는데 수집기는 못 찾는" 일이 생긴다 |
| `verify/check-deploy-sync.js` | **배포 반영 확인 (2026-07-31)** — 작업한 내용이 실제로 앱(main)에 나가 있는지 대조. 세션 종료 전 실행 |
| `.github/workflows/deploy-sync.yml` | **배포 동기화 로봇 (2026-07-31)** — 기본 브랜치(로봇이 커밋) → main(앱이 배포) 자동 병합. 이게 없어서 수집분이 앱에 안 나갔다 |
| `COLLAB.md` + `tools/merge-json-union.mjs` + `tools/setup-collab.sh` + `verify/check-collab.js` + `.gitattributes` + `.claude/` | **협업 충돌 해소 체계 (2026-07-31 11차 세션)** — 로봇 기록장은 자동 합집합 병합, 사람끼리 겹치면 세션 시작 때 경고. 규칙·경위는 `COLLAB.md` |
| `.github/workflows/main-guard.yml` | **main 직접 수정 되가져오기 (2026-07-31)** — main에 사람이 올린 변경을 기본 브랜치로 자동 병합. deploy-sync(기본→main)의 **짝**이자 반대 방향. 브랜치 보호를 못 쓰는 이유는 아래 기술 사실 참조 |
| `collector/deepfetch.mjs` + `.github/workflows/deep-fetch.yml` | 공고 본문 전문+지정 공고 첨부 원본을 `collector/extracted/`에 커밋 — 차단 샌드박스가 읽는 통로. **HWP는 미리보기 텍스트(.hwp.txt)까지 자동 추출**(`hwp-prvtext.py`, zip 내부 hwp 포함). 대상은 `run-deepfetch.txt`의 `targets:` 줄로 지정 |
| `collector/browser-collect.mjs` + `browser-targets.json` + `.github/workflows/browser-collect.yml` | **브라우저형 수집기**(진짜 Chromium) — 봇차단·동적 게시판 8개교, 매일 09:20 KST |
| `collector/collect.mjs` | 매일 09:00 KST 게시판 수집 + 공고 상세·첨부양식 수집 + notices.json 발행 + 리포트 이슈 |
| `collector/auto-register.mjs` + `auto-register-config.json` | **정식 등록 자동화 (2026-07-15)** — 수집 직후 보수적 규칙 통과분만 registered.json에 자동 등록(auto:true, 앱에 '자동 등록·검수 전' 배지). 애매한 건 '컨펌 대기'로 리포트에만. 킬스위치 enabled:false, 오등록 제거 blockIds |
| `collector/schema-from-text.mjs` | **무료 자동 스키마화 (2026-07-31)** — 원본 글자를 규칙만으로 읽어 앱 양식으로 변환(비용 0). 표형·번호형·콜론형·점선형·체크형 5가지 모양 인식, 한 파일 안의 추천서 등 제3자 서식은 제외, 같은 공고의 첨부는 한 벌로 합침. 확신 없으면 변환하지 않고 사유를 남긴다 |
| `collector/schematize-forms.mjs` + `schematize-config.json` + `mark-fetched.mjs` | **양식 스키마화 (2026-07-30)** — **무료 우선 분류기**: 글자가 깨끗이 뽑히는 원본은 API 없이 `schema-from-text.mjs`가 **그 실행에서 바로 앱 양식으로 승격**(2026-07-31 개발자 지시로 '다음 세션 대기' 폐지). PDF·글자 추출 실패·표/시간표 등 **무료 경로로 동일 문서를 장담할 수 없는 것만** Claude API(claude-opus-5, PDF는 원본 첨부) 호출. 실행당 한도 `maxApiCallsPerRun`(기본 2, **0이면 완전 정지**), 킬스위치 `enabled:false`. 원본이 매 실행 갈아엎히므로 **같은 실행 안에서** 돌아야 함 |
| `collector/schools.json` | 대상 23개 캠퍼스 게시판 주소 (null = 미확보) |
| `collector/link-hunter.mjs` + `link-hunt.json` + `.github/workflows/link-hunter.yml` | **링크 사냥꾼 (2026-08-01)** — 까다로운 원문 주소 읽기 **전담** 로봇. 수집 로봇은 매일 12캠퍼스를 훑어야 해 빨라야 하고(그래서 주소를 '조립'한다 — 이게 틀리는 원인), 사냥꾼은 남은 어려운 것만 맡아 **느려도 정확한 방법**만 쓴다: **짐작하지 않고 행을 실제로 눌러 브라우저가 간 주소를 받아 적는다.** 학교를 가리지 않고 `#n-` 표식인 공고를 전부 집어 든다. 매일 06:37 KST. `link-hunt.json`에 공고별 시도 이력 — **영구 포기는 없다**(간격을 1→3→7→14→30일로 늘려 가며 계속 재시도), 3회째에 `stuck`으로 **이슈 자동 생성**. **본업은 사냥, 순찰은 덤**(2026-08-01 개발자 지시 — 사냥이 끝나고 시간이 남을 때만 기존 링크 20건 재확인, `patrol: 0`이면 끔). 넘어져도 그때까지 찾은 것은 저장한다. 실행: `collector/run-link-hunt.txt` 수정 후 push |
| `collector/probe-links.mjs` + `run-probe.txt` + `.github/workflows/probe-links.yml` | **링크 정찰 (2026-08-01)** — '이 주소가 학생 눈에 실제로 어떻게 보이는가'를 **눈으로 확인**하는 도구. `checkUrl:` 주소를 로그인 없는 새 탭에서 열어 상태·제목·로그인 벽 여부·화면 글자를 그대로 보고하고, `findBoard:` 학교 홈에서 메뉴를 따라 들어가 진짜 게시판과 행별 주소를 받아 적는다. **로봇 판정이 의심스러우면 추측하지 말고 이걸 돌릴 것** — 동국대 12건이 '다른 글'로 떨어졌을 때 이 도구가 '주소는 처음부터 맞았다'를 증명했다. 확인용 브라우저는 하나만 재사용(주소마다 새로 만들면 학교가 연결을 끊는다) |
| `collector/detail-url.mjs` + `resolve-detail-urls.mjs` + `.github/workflows/resolve-detail-urls.yml` | **원문 링크 복구 (2026-07-31)** — '어떤 주소가 공고 원문인가'를 정하는 규칙 한 곳(detail-url) + 목록 주소로 남은 공고를 게시판에서 찾아 원문 주소로 고치는 로봇. 실행: `collector/run-resolve-urls.txt` 수정 후 push (주 1회 자동) |
| `collector/clean-title.mjs` | **제목 청소 공용 모듈 (2026-07-30)** — 게시판이 `<a>` 안에 번호·분류·조회수·작성일·기간을 함께 넣는 유형에서 부스러기 제거. 수집기(collect.mjs)와 자동 등록(auto-register.mjs)이 **같은 규칙**을 써야 중복 판정이 어긋나지 않는다 |
| `collector/probe.mjs` + `.github/workflows/probe-boards.yml` | 게시판 후보 주소 일괄 정찰 |
| `.github/workflows/fetch-page.yml` | 임의 페이지+첨부를 원격으로 받아 artifact로 — 차단 환경 우회용 |
| `_admin/` + `tools/admin-apply.mjs` + `.github/workflows/admin-apply.yml` | **관리자 페이지 (2026-08-03)** — 개발자가 학생인 척 접속하지 않고 등록 공고 전부를 한 화면에서 보고 고치고 컨펌한다. 화면 6개(오늘 할 일·공고 전체·컨펌 작업대·양식·수집망·데이터 품질). **버튼이 저장소를 직접 고치지 않는다** — `admin-apply.yml`을 깨우고 그 안에서 `audit-data.js`를 통과해야만 저장(실패 시 되돌림). 올리는 절차는 `_admin/README.md`. **2026-08-09 가동 시작 — https://hanggonggan-admin.pages.dev · Cloudflare Access 잠금 + GitHub 열쇠 완료.** 잠금 확인은 `.github/workflows/admin-lock-check.yml`(매일 14:23 KST · 풀리면 🚨 이슈, 다시 잠기면 자동으로 닫음) |
| `.github/workflows/admin-lock-check.yml` | **관리자 화면 잠금 확인 (2026-08-09)** — 로그인 없이 관리자 주소를 열어 보고 **잠김/안 잠김/판정 불가** 셋으로 답한다. 매일 14:23 KST + 수동. 안 잠겼으면 🚨 이슈(라벨 `admin-lock`), 다시 잠기면 스스로 닫는다. **설정 화면을 보는 것으로는 잠금이 증명되지 않는다**(호스트 이름이 어긋나면 실제 주소는 열린 채 남는다) — 푸시 검사 버튼과 같은 계열 |
| `collector/canon-url.mjs` | **공고 주소 정규화 규칙 한 곳 (2026-08-09 분리)** — '이 주소가 같은 글인가'를 정한다. 원래 `auto-register.mjs` 안에 있었는데 **그 파일은 불러오는 순간 실행돼서** 관리자 쓰기 경로가 가져다 쓸 수 없었다. **베끼면 사람 등록분과 로봇 판정이 갈라져 같은 공고가 두 번 등록된다** |
| `.github/workflows/push-check.yml` | **푸시 알림 검사 버튼 (2026-08-07)** — Actions 탭에서 수동 실행. `/health`(등록 폰 수·마지막 오류) + `/test`(등록된 전 기기 시험 발송). 시크릿 `PUSH_ADMIN_KEY` 필요. **KV 화면의 등록 수는 '살아 있는 폰 수'가 아니다** — 실제로 받는지는 발송해 보는 것만이 증명이라 만든 도구 |
| `server/mail-worker.js` | 완전 자동 접수 메일 서버(배포 대기 — Resend 키 필요) |
| `proposals/` | 장학팀·G-RISE 제안서 **PDF** (2026-07-06 전면 재작성 — 요청 사항 명시형). 원본 HTML·렌더 스크립트는 `proposals/src/` (`node render-pdf.js`) |

## 중요한 기술 사실 (재발견에 시간 쓰지 말 것)
- 이 저장소 **기본 브랜치는 `claude/nice-heisenberg-WESq5`** (예약 워크플로는 여기 있어야 작동 —
  GitHub의 시간 예약 실행은 **기본 브랜치 한 곳에서만** 돈다. 그래서 "하이젠버그를 하나 더 만들어
  각자 쓰자"는 안 된다: 두 번째 브랜치에서는 수집 로봇이 영영 돌지 않고, seen.json이 갈라져
  같은 공고를 두 번 담는다(이슈 #75 유형). 자세한 이유·대안은 **`COLLAB.md`**).
  **Pages 배포는 main 브랜치 소스** — 배포하려면 main에 push. 작업 브랜치는 `claude/korean-scholarship-app-8posyu`.
  세 브랜치를 모두 같은 내용으로 push하는 것이 관례 (main push 전 `git fetch origin main` — 봇이 커밋함).
- **협업 충돌 해소 체계 (2026-07-31 11차 세션 — 규칙 전문은 `COLLAB.md`)**: 두 개발자가 동시에
  작업할 때 부딪히는 파일의 대부분이 사람 코드가 아니라 **로봇이 매일 새로 쓰는 기록장**이었다
  (최근 100커밋: notices.json 45 · seen.json 36 · 리포트 28 ↔ app.js 5). 이런 파일은 '누구 것이
  맞나'가 아니라 **합치면 되는 것**이라, `.gitattributes`로 전용 병합기(`tools/merge-json-union.mjs`)를
  지정해 충돌을 자동 해소한다. 중복 판정은 수집기와 **같은 모듈**(`collector/url-key.mjs`)을 쓴다.
  · 등록: `bash tools/setup-collab.sh` — git 보안 규칙상 저장소가 지정한 프로그램은 **클론마다
    등록해야** 동작한다. Claude 세션은 `.claude/hooks/session-start.sh`가, 로봇은 워크플로 스텝이 자동 실행.
  · **`merge=ours`는 git 내장이 아니다** — 이름만 예약돼 있고 `git config merge.ours.driver true`로
    등록해야 실제로 동작한다(등록 없이 쓰면 그냥 충돌한다 — 실험으로 확인).
  · **`data/registered.json`·`data/forms.json`은 일부러 자동 병합에서 뺐다** — 여기서는 '삭제'가
    뜻을 가지므로(잘못 등록분 제거) 합집합이 지운 항목을 되살린다. 사람이 봐야 한다.
  · 겹침 조기 경보: `node verify/check-collab.js` — 상대의 아직 못 받은 작업과 같은 파일을 고치고
    있는지, main에만 있는 직접 수정이 있는지 알려준다. 세션 시작 훅이 `--brief`로 자동 실행.
- **🔴 링크 사냥꾼이 못 찾는 진짜 이유는 대개 '게시판 원제목이 없어서'다 (2026-08-21)**:
  중앙대 11건이 3주 동안 `목록에서 못 찾음`으로 떨어지며 `likelyGone`(게시판에서 내려간 듯)까지
  붙어 있었다. **셋 다 틀린 진단이었다.** 원인을 파 보니 셋이 겹쳐 있었다 —
  ① **미아**: 우리가 가진 이름은 사람이 다듬은 것(`삼일장학회 희망/동행 장학생 (중앙대 접수)`)이고
     게시판 제목은 다른 글자(`2026학년도 2학기 삼일장학회 희망/동행 장학생 선발 공고`)라
     **애초에 대조가 될 수 없었다.** 로봇이 더 뒤져서 될 일이 아니다.
     → `registered.json`의 `boardTitle`(게시판에 뜨는 제목 그대로)을 채워야 한다.
     감사(`audit-data.js`)가 이제 `#n-`인데 boardTitle 없는 항목을 경고로 잡고,
     사냥 리포트도 이걸 **'미아'로 따로** 적는다(예전엔 다른 실패와 같은 문구라 원인이 안 보였다).
  ② **1단계와 3단계가 서로 다른 게시판을 뒤지고 있었다.** 1단계는 표식 주소
     (`index.do?MENU_ID=100` = 중앙대 **전체 공지**)로 묶고, 3단계만 `boardListFor`(수집 설정)를 썼다.
     장학 공고는 `P_TAB_NO=5`(장학 탭 498건)에 있어서 전체 탭에서는 수천 건 뒤로 밀린다.
     → 1단계도 `boardListFor`를 쓴다. **수집 설정 값은 사람이 정찰로 확인해 넣은 것이라 유추보다 낫다.**
  ③ `boardListFor` 안에서도 **표식 주소 유추가 설정보다 먼저**였다. 표식 공고들끼리는 서로가
     서로의 근거라 **다 같이 틀린 곳**을 가리킬 수 있다 — 순서를 뒤집었다.
  · 결과: 표식 17 → **4건**(중앙대 11건 중 10건 해결). 게시판을 바꾸자 1단계 첫 페이지에서 바로 잡혔다.
  · **중앙대 게시판 사실(재조사 금지)**: 목록이 DOM에 없고 `POST /ajax/FR_SVC/BBSViewList2.do`로
    나중에 그려진다(JSON, `BBS_SEQ`·`SUBJECT`). 행은 `<li id="board_30089">` +
    `<a href="javascript:fn_goDetail('30089',…)">`. `pagePerCnt`를 키우면 한 번에 498건을 다 받는다 —
    제목을 손으로 대조해야 할 때 쓸 것.
  · ⚠️ **느슨한 제목 대조로 이으면 안 된다.** 실제로 시험해 보니 `복지장학금 (서울캠퍼스)`가
    `(다빈치캠퍼스)` 공고에 붙었다 — 22차 세션의 '남의 공고 원문이 붙는다'와 같은 사고다.
- **🔴 '지원 자격'에 요건이 아닌 줄이 섞이는 문제 — 규칙을 덧붙이지 말고 채점기를 볼 것 (2026-08-21)**:
  개발자가 **세 번** 같은 것을 지적했다(목포향우회 우선선발·이름표 중복, 사랑나눔 연결문장·하위제목).
  매번 "잡음 걷어냈다"고 보고했는데 다음 날 또 나왔다. **원인은 방식이었다** —
  잡음을 하나씩 이름 대서 버리는 블랙리스트라, 새 유형이 나오면 개발자가 앱을 눈으로 볼 때까지
  아무도 몰랐다. 게다가 채점기(`eligibility-report.mjs`)가 **필터가 아는 유형만** 알아서
  화면에 잡음이 있는데도 "없음"이라고 답하고 있었다(필터의 눈으로 필터를 채점한 셈).
  · 지금 구조는 **두 겹**이다. ⓐ 필터(`match-engine.js requirementLines`) — 이름 아는 잡음 9종을
    버린다(제목·연결문장·서류·인원·주소·일정·우선선발·배점·행사). ⓑ 채점기 — **다른 축**으로 본다.
  · 🔴 **채점기에 필터와 같은 규칙을 쓰지 말 것.** 그러면 새 유형을 영영 못 본다.
    지금은 '요건 낱말이 하나도 없는 줄'을 전부 올린다(`node verify/eligibility-report.mjs --bad`의
    '참고' 절 — 현재 32줄). 정밀하지 않아도 된다. **1분에 훑을 분량**으로 유지하는 것이 목적이고,
    개발자가 이상한 것을 발견하면 **여기부터 볼 것**.
  · `verify/audit-data.js`가 매 실행 채점기를 돌려 의심 줄을 경고로 올린다 — 로봇이 매일 돌린다.
    ⚠️ 참고 절은 경고로 만들지 않았다(오탐이 섞여 있어 로봇을 막으면 멀쩡한 저장이 멈춘다).
  · **경계 사례를 지우지 말 것**(전부 `test-collector.mjs` 회귀에 있다):
    `복학생인 경우, 휴학 직전학기 성적 기준`은 조건이 붙어 있어 요건이고,
    `관악구 거주 대학 재학생(0명)`은 인원이 뒤에 붙었을 뿐 요건이다.
    `증명서를 제출할 수 있는 자`·`수여식에 참석할 수 있는 학생`도 요건이다.
  · **우선 선발 기준은 버리지 않고 자리를 옮긴다** — `eligibilityPriority` →
    '자격을 갖춘 사람 중 먼저 뽑는 기준이에요' 블록(제외 대상과 같은 방식). 판정(✓/✗)은 하지 않는다.
  · **발췌 칸은 14개다**(8개였다). 잡음이 자리를 먹으면 **진짜 요건이 밀려난다** —
    사랑나눔에서 `기초생활수급자/차상위` 요건이 그렇게 통째로 잘려 있었다. 잡음보다 나쁜 실패다.
- **🔴 '자격을 못 읽는다'의 진짜 원인은 대개 '본문을 못 받았다'이다 (2026-08-23 규명)**:
  개발자 지적 — *"요건을 못 읽었다고 뜨는 공고를 API를 써도 못 읽는다는 건 말이 안 되잖아."*
  맞았다. **AI가 못 읽은 게 아니라 읽을 본문이 저장돼 있지 않았다.** 빈칸 70건의 정체:
  본문 있음(AI로 풀림) 32건 · **본문 없음 38건**.
  · **`hasText()`가 껍데기를 통과시키고 있었다** — 판정이 `Loading...`·`K2Web` 같은 **표시**만
    봤는데, 표시 없는 껍데기가 훨씬 많다. 세종이도는 5,439자를 받아 놓고 공고 본문이 **한글 191자**
    (나머지는 로그인·사이트맵·학사일정)였는데 "원문 확보"로 세어졌다. 그래서 재수집 로봇이
    대상으로 안 잡았고 리포트는 "규칙이 나빠서 못 읽는다"고 말하고 있었다.
    → 지금은 **표시가 아니라 실제 본문 분량**으로 판정한다(메뉴 제거 후 한글 `MIN_BODY` 300자).
      문턱은 전수 실측으로 골랐다: 200→껍데기 44건·오탐 4 / **300→49건·오탐 7** / 400→52건·오탐 20.
      오탐이 자격을 지우지는 않는다(발췌기는 원문이 없으면 아무것도 안 지운다).
  · **`collector/rescue-bodies.mjs`**(워크플로 `자격요건 매칭 · 공고 본문 재수집`) 신설 —
    브라우저 수집기는 **새로 발견한 공고**만 본문을 저장하고 등록된 옛 공고는 안 본다. 그 빈자리를 메운다.
    브라우저 수집기를 고치지 않은 이유: 686줄에 시간 예산·회전 커서가 얽혀 있고 이 저장소는
    수집 로봇이 시간초과로 죽어 그날 수집분을 통째로 잃은 적이 세 번 있다.
  · ⚠️ **이 로봇을 만들며 같은 실수를 두 번 했다 — 되돌리지 말 것**:
    ① `\s+ → ' '`로 눌러 본문을 **통짜 한 줄**로 저장했다. AI가 줄 번호를 못 매겨 대상에서 빠지고
       표 구조도 사라진다 → `innerText`로 줄바꿈을 그대로 받는다.
    ② 본문 판정을 **빈 말뭉치**(`indexTexts([], …)`)로 해서 메뉴를 걷어내지 못했고, 메뉴 글자가
       본문으로 세어져 정읍시민장학재단(한글 387자 전부 메뉴)이 '확보 ✅'로 통과했다.
       되찾았다던 37건 중 **24건이 그런 가짜**였다 → 발췌기·AI와 **같은 말뭉치**를 쓴다.
    회귀는 `test-collector.mjs`의 '껍데기 페이지와 브라우저 본문' 절에 정적 검사로 있다.
  · **남은 38건은 하나의 문제가 아니라 21개 학교에 흩어진 긴 꼬리다**(최다 5건). 정찰로 확인한 원인:
    | 학교 | 실제로 본 것 | 성격 |
    |---|---|---|
    | 동국대 | 200·로그인 불필요. '오늘 하루 보지 않기' 팝업 글자가 앞을 채운다 | 수집 |
    | 광운대 | 200·로그인 불필요. 게시판 안내문만 — 본문을 JS로 그리는데 안 그려졌다 | 수집 |
    | 홍익대 | 🔴 **봇 차단** — 최종 주소 `cdn-botmanager.stclab.com/…/challenge`, 제목 `Security Verification` | 신규 발견 |
    | 중앙대 | 🔴 **로그인 벽**(비밀번호 입력칸) + 최종 주소가 `index.do`로 튕김 = 그 글이 아니다 | 링크 사냥꾼 |
    | 성균관 | 페이지엔 제목·날짜·`첨부파일(3)`뿐. 내용은 **PDF 안**에 있다 | PDF 경로 필요 |
  · **짐작하지 말고 `collector/run-probe.txt`에 `checkUrl:`을 적어 정찰을 돌릴 것.**
    2026-08-23에 세 번 연속 짐작이 빗나갔고(“AI가 못 읽는다”→본문이 없었다,
    “재수집 11/12 성공”→메뉴를 본문으로 셌다, “원문이 존재하지 않는다”→못 받고 있었다),
    정찰 한 번에 원인 넷이 한꺼번에 드러났다.

- **로봇을 로컬에서 돌릴 때는 `tools/robot-run.sh`를 씌운다 (2026-08-21 개발자 지시로 신설)**:
  2026-08-21에 링크 사냥꾼을 로컬에서 돌리는 동안 **클라우드의 같은 로봇도 예약 실행으로 돌아**
  둘 다 `data/registered.json`을 고쳤고, 그 파일은 일부러 자동 병합에서 빼 둔 것이라 사람이
  손으로 풀어야 했다. 이번엔 양쪽 다 '찾아낸 주소'뿐이라 손해가 시간뿐이었지만, 한쪽이
  **등록분을 지우는** 조치였다면 그 삭제가 조용히 되살아났을 것이다.
  · 쓰는 법: `bash tools/robot-run.sh node collector/link-hunter.mjs`
  · 하는 일 셋 — ① 클라우드에서 데이터 로봇이 돌거나 줄 서 있으면 **시작하지 않는다**
    ② 시작 전에 기본 브랜치 최신을 받아 둔다 ③ 도는 동안 원격이 움직였으면 크게 알리고
    **커밋 먼저 → 그다음 merge** 순서를 알려 준다(반대로 하면 로봇 결과가 병합에 휩쓸린다).
  · 저장 안 한 변경이 있으면 막는다 — 로봇 결과와 내 편집이 섞이면 나중에 못 가른다.
  · **로봇도 워크플로도 건드리지 않는다** — 평소 동작은 그대로다.
  · **`link-hunter.mjs`처럼 `playwright`(하이픈 core 아님)를 쓰는 로봇은 `npm i playwright`를
    먼저 해야 돈다** — `verify/`가 쓰는 `playwright-core`와 별개 패키지다. `package.json`은
    `.gitignore`에 있어 로컬 설치는 커밋되지 않는다.
  · 🔴 **클라우드 로봇들의 대기줄(concurrency)을 하나로 합쳐 해결하려 하지 말 것.**
    GitHub은 같은 그룹에서 **대기 중인 실행을 새 실행이 오면 취소**하므로 예약 수집이 조용히
    사라진다(deploy-sync·main-guard를 다른 그룹에 둔 것과 같은 함정). 지금 조사해 보니
    데이터를 고치는 로봇 8종이 `collector`·`link-hunter`·`resolve-detail-urls`·`search-index`
    **넷으로 갈라져 있는데 그게 맞다** — 클라우드끼리는 push 재시도와 실패 시 🚨 이슈로
    눈에 보이게 두는 편이 조용히 사라지는 것보다 낫다.
- **이 컴퓨터(Mac)에서 `verify/` 드라이버를 돌릴 때는 CHROME_PATH를 새로 찾아야 한다 (2026-08-23)**:
  `verify/README.md`의 경로(`/opt/pw-browsers/chromium-1194/...`)는 **클라우드 샌드박스(Linux)용**이라
  Mac에는 없다. `ls ~/Library/Caches/ms-playwright/`로 실제 설치된 버전을 확인해 쓴다
  (예: `chromium-1234/chrome-mac-arm64/Google Chrome for Testing.app/Contents/MacOS/Google Chrome for Testing`).
  **버전 번호를 하드코딩하지 말 것** — playwright-core를 다시 설치할 때마다 폴더 이름이 바뀐다.
- **이미 다른 워크트리가 쓰는 브랜치는 `git checkout`이 안 된다 — 순수 되가져오기(fast-forward)라면
  `git push origin <출처>:refs/heads/<브랜치>`로 ref만 옮긴다 (2026-08-23)**: 이 저장소는 워크트리를
  여러 개 두고 쓴다(기본 브랜치 워크트리가 항상 따로 떠 있다). 기본 브랜치를 되가져오려고 다른
  워크트리에서 그 브랜치를 체크아웃하면 막힌다. 반대 방향 커밋이 없을 때는 로컬 체크아웃·병합
  커밋 없이 `git push origin origin/main:refs/heads/claude/nice-heisenberg-WESq5`로 원격 ref만
  바로 옮기면 된다.
- **main에 브랜치 보호를 걸면 앱 배포가 멈춘다 (2026-07-31 판단 — 절대 켜지 말 것)**: 이 저장소는
  deploy-sync·update-progress 로봇이 **main에 직접 push**해서 앱을 내보낸다. 그래서 브랜치 보호에서
  흔히 켜는 **"Require a pull request before merging"을 걸면 로봇이 함께 막혀 앱 업데이트가 통째로
  멈춘다**(7/31에 겨우 고친 문제가 되살아난다). 켠다면 `Block force pushes`·`Restrict deletions`까지만.
  main 직접 수정 문제는 **막는 대신 고치는** `main-guard.yml`(되가져오기)로 푼다 — 관리자 권한도
  필요 없다. 참고: 공동작업자 계정 권한은 `write`라 애초에 보호 메뉴가 보이지 않는다(소유자만 admin).
- **되가져오기 로봇의 안전장치 (2026-07-31 — 실패 가능성 11가지를 먼저 세고 막았다, COLLAB.md 부록)**:
  무한 루프(로봇 push는 다른 워크플로를 못 깨움 + 최악의 경우도 2라운드 수렴 — 실증), 충돌 시 즉시
  되돌리고 이슈, push 경합 3회 재시도, **진짜 병합일 때만 audit 관문**(빨리 감기는 main과 내용이
  같으니 불필요), 되가져올 게 없으면 조용히 종료(정상 세션에 소음 0), 라벨로 이슈 중복 방지
  (제목 검색은 이모지·한글에서 놓친다), **배포 동기화와 다른 concurrency 그룹**(같은 줄에 넣으면
  GitHub이 오래된 대기분을 취소해 되가져오기가 조용히 사라진다).
- **두 개발자의 커밋은 작성자로 구분되지 않는다 (2026-07-31 확인)**: 둘 다 Claude 세션으로 작업해
  커밋 작성자가 똑같이 `Claude <noreply@anthropic.com>`로 찍힌다. 누가 한 작업인지 알려면
  **브랜치를 봐야 한다**(선주 `claude/work-seonju` / Josehyeon `claude/work-josehyeon`).
  겹침 검사·진척도 활동 로그가 모두 브랜치 기준인 이유.
- **배포 동기화 (2026-07-31 구축 — 이게 없어서 수집분이 앱에 안 나갔다)**: 로봇(수집·자동등록·심층수집)은
  **기본 브랜치에만** 커밋하는데 앱은 **main만** 배포하므로, 사람이 손으로 옮기기 전까지 새 공고가
  앱에 하루도 안 보였다(7/30~7/31 수집분 공고 5건·정식 등록 5건이 실제로 그 상태였다). 이제
  `.github/workflows/deploy-sync.yml`이 **수집 로봇 3종이 끝날 때마다 + 기본 브랜치 push마다 + 하루 2회**
  기본 브랜치를 main에 병합해 자동 배포한다. 충돌하면 조용히 멈추지 않고 🚨 이슈를 만든다.
  세션 작업 후 확인: **`node verify/check-deploy-sync.js`** — "지금 작업분이 앱에 보이는 상태인가"를
  main과 대조해 알려준다(안 올라간 데이터 건수까지 표시). 진짜 배포된 앱까지 확인하려면
  `.github/workflows/check-live.yml`('실제 앱 반영 확인')을 실행 — 샌드박스는 github.io 접속이
  막혀 있으므로 **라이브 확인은 이 워크플로 로그로만 가능**하다.
- **로봇 저장 위치 원칙 (2026-07-31 두 개발자 판 통합 — 절대 되돌리지 말 것)**: 수집 로봇은
  **자기가 읽은 브랜치(= 기본 브랜치)에만 저장한다.** 앱 배포는 deploy-sync가 맡는다.
  읽는 곳과 쓰는 곳을 가르면(예: 기본 브랜치를 체크아웃해 실행하고 결과는 main에만 저장) —
  ① `collector/seen.json`이 안 늘어 **매일 같은 공고를 새 공고로 다시 담고**(이슈 #75 유형),
  ② main에만 있던 다른 로봇·세션의 결과가 매 실행 통째로 덮여 **사라지며**,
  ③ 기본 브랜치 데이터가 굳어 다음 Claude 세션이 옛 데이터로 작업하게 된다.
  공동작업자(Se-Hyeon-Jo)가 7/31 18:34~18:59에 main에서 직접 그 방식으로 고친 판이 있었고
  (같은 문제를 반대 방향으로 푼 것 — 예약 실행은 기본 브랜치 파일을 쓰므로 실제로 돌지는 않았다),
  7/31 세션에서 **그분의 좋은 부분(push 3회 실패 시 빨간불+이슈 코멘트, 빈 리포트 방지 가드)만
  살려 수집 로봇 3종에 공통 적용**하고 저장 위치는 기본 브랜치로 통일했다.
  감시 장치: `node verify/check-deploy-sync.js`가 워크플로에 `checkout -B main`·`push origin main`이
  들어오면 즉시 잡아낸다(되살아나면 바로 보인다).
- Claude 실행 샌드박스는 학교 사이트·github.io 접속 차단 → 외부 확인은 GitHub Actions(로봇/정찰/fetch-page)로.
- **GitHub MCP 연결이 없는 세션에서도 워크플로 실행 가능**: deep-fetch/browser-collect는 `collector/run-deepfetch.txt` / `collector/run-browser-collect.txt`를 수정해 기본 브랜치에 push하면 자동 실행된다(push-to-run). 결과는 봇이 기본 브랜치에 커밋 → `git fetch` 후 merge로 읽는다.
- **알림 체계 (2026-07-31 구축 → 2026-08-06 진짜 푸시 가동)**: ⚠️ **아래 '발송 서버가 없다'는
  설명은 2026-08-05까지의 이야기다.** 지금은 발송 서버가 Cloudflare에 올라가 있고
  `push-config.js`도 채워져 있어 **앱을 켜지 않아도 폰에 알림이 온다**(18차 세션 절 참조).
  로컬 알림은 그대로 남아 두 경로가 함께 동작한다 — 아래는 그 로컬 알림 쪽 설명이다.
  확인 시점은 ① 앱을 열 때 ② 앱을 열어 둔 동안 15분마다 ③ 화면 복귀 ④ (안드로이드 설치형만)
  백그라운드 자동 확인(periodicSync, 12시간). **"폰이 꺼져 있어도 즉시 도착"이라고 쓰지 말 것** —
  화면 문구에도 그대로 정직하게 적혀 있다. 장부(설정·읽음·중복방지·알림함)는 **IndexedDB
  `handaejang-notify`**에 두는데, 이유는 서비스워커가 localStorage를 못 읽기 때문이다. 프로필·신청내역
  사본은 saveState()가 같은 저장소의 `ctx` 키에 자동으로 남긴다. **서버 푸시(VAPID) 수신구는 sw.js에
  이미 열려 있어**, 나중에 발송 서버가 생기면 앱 코드 변경 없이 진짜 푸시로 확장된다.
- **진짜 푸시 vs 로컬 알림 (2026-07-31 — 혼동하지 말 것)**: 둘은 다른 물건이다.
  · **로컬 알림**(먼저 만든 것): 앱이 열려 있을 때만 판단·발송. 앱을 안 켜면 안 온다.
  · **진짜 푸시**(나중에 만든 것): 발송 서버가 폰을 깨워서 **앱을 안 켜도, 화면이 꺼져 있어도** 온다.
  구조의 핵심은 **서버가 알림 내용을 만들지 않는다**는 것 — 서버는 내용 없는 '깨우기' 푸시만 보내고,
  깨어난 서비스워커가 **기기 안 프로필**로 판단해 문구를 만든다. 그래서 이름·성적·소득이 서버로 나가지
  않고(서버 저장 항목은 폰 주소·학교·캠퍼스 3개), 매칭 규칙도 match-engine.js 한 벌만 쓴다.
  · 브라우저 규칙상 푸시를 받으면 **반드시 눈에 보이는 알림 1건**을 띄워야 한다(userVisibleOnly).
    그래서 규칙상 알릴 게 없으면 sw.js가 조용한 안내 1건을 대신 띄운다 — 지우면 크롬이 자기 문구로
    "백그라운드에서 업데이트됨" 알림을 대신 띄운다.
  · 사용자가 알림을 끄면 구독도 해지한다. 그래도 푸시가 오면 **서비스워커가 스스로 구독을 끊는다**.
  · iOS는 **홈 화면에 설치해야만** 푸시가 온다(16.4+). 안드로이드·PC 크롬은 설치 없이도 온다.
  · 켜는 방법: `server/push/README.md`대로 무료 서버를 올리고 `push-config.js` 두 값을 채우면 끝.
    **비워 두면 앱은 예전처럼 로컬 알림만 쓴다**(검증 드라이버가 이 두 상태를 각각 검사한다).
- **🔴 푸시 등록이 사라지는 3가지 길과 그 방어 (2026-08-07 실사고로 정리 — 되돌리지 말 것)**:
  공동개발자 폰이 발송 서버에서 조용히 사라졌던 사고의 전말. **되돌리면 같은 일이 재발한다.**
  · **KV 화면의 등록 수는 '살아 있는 폰 수'가 아니다.** 폰 쪽에서 구독이 죽어도 서버는 모른다
    (발송해 봐야 404로 알 수 있다). 이 간극 때문에 "2대 등록"인데 받는 건 1대인 상태가 생긴다.
    **실제로 받는지는 `/test` 발송만이 증명한다** → `.github/workflows/push-check.yml` 버튼.
  · ① **메모만 믿지 말 것**: `pushEnsure()`는 앱 장부가 아니라 **브라우저의 실제 구독**
    (`pushRealEndpoint()`)을 본다. 구독은 앱 모르게 죽는다(폰의 앱 정리·저장공간 삭제·주소 변경).
    메모만 보던 시절엔 한 번 죽으면 **영영 재등록되지 않았다.** 하루 1회 서버 재확인(`pushSyncedAt`)도 함께.
  · ② **'껐다'와 '못 읽었다'를 구분할 것**: 서비스워커가 장부를 못 읽으면
    `normalizeLedger(null)`이 `enabled:false` 기본 장부를 준다. 그걸 "껐다"로 읽고 구독을 끊으면
    **알림을 켜 둔 폰이 사라진다.** 판정은 `notify-rules.js`의 **`shouldSelfUnsubscribe(raw, readOk)`**
    한 곳에만 둔다(화면·서비스워커·검사 공유). 이 저장소의 '못 읽음을 사실로 단정하지 말 것' 원칙과 같은 계열.
  · ③ **갈아탄 옛 주소는 서버에서도 지울 것**: 새 주소만 등록하면 죽은 등록이 KV에 쌓이고,
    사용자가 늘면 발송 예산(실행당 바깥 요청 50건)을 갉아먹는다. `pushSubscribe(replacingEndpoint)`가
    등록 성공 후 옛 주소로 `/unsubscribe`를 부른다(실패해도 다음 발송의 404 정리가 받친다).
  · 회귀: `verify-notify-rules.js` 6항목 + `verify-push-client.js` 3항목 + `verify-push-server.mjs` 2항목.
  · **발송 속도 한계**: 2분마다 25대(`SEND_PER_TICK`) → 1,000대면 전원 깨우는 데 1.3시간.
    수백 명까지 무해. 넘길 때 해법은 걸음당 45대·학교별 회차 분할·유료 등급(20차 세션 절 4).
- **자동 업데이트 체계 (2026-07-05 구축 → 2026-07-31 전면 보강)**: ① 데이터(`data/*.json` — 공고·정식등록·양식)는 서비스워커가 네트워크 우선으로 받고, 앱을 열 때마다 + 화면 복귀 때마다(5분 간격 제한) 다시 읽는다 → **재설치 없이 자동 반영**. ② **앱 코드(html/js/css)도 이제 네트워크 우선**(sw.js v15) — 예전엔 코드가 '캐시 우선'이라 CACHE 버전을 손으로 올리는 걸 잊으면 설치된 앱에 영영 반영되지 않았다(7/30 app.js 변경이 실제로 이 상태였다). 이제 **버전 인상을 잊어도 다음 실행에 반영**된다(버전 인상은 관례로 계속 권장 — 옛 캐시 청소용). ③ **서비스워커의 fetch는 `cache:'no-cache'`로 서버에 반드시 확인한다** — 그냥 fetch하면 브라우저가 GitHub Pages의 10분 캐시를 그대로 내주기 때문. 안 바뀌었으면 304라 데이터도 거의 안 쓴다. ④ 네트워크가 3.5초 안에 응답 없으면 캐시로 폴백 → **오프라인 실행은 그대로 보장**(브라우저 실증 완료).
- **보안 체계 (2026-07-05 점검 완료)**: index.html에 CSP 메타(외부 스크립트·임의 접속 차단 — 수집 데이터가 오염돼도 코드 실행 불가) + 리퍼러 미전송. 모든 장학금 데이터 렌더링에 esc() 적용. 개인정보(프로필·서류·양식 답변)는 localStorage/IndexedDB **기기 안에만** 저장, 외부 전송 코드 없음(사용자가 직접 공유·메일 열기 시에만 나감). mail-worker는 Origin 검사+수신 도메인 허용목록(ac.kr/or.kr/go.kr/re.kr)+크기 제한으로 스팸 릴레이 차단. 워크플로 입력은 환경변수로만 셸에 전달.
- **새 양식 등록은 `data/forms.json`에만 추가**하면 된다(forms.js·app.js 무변경). registered.json 항목에 `formId`를 연결하면 신청 플로우에 양식 작성이 자동으로 붙는다. 이 경로의 검증 드라이버: `verify/verify-forms-data.js`.
- **수집 로봇이 새 공고 0건이면 새 리포트 이슈를 만들지 않고** 최근 리포트 이슈에 "정상 실행 — 0건" 코멘트만 남긴다(2026-07-05 추가).
- **예약 실행 혼잡 회피 정책 (2026-07-06)**: GitHub는 UTC 정각·자정 부근 예약을 지연·생략한다(7/5 +3h44m 지연, 7/6 완전 누락 발생). 그래서 수집은 홀수 분 하루 2회 예약 — 일반 07:41/11:41 KST, 브라우저 08:07/12:07 KST. 중복 실행은 seen.json이 걸러 무해. 일반 수집도 push-to-run 지원(`collector/run-collect.txt`).
- **앱에서 뜨는 화면(시트)은 전부 아래로 쓸어내려 닫는다 (2026-08-21 개발자 지시 — 앞으로 새로
  만드는 것도 포함)**: 지금 대상은 `#detail-sheet`·`#notify-sheet`·`#chat-sheet` 셋이고, 규칙은
  **`app.js enableSheetSwipe` 한 곳**에 있다. ✕ 버튼은 남겨 두되(보조기기·마우스) 손가락 사용자가
  ✕를 찾아 누르지 않아도 되게 하는 것이 목적이다.
  · **닫기와 스크롤을 가르는 판정은 `scrollableAtTop(target, sheet)` 하나다** — 손가락이 닿은
    요소부터 시트까지 거슬러 올라가며 `scrollTop > 0`인 영역이 하나라도 있으면 **스크롤로 본다.**
    시트 자신만 보면 안 되는 이유: 도우미 시트는 시트가 고정이고 **안쪽 대화 목록**이 스크롤돼서
    시트의 scrollTop이 늘 0이다. 그걸 놓치면 대화를 위로 올려 보려 할 때마다 닫힌다
    (학교 목록 touchstart 사고와 같은 계열 — **이 확인을 지우면 그 함정이 그대로 되살아난다**).
  · 회귀는 `verify/verify-chat.js` [5] 4항목. **반드시 TouchEvent로 '끌기'를 재현할 것** —
    마우스로는 이 유형이 한 번도 재현되지 않는다(13차 세션 학교 검색 사고).
- iOS Safari는 datalist 미지원(커스텀 자동추천 구현됨), 전역 appearance:none이 체크박스 지움(커스텀 체크 구현됨).
- 검증: **`verify/` 폴더에 Playwright 드라이버 커밋되어 있음** (`verify/README.md` 절차대로 실행 — drive.js 전체 회귀, verify-registered.js 정식등록·조병두 양식, verify-forms-data.js 데이터 주도 양식 반영). 한글 파일명 setInputFiles 실패함(ASCII 사용).
- HWP 원본 해석: `olefile`로 열어 `PrvText` 스트림을 UTF-16LE로 읽으면 양식 전문이 나온다 (조병두 스키마화에 사용한 방법).
- 광운·상명 게시판은 본문을 자바스크립트로 그려서 일반 fetch에는 메뉴만 보임 → 1차 수집 때 저장된 deadlineHint를 활용하거나 브라우저형 로봇 사용.
- **클릭형·내부 전송형 게시판 지원 (2026-07-07, 경희대 연결로 구축)**: 공고 행이 진짜 링크가 아니라
  클릭 스크립트(javascript: 주소)이고 상세 주소도 전부 같은 view.do인 게시판(경희 news.khu.ac.kr 유형)은,
  브라우저 로봇이 **행을 실제 클릭해 상세 화면에서 마감·첨부를 즉시 채집**하고 링크는 목록 주소+제목 표식으로
  기록한다. 인식이 1건 이하인 게시판은 진단(프레임 수·클릭 시도·본 글자·본 링크)이 리포트에 자동 포함 —
  새 게시판 연결이 안 되면 이 진단부터 볼 것. 경희대 연결에 8회 실행이 걸린 원인·해결 과정은 이 진단 도입 전이었기 때문.
- **워크플로 저장 단계 규칙 (2026-07-19 사고로 도입)**: `git add`에 **아직 존재하지 않을 수 있는
  파일을 넣으면 'fatal: pathspec' 오류로 실행 전체가 실패**한다 — 7/17에 추가한 pending-forms.json이
  이 이유로 7/17~19 수집 8회를 연속 침몰시켰다(수집은 성공했지만 저장 직전에 실패 — 주말이라 실제
  유실은 0건). 선택 파일은 `[ -f ... ] && git add ...` 형태로만 추가할 것. 재발 방지로 두 수집
  워크플로에 **실패 시 리포트 이슈에 🚨 코멘트를 남기는 스텝(if: failure())** 이 달려 있다 —
  조용한 중단은 이제 불가능. 로봇 이상 보고를 받으면 Actions 실행 목록의 conclusion부터 볼 것.
- **예산은 '물어보는 장치'라 멈춘 학교를 못 끊는다 → 절대 시한 필수 (2026-08-17 사고로 도입)**:
  수집 시간 예산(`harvest-budget.mjs`)은 학교를 **시작하기 전과 끝난 뒤에만** 물어볼 수 있다. 그래서
  학교 한 곳이 답을 영영 안 주면(브라우저가 화면 안 그림·응답 무한 대기) 아무도 못 물어보고 로봇이
  그 자리에 멈춰 선다 → 26분 단계 상한에 걸려 **강제 종료** → 저장 단계까지 죽어 **그날 수집분 전량 유실**.
  8/15 12:47 이후 **3회 연속**(8/16 08:29·8/16 13:20·8/17 08:29) 이렇게 날아갔고, 그중 8/16 08:29은
  학교 17곳을 7분 50초에 **다 돌고도** 덤으로 붙는 '실패 학교 재시도'(가천대)가 멈춰 전부 잃었다.
  더 나쁜 건 커서(`browser-cursor.json`)도 저장을 못 해 **다음 실행이 같은 순서로 같은 학교에서 또 멈추는**
  악순환 — 멈춘 학교를 비켜 가라고 만든 회전 커서가 그 멈춤 때문에 못 돈다. 그리고 수집 단계에
  continue-on-error가 붙어 있어 **Actions에는 초록불(success)로 보인다** — 유일한 신호는 리포트 이슈의
  🚨 코멘트다. 해법: `withDeadline()`으로 학교 한 곳에 **대답을 안 기다리는 시한**(=MIN_PER_TARGET_MS)을
  걸고, 멈춘 학교는 재시도하지 않으며, 저장을 마치면 `process.exit(0)`로 스스로 끝낸다.
  같은 유형의 시간 사고가 또 나면 **상한을 키우지 말고** 이 절부터 볼 것.
- **로봇이 고친 파일은 전부 `git add`에 넣어야 한다 (2026-08-01 감사 실패로 도입 — 이슈 #79)**:
  스키마화(`schematize-forms.mjs`)는 **`data/forms.json`(양식 원본)과 `data/registered.json`(연결)을
  한 쌍으로** 고치는데, 두 수집 워크플로의 저장 단계가 registered.json만 담고 있었다. 그래서 매 실행마다
  **연결만 저장되고 양식은 버려져** '없는 양식을 가리키는 연결'이 생겼고, 다음 날 감사가 이걸 잡아
  🚨 실패 메일이 왔다. 규칙 ①저장 단계 `git add` 목록은 로봇이 쓰는 파일과 항상 같아야 한다
  ②**데이터 관문(감사)은 저장 직전에 둔다** — 예전엔 스키마화보다 앞이라 그 실행에서 못 잡고 하루 뒤에
  터졌다 ③양식 키는 같은 공고면 같은 키를 덮어쓴다(예전엔 실행마다 `-2`, `-3`… 이 새로 생겼다)
  ④스키마화 마지막에 '없는 양식을 가리키는 연결'을 스스로 끊는 자가복구가 있다(리포트에 표기됨).
- **학자금대출의 자리 (2026-07-30 정리)**: 대출·융자는 장학금이 아니라서 **정식 등록(매칭 카드)에서
  제외**한다(entry-rules가 강제). 다만 앱에 대출 안내 카드가 따로 없어 피드에서까지 밀리면 학생이
  대출 정보를 볼 곳이 없어지므로, 실시간 공고 피드는 **장학 공고 8칸 + 대출 공고 2칸**으로 자리를
  나눠 둔다(app.js liveNoticesHtml). 예전엔 대출을 뒤로 정렬만 해서 공고가 많은 학교(중앙대 16건)에선
  통째로 잘려 나갔다. '대출 원금·이자를 지원하는 장학금'(세종이도 등)은 대출이 아니라 장학금이므로
  정식 등록 대상이다(entry-rules의 LOAN_EXCEPT).
- **학교를 더 붙일 때 먼저 걸리는 것 (2026-08-01 실측)**: 실행 시간·GitHub 요금은 안 걸린다(공개 저장소라
  Actions 무료 · 브라우저 수집은 학교 3곳씩 동시에 봐서 30개교여도 10분대). **진짜 걸림돌은 폰이 내려받는
  파일 크기 하나뿐이다** — 앱은 `data/notices.json`을 통째로 받은 뒤 자기 학교 것만 골라 쓰므로
  (`app.js liveNoticesHtml`) 고려대 학생도 동국대 공고를 같이 받는다. 1건당 654바이트 실측:
  13개교 93KB · 30개교 0.28MB · 100개교 0.93MB. 서비스워커가 no-cache 재확인이라 안 바뀌면 304(거의 0바이트)라
  실제로 다 받는 건 공고가 바뀐 날 첫 실행뿐. **실질 한계 50~100개교** — 서울권 주요 대학을 다 붙여도
  30~40곳이라 지금 구조로 충분하다. 넘길 때의 해법은 `data/notices/<학교>.json`으로 **학교별 파일 분리**
  (앱 코드도 같이 고쳐야 하므로 **40곳쯤 넘을 때** 하면 된다 — 미리 하지 말 것).
  공고 상한은 `collector/url-key.mjs`의 **`capNotices()`** — 학교 수 × 15건(최소 200). 넘칠 때 뒤에서
  뭉텅이로 자르지 않고 **학교당 몫을 함께 줄인다**(그냥 자르면 목록 뒤쪽 학교가 통째로 0건이 된다 — 실증).
- **워크플로에 `timeout-minutes`를 반드시 둘 것 (2026-08-01 도입)**: 없으면 GitHub 기본값인 **6시간**까지
  매달린다. 7/31 브라우저 수집이 실제로 360분을 돌다 취소됐고, 수집 2종이 같은 대기줄(`concurrency: collector`,
  `cancel-in-progress: false`)이라 **그동안 다음 수집이 줄을 섰다.** 로봇 6종에 부여됨
  (수집 25 · 브라우저 40 · 사냥 40 · 복구 40 · 심층 30 · 정찰 15).
- **오래 걸리는 로봇은 '넘어져도 저장'이 필수 (2026-08-01 사고로 도입)**: 링크 사냥꾼이 4분 동안 원문 주소
  13건을 찾아 놓고 **리포트 마지막 줄의 낱말 하나**(옛 이름 `GIVE_UP_AFTER`) 때문에 넘어졌는데, 저장 단계가
  통째로 건너뛰어져 **전부 버려졌다.** 지금은 ⓐ 저장 경로를 `saveAll()` 하나로 모으고 ⓑ
  `uncaughtException`/`unhandledRejection`에서 그 함수를 불러 저장한 뒤 **종료 코드 1로 끝낸다**
  (실패를 성공으로 위장하지 않는다) ⓒ 워크플로의 감사·저장 단계를 `always()`로 두었다.
  같은 장치가 복구 로봇에도 들어가 있다. **이 유형은 `verify/test-collector.mjs`의 '선언 없는 대문자 이름'
  정적 검사가 실행 전에 잡는다**(collector/*.mjs 전수).
- **⚠️ 시간초과(취소)는 '넘어져도 저장'으로 못 막는다 — 스스로 예산 안에 끝내야 한다 (2026-08-03 사고)**:
  13차 세션이 학교를 13→29곳으로 늘리자 브라우저 수집이 8분 → 40분+가 되어 `timeout-minutes: 40`에
  걸려 **4회 연속 취소**됐고(8/2 05:16·06:08, 8/3 00:07·06:31), 마지막 성공 수집이 **8/2 00:27**로
  멈춰 17개 학교 공고가 이틀간 갱신되지 않았다. 위 '넘어져도 저장'은 **크래시**만 막는다 — 시간초과는
  GitHub이 프로세스를 **강제 종료**하는 것이라 자바스크립트가 손쓸 틈이 없고, 저장 단계까지 통째로 죽는다.
  · **상한을 올리는 것은 해법이 아니다**(학교가 늘면 또 걸린다). 해법은 **전역 시간 예산**:
    `collector/harvest-budget.mjs`(순수 함수 — 학교 접속 없이 검사 가능)의 `makeBudget`으로
    **22분**(`HARVEST_BUDGET_MS`) 안에 스스로 끝내고, 남은 학교는 건너뛴 뒤 **저장 단계까지 간다.**
  · **시간을 먹던 진짜 자리는 상세 방문이었다** — 학교당 최대 40건 × 최대 30초 = **최악 20분/학교**인데
    클릭 채집(180초)에만 예산이 있고 여기엔 없었다. 지금은 학교당 4분(`DETAIL_BUDGET_MS`).
    잘려도 공고는 목록에서 확보돼 저장되고 마감·첨부만 다음 실행에서 채워진다.
  · 실패 학교 재시도가 **순차**(for)라 8곳 실패 시 학교 하나씩 처음부터 다시 돌았다 → 본 수집과 같은 병렬 풀 사용.
  · **학교 순서를 회전시킨다**(`collector/browser-cursor.json`) — 안 하면 잘리는 학교가 **매번 설정 파일
    뒤쪽으로 고정**돼 그 학교는 영영 안 돈다. 하루 2회 실행이라 모든 학교가 하루 안에 한 번은 돈다.
    **커서를 워크플로 `git add` 목록에 넣는 것까지가 한 세트**(빠뜨리면 회전이 저장 안 돼 무의미).
  · 회귀: `verify/test-collector.mjs`의 '수집 시간 예산'·'학교 순서 회전'·'예산 장치 배선' 절
    (가짜 시계라 인터넷 불필요). 예산 확인을 지우거나 상한을 예산 아래로 낮추면 **실제로 실패한다**(실증).
- **⚠️ 보강 단계가 수집을 죽이면 안 된다 — 단계마다 자기 예산을 준다 (2026-08-04 사고, 17차 세션)**:
  8/3 밤·8/4 아침 수집 4회(일반 2·브라우저 2)가 전부 **취소**로 끝나 개발자 두 명에게 리포트가 안 갔다.
  **수집 자체는 2분 만에 성공했었다** — 죽인 것은 그 뒤의 '양식 원본 확보' 보강 단계였다.
  · **원인 ①**: 그 단계가 `deepfetch.mjs`를 **인자 없이** 불러서 심층 수집 **본편**(수집 목록 전체의
    본문 재수집)을 돌렸다. 공고가 614건으로 늘자 그 재수집만 **19분** → 작업 상한 25분 초과.
    첨부 원본만 있으면 되는데 본문까지 받은 것이고, 본문은 바로 다음 단계(`--fill`)가 어차피 받는다.
    → **`--forms-only`** 신설(첨부만). 이 인자를 빼면 그날로 사고가 재현된다.
  · **원인 ②**: 표적이 **제목 앞 12자**라 `"2026학년도 2학기 "`가 **공고 100건**에 걸렸다. 그래서
    학자금대출·캠퍼스 안내의 첨부(3.9MB zip 등)까지 받고 있었다. → 표적 **30자**(auto-register),
    6자 미만 표적 무시 · 표적당 3건 · 전체 12건 · `FORMS_BUDGET_MS`(6분) 상한(deepfetch).
    **머리쪽 대괄호만** 떼는 것도 규칙이다 — 가운데 것까지 지우면 표적이 원래 제목의 이어진 한 토막이
    아니게 돼 아무 공고에도 안 걸린다(지금 큐에 그렇게 굳은 항목이 5건 있다).
  · **원인 ③ — 이게 '아무도 몰랐던' 이유**: `if: failure()`는 **시간 초과에 반응하지 않는다.**
    상한을 넘긴 작업은 '실패'가 아니라 **'취소'**로 끝나서 🚨 알림 단계까지 통째로 건너뛴다.
    → **`if: failure() || cancelled()`**. 새 로봇을 만들 때도 이 두 개를 같이 쓸 것.
  · **구조 규칙(이것만 기억하면 된다)**: 수집·저장·리포트는 본업, 나머지는 보강이다. 보강 단계에는
    **`timeout-minutes` + `continue-on-error: true`** 를 달아 무슨 일이 있어도 **그날 수집분 저장과
    리포트는 나가게** 한다. 그리고 **작업 상한 = 모든 단계 예산의 합을 담는 그릇**으로 정의한다
    (브라우저 30→34분 = 수집 22 + 양식 4 + 보충 3 + 준비·저장 3). 상한을 '넉넉히' 올리는 것과 다르다 —
    `verify/test-collector.mjs`의 '작업 상한이 수집 예산 + 단계 예산의 합보다 크다'가 이 대소관계를 검사한다.
    (같은 검사가 작업 상한과 단계 상한을 **들여쓰기로 구분**해 읽는다 — 4칸이 작업, 8칸이 단계.)
  · 못 받는 양식 원본은 **6회 뒤 표적에서 내린다**(`mark-fetched.mjs`의 `retired`). 조용히 사라지는
    게 아니라 리포트에 '자동 재시도 중단'으로 계속 뜬다. 다시 받으려면 `retired`만 지우면 된다.
  · **곁다리 교훈**: 문법 검사를 한다고 `node -e "import('./collector/x.mjs')"` 하지 말 것 —
    **모듈을 불러오면 그 자리에서 실행된다.** 이 세션에서 실제로 auto-register·deepfetch·schematize가
    로컬 데이터를 고쳐 놓았다(되돌림). 문법만 볼 때는 **`node --check`**.
- **데이터 관문이 실패 상태면 로봇 결과가 하나도 안 남는다 (2026-08-01)**: 워크플로는 감사가 실패하면
  데이터를 되돌리고 저장을 건너뛴다. 실제로 `reg-hufs-namgaju`가 없는 양식을 가리켜 감사가 실패하고 있었고,
  그 상태에서 사냥을 돌렸으면 성공해도 결과가 안 남았을 것이다. **세션 시작 때 `node verify/audit-data.js`를 먼저 돌릴 것.**
- **'못 읽음'을 '다른 글'이라고 부르지 말 것 — 동국대 사례 (2026-08-01 정찰로 규명)**: 사냥꾼이 동국대 12건을
  전부 `제목 불일치(다른 글이 열림)`로 버렸는데, 정찰로 직접 열어 보니 **전부 맞는 공고**였다(200 · 로그인 불필요).
  동국대는 짧은 시간에 여러 번 두드리면 응답을 막고, 그때 오는 것은 404가 아니라 **본문이 안 그려진 껍데기 화면**이다.
  지금은 불일치 시 4초 더 기다려 재확인하고, 본문 표지(등록일·첨부·이전글…)가 없으면 '판정 불가'로 다룬다.
  대상 간 간격도 900ms→2500ms. **동국대 목록 행은 `href="#none"`이라 주소가 없어 반드시 눌러야 하는데,
  '오늘 하루 보지 않기 X' 팝업이 화면을 덮어 클릭이 실패했다** — 팝업을 먼저 닫고, 그래도 막히면
  `el.evaluate(e=>e.click())`로 누른다.
- **분교와 이원화는 다른 물건이다 (2026-08-02 개발자 지시로 정리 — 헷갈리면 학생이 남의 학교 공고를 받는다)**:
  **분교는 법적으로 별개 대학**이라 `UNIVERSITIES`에 **학교로 따로** 올린다(캠퍼스 선택이 아니다).
  **이원화는 한 대학의 두 캠퍼스**라 `CAMPUSES_BY_SCHOOL`에 남긴다.
  · 분교 7곳(나눔): 연세 미래(원주) · 고려 세종 · 한양 ERICA(안산) · 건국 글로컬(충주) ·
    동국 WISE(경주) · 홍익 세종 · 상명 천안. 이름은 **본교는 그대로, 분교만 캠퍼스명**
    (`한양대학교` / `한양대학교 ERICA캠퍼스`) — 본교 학생 프로필·기존 등록 공고를 안 건드려도 된다.
  · 이원화(그대로 둠): 경희 국제 · 외대 글로벌 · 성균관 자연과학 · 중앙 다빈치 · 명지 자연 ·
    **단국 천안** · **부산 밀양** · **경북 상주**. 뒤 셋 주의 — 단국 천안은 설립은 분교였지만
    **지금 법적으로 이원화**이고, 부산 밀양·경북 상주는 **통합(2006·2008)으로 한 대학이 됐다.**
    "설립 당시"나 "인식"이 아니라 **현재 법적 지위**가 기준이다(개발자 확인).
  · **함정 — 게시판을 본교와 함께 쓰는 분교가 있다**: 한양·건국·홍익은 한 게시판에 같이 올라온다.
    판정은 **`match-engine.js`의 `noticeForProfile()` 하나**에 있다(`SHARED_BOARD_BRANCH`·`TITLE_CAMPUS`).
    **`data.js`에 두면 안 된다 — 서비스워커가 data.js를 안 읽어 알림 판정이 화면과 갈라진다.**
    연세 미래·고려 세종·동국 WISE·상명 천안은 **자기 게시판이 따로** 있어 공유 목록에 없다.
  · **함정 — 한양대는 제목 앞에 `[서울]`·`[ERICA]`로 캠퍼스를 가른다 (2026-08-02 개발자 지적)**:
    공고의 `campus` 칸만 보면 둘이 안 갈라져서 **ERICA 학생이 [서울] 공고를 그대로 받고 있었다.**
    `TITLE_CAMPUS`가 제목 표식을 읽어 가른다. 건국(`[교외]`·`[교내]`·`[국가]`)과
    홍익(`[문과대학…]`·`[재단명]`)의 대괄호는 **캠퍼스가 아니라 분류**라 넣으면 안 된다 — 실제 데이터로 확인.
  · **알림도 같은 함수를 써야 한다**: `notify-rules.js`는 학교 판정을 따로 갖고 있어서, 분교를 나눈 직후
    **분교 학생이 '우리 학교 새 공고' 알림을 0건 받는 상태**였다. 지금은 `matchStatus`·`notStale`과 같은
    방식으로 `noticeForProfile`을 주입받는다(notify.js·sw.js 두 호출부). 검사에 '주입 없으면 0건'
    항목을 남겨 뒀다 — 이 수정이 왜 필요했는지가 회귀로 남는다.
  · **저장된 프로필 이전 필수**: 예전엔 '한양대학교 + ERICA캠퍼스(안산)'로 저장돼 있어, 그냥 두면
    캠퍼스 값이 갈 곳을 잃고 **분교 학생이 본교 학생으로 취급된다**. `app.js migrateBranchCampus()`가
    loadState에서 한 번 옮긴다. 이런 데이터 모델 변경은 **항상 기존 저장분 이전까지가 한 세트**다.
  · 소급 적용(원칙 7): 본교가 단일 캠퍼스가 됐으므로 registered.json의 `campusOnly` 18건과
    notices.json 캠퍼스 표시 56건을 정리했고, `collector/schools.json`·`browser-targets.json`의
    분교 게시판은 새 학교명으로 바꿨다.
- **데이터 수명 규칙**: 실시간 공고는 수집일+60일 후 자동 삭제(collect.mjs), 정식 등록 장학금은 마감+30일 후 목록 자동 숨김(app.js renderExplore). 매일 09:00 KST 수집으로 유입 — 순환 구조.
- **관리자 페이지가 학생 주소에 안 뜨는 이유 (2026-08-03)**: 이 저장소엔 `.nojekyll`도 `_config.yml`도
  없어 **GitHub Pages가 Jekyll로 돈다 → 밑줄로 시작하는 폴더(`_admin/`)를 배포하지 않는다.** 관리자 화면이
  학생 주소에서 감춰지는 근거가 이것 하나뿐이므로, **`.nojekyll`이나 `_config.yml`을 추가하면 즉시 노출된다.**
  재발 방지로 `check-live.yml`이 매일 `/_admin/`이 정말 404인지 확인하고, 200이면 🚨 이슈를 만든다.
- **🔴 저장소를 비공개로 바꾸면 앱이 즉시 내려간다 (2026-08-03 확인 — 순서를 절대 바꾸지 말 것)**:
  **GitHub Pages는 무료 플랜에서 비공개 저장소를 배포하지 않는다**(Pro 이상 필요).
  비공개로 전환하는 순간 `seonju5543-web.github.io/hanggonggan`이 **404가 된다.**
  → 비공개 전환은 **앱1을 Cloudflare Pages로 완전히 옮기고 정상 작동을 확인한 뒤에만** 한다.
  개발자가 제안한 순서(①비공개 → ②Cloudflare 연동)를 그대로 따르면 그 사이에 앱이 죽는다.
- **Oracle Cloud 자체 러너 — 검토했고 2026-08-03 개발자가 '아직 쓰지 않기로' 결정**: 아래는 그 판단 근거다.
  **다시 검토하게 되면 이 항목부터 볼 것 — 재조사 불필요.**
  · **제안의 강점 2가지는 실제로 맞다**: ⓐ 자체 러너는 GitHub 분(minutes)을 소모하지 않아 비공개
    전환의 월 2,000분 제한이 무의미해진다 ⓑ 공개 저장소에서 자체 러너는 위험한데(외부인이 서버에서
    코드 실행) **비공개 전환이 그 위험을 없앤다** — 두 변경이 서로를 돕는다.
  · **하지만 지금은 필요 없어졌다**: 8/3 브라우저 수집 시간초과 수리로 Actions 사용량이
    **월 2,051분 → 약 700분**(한도의 35%)으로 내려갔다. 790분이었던 이유는 로봇이 40~360분씩
    매달려 있었기 때문이지 일이 많아서가 아니었다.
  · **기술적 걸림돌 2가지**: ⓐ **Playwright는 Linux ARM64용 Chromium을 제공하지 않는다** —
    Oracle의 쓸 만한 무료 인스턴스(Ampere A1)가 ARM이고, 옮기려는 작업의 89%가 브라우저 수집이다
    (무료 AMD는 1GB 램이라 더 불가). 우회는 ARM용 Docker 이미지·시스템 Chromium.
    ⓑ **Oracle은 유휴 인스턴스를 회수한다** — 7일간 CPU·네트워크·메모리가 전부 10% 미만이면 회수(A1 한정).
    우리 로봇은 하루 2회 몇 분만 돌아 정확히 회수 대상이다.
  · **무료 ARM이 2026년 6월부터 반토막** — 4 OCPU/24GB → 2 OCPU/12GB. 지역에 따라 '용량 없음'도 흔하다.
  · **다시 필요해지는 시점**: 학교를 50~100곳으로 크게 늘려 Actions 사용량이 한도에 다시 닿을 때.
- **Cloudflare 배포 이전 — 실측 근거 (2026-08-03 조사 · 이전 자체는 미실행)**: 개발자 질문
  "GitHub 서비스를 Cloudflare로 전부 대체할 수 있나"에 답하려 실제로 재 본 값들. **재조사하지 말 것.**
  · **결론: Cloudflare는 GitHub를 대체하지 않는다.** 저장소(git)·Issues(로봇 리포트)·범용 CI가 **없다**.
    목표 구조는 **GitHub(저장소+로봇) + Cloudflare(배포+서버)** 하이브리드 — `_admin/`이 이미 그 구조다.
  · **브라우저 로봇은 옮길 수 없다**: Browser Rendering 무료는 **하루 10분**인데 우리는 **하루 61분**
    (월 790분)을 쓴다. 6배 부족. 유료도 월 10시간까지만 포함.
  · **저장소를 비공개로 바꾸면 로봇이 월 중순에 멈춘다**: Actions 실측 **월 2,051분**(13일 889분) >
    비공개 무료 한도 **월 2,000분**. 그중 브라우저 수집이 790분(89%).
  · **Pages 빌드는 월 500회 상한** — 기본 브랜치 커밋이 **하루 평균 17건**이라 빌드 감시 경로가 없으면
    초과한다(`_admin/README.md`의 경로 설정이 선택이 아닌 이유).
  · **푸시 알림은 배포 이전과 무관하다** — 막힌 건 ⓐ `server/push/` 미배포 ⓑ `push-config.js` 빈 칸
    둘뿐이고, **앱을 GitHub Pages에 둔 채로도 켤 수 있다.** 다만 구독은 **주소에 묶이므로** 나중에
    앱 주소를 바꾸면 기존 구독이 전부 무효가 된다(사용자 0명인 지금이 가장 싸다).
    Workers 무료 등급은 **한 번 실행마다** ⓐ 바깥 요청 **50건** ⓑ 계산 시간 **10밀리초**를 건다.
    옛 구조(`MAX_SUBS_PER_RUN = 800` 한 방에 처리)는 **둘 다** 넘겼다 — ⓐ는 51번째 폰부터 조용히 누락,
    ⓑ는 공고 데이터 해석(registered 291KB + notices 468KB = 실측 6~9ms)만으로 **사용자가 0명이어도**
    실행이 강제 종료. **2026-08-05에 '한 걸음씩' 구조로 고쳤다**(`server/push/worker.js` 첫머리 주석):
    2분마다 cron → reg→notices→plan→send(25대씩) 한 걸음, KV `state:run`에 진행 기록,
    걸음이 5회 실패하면 포기하고 `state:lastError`에 사유(→ `/health`에 노출, 조용한 중단 불가),
    VAPID 서명은 푸시 서비스별로 1회만 만들어 재사용. 검증: `node verify/verify-push-server.mjs`(74항목).
    ⚠️ **남은 위험**: 데이터 해석 걸음이 계산 시간에 가장 가깝다(Node 실측 4~12ms, Workers는 더 빠를
    것으로 보이나 미검증). 배포 후 `/health`의 `lastError`에 `step:"reg"`나 `"notices"`가 뜨면 그게 이것이다.
    그때의 해법은 **GitHub Actions가 작은 요약 파일(`data/push-digest.json`)을 만들어 두고 Worker는
    그것만 읽게 하는 것** — 해석 비용이 0에 수렴한다. 데이터가 더 커지면 선제적으로 해도 된다.
  · **앱1을 옮길 때 `_admin` 명시적 제외 필수** — 지금 관리자 화면이 학생 눈에 안 보이는 근거는
    **GitHub이 밑줄 폴더를 배포 안 하는 것 하나뿐**이다(위 항목). Cloudflare엔 그 규칙이 없다.
  · **앱 주소가 박힌 곳 8군데**: `server/mail-worker.js`(CORS 2) · `server/push/worker.js`(APP_ORIGIN) ·
    `.github/workflows/check-live.yml`(3) · `proposals/src/*.html`(2종·PDF 재생성). 옮길 때
    `data.js`의 상수 하나로 모을 것 — 하나만 놓쳐도 메일 접수·푸시가 조용히 막힌다.
- **관리자 화면은 Cloudflare Pages + Access (2026-08-03 개발자 결정)**: 앱1은 GitHub Pages에 그대로 두고
  **관리자 화면만** Cloudflare에 올린다. ① Access가 '허용 이메일에 일회용 코드 발송' 로그인을 코드 없이 제공
  (개발자 원안 그대로) + 접근 기록 ② 도메인이 달라 앱1과 브라우저 저장 공간이 분리 ③ 무료(푸시 서버와 같은 계정).
  빌드 명령 `bash _admin/build.sh`, 출력 폴더 `_admin/dist`, 프로덕션 브랜치 = 기본 브랜치.
- **관리자 화면의 공용 코드는 복사하지 말 것**: `_admin/build.sh`가 빌드 때 `data.js`(접수 채널 판정)·
  `forms.js`(양식 문서 생성)·`verify/entry-rules.cjs`(등록 규칙)·`collector/url-key.mjs`(주소 정규화)를
  `dist/vendor/`로 복사한다. **원본은 한 곳뿐이고 관리자 화면은 그걸 그대로 쓴다** — 베껴 두면 로봇·감사·
  관리자 화면이 서로 다른 규칙으로 판단하게 된다. entry-rules는 Node용이라 build.sh가 앞뒤만 감싼다.
- **관리자 버튼이 작동하려면 워크플로가 기본 브랜치에 있어야 한다 (2026-08-03)**: GitHub의
  `workflow_dispatch`는 **워크플로 파일이 기본 브랜치에 있을 때만** 실행을 받아 준다. 작업 브랜치에만
  올려 두면 관리자 화면의 컨펌·수정·되돌리기가 **전부 404로 죽는다**(실제로 그 상태였다).
  관리자 쪽 워크플로를 새로 만들거나 이름을 바꾸면 **반드시 기본 브랜치까지 push할 것.**
  확인: `git ls-tree origin/claude/nice-heisenberg-WESq5 -- .github/workflows/admin-apply.yml`
- **공동작업자는 fine-grained 토큰을 만들 수 없다 (2026-08-03)**: 세밀 권한 토큰은 **자기가 소유한
  저장소에만** 붙는다. 이 저장소 소유자는 선주님이므로 Josehyeon은 **classic 토큰(`repo`+`workflow`)**을
  써야 한다. 둘 다 `Authorization: Bearer`로 동작해 화면 코드는 같다.
- **Cloudflare Pages에 빌드 감시 경로를 꼭 넣을 것 (2026-08-03)**: 수집 로봇이 기본 브랜치에 하루
  10번쯤 커밋하는데, 그대로 두면 그때마다 관리자 화면을 다시 빌드해 **무료 한도(월 500회)를 며칠 만에
  쓴다.** Settings → Build → Build watch paths 에 `_admin/*` + 공용 원본 4개만 넣는다.
  Access는 **Subdomain 칸에 `*`** 를 넣어야 운영 주소까지 잠긴다(빼면 미리보기 주소만 잠긴다).
  일회용 코드 메일은 `noreply@notify.cloudflare.com` — **Gmail 스팸함·프로모션 탭으로 자주 간다.**
- **Cloudflare 없이 화면을 먼저 보는 법**: `node tools/build-admin-preview.mjs` → `_admin/preview.html`.
  데이터를 파일 안에 넣은 한 장짜리 사본이라 인터넷·열쇠·서버 없이 열린다. **화면 코드는 진짜 화면과
  같은 파일을 쓴다**(따로 만들면 서로 달라지므로) — 바깥 통신 0건·저장 버튼 차단을 검사로 확인했다.
- **관리자 열쇠 권한 (설계 의도)**: fine-grained PAT · 이 저장소만 · **Actions 읽기·쓰기 + Contents 읽기전용**.
  파일 쓰기 권한을 일부러 주지 않는다 — 열쇠가 새어도 남이 할 수 있는 일은 '정해진 워크플로를 한 번 돌리는 것'
  뿐이고, 임의의 코드·데이터를 넣을 수 없다. 그래서 조작은 전부 `admin-apply.yml`을 경유한다.
- **CSS 함정 (2026-08-03 관리자 화면에서 실제로 겪음)**: 클래스에 `display`를 주면 브라우저 기본 규칙
  `[hidden]{display:none}`보다 우선순위가 높아져 **숨겨야 할 요소가 안 숨는다.** 숨은 상세 시트가 화면 전체를
  덮어 아무 버튼도 안 눌렸다. `_admin/admin.css`는 `[hidden]{display:none!important}`로 못 박아 뒀다.
- **재발 방지 체계 (2026-07-30 구축 — 앞으로 문제가 생기면 여기부터 볼 것)**: 그동안 모든 검사가
  '사람이 기억해서 실행'에 의존해, 로봇이 매일 데이터를 바꿔도 다음 세션까지 아무도 몰랐다. 이제 4중 장치:
  ① **등록 규칙 한 곳(`verify/entry-rules.cjs`)** — 로봇(auto-register)이 등록 '전'에 거르고, 감사가
     '사후'에 전수 검사한다. 같은 파일을 쓰므로 규칙이 갈라지지 않는다. **새 규칙은 여기에만 추가.**
     (금액 추론·파일명 제목·다운로드 주소·대출·대학원 전용·양식 정보 누락·목록 주소 등)
  ② **수집 워크플로의 데이터 관문** — 수집·자동등록 직후 `test-collector.mjs`+`audit-data.js`를 돌려,
     실패하면 **그 실행이 새로 자동 등록한 공고만 골라 되돌리고**(`collector/revert-auto.mjs` —
     파일 전체를 되돌리면 같은 실행의 원문 발췌 갱신까지 버려지므로 auto:true 신규분만 제거)
     리포트 이슈에 🚨 코멘트를 남긴다. 잘못된 공고가 앱에 하루도 못 나간다.
  ③ **수집기 규칙 테스트(`verify/test-collector.mjs`)** — 주소 정규화·중복 통합 규칙을 인터넷 없이
     검증. 규칙을 되돌리면 즉시 실패한다(실증 완료). 워크플로가 매 실행 돌린다.
  ④ **학교 연속 실패 감지(`collector/health.json`)** — 한 번 실패는 일시 장애(자동 복구), **3회 연속이면
     주소 변경으로 보고 리포트에 🚨**. 시립대가 며칠씩 조용히 빠져 있던 문제의 감시 장치.
  + 감사 범위가 정식 등록에서 **피드 중복·양식 원본 큐 무결성**까지 확대됐다.
- **원문 링크는 '그 공고 하나'로 가야 한다 (2026-07-31 개발자 지적으로 도입 — 규칙은 `collector/detail-url.mjs`)**:
  앱에서 '원문 공고 ↗'를 누르면 그 장학금이 아니라 **학교 장학 공지 목록 전체**가 열리는 공고가
  정식 등록 99건 중 19건·실시간 공고 143건 중 52건 있었다(예: 충남 재능키움 2차 — 경희대 게시).
  **결과(2026-08-01 확정): 실시간 공고 52→12건 · 정식 등록 19→16건.** 경희대는 6건 전부 원문
  도달. 동국대는 27건 도달·17건은 확인에 실패해 표식으로 되돌림(앱이 '게시판 목록 ↗' 정직 표기).
  **되돌린 17건이 중요한 교훈이다** — 4차 실행이 붙였던 주소가 실제로는 그 공고가 아니었는데,
  '제목이 보이면 통과'라는 느슨한 확인 때문에 통과했었다. 확인을 조인 뒤에야 드러났다.
  원인은 브라우저 수집기가 '진짜 상세 주소인가'를 **물음표(?) 유무로만** 판정한 것.
  · 동국대는 상세 주소가 `/article/JANGHAKNOTICE/detail/26765595`(경로형)이라 물음표가 없어
    **멀쩡한 원문 주소를 버리고** 목록 주소 + 제목 표식(`#n-제목`)으로 대체하고 있었다.
  · 경희대는 클릭이 form POST라 주소창이 안 바뀐다 → 목록 행의 **클릭 스크립트 인자에 든 글 번호**로
    `view.do?nttId=…`를 조립해야 원문 주소가 나온다.
  해결: ① 판정 규칙을 `detail-url.mjs` 한 곳으로 모으고 수집기·복구 로봇·감사가 같이 쓴다
  ② 새로 만든 주소는 **세션 없는 새 탭에서 실제로 열어 제목이 맞는지 확인**한 것만 채택한다
  (사용자는 로그인도 리퍼러도 없이 링크를 누르므로 그 조건 그대로 확인해야 한다)
  ③ 끝내 못 찾은 공고만 표식으로 남기고, 앱이 **'게시판 목록 ↗' + 목록에서 찾을 제목**을 정직하게 안내한다.
  · **주소를 유추하지 말 것 — 게시판이 쓰는 폼을 그대로 쓴다 (경희대에서 세 번 틀린 뒤 얻은 규칙)**:
    경희 목록은 `/kor/user/bbs/BMSR00040/list.do?menuNo=200318`인데 원문은
    `/kor/user/contents/view.do?menuNo=200226&boardId=322635`다 — **경로도(bbs→contents)
    파라미터 이름도(nttId→boardId) 메뉴 번호도(200318→200226) 전부 다르다.**
    정답은 목록 화면의 클릭 함수와 폼에 그대로 적혀 있다:
    `view = function(boardId,catId){ form.elements["boardId"].value = boardId; form.submit(); }` +
    `<form action="/kor/user/contents/view.do"> menuNo=200226 · boardId= (빈 칸)`.
    그래서 `detailCandidates`는 **폼의 action과 기본 필드를 그대로 쓰고 빈 칸에만 글 번호를 넣는다.**
    막히면 리포트의 `(클릭 함수)`·`(폼)` 진단부터 볼 것 — 매 실행 남는다.
  · **함정 — 이름만 보고 글 번호로 믿지 말 것**: 동국 상세 화면에는 `name="no" value="dongguk.edu"`
    같은 칸이 있어서, 그대로 조립하면 `…/detail/dongguk.edu`라는 없는 주소가 만들어졌다(1차 실행에서 잡음).
    값이 숫자를 포함하고 점·슬래시가 없어야 글 번호로 본다(`looksLikeId`).
  · **함정 — '제목이 보이면 통과'로 확인하지 말 것**: 게시판 목록에도 그 제목이 있어서 목록을
    원문으로 오인한다(동국 진담거사에 안내 페이지 번호가 붙었다). 다른 공고 제목이 3개 이상
    같이 보이면 목록으로 본다(`looksLikeList`).
  · **함정 — '못 읽음'과 '읽었는데 다른 화면'을 뭉뚱그리지 말 것 (2026-08-01)**: 되돌리기
    안전장치가 '실패 비율'만 보다가, 주소가 틀린 게 확실한 17건까지 '학교가 막았나 보다'며
    두 번이나 그대로 뒀다. 404·시간초과·연결끊김은 **닿지 못한 것**이라 판정할 수 없지만,
    제목 불일치·목록 화면은 **페이지를 멀쩡히 받아 보고 그 공고가 아니었던 것**이라 되돌려야 한다.
    안전장치의 30% 상한은 '못 읽음'만 센다.
  · **함정 — 조립한 주소를 게시판이 실제로 쓰는 주소보다 먼저 시도하지 말 것 (2026-08-01)**:
    동국대에 `…/view?nttId=26765245`를 붙였는데 **33건이 전부 404**였다 — 그런 주소 형태가
    동국대엔 아예 없다(진짜는 경로형 `…/detail/26765595`, 5건 전부 통과). 후보 순서가
    조립형 먼저였던 게 원인. 지금은 경로형·폼 유래가 먼저, 조립형은 마지막 수단이다.
    **실패 사유를 리포트에 남기지 않았으면 이걸 못 찾았다** — 두 번이나 '학교가 막았다'고
    잘못 짚었고, 사유별로 갈라 적자마자 404 33건 대 통과 5건으로 원인이 한눈에 드러났다.
  · **함정 — 같은 학교를 하루에 여러 번 두드리지 말 것**: 7/31에 동국대를 7번 돌렸더니 서버가
    막아 **40분 전 통과했던 주소 17건이 전부 404**가 났다. 재검사가 그걸 '틀린 주소'로 보고
    멀쩡한 링크를 되돌릴 뻔했다. 그래서 되돌리기에 **카나리아(게시판 목록이 아직 열리나)**와
    **실패 비율 30% 상한**을 뒀다 — 둘 중 하나라도 걸리면 아무것도 되돌리지 않는다.
    급히 여러 번 돌려야 하면 저장소 변수 `RESOLVE_ONLY_BOARD`로 고칠 게 남은 학교만 돈다.
  · **함정 — 확인용 브라우저를 후보마다 새로 만들지 말 것**: 학교 서버가 연결을 끊는다
    (ERR_CONNECTION_CLOSED). 하나만 만들어 쿠키만 비워 재사용한다.
  · **목록은 여러 페이지 훑어야 한다**: 앱에 담긴 공고 상당수가 이미 1페이지에서 밀려나 있다.
  · **함정 — curl로 안 나온다고 '학생도 못 본다'고 단정하지 말 것 (2026-08-02 개발자 지적으로 규명)**:
    인하대를 curl로 열었더니 `ssologin.do`로 튕기는 596바이트가 와서 **'로그인 벽'이라고 잘못 보고했다.**
    개발자가 "나는 로그인 안 했는데 잘 들어가진다"고 알려 줘 세션 없는 브라우저로 열어 보니 **정상이었다.**
    인하대 SSO는 자바스크립트 자동제출 폼으로 한 번 갔다 `ac=N`(로그인 불필요)으로 되돌아오는 구조인데,
    **curl은 자바스크립트를 실행하지 않아 그 중간 페이지에서 멈춘 것**이다. 경북대 `<frameset>`도 같은 유형
    (겉은 905바이트 껍데기지만 브라우저는 프레임을 따라 들어가 정상 표시).
    → 규칙: **'로봇이 못 읽는다'와 '학생이 못 본다'는 완전히 다른 판정이다.** curl 결과만으로 후자를
      말하지 말고 반드시 **세션 없는 브라우저로 한 번 열어 보고** 구분해서 적을 것. 이 둘을 섞으면
      멀쩡한 게시판을 '막혔다'고 버리게 된다(동국대 '못 읽음 ≠ 다른 글' 교훈과 같은 계열의 실수).
      로봇용 주소(프레임 안쪽·enc 푼 것)와 학생에게 보여 줄 주소는 달라도 된다.
  검사: `node verify/verify-source-links.js`(브라우저로 카드를 열어 링크·라벨 대조) +
  `verify/test-collector.mjs`의 '공고 원문 주소 판정' 절(물음표 판정으로 되돌아가면 실패).
- **공고 주소 정규화 (2026-07-30 이슈 #75로 도입)**: 일부 게시판(서울시립대 korNotice)은 상세 주소에
  목록에서의 정렬 순번 `sort=`가 붙어, 글이 한 칸만 밀려도 주소가 달라진다. 이 때문에 수집기가 어제
  본 글을 매일 '신규'로 다시 담아 시립대 피드 40건 중 실제 공고는 13건뿐이었고, 학교당 40건 상한을
  중복이 채워 **진짜 새 공고를 밀어냈다**. 해결: `collector/url-key.mjs`의 `urlKey()`로 휘발성
  파라미터(sort·pageIndex·검색어·권한 등)를 떼어낸 값을 seen 열쇠·중복 판정에 쓴다(보여주는 url은
  원본 유지 — 링크가 열려야 하므로). 수집기 2종에 적용 + 기존 notices/seen도 소급 정리(원칙 7).
- **자동 등록이 지어내던 값 (2026-07-30 전수 감사에서 발견)**: auto-register가 등록하는 모든 공고에
  `amountValue: 500000`을 넣고 있었다 — 확인한 적 없는 금액이라 원칙 8-1 위반이고, 홈 화면 '예상 최대
  수혜액' 합계가 부풀려져 있었다. 이제 0으로 넣는다. 사람이 등록한 항목에도 '무상 기숙사 제공'에
  300만원처럼 추론해 넣은 값이 7건 있어 전부 0으로 소급. **감사 도구가 이 유형을 오류로 잡는다**
  (금액 문구에 숫자가 없는데 amountValue가 있으면 실패).
- **마감 없는 공고의 수명 (2026-07-30)**: 마감을 확정 못 한 공고는 dday가 '기한 원문 확인'이라
  목록에서 영영 안 사라졌다(44건 방치). 이제 `listedAt`(등록일)을 남기고 앱이 **등록 후 60일**이
  지나면 감춘다(app.js `notStale`, 실시간 공고 60일 규칙과 동일 기준). 자동 등록분은 로봇이 자동으로
  listedAt을 넣는다. 마감이 있는 공고는 기존 '마감+30일' 규칙 그대로.
- **양식 원본 유실 (2026-07-30 수정)**: deepfetch가 실행할 때마다 `collector/extracted/`의 form-* 를
  전부 지우고 1번부터 다시 채웠다 → '스키마화 대기'로 큐에 남아 있던 공고의 원본이 다음 수집 때
  사라져 다음 세션이 양식을 만들 수 없었다(도레이·염곡 원본이 이렇게 유실). 이제 파일명에 공고별
  표식을 넣어(`form-<표식>-<n>.<확장자>`) **이번에 다시 받는 공고의 파일만** 갈아끼운다.
- **검증 드라이버에 공고 id를 박지 말 것 (2026-07-30 재발)**: 마감된 공고는 신청 버튼이 잠겨 클릭이
  실패한다. verify-new-forms.js가 reg-hufs-samil·reg-mj-gosi를 하드코딩해 마감과 함께 깨졌다 —
  이제 `pickTarget(formId)`로 **같은 양식을 쓰면서 마감 안 지난 접수분**을 그때그때 고르고, 하나도
  없으면 건너뛴다고 알린다. verify-forms-data.js는 이미 같은 방식(2026-07-15).
- **학교 서버 일시 장애 대응 (2026-07-30 이슈 #75)**: 브라우저 수집기는 `page.goto` 시간초과 1회로
  그 후보 주소를 버렸고, 시립대는 후보 2개가 같은 호스트라 학교 전체가 통째로 누락됐다(7/16·7/20·7/30
  발생 — 실행 자체는 success라 조용히 지나감). 해결: ① 열기 재시도 3단계(30초 → 45초 → 45초+commit
  완화) ② 후보 전부 실패한 학교는 다른 학교를 다 돈 뒤 **한 번 더 시도**(단 절반 넘게 실패하면 로봇 쪽
  장애로 보고 생략) ③ 리포트 하단에 `⚠️ 접속 실패한 학교` 요약. 실패해도 12시간 뒤 실행에서 자동 복구.

## 정식 등록 이력
- 2026-07-31 (10차 세션, 컨펌 대기 19건 전수 검수 — 선조치후보고): 로봇이 '애매하다'며 멈춰 둔 19건을
  원문 근거로 판정 → **정식 등록 101 → 99건**(중복 2건 정리). ① **전국 승격 4건**: 여러 대학이 함께
  게시한 외부 재단 사업은 학교 한정으로 두면 다른 학교 학생이 못 본다 — 서울인재대학장학금(마감 8/10,
  외대 게시 제목 근거)·화성시 코나아이 소상공인(8/11)·사랑의열매 사랑나눔·5·18희망장학생.
  (호반·정읍 선례와 같은 판단) ② **중복 제거 2건**: 같은 사업이 두 건 등록돼 전국 카드가 겹치던 것
  (화성시 경희 게시분·사랑나눔 광운 게시분) — auto-register-config blockIds에 넣어 재등록도 막음.
  ③ **마감 확정 1건**: 재능키움 2026-07-27(연세 게시 원문 "신청기간 7.13~7.27 16:00").
  ④ **제외 12건**: 마감 경과(가송 경희 7/27·곰두리 경희 7/19·7/21·큰사랑 연세 7/22·재능키움 외대 7/27),
  2027년 이후 유학 사업(DAAD 2건), 오늘 마감(슬로바키아 2건 — 7/31), 신청 절차 없는 안내(포스코비전
  프로그램 안내), 이미 등록분과 동일한 재게시.
- 2026-07-30 (이슈 #75 후속, 전수 감사): 개발자 지시("등록된 모든 공고 전수 분석·문제 해결 + 수집분 전수 컨펌")
  → **정식 등록 89 → 93건 · 양식 29종**. ① **문제 10종 발견·수정**(지어낸 금액 20건, 자동 등록 이중 등록
  1건, 첨부 PNG가 장학금으로 등록 1건, 학자금대출 2건·대학원 전용 1건 혼입, 양식 정보 누락 9건, 마감 없는
  공고 영구 잔존 44건, 게시판 목록 주소 2건, 원문 미확보 발췌, 양식 원본 유실, 검증 드라이버 스테일).
  ② **신규 등록 9건**: 외대 남가주동문장학회·가송재단·양천장학회(원문 전문 확보 — 발췌 그대로), 광운 가송재단,
  중앙 일취월장·복지장학금 서울/다빈치, 동국 보건장학회 다문화(bogun-multi-apply 재사용)·롯데장학관
  (lotte-dorm-apply 재사용). ③ **양식 1종 신규**: yeonjae-consent-apply(연재장학재단 개인정보 동의서 —
  HWP 원문 그대로). ④ 제외(정직 원칙): T/AS 강의보조 6건(대학원·고용), 장학조교 채용 2건, 2027 DAAD·
  슬로바키아, 김해시 수기 공모전, 자생한방 수련의, 삼송(~7/29 마감), 시립대 활동도우미(~7/29 마감),
  상명 첨부 파일명 파편 8건, 이미 등록된 사업의 타교 게시분 다수. ⑤ sw CACHE **v11→v12**(app.js 변경).
- 2026-07-15 (8차 세션): 개발자 지시 3건(앱 서핑 개선·개발자 업무 정리·전수 컨펌+자동 등록 검토) 처리 →
  **정식 등록 73건 · 양식 19종**. ① 피드 191건 전수 컨펌으로 **11건 신규 등록**: UOS 빅데이터 성과형(자격증),
  SKKU 성적우수·가송재단, 외대 씨앗·동문홍보대사, 포스코비전(전국), 대청교 멘토(전국), 동국 진담거사
  지역미래불자육성·부남, 경희 충남 재능키움 + 정읍시민 **전국 승격·통합**(공고 PDF 원문 확인: 부/모 정읍 거주
  국내 대학 재학생 전체, 주민센터 접수 — 호반 선례). 갱신 2건: 외대 국가근로 7월 재공고(~7/16)·광운 곰두리
  formId 연결(소급). ② deep-fetch로 **양식 2종 스키마화**: uos-bigdata-cert-apply·gasong-apply.
  ③ 제외(선조치후보고): DAAD 2027-28(미래 사업), 삼성장학생 연수(기존 장학생 대상 프로그램), 홍익 T/AS
  6건(대학원 강의보조·고용성), 한양 장학조교(고용), 점프업(자동 선발), 국가장학금 안내(상시 제도),
  미산 러닝(마감), 대청교 출근부(행정), SNU 채용설명회(행사), 상명 청년창업농 포스터(파편 — 상명 게시판이
  동적이라 공고 본문 미확보, 2026-2 재공고 원문 확인 필요).
- 2026-07-05: 개발자가 채팅에서 "수집된 40건 모두 정식 등록" 지시 → 40건 중 **진짜 개별 공고 17건** 등록
  (성균관 6·광운 7·명지 1·홍익 2·상명 1 + 기존 AI멘토 1 = 18건).
  나머지 22건은 상시 안내 페이지·메뉴·중복이라 장학금으로 등록하면 정직 원칙에 어긋나 피드에만 유지 — 선조치후보고로 보고됨.
- 양식 엔진 2호: 조병두장학금 신청서(붙임1 HWP 원본 구조 그대로). 자소서·추천서·동의서 원본은 상세 화면에서 다운로드 링크 제공.
- 2026-07-05 (3차 세션): 개발자 지시("1번의 가정 하에 양식 등록 진행")로 양식 5종 추가 등록 —
  삼일장학회 지원신청서, 보건장학회 학업지원/다문화가정 지원신청서, 산학협동재단 외국인 근로자 자녀
  신청서(서식Ⅰ+자소서 서식Ⅱ), 명지대 고시장학금 신청서(제1호 서식). 총 **양식 7종**.
  홍익 미래이음은 첨부 전수 확인 결과 채울 신청서 양식이 없어 noForm 사유 기재(동의서 HWP는 다운로드 제공).
- 2026-07-06 (4차 세션): 리포트 #8·#9 개발자 컨펌("밑 모든 내용을 반영")으로 4건 추가 등록 —
  삼일장학회 외대 접수분(별지1 원본이 성균관분과 동일 확인 → samil-apply 재사용), 외대 하계 국가근로
  추가모집(신규 양식 + **applyEmail 첫 연결 — 이메일 원클릭 실증**), 윤세영 스칼라십(전국 1학년, noForm),
  세종이도 디딤돌(세종시 거주자, 신규 양식). 안내성 공고(외대 메뉴·가이드 6건, 대청교 출근부, AI멘토
  외대 재게시=기존 kosaf-ai-mentor와 동일 사업)는 정직 원칙에 따라 피드에만 유지 — 선조치후보고.
  **정식 등록 21건 · 양식 9종.**
- 2026-07-06 (4차 세션, 전수 대조): 개발자 질문("유효기간 내 전체 공고인지 확인, 미싱 추가")으로
  피드 63건 전수 대조 → 5건 추가 등록: 윤성원(외대 부산 출신, 신규 양식), 불광장학회(applyEmail 2호),
  경기 RISE G7(명지), 화도·동해(광운, 원문 확인), 산학 외국인 자녀 동국 접수분(재단 공통 양식 연결).
  호반 이노베이션은 SKKU 게시 원문으로 전국·이메일 접수(3호) 확인 → 광운 한정을 전국으로 승격(소급 원칙).
  7/5 피드 정리 때 유실된 실공고 4건 복원. 제외(정직 원칙): 점프업(신청 절차 없음 — 자동 선발),
  일본어학연수 2027(사전 계획), 전북 참가팀(행사), 마감 경과분(미산 러닝 등), AI멘토 재게시(중복).
  **정식 등록 26건 · 양식 10종 · 이메일 접수 3건.**
- 2026-07-08 (6차 세션): 개발자 지시("현재 수집된 장학 공고 모두 컨펌")로 피드 124건 전수 분류(`verify/list-unregistered.js`) →
  실제 신규 실공고는 **1건뿐**(나머지는 이미 등록·메뉴·대출·행사·중복). **중앙대 보건장학회 다문화가정** 등록
  (bogun-multi-apply 재사용) → **정식 등록 48건**. 이전 이력 사이(브랜치)에서 쌍용곰두리(gomduri-apply, 13번째 양식)도 등록됨.
- 2026-07-14 (7차 세션): 개발자 지시("모두 진행")로 3건 처리 → **정식 등록 63건 · 양식 17종**.
  ① **양식 4종 스키마화**: 7/9 예약한 deep-fetch가 HWP 원본 확보 성공 → sanhak-didim-apply·namjung-apply·hufs-gosi-apply·uos-module-apply
  스키마화하고 해당 등록(산학디딤돌·남정·외대고시·시립모듈)의 noForm→formId 승격(앱 내 작성으로 전환). ② **7/10~14 신규 수집 5건 등록**:
  상명 우수인재장학·시립대 발전기금 기여장학·홍익 정읍시민장학재단·중앙대 교내장학 묶음·성균관 큰사랑이한애(~7/16). 제외: 광운 DAAD
  독일정부초청(2027-2028 유학·미래계획). ③ **채널 라벨 정확화**: submitChannelLabel에 '📎 원본 양식 다운로드형' 추가 —
  noForm+첨부양식 6건이 '복사·붙여넣기'로 잘못 안내되던 것 교정(sw.js v7→v8). 채널 분포: 앱 양식작성 23·다운로드형 6·포털 29·이메일 3·재단 2.
  ④ 검증 드라이버 소급 수정: verify-registered.js의 스테일 단언 2건(호반=전국 승격 반영, 조병두 클릭 explore로 범위한정) 고침.
- 2026-07-08 (6차 세션 후속): 개발자 지시("main 반영 + 어제 오늘 추가분 전부 컨펌")로 봇의 7/8~7/9 수집분(143건) 병합 →
  신규 실공고 **10건 등록 → 정식 등록 58건**: 쌍용곰두리 3개교(중앙·동국·외대, gomduri-apply 재사용), 성균관 롯데장학관 입주생·
  산학협동재단 디딤돌(1학년), 외대 남정장학재단·유흥수(융합일본지역학부)·고시장학금·면학장학금, 시립대 차세대통신 성적우수.
  HWP 신청서 4건(남정·산학디딤돌·외대고시·시립모듈)은 첨부 원본 링크+noForm으로 등록하고 deep-fetch 트리거(run-deepfetch.txt)로
  원본 확보 예약 — 다음 세션에서 스키마화. 제외(선조치후보고): UOS원정대(기등록)·빅데이터 근로(마감 6/25)·자생한방 수련의
  (졸업후 대상)·한양 장학조교 채용(고용)·세종이도 디딤돌 대출(융자)·각종 지급안내/메뉴. **main·default·작업 3브랜치 동일 배포.**
- 2026-07-07 (5차 세션): 경희대 게시판(news.khu.ac.kr, 클릭형) 연결 완료 + 개발자 컨펌("7/7 리포트 전체 등록")으로
  **19건 추가 등록 → 정식 등록 45건**. 동국대 교내장학 10종(인재육성·교내전체·종단추천·전문자격·복지1/2·보훈·교육보호),
  경희대 4종(의암 손병희·신격호 롯데장학관·양천장학회·삼일 경희 접수분), 중앙대 3종(삼일·다빈치 국가근로 2),
  숙명 국가유공자자녀·새터민 장학, 서울시립대 UOS커리어원정대. 중앙대·동국대 게시판도 이날 첫 수집 성공.
  **누락 원인 해결(개발자 지적)**: 게시판당 수집 상한 10→25, 학교당 15→30 — 상단 고정(공지) 공고가 10건 넘는
  게시판(경희 등)의 실공고가 잘리던 문제. 제외(정직 원칙): KOSAF 공통 학자금대출(학교별 중복)·점프업(자동 선발,
  신청 절차 없음)·마감 경과분·게시판 메뉴. 양천장학회(경희)·숙명 유공자/탈북 신청서 hwp를 확보해
  **양식 스키마화 완료 → 양식 12종**(hufs-workstudy·sejong-didim·yunsungwon·yangcheon·sm-yugong 신규 5종 추가).
- 2026-07-05 (3차 세션): 개발자 지시로 **가짜 공지 전량 삭제** — 임의 세부의 교외 샘플 3종(서울희망·관정·미래에셋)과
  가상 교내 템플릿 삭제. 국가장학금 등 실존 상시 제도 6종은 임의 마감일 제거 후 '상시 제도'로 유지(선조치후보고).

## 전체 현황 (2026-07-07 기준 — 3단 정리)

### 1. 완성된 것 (배포되어 작동 중)
- **앱 배포**: PWA(홈 화면 설치형), https://seonju5543-web.github.io/hanggonggan/ — main push 시 자동 배포
- **온보딩**: 학교 자동추천(별칭 "외대"·"서울" 지원) → 캠퍼스(이원화 13개교) → 계열 → 학과 자동추천(학교 반영) → 성적·소득 → 특별자격(체크 표시) → 공통 서류정보(학번·연락처·계좌)
- **매칭 엔진**: 12개 신호 자격 진단(학교한정 포함) + 4단계 판정 + 요건별 사유 표시 + 적합도 점수(0~99%) 정렬
- **실시간 공고 피드**: 수집 로봇이 매일 게시판 수집(일반 07:41/11:41 + 브라우저형 08:07/12:07 KST) → 공고 상세 방문 → 첨부양식·마감단서 채집 → 앱 '장학금' 탭에 자동 표시 (실공고 피드 100+건)
- **정식 등록 166건**(2026-08-09 실측): 실공고가 매칭 엔진에 들어가 해당 학교 재학생에게 카드로 노출 — 마감 D-day, 자격 진단, 원본 첨부 다운로드, 신청 준비 플로우 전부 작동 (피드와 자동 중복 제거). 학교별: 성균관·광운·외대·경희·동국·중앙·명지·홍익·상명·숙명·시립·세종연구원 등
- **양식 엔진**: 실제 공고 양식과 동일 구조 문서 생성 — 질문형 입력(프로필 자동 채움) → 체크박스·시간표·서약문까지 재현 → 수정 → .doc 저장/인쇄/공유 (**등록 12종**: AI멘토·조병두·삼일·보건 2종·산학·명지 고시·외대 국가근로·세종이도 디딤돌·윤성원·양천·숙명 유공자)
- **진척도 4단계 트래커**: 신청 준비 완료 → 공식 제출(사용자 기록) → 심사(제출+마감 경과 시 자동 표시) → 선정 발표(사용자 기록 — 선정 축하·미선정 위로). 신청 내역 요약에 심사 중·선정 건수·선정 금액 집계
- **접수 채널 표시 + 바로 제출 버튼**: 채널 4종(이메일/한국장학재단/양식 제출/포털 입력) 상세에 명시, '내용 복사하고 제출처 열기' 원버튼. **이메일 접수 3건**(외대 국가근로·불광장학회·호반)은 메일 자동완성 연결됨
- **알림 시스템 (2026-07-31)**: 최초 1회 동의 → 알림 5종(새 맞춤 공고·마감 하루 전/당일·제출 리마인드·
  우리 학교 새 공고·결과 기록) 자동 발송. 홈 종 아이콘 배지 + 앱 내 알림함(동의 안 해도 쌓임) +
  MY 항목별 켜기/끄기. 발송 서버가 없어 로컬 알림으로 동작한다는 점을 화면에 그대로 안내
- **서류 보관함**: 증명서류 8슬롯 기기 내 저장, 신청 시 자동 첨부 체크리스트, 재사용
- **신청 준비 플로우**: 단건/일괄, 자유서식 도우미(자소서·계획서·사유서), 제출처별 단계 가이드, 복사·파일 공유, 접수메일 자동완성(mailto)
- **컨펌 파이프라인**: 리포트 이슈 자동 생성(개발자 담당 지정) → 채팅 컨펌 → 등록
- **인프라**: 정찰 도구(probe), 원격 열람(fetch-page), 자동발송 서버 코드(server/), 클릭형 게시판 수집기
- **문서**: 제안서 2종(proposals/ — 장학팀·G-RISE **PDF**, 요청 사항 명시형), BUSINESS.md, 본 인수인계 문서

### 2. 완성해야 하는 것 (개발자 액션 → 완료 시 결과) — 다음 채팅 필독
| 개발자가 할 일 | 하고 나면 생기는 결과 | 상태 |
|---|---|---|
| ① **Resend 가입 → API 키 발급** (server/README.md, 10분) | '보내기'조차 없는 **완전 자동 이메일 접수** 활성화 (이미 이메일 접수 공고 3건 대기 중) | 대기 |
| ② **미확보 게시판 주소 제공** — 연세신촌·연세미래·서강·서울대·가천(브라우저로도 실패) + 고려세종·한양ERICA·건국글로컬·동국WISE(주소 미확보) | 수집망 23/23 완성 · 해당 학교 학생에게 실공고 노출 | 대기 |
| ③ **제안서 2종 검토·제출** (proposals/ PDF, 인적사항만 확인) | 장학팀 파일럿(진짜 원클릭의 열쇠)·G-RISE 지원 절차 개시 | 대기 |
| ④ **스토어 결정** — 웹은 지금 배포 중이라 즉시 공유 가능 / 스토어면 Google Play $25 등록 → 이후 Apple $99/년 | 스토어 배포 — TWA/Capacitor 패키징은 Claude가 수행 | 대기 |
| ⑤ **커리어넷 학과정보 API 키** — 2026-08-02 개발자가 **신청 완료**, 관리자 승인 대기 중 | 학과 자동추천이 '그 학교에 실제로 있는 학과'만 뜬다 (아래 절 참조) | **승인 대기** |
| ⑥ ~~무료 푸시 서버 올리기~~ | ~~앱을 안 켜도 폰에 알림 도착~~ | ✅ **2026-08-06 완료** — 실제 폰 2대 도착 실증 (18차 세션 절) |
| ~~⑦ 관리자 화면 올리기~~ | 학생인 척 접속하지 않고 등록 공고 전부를 한 화면에서 보고 컨펌 | ✅ **2026-08-09 완료** — Access 잠금 + GitHub 열쇠까지. 화면에 166건 표시·작동 확인 |
| ⑧ **회원가입·로그인 (Supabase)** — 출시 전에 붙이기로 결정(2026-08-07). 개발자 몫은 Supabase 가입·소셜 앱 등록뿐 | 기기를 바꿔도 프로필·신청내역이 이어짐 | **나중에** — 아래 절 참조 |

### ⑧ 회원가입·로그인 — 설계 확정, 착수는 나중에 (2026-08-07 개발자와 정리)

> **⚠️ 지금 하는 일이 아니다.** 개발자 지시: *"기존에 하던 것 먼저 하고 나중에 추가해도 되는 기능"*.
> 이 절은 **그때 다시 논의하지 않으려고** 남기는 결론이다. 재설계·재조사 금지.

- **개발자 결정 (2026-08-07)**: **출시 전에** 아이디/비밀번호 + 소셜 로그인을 붙인다.
  목적은 **(가) 기기를 바꿔도 프로필·신청내역 이어쓰기** — 신원 확인이나 이용자 집계가 아니다.
  · 그래서 "이메일만 저장하는 얇은 로그인"은 의미가 없다(이어쓸 데이터가 서버에 없으므로).
  · 백업 파일 내보내기/가져오기(서버 0·1~2일)를 대안으로 제시했으나 **개발자가 계정 방식을 선택**했다.
- **DB는 Supabase로 확정** (Cloudflare D1 직접 구현 아님). 이유:
  ⓐ 인증(비번 해싱·이메일 인증·비번 재설정·소셜·세션 회전)은 **직접 만들면 안 되는 것**이고
     Supabase에 이미 다 들어 있다 — 직접 구현 2~3주 → **3~5일**.
  ⓑ 앱이 Supabase에 직접 붙는 게 정상 사용법이라 **계정 서버 Worker가 아예 필요 없다**
     → Cloudflare Workers 유료 $5/월도 **안 든다**(유료가 필요했던 이유는 DB가 아니라
     비밀번호 해싱이 무료 등급의 **계산 시간 10ms**를 넘기기 때문이었다).
  대가: 살림이 하나 늘고(개인정보가 또 다른 회사에 — **국외 이전 고지 필요**), 네이버는 미지원.
- **구조 (이 세 줄이 전부)**:
  ① **회원 데이터는 git을 한 번도 안 탄다** — 폰 ↔ Supabase 직행. GitHub·Cloudflare 안 거침.
  ② **git과 만나는 건 표 설계도(`supabase/migrations/*.sql`) 하나뿐.** Cloudflare Workers와 달리
     **Supabase는 자동 배포가 없으므로 GitHub Actions로 적용하는 길을 깔아야 한다**(반나절).
     안 깔면 대시보드에서 손으로 고치게 되고 **"언제 왜 바꿨는지"가 안 남는다**.
  ③ **🔴 2026-08-24 개발자 지시로 이 항목은 뒤집혔다** — 예전 방침은 "매칭은 폰이 하고 성적·소득은
     서버로 안 보낸다"였으나, **기능 우선 · 보안은 전면 외주** 방침으로 전환했다. 프로필(성적·소득
     구간·전공 등)을 회원 DB에 축적하고, 그 데이터가 B2B 타깃 광고의 근거가 된다(`BUSINESS.md` 2·7절).
     보안·암호화는 자체 설계하지 않고 전문 업체에 위탁한다.
     · 그대로 유지되는 것: DB는 공고를 **복사하지 않고 이름표(공고 id)만** 갖는다 → 공고가 바뀌어도
       따라 고칠 게 없다. 서류 보관함(증명서 실물 스캔)은 기본 기기 저장 + '서버 백업' 선택 스위치.
     · 앱 코드는 아직 옛 구조다(푸시 서버는 폰 주소·학교·캠퍼스 3개만 보관). 착수 시점에 바뀐다.
- **🔴 선행 필수 — 약관·개인정보처리방침**: 앱에 **지금 0건**(index.html·app.js 검색 결과 0).
  회원을 받는 순간 법적 필수다. **로그인 코드보다 먼저** 만든다(초안은 Claude가 쓴다).
  · 프로필에 **민감정보가 들어 있다**: `disabled`(장애) · `basicLiving`(기초수급) · `merit`(국가유공자)
    + 성적·소득분위·**계좌번호** (`data.js` FLAG_LABELS). 민감정보는 **별도 동의**가 필요하다.
  · **서류 보관함은 정부 발급 증명서 실물 스캔**(IndexedDB `handaejang-docs`, 파일 Blob 8칸).
    → **기본은 폰에만 두고, "서류도 백업" 스위치를 선택으로** 주는 것을 권고했다(구현 난이도 동일).
- **도메인은 로그인의 전제조건이 아니다** — Supabase는 쿠키가 아니라 토큰으로 로그인을 유지해
  주소가 달라도 된다. (직접 구현안에서는 전제조건이었다. 그 설명은 폐기.)
  도메인은 여전히 사고 싶지만 이유가 다르다: 앱 주소 안정 · 소셜 콜백 · **푸시 구독이 주소에 묶여
  있어 나중에 옮기면 전부 무효**가 되는 문제(사용자 0명인 지금이 가장 싸다).
- **로그인은 반드시 '선택'으로** — 지금 앱의 강점이 "온보딩 5단계면 바로 내 장학금이 보인다"인데
  가입을 앞세우면 그 자리에서 이탈한다. **게스트로 다 쓰다가 '이어쓰기'를 원할 때 권하기.**
- **애플 로그인 강제 규정** — 구글·카카오를 넣은 앱은 iOS 앱스토어 심사(4.8)에서 **애플 로그인도
  요구**한다. 웹으로만 낼 거면 무관하지만 스토어 계획이 있으면 자리를 비워 둘 것.
- **⚠️ 아직 확인 안 한 것 2가지 (착수 전에 공식 문서로 볼 것)**:
  ⓐ Supabase가 **카카오 로그인을 기본 지원**하는지 ⓑ 무료 등급의 정확한 한도, 특히
  **7일간 활동이 없으면 프로젝트가 일시정지**된다는 규칙(사용자가 없는 초기에 위험).
  이 둘은 결론을 뒤집을 수 있는 항목이라 **Supabase 확정 전에 확인**해야 한다.
- **작업 순서 (착수하게 되면)**: ① 약관·개인정보처리방침 → ② Supabase 프로젝트 + 표 설계 +
  행 단위 접근 규칙(자기 행만) → ③ 앱 로그인·가입 화면(게스트 유지) → ④ 소셜(구글→카카오) →
  ⑤ 프로필·신청내역 동기화 → ⑥ 서류 동기화(선택 스위치).
  개발자 몫은 **Supabase 가입 · 구글/카카오 개발자 앱 등록**뿐이다.

### ⑤ 학과 자동추천 — 2026-08-06 구축 완료 (커리어넷 API 연동)
> 아래 2026-08-02 절은 키를 받기 전의 계획이다. 실제 구축 내용은 이 절을 볼 것.

- **키 승인됨** (만료 2028-08-03). GitHub Secret `CAREERNET_API_KEY`에 등록됨.
  ⚠️ 개발자가 처음 등록할 때 **키 첫 글자가 잘린 채 복사**돼 '인증키 실패'가 났었다 —
  키 문제로 보이면 첫/끝 글자 누락부터 의심할 것.
- **API 실측 사실 (재조사 금지)**:
  · 상세(MAJOR_VIEW)에도 **`gubun=univ_list`가 필수** — 빼면 -7 '결과 없음'이 온다
    (매뉴얼 예시엔 없어서 처음에 이것 때문에 헤맸다).
  · 상세 응답 `<university>`에 개설대학이 온다: schoolName·campus_nm·majorName·area.
    **campus_nm은 이름이 아니라 '제1/제2캠퍼스' 번호다** — 경희대 일본어학과는
    "경희대학교 | 제2캠퍼스 | 경기도"로 온다(= 국제캠퍼스).
  · 목록은 501건, perPage=1000이면 한 페이지에 다 온다. 한글 파라미터는 URL 인코딩 필수.
  · **핵심 검증 통과**: 일어일문학과 개설대학 57곳에 경희대 없음(정답 — 경희대엔 일본어학과만
    있고 그건 '일본학과' seq 434에 들어 있다). 개발자가 지적한 버그가 데이터로 교정된다.
- **만든 것**: `collector/majors.mjs`(수확기 — 학교명 정규화·분교 매핑·20% 실패 시 저장 거부) →
  `data/majors.json` 발행 → `app.js loadMajors()`가 읽어 MAJORS_BY_SCHOOL에 합침
  (손 검수한 외대 목록은 덮어쓰지 않음). 갱신은 `.github/workflows/refresh-majors.yml`
  (수동 실행 전용 — 학과 개편은 학기 단위라 예약 없음).
- **캠퍼스 단위 분리는 하지 않았다**: API가 캠퍼스를 번호로만 줘서 앱의 캠퍼스 이름과 잇는
  매핑이 학교마다 필요하다. 학교 단위 목록만으로도 지적된 오류(없는 학과가 뜸)는 해결되므로
  이원화 캠퍼스는 학교로 합쳤다. 분교는 별개 학교이므로 BRANCH_MAP으로 정확히 갈랐다.

### (키 받기 전 계획) 학과 자동추천 — 키가 오면 바로 할 일 (2026-08-02 준비 완료)
**개발자 지적**: "경희대 국제캠을 고르고 '일'을 치면 일어일문학과가 뜨는데 경희대엔 일본어학과만 있다."

- **그릇은 이미 만들어 뒀다(배포됨)**: `app.js majorSuggestions()`가 `MAJORS_BY_SCHOOL['학교 캠퍼스']`
  → `MAJORS_BY_SCHOOL['학교']` → `MAJORS_COMMON` 순으로 보고, **학교 목록이 있으면 그 목록만 쓴다**
  (예전엔 `.concat(MAJORS_COMMON)`이라 외대에서도 없는 '일어일문학과'가 떴다 — 이게 진짜 버그였다).
  그래서 **데이터만 채우면 앱 코드 변경 없이 작동한다.**
- **지금 데이터**: `MAJORS_BY_SCHOOL`에 한국외대 하나뿐. 나머지 76곳은 전국 공통 목록이 힌트로 뜬다.
- **키가 오면**: 개발자가 GitHub Secret `CAREERNET_API_KEY`에 넣는다(채팅에 붙여넣지 않기로 함).
  그 뒤 `collector/majors.mjs`를 만들어 `data/majors.json`으로 발행 → 학기마다 자동 갱신.
- **API 명세 (2026-08-02 확인)**:
  `https://www.career.go.kr/cnet/openapi/getOpenApi?apiKey=<키>&svcType=api&svcCode=MAJOR&contentType=json&gubun=univ_list`
  · `svcCode`: 목록 `MAJOR` / 상세 `MAJOR_VIEW` · `gubun`: 학교 분류 · `univSe`: 대학 분류
  · 그 밖에 `subject`(계열) `thisPage` `perPage` `searchTitle` `majorSeq`(상세용).
- **⚠️ 먼저 확인할 것 — 이게 안 되면 이 경로는 실패다**: 이 API는 **학과 중심**이라 목록(`MAJOR`)은
  전국 표준 학과명이다. **'어느 대학에 그 학과가 있는가'는 상세(`MAJOR_VIEW`)의 개설대학 정보를
  훑어 뒤집어야** 학교별 목록이 나온다. **캠퍼스 구분(경희 서울/국제)까지 나오는지는 미확인.**
  키 받으면 **가장 먼저 이걸 실측하고 개발자에게 솔직히 보고할 것** — 안 되면 학교 사이트 수집으로 전환.
- **막힌 경로(다시 시도하지 말 것)**: 대학알리미(academyinfo.go.kr)는 홈이 0KB 껍데기(JS 렌더),
  내부 API 후보 3종 전부 404, 브라우저 도구는 정책상 이 사이트 읽기 차단. 경희대 사이트 직접 수집도
  학과명 0개(JS 렌더). **키 없이 되는 길은 확인된 범위에서 없다.**
- **원칙**: 학과명을 기억으로 지어내지 않는다(원칙 6). 틀린 학과명은 지금 상태보다 나쁘다.
  데이터가 없는 학교는 전국 공통 목록을 힌트로 두고, 학생이 직접 입력한 값은 그대로 저장된다(매칭 정상).

### 자격 요건 채우기 — 2026-08-03 무료 경로로 구축 완료 (54 → 81건)
> 아래 2026-08-02 절은 착수 전 계획이다. 실제로 한 일과 남은 것은 이 절을 볼 것.

**개발자 지적**: "앞으로 너가 못 쓴 몇십 건의 공고를 다 하나하나 고치라고 할 수는 없잖아."
**개발자 결정**: API는 쓰지 않는다(비용 0). 규칙만 개선한다.

- **한 건씩 고치기를 끝낸 도구 — `node verify/eligibility-report.mjs`**: 138건을 한 번에 채점해
  확보/미확보를 **원인별**(원문 잘림·자격 제목 없음·원문 미확보)로 나누고, 자격 자리에 들어간
  엉뚱한 줄을 유형별로 센다. **규칙을 고치기 전후로 돌려 숫자를 비교하는 것이 이 작업의 방식이다.**
  `--list`(미확보 전부) `--bad`(의심 줄 전부). 실패를 만들지 않는다(빌드 차단은 audit-data의 몫).
- **진짜 원인은 규칙이 아니라 원문이었다**. 실패 84건을 세어 보니 제목 낱말을 늘려 늘릴 수 있는 건
  4건뿐이었고, **32건은 원문이 5,000자에서 잘려** 자격 절이 날아갔으며(잘린 12건을 실제로 다시 받아
  보니 5건이 자격 절을 5,302~6,213자 지점에 두고 있었다), **17건은 원문이 통째로 없었다**.
- **`collector/deepfetch.mjs` 두 곳을 고쳤다 — 여기를 되돌리면 자격이 다시 사라진다**:
  ① 컷 5,000 → **15,000자**(`LIMIT`). ② **원문 파일을 통째로 새로 만들지 않는다** — 등록 공고가
  참조하는 원문은 수집 목록에서 밀려나도 보존하고, 없거나 잘린 것은 `--fill`(증분)로 보충한다.
  예전엔 실시간 공고가 60일 뒤 목록에서 지워질 때 **등록 장학금의 원문도 같이 사라져**,
  발췌기가 이미 뽑아 둔 자격까지 지웠다. 그게 '원문 미확보 17건'의 정체다.
- **`--fill`은 매일 수집 워크플로 2종에 들어가 있다**(발췌 갱신 **바로 앞**). 증분이라 평소엔 몇 건뿐.
  안전장치 3개: 한 실행 상한 `FILL_CAP=120` · 연속 실패 3회면 물러섬(`GIVE_UP_AFTER`, 15건이 매번
  20초씩 기다려 5분을 먹고 있었다) · 지금 한도로 이미 잘린 것은 다시 안 받음.
- **`cut`은 true/false를 항상 남긴다** — 잘렸을 때만 표시했더니 잘리지 않은 긴 원문이 '옛 5,000자 컷'으로
  오인돼 **매 실행 다시 받는 무한 반복**이 됐다(두 번째 실행이 또 120건을 받아서 발견).
- **`collector/notice-source.mjs` 신설** — '등록 공고 ↔ 저장된 원문'을 잇는 규칙 한 곳. 발췌기·수집기·
  재채점 도구가 같은 함수를 쓴다. 갈라지면 "발췌기는 찾았는데 수집기는 못 찾는" 일이 생긴다.
- **발췌기(`extract-excerpts.mjs`)**: ⓐ **원문이 없으면 아무것도 지우지 않는다**("없다"가 아니라 "모른다").
  ⓑ 절 머리글 기호에 **■□▣** 추가 — 세종대·서울과기대처럼 '■ 자격요건 / ■ 장학금액 / ■ 유의사항'으로
  나누는 공고에서 금액·문의까지 자격으로 읽고 있었다(의심 줄 22개 중 12개가 이 하나).
  ⓒ **2차 경로**: 자격 제목이 아예 없는 공고(35건)에서만 줄 단위로 줍는다. 분명한 요건 신호가 있고
  2줄 이상일 때만 채택 — 넓게 주우면 제출서류·브라우저 제목줄·부서명이 자격으로 둔갑한다.
- **`match-engine.js tidyRequirement`의 앞머리 기호 목록에도 ■□▣▷★를 넣었다**(화면 쪽 2차 방어).
- **회귀 검사**: `verify/test-collector.mjs`에 '본문 컷이 10,000자 미만이면 실패'(자기검증 2항목 포함).
- **남은 것 (무료로는 여기까지)**: 미확보 57건 = 자격 제목 없음 31 · 접속 실패 등 원문 미확보 12 ·
  15,000자에서도 잘림 14. **자격이 첨부 HWP 안에만 있는 공고**는 본문을 아무리 늘려도 안 나온다.
  못 읽은 공고는 지금처럼 "지원 자격을 아직 읽지 못했어요"로 정직하게 표시된다(원칙 8-1).
  나중에 AI를 쓰기로 하면 실패분만 넘기면 되고, `schematize-forms.mjs` 방식(무료 우선 → 실패분만 API,
  `enabled` 스위치·실행당 한도)을 복사하면 된다. 실측 비용은 최초 74건 500원(싼 모델) 수준.

### (착수 전 계획) 자격 요건 채우기 — 구축이 필요한 과제 (2026-08-02 개발자와 정리)
**개발자 지적**: "자격 진단이 맨날 별도 자격 제한이 없다는데, 원문 보면 다 제한이 있다.
5·18희망장학생은 민주화운동·국가폭력 피해자 유자녀 / 5·18 정신 계승 활동자 요건이 있다."

- **지금 상태(배포됨)**: 등록 공고 전부가 `eligibility`에 `selective:true` 하나뿐이라 판정할 게 없다.
  예전엔 그걸 "✓ 별도 자격 제한이 없는 공고예요"로 단정했는데(원칙 8-1 위반), 지금은
  **"? 지원 자격은 공고 원문에서 확인하세요 — 앱이 아직 요건을 읽지 못했어요"**로 정직하게 말한다.
  원문에서 '제한 없음'을 실제 확인한 공고만 `eligibilityVerified: true`를 달면 확신 문구가 나온다
  (현재 붙은 공고 0건 — 확인한 적이 없으므로).
- **왜 지금 안 채웠나**: 122건을 손으로 채우는 건 한 세션에 무리고, **자동 추론은 금지**다
  (틀린 자격 판정은 '모른다'보다 나쁘다 — 자격도 안 되는 학생이 서류 준비하다 헛수고한다).
  개발자와 "구축이 필요한 일"로 합의했고, **앱에 아직 사용자가 없어 급하지 않다**고 판단했다.
- **재료는 이미 있다**: `collector/extract-excerpts.mjs`가 공고 원문에서 신청자격 문장을
  발췌해 registered.json `excerpts`에 넣어 둔다. 이걸 보고 채우면 지어내지 않아도 된다.
- **구축할 때의 설계 방향(제안)**: ⓐ `eligibility`에 사람이 읽는 자유 문구 칸(예: `requireText`)을
  두어 원문 발췌를 그대로 보여 주는 것부터 — 기계 판정 없이도 학생에게 진짜 요건이 전달된다
  ⓑ 그 다음에 자주 나오는 요건(유자녀·기초/차상위·지역 거주·학년·계열)만 구조화해 flags와 잇는다
  ⓒ 전수를 목표로 하지 말고 **요건이 뚜렷한 공고부터** 나눠서. 검수한 것만 `eligibilityVerified`.
- **주의**: 자격 요건은 매칭·알림·홈 합계에 모두 영향을 준다. 채우기 시작하면
  `match-engine.js` 한 곳에서만 판정하는 구조를 유지할 것(화면·알림이 갈라지면 안 됨).

**완료된 개발자 액션**: 정식 등록 컨펌(누적 45건) / 브라우저형 수집기 승인·가동 / 외대 게시판 제공(2026-07-06 연결) / **경희대 게시판 제공(2026-07-07 연결)** / 제안서 재작성 지시(PDF 2종 완성).

### 3. 전체 현황 요약
- **수집망 (2026-08-09 실측 — 45개 캠퍼스)**: `schools.json` 44곳 중 주소 확보 **37곳** +
  브라우저 수집기 17곳. `health.json` 기준 **최근 5일 안에 정상 수집된 곳 41곳**(실시간 공고 600건).
  · 주소 미확보 7곳: 연세 미래 · 고려(안암·세종) · 중앙 공통 · 동국 WISE · 부산대 · 계명대
  · 8/2 이후 수집이 멈춘 4곳: 연세 신촌 · 동국 서울 · 상명 서울 · 고려 안암 — **원인 미조사(다음 세션 과제)**
  최근 14일 안에 새 공고가 들어온 학교도 41곳 전부. 세는 법:
  `schools.json`은 `boardUrl` 있는 항목, `browser-targets.json`은 `targets` — 둘을 학교+캠퍼스로 합집합.
  · **거의 안 잡히는 곳 2곳(손볼 후보)**: 부산대(1건 — 브라우저 대상이 아님) · 상명대(1건 — 동적 렌더).
    수집은 되는데 그 학교 학생 화면이 사실상 비어 있다.
  · **아직 주소를 못 받은 분교 3곳**: 연세 미래(원주) · 고려 세종 · 동국 WISE(경주).
  · 옛 기록의 '12개 캠퍼스'·'13→29곳'은 각각 7월·13차 세션 시점 숫자다 — 지금은 41곳.
- **데이터 (2026-08-09 실측)**: 정식 등록 **166건**(사람 검수 79 + 로봇 자동 등록 **검수 전 87**) ·
  양식 **56종** · 실시간 공고 피드 **600건** + 실존 상시 제도 6종. **가상 샘플 0건**.
  · formId 연결 39건 · noForm 121건 · 금액 미확인 158건(지어내지 않은 정직한 0)
- **신청 채널별 도달 수준**: 이메일 접수=원클릭 가능(자동발송은 Resend 키 대기 — 공고 3건 연결됨) / 양식 제출=원본 동일 문서 생성(12종) / 포털 입력=바로 제출 버튼(복사+제출처 열기) / 한국장학재단=준비+안내(본인 인증 필수 — 법적 상한)
- **다음 마일스톤**: 개발자 4개 액션 완료 → 즉시 출시 가능 → 베타 사용자 확보 → 장학팀 파일럿(진짜 원클릭) → Play 스토어

## 제안서 → 진짜 원클릭 연결 경로 (개발자에게 설명할 때 이 논리 사용)
지금 앱은 서류 준비까지 완벽하지만 마지막 제출은 학교 시스템이라 대신 못 누른다. 장학팀 제안서가 이 문을 여는 열쇠:
1. **정보 연계**: 장학팀이 공고를 직접 제공 → 수집 추측 불필요, 정확도 100%
2. **표준화 접수(파일럿)**: 장학팀이 지정 장학금의 이메일 접수를 공식 인정 → 앱 생성 원본양식+보관함 서류가 버튼 하나로 접수 → 이때부터 "신청 완료" 표기가 정직하게 가능 = 진짜 원클릭
3. **학사 연동**: 성적·재학 정보 연동 → 증명서 발급 자체가 소멸 → 문자 그대로 클릭 한 번

## 세션별 작업 기록은 `SESSIONS.md`로 옮겼다

지난 세션들의 서술 기록(24차~6차)은 파일 크기 때문에 `SESSIONS.md`로 옮겼다(2026-08-23,
claude-md-improver 검사 결과 — 당시 이 문서의 60%가 세션 기록이었다). **삭제가 아니라 이동**이다 —
위 "중요한 기술 사실"의 "(OO차 세션)" 인용은 전부 `SESSIONS.md`를 가리킨다. 옛 사고의 경위가
궁금할 때만 열어 본다. **평소엔 안 읽어도 된다** — 위 절들에 이미 결론이 정제돼 있다.

| 회차 | 날짜 | 제목 |
|---|---|---|
| 24차 | 2026-08-18 | 신청서 질문 방식 최적화 |
| 23차 | 2026-08-17 | 홈 화면 장학금 도우미(챗봇) 신설 |
| 22차 | 2026-08-14 | 밀린 컨펌 전량 검수 + 결함 2종 수리 |
| 21차 | 2026-08-09 | 관리자 화면 가동 + 개편 계획 전 단계 완료 |
| 병행 | 2026-08-07~09 | 브라우저 수집 수리 · 회원가입 설계 확정 |
| 20차 | 2026-08-07 | 관리자 화면 Cloudflare 게시 · 푸시 등록 버그 3종 수리 |
| 18차 | 2026-08-06 | 🎉 진짜 푸시 실가동 · Cloudflare 1단계 완료 |
| 16차 | 2026-08-03 | 수집 로봇 시간초과 수리 · 인프라 개편안 검토 |
| 15차 | 2026-08-03 | 자격 요건 무료 확보, 54 → 81건 |
| 14차 | 2026-08-03 | 관리자 페이지 1단계 구축 |
| 13차 | 2026-08-02 | 수집망 2배 확장 · 수집기 버그 5건 · 검색 UI |
| 12차 | 2026-08-01 | 원문 링크 사냥꾼 안정화 · 학교 확장 대비 |
| 11차 | 2026-07-31 | 앱 알림 시스템 구축 |
| 병행 | 2026-07-31 | 협업 세션 — 충돌 해소 체계 · 공동작업자 Josehyeon |
| 10차 | 2026-07-31 | 앱 자동 반영 점검·수리 |
| 9차 | 2026-07-30 | 양식 스키마화 무인화 + 첨부 누락 버그 |
| 8차 후속3 | 2026-07-15 | 정직 페이스리프트: 추론 삭제·원문 발췌 |
| 8차 후속2 | 2026-07-15 | 준비용 지원문서 + 양식 원본 자동 확보 |
| 8차 후속 | 2026-07-15 | 다운로드형 전량 승격 |
| 8차 | 2026-07-15 | 서핑 개선·자동 등록 체계·전수 컨펌 |
| 6차 | 2026-07-08 | UX·보안 점검·전수 컨펌 |

## 공동 개발 진척도 문서 (PROGRESS.md) — 세션 시작 시 필독 (2026-07-28 신설)
- 두 개발자 공용 진척도 문서. **정본은 항상 `main`의 `PROGRESS.md`** (열람: https://github.com/seonju5543-web/hanggonggan/blob/main/PROGRESS.md).
  개발자별 작업 브랜치: 선주=`claude/work-seonju`, 공동작업자(Josehyeon)=`claude/work-josehyeon`.
- **새 세션 시작 시**: `git show origin/main:PROGRESS.md`로 최신본을 읽고, 두 개발자의 완료/개발중/구상중 상태를 사용자에게 먼저 요약 보고한다.
- **작업 종료 시**: 자기 담당 칸(완료/개발중/구상중)만 수정해 **`main`에 커밋**한다. 상대 칸은 건드리지 않는다(충돌 방지).
  '🤖 활동 로그'(AUTO-ACTIVITY 마커 사이)는 매일 08:07·22:07 KST `update-progress.yml` 워크플로가 브랜치별 커밋으로 자동 갱신하므로 **손대지 않는다**.

## 커밋 규칙
- 커밋 끝에 Claude-Session 링크 추가 관례 유지. PR은 요청 시에만.
